# DATA — 数据是怎么来的

> **这份文件回答一个问题：手上任何一个数字，它背后那批数据是怎么造出来的。**
>
> 三层账之外的第四份常驻文件，和另外三份的分工：
>
> | 文件 | 回答什么 |
> |---|---|
> | `TIMELINE.md` | 当初为什么这么定 |
> | `RESULTS.md`（← `ops/runs.jsonl`） | 数据长什么样 |
> | `WORKPLAN.md` / `plans/2026-07-30-task-plan.md` | 现在要干什么 |
> | **`DATA.md`（本文）** | **这批数据是怎么造出来的** |
>
> 术语一律按 `plans/PLAINWORDS.md`。本文只写设定与口径，不写结论——结论查 `RESULTS.md`。
> 每一节末尾的「出处」是这些设定的原始落盘位置，本文与出处冲突时以出处为准。
>
> **开工前必读**：§7 是新实验动手前的检查清单，§4 和 §6 各有一个已经踩过的坑。

---

## 1. 轨迹是怎么采的

探针这条线的一切数据都从**轨迹**来——agent 做完一个任务的完整记录，一个任务一个 jsonl 文件。
到 2026-07-31 为止只有两个采集批次。

### 1.1 批次 `full_v1`（2026-07-29 发射）

三个 agent 模型 × 三个环境，全部本地起服务，零付费 API。

| agent 模型 | 权重 | 服务 | 说明 |
|---|---|---|---|
| qwen3.5-27b | 本地 | tokyo108 H200 g3，端口 8101 | raw 模式自拼模板，思考段不丢 |
| qwen3.6-27b | 本地 | tokyo108 H100 g0，端口 8104 | raw 模式；Mamba 混合架构，`--max-num-seqs` 必须降到 256，否则引擎起不来 |
| gpt-oss-120b | 本地 | tokyo108 H200 g5，端口 8103 | chat 模式，思考取 `message.reasoning` |

两个 Qwen 服务挂了双 served-model-name（别名 + 权重全路径），因为 BFCL 用全路径当模型名
发请求，只挂别名会 404。

三个环境各自的题目和参数：

| 环境 | 题目 | 每题步数上限 | 规模 |
|---|---|---|---|
| AppWorld | dev split 全量 57 题 | 30 | 57 题 × 3 模型 |
| TALES（烹饪世界） | seed 101–120，难度串两档见下 | L1 档 60 / L2 档 80 | 20 seed × 3 模型 × 2 档 |
| BFCL | multi_turn_base 全 200 题 | 官方默认 | 200 题 × 2 个 Qwen（**这一批没有 gpt-oss**） |

TALES 的两个难度档是调出来的，不是随便定的：

- **L1 档**：`numLocations=6, numIngredients=3, numDistractorItems=10, includeDoors=0, limitInventorySize=0`
- **L2 档**：`numLocations=11, numIngredients=4`，其余同上

为什么要两档：单房间版三个模型都能打穿，而且有死局（菜谱要求烧烤但房间里没有烤架，游戏无解）。
多房间同时解决"太浅"和"死局"。L1 出来之后 q36 二十局全通（1.00），还是饱和，所以补了 L2，
q36 掉到 0.65 落进 0.6–0.9 这个有区分度的带里。**两档都要留**：q36 的可用档是 L2，
q35 的可用档是 L1（0.90，到 L2 掉到 0.55 出带），gpt-oss 两档都在带外（0.40 / 0.15）。

这批采完的实测成绩（探针数据的"原料成色"，`collect/summarize_full.py` 汇总）：

| 格子 | n | 做成率 | 思考中位（字符/步） | 调用/轨迹 |
|---|---|---|---|---|
| AppWorld × q35 | 57 | 0.21 | 378 | 20.3 |
| AppWorld × q36 | 57 | 0.63 | 261 | 17.6 |
| AppWorld × gptoss | 57 | 0.35 | 387 | 13.7 |
| TALES L1 × q35 | 20 | 0.90 | 674 | 25.1 |
| TALES L1 × q36 | 20 | 1.00 | 835 | 20.5 |
| TALES L1 × gptoss | 20 | 0.40 | 2356 | 46.9 |
| TALES L2 × q35 | 20 | 0.55 | 972 | 52.5 |
| TALES L2 × q36 | 20 | 0.65 | 1493 | 45.5 |
| TALES L2 × gptoss | 20 | 0.15 | 3108 | 74.0 |
| BFCL × q35 | 200 | 0.70 | — | — |
| BFCL × q36 | 200 | 0.59 | — | — |

三件从这张表里读出来、会影响后续实验设计的事：

1. **TALES L2 是最肥的轨迹来源**：45–74 调用/轨迹、思考中位 972–3108 字符/步，
   远超 AppWorld（13.7–20.3 调用、261–387 字符）。要长链条 + 长思考就用 L2。
2. **gpt-oss 在 TALES 上"想得多做得差"**（思考量是 Qwen 的 3 倍，做成率只有 0.40）。
   对投机研究这反而是好料。
3. **BFCL 上 q36 反低于 q35（0.59 vs 0.70）**，与另外两个环境的排序相反。
   原因未查清：BFCL 给两个模型套的都是 `Qwen/Qwen3-32B` 这个 handler 的聊天模板，
   q36 的模板差异可能被抹掉了。**这条至今是待办，别拿它当模型能力结论。**

### 1.2 批次 `full_v2_topup`（2026-07-30）

只补一件事：给 BFCL 补 gpt-oss 的 200 条轨迹，因为跨模型双向矩阵要求两侧都有，
而 `full_v1` 的 BFCL 只有两个 Qwen。

- 服务：tokyo108 H100 g0，端口 8103，gpt-oss-120b，reasoning high
- 命令走 `bfcl generate --model openai/gpt-oss-120b --skip-server-setup`
  （**`--skip-server-setup` 不能漏，漏了 bfcl 会自己起一个 vLLM 把卡占掉**）
- 收轨迹卡两条判据，都过才算数：思考文本非空、发出的调用能被解析出来
- 结果 200/200 全落盘，两条判据全过

一个已经踩过的坑记在这里：**思考文本在结果条目的顶层 `reasoning_content` 字段**，
形状是 list[list[str]]（按轮按步）。早先的脚本去 `inference_log` 里找 `role=assistant`，
拿到的是空列表——**假阴性，会让人误判"这批没有思考"**。

### 1.3 一个对所有环境都生效的稳定性修复

TALES L2 的 gpt-oss 分片跑到 s120 时被服务端 500 打断
（`unexpected tokens remaining in message header`，思考特别长时 harmony 解析器收不干净），
异常直接冒泡，**整条分片剩下的 seed 全丢**。已修：`collect/common.py` 的 `Chat.__call__`
加了 4 次退避重试（1/2/4 秒），重试完仍失败才抛。这个改动对所有环境生效，
以后长跑不会再因为服务端抖一下丢掉整条分片。

**出处**：`envs/runs/FULL_RUN_v1.md`、`envs/collect/bfcl_gptoss/RUNBOOK.md`、
`envs/runs/CALIB_v0.md`、`envs/collect/run_appworld.py` / `run_tales.py` / `common.py`

---

## 2. 轨迹怎么变成样本

一条轨迹切成很多**样本**，规则是 2026-07-29 敲定的终稿，写死在
`envs/collect/build_dataset.py` 的文件头里，之后没改过。

- **一条样本 = 一个前缀**。前缀永远从思考的第一个字开始，尾巴停在句子边界。
  句子边界的定义是换行，或者 `.!?` 后面跟空白——小数点和 `apis.x.y` 里的点后面没有空白，
  天然被排除掉。
- **全边界切**：一个事件把它所有的句子边界都切出来，上限 64 个（超了就均匀抽，
  但末尾那个永远保留）。训练损失用 `w=1/m_i` 让每个事件等权——一个事件切出多少条样本，
  它在损失里的总份量都一样。**这么做是为了和部署时的分布对齐**：真跑起来的时候，
  每个句子边界都会调一次探针。
- **标签 = 这一步真实调用的工具名**，全自动、零人工。
- **一个环境一个数据集**，三个环境不合训。
- **样本的 text = 任务描述 + `[HISTORY]` 段 + `[THINKING]` 前缀**。
  历史段只保留最近 3 轮，每条环境返回在题干里最多留 400 字符。
- 思考短于 40 字符的事件直接丢（没有可切性）。

切分的单位是**任务实例**，不是轨迹：AppWorld 的 task_id / TALES 的 seed / BFCL 的 entry id。
同一个任务实例在不同 agent 模型下产生的几条轨迹**同进同出**，防的是姊妹泄漏——
同一道题的 q35 版本进训练堆、q36 版本进测试堆，探针就等于提前看过答案。

四堆按 70/10/10/10 切：训练堆 / 拟温度堆 / 扫门槛堆 / 测试堆。
扫门槛堆和测试堆天生含全部边界，可以直接拿来跑回放协议。

超过 4096 token 的样本**不在数据里截**，由训练脚本左截（`truncation_side=left`）。

**出处**：`envs/collect/build_dataset.py` 文件头 + `boundaries()`

---

## 3. 数据集有哪些版本

一共四个主版本加四个派生版本。主版本之间的关系是"加数据"和"改规则"两类，分清楚：

```
v1  ── 规则不同，已弃用（三路切分、三环境合训、193 类工具词表）
        ↓ 规则重写
v2  ── 终稿规则第一版；数据源 = full_v1
        ↓ 加数据（full_v2_topup 并入）
v3  ── 数据源 = full_v1 + full_v2_topup
        ↓ 只重建 BFCL（把 gpt-oss 那 200 条并进去）
v3_1 ── AppWorld / TALES 与 v3 逐字节相同，只有 BFCL 不同

派生（都从主版本物化出来，不重新采集）：
  v3_params    ← v3      参数在文本里的位置标注
  v3_1_params  ← v3_1    同上
  v3_xmodel    ← v3      按 agent 模型切成两侧，供跨模型矩阵用
  v3_1_xmodel  ← v3_1    同上
```

**v1 为什么弃用**：它是三路切分（train/val/test）、三个环境合训一个 193 类的工具词表、
按轨迹切分而不是按任务实例切分。和 v2 起的终稿规则不是一回事，**它的数字不能和后面任何一版比**。

**v3 和 v3_1 的区别只在 BFCL**：v3 的 BFCL 是纯 qwen 轨迹（因为 `full_v1` 就没采 gpt-oss），
另外两个环境的 v3 本来就含两种模型。这个不对称就是"BFCL 混训"那条实验存在的理由。

这条 2026-07-31 实测复核过（`md5sum` 逐环境比对全部 jsonl）：
appworld 两版 md5 同为 `f594c252…`、tales 同为 `b913a10e…`、bfcl 两版不同。
所以 **appworld 和 tales 的 v3 与 v3_1 成绩可以直接互引，bfcl 不行**。

**一个命名上的小地雷**：v3 和 v3_1 的 `BUILD_REPORT.md` 标题都还写着"bert_data v2 出厂报告"，
是脚本里的字符串没跟着改。看报告认目录名，别认标题。

---

## 4. 每版每环境的规模与先验基线

**先验基线不是常数**——它是"永远猜训练集里最高频的那个工具"在测试堆上的正确率，
数据一换它就变。跨版本比较探针成绩时必须连它一起看。

| 版本 | 环境 | 轨迹 | 任务实例 | 事件 | 样本 | 工具类数 | 训练堆样本 | 测试堆事件 | 先验基线 | 最高频工具 |
|---|---|---|---|---|---|---|---|---|---|---|
| v2 | appworld | 171 | 57 | 2772 | 50880 | 79 | 35302 | 305 | **0.226** | `apis.api_docs.show_api_doc` |
| v2 | tales | 60 | 20 | 2039 | 99040 | 13 | 70252 | 201 | **0.672** | `open` |
| v2 | bfcl | 396 | 200 | 2265 | 36343 | 101 | 26309 | 237 | **0.038** | `startEngine` |
| v3 | appworld | 351 | 147 | 5552 | 111767 | 120 | 71002 | 791 | **0.298** | `apis.api_docs.show_api_doc` |
| v3 | tales | 120 | 50 | 3907 | 207552 | 14 | 142621 | 408 | **0.556** | `open` |
| v3 | bfcl | 396 | 200 | 2265 | 36343 | 101 | 25061 | 226 | **0.049** | `startEngine` |
| v3_1 | appworld | 351 | 147 | 5552 | 111767 | 120 | 71002 | 791 | **0.298** | 同 v3（逐字节相同） |
| v3_1 | tales | 120 | 50 | 3907 | 207552 | 14 | 142621 | 408 | **0.556** | 同 v3（逐字节相同） |
| v3_1 | bfcl | 595 | 200 | 3325 | 68506 | 106 | 47799 | 335 | **0.042** | `startEngine` |

**⚠️ BFCL 的先验基线有三个值**：v2 是 0.038、v3 是 0.049、v3_1 是 0.042。三个都对，
分别属于三个版本。引用之前先确认手上这个探针成绩是哪一版训出来的。
（`RESULTS.md` 里 `20260729_2235` 那条用的是 0.038，`20260730_0204` 那条用的是 0.049。）

v3 的 BFCL 事件数和 v2 一样是 2265，但训练堆样本从 26309 变成 25061——
**同样的数据重新切了一次**，切点不同。所以这两版的 BFCL 成绩之间的差叫切分方差，
不是数据量效应，`RESULTS.md` 里 `20260730_0204` 那条量的就是这个（约 ±3pt，即误差棒）。

一个已经修掉的坑：建库脚本原来用递归通配符找文件，返回顺序不定，又按 id 去重，
**两次重建出来的事件数会在 2265 和 2267 之间飘**。修完（改成 `sorted(glob(...))`）之后才建的 v3_1，
验收时做了两次重建逐字节对比。

**出处**：`envs/bert_data/{v1,v2,v3,v3_1}/BUILD_REPORT.md`、各环境 `router_stats.md`

---

## 5. 派生数据集

### 5.1 参数定位（`*_params`）

给抽取头用的：标出每个参数的值在样本文本里的起止位置。

- **定位口径**：在样本自己的 text 里做 `str.rfind`，取最靠末尾那次出现；找不到就标"抽不到"。
- 参数的 key = 工具名.参数名（位置参数记 `pos0/pos1/...`，TALES 固定记 `arg`），空值跳过。
- 每条都做了区间断言 `text[start:end] == value`，全过。

总定位率：appworld 0.833 / tales 0.807 / bfcl 0.917。
定位率随思考读得越多越高（这正是抽取头的难点所在），十桶里最左和最右的对比：

| 环境 | 思考刚开头（0.0 桶） | 思考末尾（0.9 桶） | 整段读完 |
|---|---|---|---|
| appworld | 0.672 | 0.911 | 0.874 |
| tales | 0.416 | 0.970 | 0.963 |
| bfcl | 0.768 | 0.973 | 0.959 |

**三个必须知道的噪声/边界**：

1. **短值会撞上巧合子串**。值 ≤3 字符的占比 appworld 0.079 / tales 0.001 / **bfcl 0.185**，
   这是标签噪声的上界。训练和评测用同一口径打分，所以不偏袒任何一方，但报数字时要声明。
2. **不是所有定位都落在思考段里**：落在 `[THINKING]` 段内的占 appworld 0.633 / tales 0.936 / bfcl 0.912，
   其余是从题干或历史里抄来的。
3. **bfcl 重抽事件 2482，v3 事件 2265，多出 217 个**（另外两个环境完全对齐、零差）。
   这个差没查清，用 bfcl 的参数标注前先确认它影响不到你要的那部分。

两个对照锚点（写论文时要用）：先行试点测出提前 25 token 时参数字面串只有 33.8% 已经出现；
竞品 SPORK 的参数起步正确率 7.6%，是抽取头正确率的下限锚。

**参数三档**（无参 / 选择 / 自由）的档位表按 v2 事件算，规则是"逐字命中率 ≥0.9 算选择档"：

| 环境 | 无参 | 选择 | 自由 |
|---|---|---|---|
| tales | 1.3%（1 类工具，26 事件） | 81.6%（9 类，1663 事件） | 17.2%（3 类，350 事件） |
| appworld | 18.9%（8 类，523 事件） | 72.5%（58 类，2010 事件） | 8.6%（13 类，239 事件） |
| bfcl | 9.7%（16 类，219 事件） | 75.0%（71 类，1699 事件） | 15.3%（14 类，347 事件） |

命中率只是候选集封闭性的**代理指标**，会高估自由档，真正现编的尾巴约 5%，终裁要等抽取头实测。

**出处**：`envs/bert_data/v3_params/PARAM_LABEL_REPORT.md`、`v3_1_params/PARAM_LABEL_REPORT.md`、
`CHECK_50.md`（逐条人工核对 50 例）、`envs/bert_data/v2/PARAM_TIERS.md`

### 5.2 跨模型两侧（`*_xmodel`）

按样本的 `model` 字段前缀（`qwen*` / `gpt-oss*`）把主版本切成两侧，物化出这些目录：

- `train-qwen` / `train-gptoss`：各自训一个探针
- `eval-<考侧>_cal-<校准侧>` 六个组合：换考试侧、换校准侧

四种用法说清楚（`--out` 必须显式给，同一个模型评多个数据配置**要用符号链接分身**，
否则回放报告会互相覆盖）：

| 用法 | 怎么配 |
|---|---|
| 主场 | train-X 的模型 × eval-X_cal-X |
| 冷迁移客场 | train-X 的模型 × eval-Y_cal-X（什么都不重调） |
| 只换校准客场 | train-X 的模型 × eval-Y_cal-Y（权重不动，只换拟温度堆和扫门槛堆） |
| 混训天花板 | 主线 v3 模型 × eval-<侧>_cal-full |

v3_1_xmodel 的训练侧事件数（qwen / gptoss）：tales 1425 / 1235，appworld 2434 / 1211，bfcl 1605 / 748。
tales 和 appworld 的 qwen 侧事件数比规划书按 v2 口径估的多（1425 vs ≈920，2434 vs ≈1426），
报告里已逐档核对，增量来自 `full_v2_topup` 补采，计数无误。

**出处**：`envs/bert/make_xmodel_splits.py`、`envs/bert_data/v3_1_xmodel/XMODEL_REPORT.md`

---

## 6. C3 那条线的数据

跟探针线完全不同的一套，别混。

### 6.1 相似度档：⚠️ 两套编号，取值正好颠倒

**这是全项目最容易引错的地方。**

| 代码里的标签 | 正式编号 | 说的是什么题 |
|---|---|---|
| `L0` | **L2** | 完全一样的题（同一个 trial 重复 k 次） |
| `L1` | **L2⁻** | 几乎一样的题（同任务类型+物体+容器，换场景/trial） |
| `L2` | **L1** | 同类但换了东西的题（同任务类型，换物体/容器） |

两套编号里 `L1` 和 `L2` 这两个符号都存在，指的却不是同一档。
**原始数据的文件名用代码那一套，写进正式文档的数字用正式那一套。**
引用任何一个带 L 的数字之前，先确认它属于哪一套——引错了会把
"同类但换了东西的题上存解法净亏"说成"完全一样的题上净亏"，一个是省一个是亏，正好说反。

转换器：`benchmark_design/metrics.py --legacy-levels`，映射表 `LEGACY_MAP = {"L0":"L2","L1":"L2-","L2":"L1"}`。

题目来自 ALFWorld，从游戏文件路径构造
（`任务类型-物体-可移动容器-容器-场景号/trial_X/game.tw-pddl`）。

### 6.2 存解法全矩阵的跑法

- 一次"连做十轮" = 一个 (档, 种子, 条件) 格；种子 0–4，k=10
- 两条臂各**完整连做十轮**，不是两条臂分掉十轮
- 存解法这条臂：每轮做完，做成了就把题目原文和成功的动作串存下来；
  下一轮取最近五轮里做成了的放进提示、整段放最前面
- 不存解法这条臂：每轮从零开始
- 解码取概率最高的词，所以**完全一样的题这一档、不存解法那条臂，同一个种子内十轮逐字相同**

**耗时不可比的两批**：`20260728_fig1_8bfull` 那批三十次"连做十轮"跨了不同型号的显卡，
耗时那一整列已经作废删掉。全历史臂那批（`20260730_fig1_fullhist_8b`）专门在同一张卡上
先跑不存解法再跑全历史臂，就是为了拿到能比的耗时。
**这两批的不存解法臂不是一个口径，全历史臂的三个比值只能在它自己那批内部算。**

### 6.3 L3 / L4 任务对生成

三个纯 CPU 脚本，零 GPU 零 API，种子默认 20260729，同参数两次运行输出 md5 逐字节相同：

| 脚本 | 产出 | 条数 | 源数据 |
|---|---|---|---|
| `gen_l3_2wiki.py` | `l3_2wiki_items.jsonl`（两跳拼接） | 4244 | 2WikiMultihopQA，种子集 20000 |
| `gen_l4_2wiki.py` | `l4_2wiki_pairs.jsonl`（极性翻转 + 易混实体替换） | 6494 | 同上 |
| `gen_l4_alfworld.py` | `l4_alfworld_pairs*.jsonl`（位置改换对） | 10 / 259 | ALFWorld 真引擎回放验证 |

L4 位置改换对的收录判据很硬：在种子 trial 上跟着自带的专家走到通关拿到解法，
把这串解法原样喂进目标 trial 的真引擎，**终局失败才收录**——收的就是"看着像但照抄会翻车"的对。
有序枚举，因为方向不对称（实测同一对反过来当种子，矛盾点从 0.25 变成 0.5）。

**表面相似度过滤（τ）还没做**，等 bge-large-en-v1.5 就位后补。三个脚本都留了 `--sim-filter TAU` 接口，
现在传进去只打告警并记进 `sim_filter` 字段，不做任何过滤。接上之后只要在收录判据里加一道
`sim(x', x) >= τ`，记录结构不用动。

**出处**：`benchmark_design/GEN_REPORT.md`、`benchmark_design/L3L4_and_metrics_draft.md`、
`fig1_pilot/fig1_run.py`、`fig1_pilot/NOTES_8B.md`、`fig1_pilot/ANALYSIS_8bfull.md`

---

## 7. 新实验开工前的检查清单

按顺序过，每条都有对应的坑：

1. **用的是哪一版数据？** v1 和 v2 起的规则不同，数字不可比（§3）。
2. **先验基线取对了吗？** 它随版本变，BFCL 有 0.038 / 0.049 / 0.042 三个值（§4）。
3. **带 L 的档位编号是哪一套？** 代码那套和正式那套取值颠倒（§6.1）。
4. **要比耗时吗？** 先确认两条臂跑在同一型号的卡上，否则这一列作废（§6.2）。
5. **种子写死了吗？** 全线固定 20260729（`build_dataset.py`、参数定位、L3/L4 生成脚本都是它）；
   探针训练那几条用的是 42 或 20260729，看 `RESULTS.md` 各条的"模型 / 种子"栏。
   重跑必须逐样本一致，这是铁律。
6. **重建过的数据做逐字节对比了吗？** 建库脚本的排序问题修过一次，
   修之前两次重建的事件数会飘（§4）。
7. **同一个探针要评多个数据配置吗？** 用符号链接分身，否则回放报告互相覆盖（§5.2）。
8. **动了 `WORKPLAN.md` 里任何一条判断吗？** 补一条 `TIMELINE.md`。

---

## 8. 已知缺口

写论文和排下一批实验时要记得这几件还没做：

- **BFCL 的聊天模板没查**：q36 用 `Qwen/Qwen3-32B` 的 handler 是否合适，
  这是 q36 在 BFCL 上反低于 q35 的可疑原因（§1.1）。
- **BFCL 参数重抽多出 217 个事件**，原因未查（§5.1）。
- **表面相似度过滤 τ 未接**，等 bge-large-en-v1.5（§6.3）。
- **参数三档的档位表还是 v2 事件算的**，没跟着 v3/v3_1 更新；
  且命中率只是代理指标，会高估自由档（§5.1）。
- **全历史臂的预算 24576 token 是怎么定的，账上没记**（§6.2）。

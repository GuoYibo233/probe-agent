# PLAINWORDS — 一物一名词表

> 这份表由 `exp-status` skill 维护，只增不改。
> 第二列是全篇唯一的称呼，括号里是第一次出现时要给的白话解释。
> 第三、四列都是写的时候手滑会用出来的词。**第三列（永远违规）**交稿前拿去跑字符串搜索，命中即改；
> **第四列（看语境）**这个词另有合法用法（比如"发射"指发射 GPU 任务是合法的、指探针触发就违规），
> 搜出来要人眼判断，别直接改。两列分开是 2026-07-31 的教训：混在一列时 139 个词命中 31 个，真违规只有 7 个。
> 项目内已有词表在 `plans/2026-07-30-task-plan.md` §0，本表与它一致，只是补上白话解释。

| 项目里的原词 / 字段名 | 全篇唯一的称呼（含白话解释） | 永远违规的别名 | 看语境的别名 |
|---|---|---|---|
| 探针 | **探针**（一个小的文本分类模型，读 agent 写到一半的思考文本，猜它接下来要调哪个工具） | 分类器、小模型、router、路由器、判别器 | — |
| 轨迹 | **轨迹**（agent 做完一个任务的完整记录，存成一个 jsonl 文件） | 录像、录像带、日志 | 数据、trace |
| 事件 | **事件**（轨迹里的一次工具调用，连同它前面那段思考） | 调用点、触发点 | event |
| 样本 | **样本**（把一个事件的思考按句子边界切开，每个前缀算一条） | — | 训练数据、条目 |
| 投机执行 | **投机执行**（在 agent 自己写完之前，就把它要调的工具提前发出去） | 投机、抢跑、speculation、预判 | — |
| 可行工作点 | **达标 / 不达标**（在预设的精度门槛下能不能找到一个可用的触发门槛） | 投机门开了、开门、过关、点亮、亮绿灯 | — |
| 先验基线 | **先验基线**（不动脑子，永远猜训练集里出现最多的那个工具，猜对的比例） | 瞎猜基线 | prior、baseline |
| trig_acc | **触发时的预测准确率**（探针决定出手的那些事件里，猜对工具的比例） | trigger accuracy、命中率 | 精度 |
| coverage | **触发比例**（全部事件里，探针敢出手的比例） | 覆盖率、召回 | cov |
| θ / theta | **触发门槛**（探针的置信度超过这个值才出手） | 阈值、门限 | theta |
| best_calA_weighted_acc | **校准集上的加权准确率**（训练过程中在一小块留出数据上取到的最好成绩，用来挑权重） | 训练侧分数、val acc | calA |
| 回放 | **回放**（把已经存好的轨迹按原顺序重读一遍，模拟探针在当时会怎么判） | 考试、复盘、重播 | 评测 |
| 训练跑完 | **训练跑完** | 毕业、出炉、收工 | — |
| episode | **一轮**（让 agent 做一道题，从接到指令到判定成败） | 一集 | 一次、一个 episode |
| mem / nomem 两条臂 | **存解法 / 不存解法** | 开记忆、关记忆、带记忆、记忆臂、有无记忆 | — |
| 因果探针 | **因果探针**（把探针换成只能从左往右读的语言模型，读过的部分能缓存下来不用重读） | causal probe、生成式探针 | — |
| 闭环 | **闭环**（把探针接进正在真跑的 agent，真发调用、真掐表） | 在线、online、上线、实战 | — |
| fork 对照 | **fork 对照**（在探针出手的那一刻把现场复制一份，让副本走投机路线跑到底，和原线对比） | 分叉、复制体、双开 | — |
| 三档（参数产线） | **参数三档**（无参数 / 参数是上文里能找到的原字符串 / 参数得现编） | tier | 三档 |
| 三档（任务相似度） | **相似度三档**（完全一样的题 / 几乎一样的题 / 同类但换了东西的题） | L0L1L2、正典编号 | 档位 |
| 输入税 | 不许用这个词，写全：**存解法要整段放进提示，模型每一步都要重读一遍，所以读进去的 token 变多** | 输入税、input tax | — |
| 记忆饥饿 | 不许用这个词，写全：**只存做成功的解法，所以模型本来就做不成的题攒不下任何能用的东西** | 记忆饥饿、饥饿 | — |

## 2026-07-30 第一轮检查员报回来后追加

| 项目里的原词 / 字段名 | 全篇唯一的称呼（含白话解释） | 永远违规的别名 | 看语境的别名 |
|---|---|---|---|
| calA 那一成数据 | **拟温度堆**（用来拟合温度的那一成，温度是把模型输出的分数拉成可信概率的参数） | calA 集 | 校准集、校准数据 |
| calB 那一成数据 | **扫门槛堆**（用来找触发门槛的那一成） | calB 集 | 校准集 |
| test 那一成数据 | **测试堆**（留出来不参与训练的那一成） | 考试题、考卷、held-out | — |
| bf16 权重直接训练导致的问题 | **浮点格式**（用几位二进制存一个小数） | 数值精度 | 精度 |
| 完美复用的最大省量 | **上限** | 屋顶 | 天花板、ceiling |
| 存解法省不省 token 的全矩阵实验 | **存解法全矩阵** | 记忆红利、fig1 矩阵 | 红利 |
| 闭环的五种运行条件 | **五种跑法** | 五种口径、五个口径（"口径"只用于"这个数怎么算出来的"） | — |
| 一条实验的收尾登记 | **收尾记录**（跑完追加的那条带结论和数字的记录） | finish 记录、销号记录 | — |
| 探针决定预测的那一刻 | **触发** | 出手、开火、抢跑 | 发射 |
| 一次小规模试跑 | **冒烟**（只验整条链子能不能从头跑到尾、不看成绩） | 跑通测试 | smoke |
| 每一种实验条件 | **一条臂** | 一个 arm | 一组、一路 |

## 2026-07-31 第二次跑这个 skill 时追加

| 项目里的原词 / 字段名 | 全篇唯一的称呼（含白话解释） | 永远违规的别名 | 看语境的别名 |
|---|---|---|---|
| no-think 消融 | **砍思考**（把样本里的思考段整段删掉，其余不动，重训一遍） | 去思考、无思考、no-think、屏蔽思考 | — |
| no-hist 消融 | **砍历史**（把样本里的历史段整段删掉，其余不动，重训一遍） | 去历史、no-hist、屏蔽历史 | — |
| fullhist 臂 | **全历史臂**（把之前每一轮的完整记录全塞进提示的那条臂） | 第三臂、全上下文、full history、全塞 | — |
| 混训 | **混训**（把两种模型的轨迹合在一起训一个探针） | 混合训练、合训 | mixed |
| cold transfer | **冷迁移**（换了 agent 模型之后，拟温度堆和扫门槛堆都还用原来那一侧的数据，什么都不重调） | 冷启动、直接迁移、零调 | — |
| recal | **只换校准**（模型权重不动，只把拟温度堆和扫门槛堆换成新那一侧的数据） | 重新校准、换校准 | recal |
| 抽取头 | **抽取头**（探针上加的第二个输出头，指出参数的值在文本里的起止位置） | span 头、抽取器、参数头 | — |
| 7-30 夜里排的九条回放 | **深夜那批评测** | 夜批、九格、夜批九格 | — |
| 参数三档的三个档名 | **无参 / 选择 / 自由**（无参=这次调用不带参数；选择=参数的值是上文里能找到的原字符串；自由=参数的值得现编） | tier、档位编号 | — |
| param_present_rate | **触发时值已出现的比例**（触发那一刻，参数的值是不是已经写在文本里了） | present rate | 出现率 |
| full_call_ok | **完整调用正确率**（工具名和全部参数一起都对的事件比例。注意别和 `params_all_ok` 那个"参数全对率"混起来，那是另一个数） | 端到端正确率、调用全对率 | — |
| 探针达到判定标准 | **达标** | 破线、过线、开门、点亮 | — |
| hf_server.py 那套服务 | **朴素服务**（用 transformers 直接起的推理服务，一次只处理一个请求） | HF 服务、plan B 服务 | — |

## 2026-07-31 合并 `2026-07-30-task-plan.md` §0 时补入

原任务卡 §0 有一份并行词表，已收编到这里，§0 改成指针。下面四条是 §0 有、本表原先没有的。

| 项目里的原词 / 字段名 | 全篇唯一的称呼（含白话解释） | 永远违规的别名 | 看语境的别名 |
|---|---|---|---|
| train/calA/calB/test 四份 | **四堆**（一个数据集按任务实例切成的四份，比例 70/10/10/10：训练堆 / 拟温度堆 / 扫门槛堆 / 测试堆） | 四刀、四路切分、四个 split | 切分 |
| 回放协议 | **回放协议**（测试堆的轨迹按原顺序读前缀，每个事件在置信度第一次超过触发门槛的那一刻记下预测，报触发比例 / 触发时的预测准确率 / 提前量 / 错误投机率四个数） | 评测协议、考法 | 评测 |
| prefetch 用法 | **预取**（触发之后照常把思考写完，只是把工具调用放到后台先发出去。不改变答案，省的是延迟不是 token，只能用于只读调用） | 预抓、预执行、后台调用 | — |
| truncate 用法 | **截断**（触发之后立刻掐断思考、直接提交预测的那个调用。token 省在这里，但猜错就直接变成任务做错，触发门槛就是"省多少 vs 错多少"的旋钮） | 掐断、提前提交、早停 | — |

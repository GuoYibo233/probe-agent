# research-loop 设计定稿：交接文档（2026-08-16 夜写，给「统筹 session」和「part session」用）

这份文档写给两种会话读：一种是 gyb 开的「统筹 session」，只开一个，常驻，读了这份就接管全局：管哪份 part 在谁手上、把各份定稿的改动传播到别的 part 和两份源文档、收 part session 报上来的同步事项、攒问题等 gyb 裁；另一种是 gyb 开的「part session」，可以同时开很多个，一个只定稿一份 part，遇到牵连别处的事不自己动手，回报给统筹 session。写这份文档的会话到此结束，gyb 不再回来，之后的一切以这份和目录里的文件为准。

## 一、现在的状态

research-loop 是 new1 工程里的一个 Claude Code 插件，设计已经收口但代码一行没写（旧代码按裁决整体退役，见 `00-overview.md` 第三节裁决 1）。设计写在两份源文档里，然后被拆成这个目录下的 22 份 part，其中 21 份写成了：

- 设计文档 `plans/2026-08-16-research-loop-next-steps.md`：做成什么样、为什么，十一条设计原则在它第二节。
- 施工计划 `plans/2026-08-16-research-loop-build-plan.md`：字段、转移表、命令表、阈值、待验证、测试、施工步骤。可枚举的东西只在它里面写一遍。
- 两轮模拟的原始摩擦：`plans/2026-08-16-research-loop-simulation-round1.md`（59 条，核实过）、`plans/2026-08-16-research-loop-simulation-round2.md`（234 条，没核实过）。
- 这个目录 `plans/research-loop-parts/`：`README.md` 是索引和每份留给 gyb 的点，`00` 到 `25` 是 21 份 part，`30-build-steps-verify-tests.md` 没写成。

全部已 commit：`601f835`。工作树干净。（2026-08-17 夜的最新状态见第七节，以第七节为准。）

拆分的写法铁律是只搬运不发明：源文档怎么写就怎么写；两处不一致的在 part 里标「两处原文不一致」不裁；题目里缺的写进每份末尾「源文档没写清的」；每份最后一节把第二轮摩擦原样抄了一遍。所以现在 21 份 part 加起来的内容等于两份源文档，外加每份 agent 自己发现的没写清的点。

## 二、每份 part 的固定结构（part session 改的时候要保持）

```
# 标题
> 覆盖什么、不覆盖什么；源：设计文档哪几节、施工计划哪几节
## 正文（若干小节）
## 和别的 part 的接口
## 源文档没写清的（留给 gyb）
## 第二轮模拟里归到这一份的摩擦（原样，未核实）
```

part session 定稿之后会多两节：

```
## 裁决记录（日期）
## 要同步到别处的
```

「裁决记录」一行一条带日期，是 gyb 说的原话或原意；「要同步到别处的」一行一条，写清目标文件名和要改成什么。摩擦那一节永远原样保留。

## 三、统筹 session 和 part session 怎么通话

统筹 session 开起来之后第一件事 `/rename rl-hub`，part session 开起来第一件事 `/rename rl-part-<编号>`（比如 `rl-part-03`），名字定了两边才找得到对方。同一台机器上的 Claude Code 会话互相能看见：用 `ListAgents` 列出来，用 `SendMessage` 按名字发消息（收件人名字照 `ListAgents` 那一行原样抄）。part session 有事回报就 `SendMessage` 给 `rl-hub`；统筹 session 有话交代就 `SendMessage` 给 `rl-part-<编号>`。

消息发不出去（对方还没开、`ListAgents` 里没有）就走文件邮箱：往 `plans/research-loop-parts/sync-inbox.md` 末尾追加一段，格式固定：

```
## <日期 时间> 来自 <会话名> 关于 <part 文件名>
- 事项：<一句话>
- 裁决原文：<gyb 说的>
- 要改的地方：<目标文件名>：<哪一句改成什么>
- 状态：待处理
```

统筹 session 每次开工先读 `sync-inbox.md`，处理完把那段的状态改成「已处理 <日期>」。消息和邮箱两条路的内容一样，邮箱是兜底，不是可选的第二份：发了消息也要往邮箱补一段，这样会话崩了记录还在。

part session 要回报的三种事：（1）一条裁决动到了别的 part 或两份源文档里的东西（字段、状态、命令、接口一节列的），把裁决原文和要改的目标句子报上去，自己不去改；（2）自己这份里的一句和别的 part 打架，两边原文报上去等裁，不自己定；（3）这份过完了，报一句「`<文件名>` 定稿，commit `<hash>`」。回报之后不等回音接着往下过；统筹 session 有回话（比如「这条别处已经裁成另一个值」）再照回话改。

统筹 session 要交代给 part session 的两种事：（1）它手上那份 part 里的某一句，因为别的 part 先裁了，现在要改成什么，附来源 part 的裁决原文；（2）它报上来的打架 gyb 裁了，结果是什么。

## 三点五、part session 怎么开（gyb 贴给每个 part session 的话）

> 先 `/rename rl-part-<编号>`。读 `plans/research-loop-parts/HANDOFF.md` 第二、三、四点五节，再全文读 `plans/research-loop-parts/<文件名>`。这份是 research-loop 插件设计的一个部分，我要把它逐条定稿。规矩：只在这一个文件里改，别的 part 和两份源文档不动；文件末尾「第二轮模拟里归到这一份的摩擦」那一节原样保留不改；十一条设计原则在 `00-overview.md` 第二节，改任何一条规矩都要能对回其中一条。先从「源文档没写清的」那一节开始，一条一条问我要裁决，一次只问一个问题，问完等我答，答了就把正文对应的那一处改掉、把那条从「没写清」里删掉；正文里标了「两处原文不一致」的地方同样一处一处问我。我说的每一条裁决都追加到文件末尾新开的「裁决记录（日期）」一节里，一行一条带日期。改动牵连别的 part 的（字段名、命令、状态、接口一节里列的东西），不去改那边：在文末新开的「要同步到别处的」一节里列一条，同时按 HANDOFF 第三节 `SendMessage` 报给 `rl-hub` 并往 `sync-inbox.md` 追加一段。收到 `rl-hub` 的消息照它说的改。全部过完之后把「和别的 part 的接口」一节按定稿重写一遍，commit（前缀 `research-loop parts:`），再报 `rl-hub` 一句定稿。

建议顺序：先 `03-ledgers.md`、`04-handoffs-and-sessions.md`、`05-rl-cli.md`（底座，角色和角色对那十一份的字段、状态、命令全指向它们），再 `02-decisions.md`、`06-hooks-and-permissions.md`，再角色五份 `10` 到 `14`，最后角色对六份 `20` 到 `25` 和 `07`、`08`、`09`。同时开的几份尽量别互为定义处和引用处（第四点五节的表），底座三份最好先过完再开别的。`30` 那份等底座定稿之后再写，源在施工计划第九、十、十一节。

## 四、统筹 session 干什么

统筹 session 常驻，干四件事：

1. 记谁在干什么。维护 `README.md` 进度表里每份的状态（未开、在 `rl-part-NN` 手上、已定稿 commit 几号），part session 报定稿就更新。
2. 收回报、传播。收到 part session 的消息或 `sync-inbox.md` 里的待处理段，先按第四点五节的表判定义处：定义处在别的 part，就把裁决搬回定义处那一份（改它的正文并补进它的「裁决记录」，注明来自哪份），再从定义处往所有引用处传，包括两份源文档（源文档改正文那一句，并在设计文档最后一节或施工计划第十四节补一行「哪天、按哪份 part 的裁决、改了哪句」）。目标 part 正在某个 `rl-part-NN` 手上的，不直接改它的文件，`SendMessage` 交代给那个 session 去改（避免两个会话同时写一个文件），它不在了才自己改。传播完把来源 part 里那条标「已同步 <日期>」，把邮箱那段标「已处理」，commit 前缀 `research-loop sync:`。
3. 攒问题等 gyb。传播时发现目标 part 已经有相反的裁决，或者 part session 报上来的打架，不自己裁：把两边裁决原文并排记进 `sync-inbox.md` 一段（状态「等 gyb」），gyb 来了一次问一个，裁了再改并回话给相关的 part session、补进两份 part 的「裁决记录」。
4. 补漏。第一次开工先按第四点五节末尾那步核关联表；空下来的时候做覆盖检查（第五节）。

统筹 session 开工时先做四件事：`/rename rl-hub`；读这份文档全文；`git log --oneline -20` 加 `git status`；读 `sync-inbox.md` 有没有待处理的。铁律和 part session 一样：只搬运不发明；改任何规矩都要能对回十一条原则；两处不一致不自己裁；摩擦那一节永远不动；每份的固定结构不动；gyb 不在场时不猜他的意思，把问题攒着等他。

## 四点五、总体关联：谁是什么的唯一定义处，谁引用谁

传播的时候先按这张表判「这条改动该落在哪一份」：一样东西只有一份 part 是定义处，其余都是引用；改动从定义处出发往引用处传，引用处的 part session 裁了定义处的东西，统筹 session 先把裁决搬回定义处，再从定义处往所有引用处传。

| 东西 | 定义处 | 引用处 |
|---|---|---|
| 十一条原则、九条裁决、（a）到（i）、这套文档的索引 | `00-overview.md` | 全部（`05`、`24` 把「阈值表归哪一份」也指到 `00` 的索引去查） |
| gyb 的身份规矩（裸终端就是 gyb、actor 判定、`--as-gyb` 加 `--quote`、`--force --reason`、豁免范围、grants 只收裸终端——2026-08-17 gyb 裁这一组定义处归 `01`，`05`「actor 怎么定」是命令行写法算写了两遍）、gyb use case 表、`rl status` 十段、推送表与 `rl notify`（2026-08-17 gyb 裁：一件事，归 `01`；`05` 只留签名行）、定期提醒 | `01-gyb.md` | 全部除 `08`（原表只列 `05`、`04`、`14`、`25`；核下来其余十六份的接口一节都指了它） |
| 决定账：行格式、来源三类、版本、root_id、stale、reissue、`rl decision` | `02-decisions.md` | `03`（只指过去）、`07`、`09`、`11`、`20`、`23`、`25`。原表列的 `10`、`14`、`21` 把决定字段（`root_id`、`quote`、`actor`、`sources`）指到了 `03`，`13`、`22` 接口一节没提决定账，`05` 把 `rl decision` 五条子命令「新版本还是新条」的判据指到 `10`：这五处指错了定义处，同步时以 `02` 为准 |
| 九本账公共骨架、七本账行格式（issues、runs、grants、feedback、evaluations、sessions、scratch）、退出码 | `03-ledgers.md` | 全部；`09` 抄了 issues/grants/feedback 三本的规则、`07` 抄了 scratch、`04` 抄了 sessions——这四处是同一件事写了两遍，改一处必改另一处。另外 `10`、`11`、`12`、`13`、`20`、`21`、`22` 把 handoffs 的字段指到 `03`，而 `03` 自己说 handoffs 字段定义在 `04`：handoffs 行格式的定义处按本表是 `04` |
| handoffs 行格式、七个状态、holder 不变量、转移表、dispatch、交付物、销号、reclaim、三种通知、sessions 账 | `04-handoffs-and-sessions.md` | 全部除 `08`（原表只列九份；核下来 `00`、`01`、`02`、`03`、`06`、`07`、`09`、`14`、`23`、`25` 也引了它） |
| `bin/rl` 每条子命令的签名和「谁能调」、actor 判定的命令行写法、inbox、trace、doctor 十九项、`rl notify --text` 的签名行（推送表与机制归 `01`）、锁 | `05-rl-cli.md` | 全部；`04` 里 reclaim/session、`02` 里 decision、`07` 里 ql/scratch、`09` 里 feedback/issue/grant 各抄了自己那几条的签名。`05` 自己的接口一节把子命令背后的判据往角色 part 指：`rl decision` 指 `10`（应为 `02`）、`rl eval` 四态指 `13`、`rl handoff estimate` 与看门狗指 `12`、`rl session focus` 指 `14`、`rl ql` 指 `07`、`rl init` 与宿主模板指 `08`、`rl feedback accept` 指 `09` |
| 三层约束、钩子拦放、CLAUDE.md 三句、五份角色 json 四栏、test_skill_refs、读的纪律、会话状态文件 | `06-hooks-and-permissions.md` | 全部（原表只列五份角色和 `08`；五份角色 part 各抄了自己那份 json，`08` 抄了 CLAUDE.md 三句） |
| 快车道进出、scratch 三态、ql 命令、补单规矩、`ql_tag` | `07-quick-lane.md` | 全部除 `14`、`24`、`25`（原表只列 `11`、`13`、`20`、`21`、`23`） |
| init 建什么、research-loop.json 的键、阈值表、插件树、入口 skill、宿主对接、宿主命令模板 | `08-trees-init-and-host.md` | `00`、`01`、`03`、`05`、`06`、`07`、`09`、`10`、`11`、`12`、`13`、`21`、`22`。阈值表的定义处是这一份，但各 part 指法散：`04` 指「`05` 指到的施工计划第八节」、`14` 指「`05` 对应的配置表」、`21` 指 `30`、`23` 直接指施工计划第八节、`05` 和 `24` 指 `00` 的索引；同步时以 `08` 为准 |
| common/ 四文件、公共规矩八条、rules_version、feedback 流程、issues 九种 kind 与 reply/close、grants | `09-common-and-feedback.md` | `00`、`01`、`03`、`05`、`06`、`08`、`10`、`11`、`12`、`13`、`14`、`24`；issues 的 kind 在 `04`、`06`、`10`、`21`、`22`、`23`、`24` 都指到 `03`（`03` 与 `09` 写了两遍，见判断规矩 2） |
| 角色的职责、use case 表、模型 | `10` 到 `14` 各自 | 对应的角色对 part 和 `06`（json 四栏是从 use case 表倒推的，use case 改了 json 必改）。底座几份也指进角色 part 要具体走法：`03`、`04`、`05`、`07`、`08`、`23` 指 `12`（看门狗、estimate、runs 两版、smoke 日志）；`03`、`05`、`07`、`22`、`24` 指 `13`（口径两类四态、先问再提）；`04`、`06`、`07`、`08`、`24` 指 `11`（报告两份、宿主纪律、正式重跑）；`05`、`08`、`09`、`22`、`25` 指 `10`（read:notes 申请、决定判据）；`05`、`06`、`09`、`13` 指 `14`（focus、读顺序、清单五栏） |
| 一条通道上的字段、状态走法、issue 往返 | `20` 到 `25` 各自 | 通道两头的角色 part。角色对之间也互相引用：`20` 指 `21`（发射单继承）、`22`（分析单）、`25`（reviewer 审 code_paths）；`21` 指 `20`、`23`、`25`；`23` 指 `21`、`22`；`24` 指 `22`；`25` 指 `20`；底座里 `03` 指 `22`，`04` 指 `20`、`21`、`22`，`09` 指 `21` |
| 待验证十一条、测试十七条、施工步骤 0 到 8 | `30-build-steps-verify-tests.md`（没写成） | `00` 到 `09`、`11`、`12`、`13`、`21`；`04` 和 `12` 明说待验证第 5、8、9、10 条不成立会改它们的正文 |

三条判断规矩：
1. 角色对 part（`20` 到 `25`）里定的东西，只要是字段、状态、命令，定义处永远在 `03`、`04`、`05` 之一，角色对 part 只是「这条通道用了哪些」；同理角色 part 里的 json 四栏定义处在 `06`。
2. 上表里标了「写了两遍」的五处（`03` 对 `09`/`07`/`04`、`05` 对 `04`/`02`/`07`/`09`、`06` 对五份角色、`06` 对 `08`、`01` 对 `05` 的 `rl status` 十段——README 里 `01` 和 `05` 的覆盖栏都写着 status 十段、`01` 第二节对 `05`「actor 怎么定」、`03` 的「锁与写序」和退出码表对 `05` 的照抄、`04` 第八节 reclaim 处置对 `05`「rl reclaim」一节、`08` 阈值默认值对 `04`/`05` 印的那几份——2026-08-17 三份互查后加），每次同步结束都要各对一遍，两边一字不差。
3. 一条改动找不到定义处（比如新加一个字段、新加一条子命令），先问 gyb 归哪一份，不自己定。

首次核对（2026-08-16 夜，统筹 session）：上表是把 21 份的「和别的 part 的接口」一节全读一遍之后改出来的，改动都是加引用处和记「part 指错了定义处」，没有动定义处那一栏。核对时发现三样东西各 part 指的定义处不一致、原表也没说死，攒着等 gyb 裁：
- actor 判定、`--as-gyb` 加 `--quote`、`--force --reason`：`02`、`10` 指 `01`，`03`、`06` 指 `05`，`04`、`14`、`22` 指 `01` 和 `06`。原表把「gyb 的身份规矩」归 `01`、命令签名归 `05`、钩子归 `06`，按这个分法三处各占一段。——2026-08-17 gyb 裁：归 `01`（原话「这个归01吧」）；`03`、`06`、`14`、`22` 的指向已改，`04`、`05` 在 part session 手上已交代；`05`「actor 怎么定」列进写了两遍。
- 推送表和 `rl notify`：`07`、`10`、`11`、`20`、`25` 把推送表指到 `01`，`09`、`22`、`23`、`24` 指到 `05`。原表把推送表归 `01`、notify 归 `05`。——2026-08-17 gyb 裁：一件事，归 `01`（原话「算一件事」「给rl notify指到01吧」）；`09`、`22`、`23`、`24` 的指向已改，`05` 缩成一句指 `01` 交 rl-part-05。
- 产物目录约定 `<artifact_root>/<run_id>/`：2026-08-17 gyb 裁归 `08` 第一节（原话「问题4 给8」）；`03`、`12`、`21`、`23` 只引。
- 「closed 会话再写账拒收」入账校验：2026-08-17 gyb 裁归 `03`「账本的总规矩」（原话「问题5 给3」）；`04` 第七节那句只引，交 rl-part-04。
- 阈值表：见 `08` 那一行。——2026-08-17 gyb 裁：照 `08`（原话「按照08吧」）；`14`、`21`、`23`、`24` 指向已改，`04` 已指 `08`，`05` 交 rl-part-05。

以后每次同步遇到表外的引用也照此补进表里。

## 五、还悬着的、统筹 session 要记着的

- 施工计划第一节末尾（a）到（i）九条是我改的设计、gyb 还没裁；哪一份 part 的裁决碰到它们，就等于 gyb 裁了那一条，同步的时候把结果回写到施工计划第一节那一条后面标日期。
- 五处两份源文档互相不一致，各 part 已标出、等 part session 裁：决定行落哪个文件三处口径（`02`）；发射单引不引决定（`21`、`02`）；快车道数字进不进 runs（`07`、`23`）；actual_seconds 谁算（`21`、`05`——2026-08-17 已裁：rl run finish 算、handoff done 不带参数，03/04/05 都改了）；reviewer 只由 gyb 手动开还是也能当 subagent（`14`）。哪份先裁到，统筹 session 负责把另一份改成一样。「快车道数字进不进 runs」03 定稿里两句并存（第 104 行 vs 第 213 行），已立为 sync-inbox 问题 22。
- 覆盖检查没跑成：两份源文档每一段是不是都落进了某个 part 没有机器核过。统筹 session 手头空的时候可以做：把两份源文档从头过一遍，每段在某个 part 里找到落点，找不到的补进最合适的 part 并在 README 里记一笔。
- `30-build-steps-verify-tests.md` 没写。
- 第三轮模拟跑不跑、跑几个场景，gyb 定；跑的话 workflow 脚本在会话目录里（`rl-scenario-simulation-r2-wf_0b204aaa-355.js`），场景清单和 schema 可以直接复用，模型一律显式写 opus。
- 用量：subagent 一次派 22 个会撞月度上限，上一轮 22 个里 13 个报错（文件都写完了只是最后一步没返回）；再派一律六个以内。

## 六、跟这件事有关的工程规矩（从 new1 的 CLAUDE.md 和记忆里抄的，统筹 session 也要守）

- 动手前取得同意，一个请求只做一件事；一轮只问一个问题。
- 派 subagent 一律显式写模型，默认 sonnet 或 opus，不用 fable；插件里 idea 和 reviewer 用 fable 是 gyb 2026-08-16 点名的例外，只对插件运行时生效，不对派去写文档的 subagent 生效。
- 写作按 humanizer-gyb：白话、断言、一物一名（术语用施工计划第二节词表的英文名）、事实和解读分开、不用 emoji、不堆粗体。
- 不读 `research-loop/` 目录下的旧代码（已退役会误导），不碰 `/home/y-guo/ACL2026`。
- 提交前 `git status` 看清楚，只 add 这次改的文件；commit message 前缀按上面两种。

## 七、2026-08-17 夜的交接：三份基础 part 定稿之后，统筹换地方接着干

上一个统筹 session 叫 `rl-hub`（ListAgents 里的名字），做到这里停。新的统筹 session 从这一节接手，先读这一节，再按第四节干。

### 现在的状态（HEAD 见 `git log`，前缀 `research-loop sync:` 的都是统筹的 commit）

- 已定稿三份：`03-ledgers.md`（`a5d05d4` 起）、`04-handoffs-and-sessions.md`（`130ec90` 起，最新 `bc9bc90`）、`05-rl-cli.md`（`656c8a9` 起，最新 `c2f69c8`）。三份定稿时报的同步事项全部传完并标已同步（03 六条、04 五条、05 九条），传到了 `01`/`02`/`07`/`08`/`09`/`12`/`14`/`20`/`21`/`22`/`23`/`24` 和两份源文档，源文档文末各有一节「按 part 裁决回写」逐条记着。
- 五个定义处问题（sync-inbox 第一段问题 1–5）gyb 都裁了并落地：actor/`--as-gyb --quote`/`--force --reason` 归 `01` 第二节；推送表和 `rl notify` 一件事归 `01` 第五节；阈值表归 `08` 第三节；产物目录约定 `<artifact_root>/<run_id>/` 归 `08` 第一节；closed 会话再写账拒收归 `03`「账本的总规矩」。四点五节的表和备注已改。
- 三份互查做过一轮（四路 opus 审读、统筹逐条对原文核）：引用处没跟定义处的都已改（03 统筹改，04/05 交各自 session 改完）；真空白和两说 23 条立成 sync-inbox 问题 7–29 等 gyb。结论：三份之间没有两条规矩正面相反的地方，全是引用滞后、措辞不齐、边界空白。
- 其余 18 份未开（`00`、`01`、`02`、`06`–`14`、`20`–`25`），`30` 未写（README 那行给它留了 05 定稿带出的三条）。
- 备案：「gyb」当机器标识符要改成 `admin`/`sudo` 之类，gyb 定全部定稿后最后一次扫，只改机器看得见的四种（`--as-gyb`、actor/owner/assignee 取值 `gyb`、`decisions.gyb.jsonl`、`01-gyb.md` 文件名），白话里指人的不动。

### 等 gyb 裁的（全在 `sync-inbox.md`，别处不再抄）

- 第一段问题 6：reviewer 查出的问题是 `rl issue open` 落账还是只写清单交 gyb（05 定稿裁的和 `14` 第八节、公共规矩第 6 条冲突）。裁了改 `14`（已写进第一节并标冲突）、`09` 问题清单那行、公共规矩第 6 条、可能 `05` 第 216 行。
- 末段问题 7–27（三份互查挖出的边界空白）和 28–29（rl-part-05 定的两处，待过目）。每条写清了在哪两行、差在哪、选项是什么。裁一条落一条：按定义处改，再传引用处，04 的转 rl-part-04、05 的转 rl-part-05（两个 session 若还活着；不在了统筹自己改，见第四节规矩），03 的统筹直接改，两份源文档对应句子回写并加回写行。
- 问 gyb 的规矩：一轮问一个；gyb 说「一口气给」才成批。gyb 已经问过一次「现在有什么要我决定的」，把 inbox 里状态是「等 gyb」的按编号顺着往下问就行。

### part session 的现状

- `rl-part-04`、`rl-part-05` 2026-08-17 夜还活着（04 曾断过一次、gyb 复活的；同名新会话）。给它们发消息用 SendMessage 到 `rl-part-04` / `rl-part-05`；它们发回来的会到统筹 session 的名字上——**新统筹 session 起来后要先告诉它们新名字**（它们现在只认 `rl-hub`），或者让它们改写 `sync-inbox.md`（Monitor 盯着这个文件的 md5 就能收到）。
- 04、05 定稿了但 session 没关，是为了收后续裁决；每份定稿之后统筹再动它，仍按第四节规矩先发消息不直接改，session 没了才直接改。
- 别的 part session 还没开。开的时候 gyb 贴三点五节那段话，统筹在 README 状态栏记「进行中（rl-part-NN）」。

### 新统筹 session 起手要做的

1. `git status` 看清、`git log --oneline -15` 过一遍最近的 sync commit；读 `sync-inbox.md` 全文（五段，最后一段是互查）。
2. 起一个 Monitor 盯 `plans/research-loop-parts/sync-inbox.md` 的变化（上一个用的是 md5 轮询），part session 写信箱就能收到。
3. ListAgents 看 `rl-part-04`、`rl-part-05` 在不在，在就发一条「统筹换成 <新名字>，以后发这里」。
4. 接着问 gyb 问题 6、7、8……一条一条落。
5. 空的时候做第五节还悬着的：覆盖检查、`30` 未写。

### 这一晚踩过的坑（新统筹别再踩）

- 复合命令里 `cd` 到已经在的目录会失败、后半截静默不跑，commit 就漏了改动：一律用仓库根相对或绝对路径，改完 grep 一眼再 commit。
- 定稿裁决要传的时候，写了两遍的地方（03 sessions 对 04、05 签名对 04、锁与写序和退出码表对 05、reclaim 处置对 05、阈值默认值对 04/05、01 第二节对 05「actor 怎么定」、01 status 十段对 05）每次都要各对一遍，一处漏了下一轮互查就翻出来（这次漏的是 sessions amend 版没进 04）。
- part session 报的「要同步到别处的」写的是它看得见的目标；统筹要自己再查两份源文档和写了两遍清单，它们看不见的地方常常也要动。
- 派审读的 subagent 一律显式写 model（opus 审、sonnet 找行），不用 Fable；审读的每一条统筹都要对着原文再核一遍才能报 gyb，这次四路 58 条核下来都属实，但分类（传丢 / 真矛盾 / 不是问题）是统筹做的判断，别让 subagent 替你分。

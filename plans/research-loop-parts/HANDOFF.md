# research-loop 设计定稿：交接文档（2026-08-16 夜写，给「统筹 session」和「part session」用）

这份文档写给两种会话读：一种是 gyb 开的「统筹 session」，只开一个，常驻，读了这份就接管全局：管哪份 part 在谁手上、把各份定稿的改动传播到别的 part 和两份源文档、收 part session 报上来的同步事项、攒问题等 gyb 裁；另一种是 gyb 开的「part session」，可以同时开很多个，一个只定稿一份 part，遇到牵连别处的事不自己动手，回报给统筹 session。写这份文档的会话到此结束，gyb 不再回来，之后的一切以这份和目录里的文件为准。

## 一、现在的状态

research-loop 是 new1 工程里的一个 Claude Code 插件，设计已经收口但代码一行没写（旧代码按裁决整体退役，见 `00-overview.md` 第三节裁决 1）。设计写在两份源文档里，然后被拆成这个目录下的 22 份 part，其中 21 份写成了：

- 设计文档 `plans/2026-08-16-research-loop-next-steps.md`：做成什么样、为什么，十一条设计原则在它第二节。
- 施工计划 `plans/2026-08-16-research-loop-build-plan.md`：字段、转移表、命令表、阈值、待验证、测试、施工步骤。可枚举的东西只在它里面写一遍。
- 两轮模拟的原始摩擦：`plans/2026-08-16-research-loop-simulation-round1.md`（59 条，核实过）、`plans/2026-08-16-research-loop-simulation-round2.md`（234 条，没核实过）。
- 这个目录 `plans/research-loop-parts/`：`README.md` 是索引和每份留给 gyb 的点，`00` 到 `25` 是 21 份 part，`30-build-steps-verify-tests.md` 没写成。

全部已 commit：`601f835`。工作树干净。（2026-08-17 夜的状态见第七节；2026-08-17 晨问题 6–29 裁完待落地的状态见第八节，以第八节为准。）

拆分的写法铁律是只搬运不发明：源文档怎么写就怎么写；两处不一致的在 part 里标「两处原文不一致」不裁；题目里缺的写进每份末尾「源文档没写清的」；每份最后一节把第二轮摩擦原样抄了一遍。所以现在 21 份 part 加起来的内容等于两份源文档，外加每份 agent 自己发现的没写清的点。

## 二、每份 part 的固定结构（part session 改的时候要保持）

```
# 标题
> 覆盖什么、不覆盖什么；源：设计文档哪几节、施工计划哪几节
## 正文（若干小节）
## 和别的 part 的接口
## 源文档没写清的（留给 gyb）
## 第二轮模拟里归到这一份的摩擦（原样，未核实）
```

part session 定稿之后会多两节：

```
## 裁决记录（日期）
## 要同步到别处的
```

「裁决记录」一行一条带日期，是 gyb 说的原话或原意；「要同步到别处的」一行一条，写清目标文件名和要改成什么。摩擦那一节永远原样保留。

## 三、统筹 session 和 part session 怎么通话

统筹 session 开起来之后第一件事 `/rename rl-hub`，part session 开起来第一件事 `/rename rl-part-<编号>`（比如 `rl-part-03`），名字定了两边才找得到对方。同一台机器上的 Claude Code 会话互相能看见：用 `ListAgents` 列出来，用 `SendMessage` 按名字发消息（收件人名字照 `ListAgents` 那一行原样抄）。part session 有事回报就 `SendMessage` 给 `rl-hub`；统筹 session 有话交代就 `SendMessage` 给 `rl-part-<编号>`。

消息发不出去（对方还没开、`ListAgents` 里没有）就走文件邮箱：往 `plans/research-loop-parts/sync-inbox.md` 末尾追加一段，格式固定：

```
## <日期 时间> 来自 <会话名> 关于 <part 文件名>
- 事项：<一句话>
- 裁决原文：<gyb 说的>
- 要改的地方：<目标文件名>：<哪一句改成什么>
- 状态：待处理
```

统筹 session 每次开工先读 `sync-inbox.md`，处理完把那段的状态改成「已处理 <日期>」。消息和邮箱两条路的内容一样，邮箱是兜底，不是可选的第二份：发了消息也要往邮箱补一段，这样会话崩了记录还在。

part session 要回报的三种事：（1）一条裁决动到了别的 part 或两份源文档里的东西（字段、状态、命令、接口一节列的），把裁决原文和要改的目标句子报上去，自己不去改；（2）自己这份里的一句和别的 part 打架，两边原文报上去等裁，不自己定；（3）这份过完了，报一句「`<文件名>` 定稿，commit `<hash>`」。回报之后不等回音接着往下过；统筹 session 有回话（比如「这条别处已经裁成另一个值」）再照回话改。

统筹 session 要交代给 part session 的两种事：（1）它手上那份 part 里的某一句，因为别的 part 先裁了，现在要改成什么，附来源 part 的裁决原文；（2）它报上来的打架 gyb 裁了，结果是什么。

## 三点五、part session 怎么开（gyb 贴给每个 part session 的话）

> 先 `/rename rl-part-<编号>`。读 `plans/research-loop-parts/HANDOFF.md` 第二、三、四点五节，再全文读 `plans/research-loop-parts/<文件名>`。这份是 research-loop 插件设计的一个部分，我要把它逐条定稿。规矩：只在这一个文件里改，别的 part 和两份源文档不动；文件末尾「第二轮模拟里归到这一份的摩擦」那一节原样保留不改；十一条设计原则在 `00-overview.md` 第二节，改任何一条规矩都要能对回其中一条。先从「源文档没写清的」那一节开始，一条一条问我要裁决，一次只问一个问题，问完等我答，答了就把正文对应的那一处改掉、把那条从「没写清」里删掉；正文里标了「两处原文不一致」的地方同样一处一处问我。我说的每一条裁决都追加到文件末尾新开的「裁决记录（日期）」一节里，一行一条带日期。改动牵连别的 part 的（字段名、命令、状态、接口一节里列的东西），不去改那边：在文末新开的「要同步到别处的」一节里列一条，同时按 HANDOFF 第三节 `SendMessage` 报给 `rl-hub` 并往 `sync-inbox.md` 追加一段。收到 `rl-hub` 的消息照它说的改。全部过完之后把「和别的 part 的接口」一节按定稿重写一遍，commit（前缀 `research-loop parts:`），再报 `rl-hub` 一句定稿。

建议顺序：先 `03-ledgers.md`、`04-handoffs-and-sessions.md`、`05-rl-cli.md`（底座，角色和角色对那十一份的字段、状态、命令全指向它们），再 `02-decisions.md`、`06-hooks-and-permissions.md`，再角色五份 `10` 到 `14`，最后角色对六份 `20` 到 `25` 和 `07`、`08`、`09`。同时开的几份尽量别互为定义处和引用处（第四点五节的表），底座三份最好先过完再开别的。`30` 那份等底座定稿之后再写，源在施工计划第九、十、十一节。

## 四、统筹 session 干什么

统筹 session 常驻，干四件事：

1. 记谁在干什么。维护 `README.md` 进度表里每份的状态（未开、在 `rl-part-NN` 手上、已定稿 commit 几号），part session 报定稿就更新。
2. 收回报、传播。收到 part session 的消息或 `sync-inbox.md` 里的待处理段，先按第四点五节的表判定义处：定义处在别的 part，就把裁决搬回定义处那一份（改它的正文并补进它的「裁决记录」，注明来自哪份），再从定义处往所有引用处传。两份源文档不再回写（2026-08-18 gyb 裁，`00` 定稿：拆分文档定稿为准，源文档开头已加「已被取代、只留作历史」一句；此前「源文档改正文那一句并补一行回写记录」的做法到 2026-08-17 为止）。目标 part 正在某个 `rl-part-NN` 手上的，不直接改它的文件，`SendMessage` 交代给那个 session 去改（避免两个会话同时写一个文件），它不在了才自己改。传播完把来源 part 里那条标「已同步 <日期>」，把邮箱那段标「已处理」，commit 前缀 `research-loop sync:`。
3. 攒问题等 gyb。传播时发现目标 part 已经有相反的裁决，或者 part session 报上来的打架，不自己裁：把两边裁决原文并排记进 `sync-inbox.md` 一段（状态「等 gyb」），gyb 来了一次问一个，裁了再改并回话给相关的 part session、补进两份 part 的「裁决记录」。
4. 补漏。第一次开工先按第四点五节末尾那步核关联表；空下来的时候做覆盖检查（第五节）。

统筹 session 开工时先做四件事：`/rename rl-hub`；读这份文档全文；`git log --oneline -20` 加 `git status`；读 `sync-inbox.md` 有没有待处理的。铁律和 part session 一样：只搬运不发明；改任何规矩都要能对回十一条原则；两处不一致不自己裁；摩擦那一节永远不动；每份的固定结构不动；gyb 不在场时不猜他的意思，把问题攒着等他。

## 四点五、总体关联：谁是什么的唯一定义处，谁引用谁

传播的时候先按这张表判「这条改动该落在哪一份」：一样东西只有一份 part 是定义处，其余都是引用；改动从定义处出发往引用处传，引用处的 part session 裁了定义处的东西，统筹 session 先把裁决搬回定义处，再从定义处往所有引用处传。

| 东西 | 定义处 | 引用处 |
|---|---|---|
| 十一条原则、九条裁决、（a）到（i）、这套文档的索引 | `00-overview.md` | 全部（`05`、`24` 把「阈值表归哪一份」也指到 `00` 的索引去查） |
| gyb 的身份规矩（裸终端就是 gyb、actor 判定、`--as-gyb` 加 `--quote`、`--force --reason`、豁免范围、grants 只有 gyb 能写（裸终端直接写，角色会话里 `--as-gyb --quote` 替 gyb 写也收，2026-08-17 问题 27 裁）——2026-08-17 gyb 裁这一组定义处归 `01`，`05`「actor 怎么定」是命令行写法算写了两遍）、gyb use case 表、`rl status` 十段、推送表与 `rl notify`（2026-08-17 gyb 裁：一件事，归 `01`；`05` 只留签名行）、定期提醒 | `01-gyb.md` | 全部除 `08`（原表只列 `05`、`04`、`14`、`25`；核下来其余十六份的接口一节都指了它） |
| 决定账：行格式（含 `op` 字段，2026-08-18 加）、按编号前缀落文件、来源三类、版本、root_id、过版判定（跳过 confirm 版）、stale、reissue、`rl decision`（retire 必带理由，2026-08-18 `09` 裁；理由落哪一栏等问题 36） | `02-decisions.md` | `03`（只指过去）、`07`、`09`、`11`、`20`、`23`、`25`。原表列的 `10`、`14`、`21` 把决定字段（`root_id`、`quote`、`actor`、`sources`）指到了 `03`，`13`、`22` 接口一节没提决定账，`05` 把 `rl decision` 五条子命令「新版本还是新条」的判据指到 `10`：这五处指错了定义处，同步时以 `02` 为准 |
| 九本账公共骨架（可选栏 `force_reason`、`via`）、七本账行格式（issues、runs——三版 launched/finished/adopted、grants、feedback、evaluations、sessions、scratch）、退出码六个（0/1/2/3/4/5）与原因种类 | `03-ledgers.md` | 全部；`09` 抄了 issues/grants/feedback 三本的规则、`07` 抄了 scratch、`04` 抄了 sessions——这四处是同一件事写了两遍，改一处必改另一处。另外 `10`、`11`、`12`、`13`、`20`、`21`、`22` 把 handoffs 的字段指到 `03`，而 `03` 自己说 handoffs 字段定义在 `04`：handoffs 行格式的定义处按本表是 `04` |
| handoffs 行格式（含 `ql_tag`、`adopted`、`attempts[].actual_seconds`——2026-08-17 问题 8/17/13 加）、七个状态、holder 不变量、转移表（含 estimate 行）、dispatch、交付物、销号、reclaim、三种通知、sessions 账 | `04-handoffs-and-sessions.md` | 全部除 `08`（原表只列九份；核下来 `00`、`01`、`02`、`03`、`06`、`07`、`09`、`14`、`23`、`25` 也引了它） |
| `bin/rl` 每条子命令的签名和「谁能调」、actor 判定的命令行写法、inbox、trace、doctor 十九项、`rl notify --text` 的签名行（推送表与机制归 `01`）、锁 | `05-rl-cli.md` | 全部；`04` 里 reclaim/session、`02` 里 decision、`07` 里 ql/scratch、`09` 里 feedback/issue/grant 各抄了自己那几条的签名。`05` 自己的接口一节把子命令背后的判据往角色 part 指：`rl decision` 指 `10`（应为 `02`）、`rl eval` 四态指 `13`、`rl handoff estimate` 与看门狗指 `12`、`rl session focus` 指 `14`、`rl ql` 指 `07`、`rl init` 与宿主模板指 `08`、`rl feedback accept` 指 `09` |
| 三层约束、钩子拦放（含路径怎么判与白名单，2026-08-18 加；白名单配置项在 `08`；钩子管 Write/Edit/Bash 三个工具、一份插件级钩子文件按 `agent_type` 再状态文件判角色、`agents/` 层五份角色 agent 定义只塑形——2026-08-18 `08` 第 8 条实测后裁，落 `06`）、CLAUDE.md 三句、五份角色 json 四栏（`reads` 两种写法、`dispatches_to` 机器不查）、test_skill_refs 三样都查、读的纪律、会话状态文件（销号删、doctor 扫）、两句 SKILL.md 纪律 | `06-hooks-and-permissions.md` | 全部（原表只列五份角色和 `08`；五份角色 part 各抄了自己那份 json，`08` 抄了 CLAUDE.md 三句） |
| 快车道进出、scratch 三态、ql 命令、补单规矩、`ql_tag` | `07-quick-lane.md` | 全部除 `14`、`24`、`25`（原表只列 `11`、`13`、`20`、`21`、`23`） |
| init 建什么（幂等、逐项问、播 analysis 三样、不碰宿主代码）、research-loop.json 的键（含 `launcher.abort_cmd`、`repo_run`、`host_ledgers`，2026-08-18 加；账路径钉死不进配置）、阈值表、插件树（含 `agents/` 层，不建 `workflows/`）、入口 skill、宿主对接、宿主命令模板、待验证第 8 条的实测事实 | `08-trees-init-and-host.md` | `00`、`01`、`03`、`05`、`06`、`07`、`09`、`10`、`11`、`12`、`13`、`21`、`22`。阈值表的定义处是这一份，但各 part 指法散：`04` 指「`05` 指到的施工计划第八节」、`14` 指「`05` 对应的配置表」、`21` 指 `30`、`23` 直接指施工计划第八节、`05` 和 `24` 指 `00` 的索引；同步时以 `08` 为准 |
| common/ 五文件（含 `REVIEW-CHECKLIST.md`，2026-08-18 定名）、`SPEC-TEMPLATE.md` 五栏是 SKILL.md 骨架、公共规矩八条与 `rule-NN` 编号不变、rules_version（整数、`GLOBAL-RULES.md` 头部一行唯一真源、accept 写回）、feedback 流程（`target` 两种、`rejected` 终态）、issues 九种 kind 与 reply/close、grants（revoke 列会话 gyb 挑） | `09-common-and-feedback.md` | `00`、`01`、`03`、`05`、`06`、`08`、`10`、`11`、`12`、`13`、`14`、`24`；issues 的 kind 在 `04`、`06`、`10`、`21`、`22`、`23`、`24` 都指到 `03`（`03` 与 `09` 写了两遍，见判断规矩 2） |
| 角色的职责、use case 表、模型 | `10` 到 `14` 各自 | 对应的角色对 part 和 `06`（json 四栏是从 use case 表倒推的，use case 改了 json 必改）。底座几份也指进角色 part 要具体走法：`03`、`04`、`05`、`07`、`08`、`23` 指 `12`（看门狗、estimate、runs 两版、smoke 日志）；`03`、`05`、`07`、`22`、`24` 指 `13`（口径两类四态、先问再提）；`04`、`06`、`07`、`08`、`24` 指 `11`（报告两份、宿主纪律、正式重跑）；`05`、`08`、`09`、`22`、`25` 指 `10`（read:notes 申请、决定判据）；`05`、`06`、`09`、`13` 指 `14`（focus、读顺序、清单五栏） |
| 一条通道上的字段、状态走法、issue 往返 | `20` 到 `25` 各自 | 通道两头的角色 part。角色对之间也互相引用：`20` 指 `21`（发射单继承）、`22`（分析单）、`25`（reviewer 审 code_paths）；`21` 指 `20`、`23`、`25`；`23` 指 `21`、`22`；`24` 指 `22`；`25` 指 `20`；底座里 `03` 指 `22`，`04` 指 `20`、`21`、`22`，`09` 指 `21` |
| 待验证十一条、测试十七条、施工步骤 0 到 8 | `30-build-steps-verify-tests.md`（没写成） | `00` 到 `09`、`11`、`12`、`13`、`21`；`04` 和 `12` 明说待验证第 5、8、9、10 条不成立会改它们的正文 |

三条判断规矩：
1. 角色对 part（`20` 到 `25`）里定的东西，只要是字段、状态、命令，定义处永远在 `03`、`04`、`05` 之一，角色对 part 只是「这条通道用了哪些」；同理角色 part 里的 json 四栏定义处在 `06`。
2. 上表里标了「写了两遍」的五处（`03` 对 `09`/`07`/`04`、`05` 对 `04`/`02`/`07`/`09`、`06` 对五份角色、`06` 对 `08`、`01` 对 `05` 的 `rl status` 十段——README 里 `01` 和 `05` 的覆盖栏都写着 status 十段、`01` 第二节对 `05`「actor 怎么定」、`03` 的「锁与写序」和退出码表对 `05` 的照抄、`04` 第八节 reclaim 处置对 `05`「rl reclaim」一节、`08` 阈值默认值对 `04`/`05` 印的那几份——2026-08-17 三份互查后加），每次同步结束都要各对一遍，两边一字不差。
3. 一条改动找不到定义处（比如新加一个字段、新加一条子命令），先问 gyb 归哪一份，不自己定。

首次核对（2026-08-16 夜，统筹 session）：上表是把 21 份的「和别的 part 的接口」一节全读一遍之后改出来的，改动都是加引用处和记「part 指错了定义处」，没有动定义处那一栏。核对时发现三样东西各 part 指的定义处不一致、原表也没说死，攒着等 gyb 裁：
- actor 判定、`--as-gyb` 加 `--quote`、`--force --reason`：`02`、`10` 指 `01`，`03`、`06` 指 `05`，`04`、`14`、`22` 指 `01` 和 `06`。原表把「gyb 的身份规矩」归 `01`、命令签名归 `05`、钩子归 `06`，按这个分法三处各占一段。——2026-08-17 gyb 裁：归 `01`（原话「这个归01吧」）；`03`、`06`、`14`、`22` 的指向已改，`04`、`05` 在 part session 手上已交代；`05`「actor 怎么定」列进写了两遍。
- 推送表和 `rl notify`：`07`、`10`、`11`、`20`、`25` 把推送表指到 `01`，`09`、`22`、`23`、`24` 指到 `05`。原表把推送表归 `01`、notify 归 `05`。——2026-08-17 gyb 裁：一件事，归 `01`（原话「算一件事」「给rl notify指到01吧」）；`09`、`22`、`23`、`24` 的指向已改，`05` 缩成一句指 `01` 交 rl-part-05。
- 产物目录约定 `<artifact_root>/<run_id>/`：2026-08-17 gyb 裁归 `08` 第一节（原话「问题4 给8」）；`03`、`12`、`21`、`23` 只引。
- 「closed 会话再写账拒收」入账校验：2026-08-17 gyb 裁归 `03`「账本的总规矩」（原话「问题5 给3」）；`04` 第七节那句只引，交 rl-part-04。
- 阈值表：见 `08` 那一行。——2026-08-17 gyb 裁：照 `08`（原话「按照08吧」）；`14`、`21`、`23`、`24` 指向已改，`04` 已指 `08`，`05` 交 rl-part-05。

以后每次同步遇到表外的引用也照此补进表里。

## 五、还悬着的、统筹 session 要记着的

- 施工计划第一节末尾（a）到（i）九条是我改的设计——2026-08-18 已全裁（`00` 定稿：（b）（c）（d）2026-08-17 问题 27 裁，（a）（e）（f）（g）（h）（i）六条 2026-08-18 一并认，不写回退清单，gyb 原话「a」）。
- 五处两份源文档互相不一致，各 part 已标出、等 part session 裁：决定行落哪个文件三处口径（`02`——2026-08-18 已裁：按编号前缀落文件，`actor` 另记）；发射单引不引决定（`21`、`02`——2026-08-18 已裁：发射单从父单抄 `decision_refs`，「不引决定」作废）；快车道数字进不进 runs（`07`、`23`）；actual_seconds 谁算（`21`、`05`——2026-08-17 已裁：rl run finish 算、handoff done 不带参数，03/04/05 都改了）；reviewer 只由 gyb 手动开还是也能当 subagent（`14`）。哪份先裁到，统筹 session 负责把另一份改成一样。「快车道数字进不进 runs」03 定稿里两句并存（第 104 行 vs 第 213 行），已立为 sync-inbox 问题 22——2026-08-17 已裁：不进 runs，`03:104`「快车道用 `ql_tag`」已删（`81377ac`），引用处 rl-hub-v3 已传。
- 覆盖检查没跑成：两份源文档每一段是不是都落进了某个 part 没有机器核过。统筹 session 手头空的时候可以做：把两份源文档从头过一遍，每段在某个 part 里找到落点，找不到的补进最合适的 part 并在 README 里记一笔。
- `30-build-steps-verify-tests.md` 没写。
- 第三轮模拟先不跑，等整套拆分文档定稿、施工完成之后再说（2026-08-18 gyb 裁，`00` 定稿，原话「第三轮先不跑，等整个plan完事，施工完成后再说」）；跑的话 workflow 脚本在会话目录里（`rl-scenario-simulation-r2-wf_0b204aaa-355.js`），场景清单和 schema 可以直接复用，模型一律显式写 opus。
- 用量：subagent 一次派 22 个会撞月度上限，上一轮 22 个里 13 个报错（文件都写完了只是最后一步没返回）；再派一律六个以内。

## 六、跟这件事有关的工程规矩（从 new1 的 CLAUDE.md 和记忆里抄的，统筹 session 也要守）

- 动手前取得同意，一个请求只做一件事；一轮只问一个问题。
- 派 subagent 一律显式写模型，默认 sonnet 或 opus，不用 fable；插件里 idea 和 reviewer 用 fable 是 gyb 2026-08-16 点名的例外，只对插件运行时生效，不对派去写文档的 subagent 生效。
- 写作按 humanizer-gyb：白话、断言、一物一名（术语用施工计划第二节词表的英文名）、事实和解读分开、不用 emoji、不堆粗体。
- 不读 `research-loop/` 目录下的旧代码（已退役会误导），不碰 `/home/y-guo/ACL2026`。
- 提交前 `git status` 看清楚，只 add 这次改的文件；commit message 前缀按上面两种。

## 七、2026-08-17 夜的交接：三份基础 part 定稿之后，统筹换地方接着干

上一个统筹 session 叫 `rl-hub`（ListAgents 里的名字），做到这里停。新的统筹 session 从这一节接手，先读这一节，再按第四节干。

### 现在的状态（HEAD 见 `git log`，前缀 `research-loop sync:` 的都是统筹的 commit）

- 已定稿三份：`03-ledgers.md`（`a5d05d4` 起）、`04-handoffs-and-sessions.md`（`130ec90` 起，最新 `bc9bc90`）、`05-rl-cli.md`（`656c8a9` 起，最新 `c2f69c8`）。三份定稿时报的同步事项全部传完并标已同步（03 六条、04 五条、05 九条），传到了 `01`/`02`/`07`/`08`/`09`/`12`/`14`/`20`/`21`/`22`/`23`/`24` 和两份源文档，源文档文末各有一节「按 part 裁决回写」逐条记着。
- 五个定义处问题（sync-inbox 第一段问题 1–5）gyb 都裁了并落地：actor/`--as-gyb --quote`/`--force --reason` 归 `01` 第二节；推送表和 `rl notify` 一件事归 `01` 第五节；阈值表归 `08` 第三节；产物目录约定 `<artifact_root>/<run_id>/` 归 `08` 第一节；closed 会话再写账拒收归 `03`「账本的总规矩」。四点五节的表和备注已改。
- 三份互查做过一轮（四路 opus 审读、统筹逐条对原文核）：引用处没跟定义处的都已改（03 统筹改，04/05 交各自 session 改完）；真空白和两说 23 条立成 sync-inbox 问题 7–29 等 gyb。结论：三份之间没有两条规矩正面相反的地方，全是引用滞后、措辞不齐、边界空白。
- 其余 18 份未开（`00`、`01`、`02`、`06`–`14`、`20`–`25`），`30` 未写（README 那行给它留了 05 定稿带出的三条）。
- 备案：「gyb」当机器标识符要改成 `admin`/`sudo` 之类，gyb 定全部定稿后最后一次扫，只改机器看得见的四种（`--as-gyb`、actor/owner/assignee 取值 `gyb`、`decisions.gyb.jsonl`、`01-gyb.md` 文件名），白话里指人的不动。

### 等 gyb 裁的（全在 `sync-inbox.md`，别处不再抄）——2026-08-17 晨已全部裁完，见第八节

- 第一段问题 6：reviewer 查出的问题是 `rl issue open` 落账还是只写清单交 gyb（05 定稿裁的和 `14` 第八节、公共规矩第 6 条冲突）。裁了改 `14`（已写进第一节并标冲突）、`09` 问题清单那行、公共规矩第 6 条、可能 `05` 第 216 行。
- 末段问题 7–27（三份互查挖出的边界空白）和 28–29（rl-part-05 定的两处，待过目）。每条写清了在哪两行、差在哪、选项是什么。裁一条落一条：按定义处改，再传引用处，04 的转 rl-part-04、05 的转 rl-part-05（两个 session 若还活着；不在了统筹自己改，见第四节规矩），03 的统筹直接改，两份源文档对应句子回写并加回写行。
- 问 gyb 的规矩：一轮问一个；gyb 说「一口气给」才成批。gyb 已经问过一次「现在有什么要我决定的」，把 inbox 里状态是「等 gyb」的按编号顺着往下问就行。

### part session 的现状

- `rl-part-04`、`rl-part-05` 2026-08-17 夜还活着（04 曾断过一次、gyb 复活的；同名新会话）。给它们发消息用 SendMessage 到 `rl-part-04` / `rl-part-05`；它们发回来的会到统筹 session 的名字上——**新统筹 session 起来后要先告诉它们新名字**（它们现在只认 `rl-hub`），或者让它们改写 `sync-inbox.md`（Monitor 盯着这个文件的 md5 就能收到）。
- 04、05 定稿了但 session 没关，是为了收后续裁决；每份定稿之后统筹再动它，仍按第四节规矩先发消息不直接改，session 没了才直接改。
- 别的 part session 还没开。开的时候 gyb 贴三点五节那段话，统筹在 README 状态栏记「进行中（rl-part-NN）」。

### 新统筹 session 起手要做的

1. `git status` 看清、`git log --oneline -15` 过一遍最近的 sync commit；读 `sync-inbox.md` 全文（五段，最后一段是互查）。
2. 起一个 Monitor 盯 `plans/research-loop-parts/sync-inbox.md` 的变化（上一个用的是 md5 轮询），part session 写信箱就能收到。
3. ListAgents 看 `rl-part-04`、`rl-part-05` 在不在，在就发一条「统筹换成 <新名字>，以后发这里」。
4. 接着问 gyb 问题 6、7、8……一条一条落。
5. 空的时候做第五节还悬着的：覆盖检查、`30` 未写。

### 这一晚踩过的坑（新统筹别再踩）

- 复合命令里 `cd` 到已经在的目录会失败、后半截静默不跑，commit 就漏了改动：一律用仓库根相对或绝对路径，改完 grep 一眼再 commit。
- 定稿裁决要传的时候，写了两遍的地方（03 sessions 对 04、05 签名对 04、锁与写序和退出码表对 05、reclaim 处置对 05、阈值默认值对 04/05、01 第二节对 05「actor 怎么定」、01 status 十段对 05）每次都要各对一遍，一处漏了下一轮互查就翻出来（这次漏的是 sessions amend 版没进 04）。
- part session 报的「要同步到别处的」写的是它看得见的目标；统筹要自己再查两份源文档和写了两遍清单，它们看不见的地方常常也要动。
- 派审读的 subagent 一律显式写 model（opus 审、sonnet 找行），不用 Fable；审读的每一条统筹都要对着原文再核一遍才能报 gyb，这次四路 58 条核下来都属实，但分类（传丢 / 真矛盾 / 不是问题）是统筹做的判断，别让 subagent 替你分。

## 八、2026-08-17 晨的交接：问题 6–29 全部裁完、一条没落地，新统筹从这一节接手

上一个统筹叫 `rl-hub-v2`（第七节那个叫 `rl-hub`），只干了一件事：把 sync-inbox 里等 gyb 的问题 6–29 一条一条问完，裁决原话逐条记在 inbox 那两段里（问题 6 在第一段，7–29 在末段，每条后面「——已裁 2026-08-17（待落地）：……原话「……」」），commit `5401b7b`。**gyb 明说：先不落地，攒齐了一起改**，所以正文一处没动。新统筹接手就是把下面的清单一口气落完。

### gyb 这一轮定的三条工作规矩（以后每一轮都照做）

1. 问裁决只讲逻辑：场景 + 两难 + 白话选项，不带文件名、行号、命令名、字段名。gyb 原话「你给我问题的时候不要提到具体的代码或者是文件名，让我做逻辑上的判断」。落到哪些文件是统筹自己查的事。已存记忆 `ask-gyb-in-logic-not-files`。
2. 一轮问一个，问完等答；答案全收齐再一起改，不要问一条改一条。gyb 原话「先不要问一个修一个，给问题答案收集好一起修」。
3. 讨论期间不开 Monitor（gyb 说吵），落地和空闲时再开；开的话盯 `sync-inbox.md` 的 md5 即可。这一轮两个旧 Monitor（`rl-hub` 留下的和 `rl-hub-v2` 自己起的）都已停。

### part session 的现状

`rl-part-04`、`rl-part-05` 2026-08-17 晨还活着，已经被告知统筹叫 `rl-hub-v2`。**新统筹起来先 ListAgents，再给它们各发一条自报新名字**（它们只认最后一次告知的名字）。gyb 2026-08-17 晨说「你现在就交给04和05让他们改了」，rl-hub-v2 已把下表里标「交 rl-part-04」的十一条和标「交 rl-part-05」的十九条打包发出（inbox 末段记着），**两包都已收完**：04 `a7d1ec9`，05 `1b37593`（inbox 末段记着它们的回话）。05 定的三处 03 要一字不差跟上：锁那句去掉 batch；退出码表六个、种类词 validation/forbidden/lock_timeout/usage/internal；runs 三版 launched/finished/adopted、adopted 由 `rl handoff start` 顺带写不另设子命令；可选栏 `force_reason`、`via` 两个。04 提醒 `20`/`21`/`22` 抄的转移表行（新 estimate 行、start 行 adopted、withdraw 行通知、reissue 行到栏、两条 amend 行）要统筹同步。ListAgents 里没有 04/05 了才自己改它们。

### 落地清单（按定义处排；「引用处」是统筹 grep 到的，落地时每条再 grep 一遍，写了两遍的地方见第七节的坑）

问题号对应 sync-inbox 编号；裁决原话在 inbox，这里只写落法和落点。

| 号 | 裁决一句话 | 定义处要改的 | 引用处与源文档 |
|---|---|---|---|
| 6 | reviewer 查出的问题只写 review/ 清单，gyb 用自己的权限开 issue | `14` 第一节第 15 行那段：删「`rl issue open --to <owner>` 落账」，改成写进 review/ 清单、gyb 看完自己开 issue；删「与第八节冲突」句；裁决记录补一行 | `05:218`（交 rl-part-05）、`05:638`/`05:658` 裁决记录与同步清单里那句加「后改」；`09:17` 表行；`09:356` 裁决记录；设计文档 `:248` 与施工计划 `:332` 回写行各补一句「查出的写清单不开 issue」；`14` 第八节、`09:42`、`25:9` 不动（本来就对） |
| 7 | 填分步表是正式动作，转移表加行 | `04` 转移表加一行：`in_progress` → `in_progress`（内容追加），谁能写 holder（run），前提「只改 `attempts` 最新一次尝试的 `step_table` 与 `estimated_seconds`」，命令 `handoff estimate`；`04:77` 那段补「`estimate` 也是表里的行」（交 rl-part-04） | `05:65` 命令表「按转移表」已覆盖，`05:229` 十二条改十三条（交 rl-part-05）；`20`/`21`/`22` 抄的转移表若抄了 amend 行也补这一行；施工计划第四节转移表加行并加回写行 |
| 8 | 快车道合回先开补单再关杂账；单子上加一栏存快车道标签 | `04:60` 补单行前提「关联的 scratch 行状态是 `merged`」改成「是 `open`」；`04` handoffs 字段表加 `ql_tag`（补单必填，其余空）（交 rl-part-04）；`03:208` scratch `merged` 版 `handoff_id` 必填不变（统筹确认顺序通了即可）；`07` 第七节合回六步顺序改成「开补单 → 拿编号 → `ql close --merged --handoff ID`」 | `05:64` `--ql QL` 现在有字段存了、`05:87` `ql close` 前提（交 rl-part-05）；`11:120`；`07:99` 抄的转移表行；设计文档快车道一节、施工计划第四节那行与测试 16 回写 |
| 9 | batch 调用者自由文本、可选，rl 不分配 | `03:19` 锁那句去掉 `batch`；`04:26` `batch` 栏写「可选，自由文本，调用者传；`launch_order` 从父单抄」（交 rl-part-04） | `05:64`（已是可选，加「自由文本」，交 rl-part-05）；施工计划 `:172` 锁那句同改；`21`/`23` 提到 batch 处 grep 一遍 |
| 10 | amend 放宽跟 doctor 修法走 | `04:62`、`04:66` 两行 amend 允许换 `decision_refs`/`evaluation_refs`；`04:66` 允许改 `code_paths`（交 rl-part-04） | `05:65` amend 签名加 `--decision ID@V`（交 rl-part-05）；`05:191`/`05:194` doctor 修法不动；施工计划第四节表两行回写 |
| 11 | issue 一问一答，不加追问；回答不管用 close 再开新的引旧编号 | `05:200` doctor 第 11 项修法删「追问」半句，改成「回答不管用就 close 并另开一条引旧编号」（交 rl-part-05） | `03:92` 写权四条不动；`09` issues 段若抄了第 11 项同改 |
| 12 | runs 默认列表：最新一次尝试且 ok，失败不出 | `03:124` 改成「每张单最新一次尝试的行，且退出状态是 `ok`；最新一次不是 `ok` 的这张单不出」 | `05:73`（已是这个意思，交 rl-part-05 核）；`23` 若抄了同改；施工计划 runs 段回写 |
| 13 | handoffs 记 `actual_seconds`，rl 从 runs 自动抄 | `04` 字段表 `attempts` 每项加 `actual_seconds`（`rl run finish` 时 rl 填，人不填）；`04:41` 那句改成「handoffs 上的 `actual_seconds` 由 rl 在 `run finish` 时从 runs 抄」（交 rl-part-04） | `03:118` 同句；`21:19`/`21:30` attempts 形状加栏；`12` runs 两版处；设计文档 run 一节回写 |
| 14 | closed 会话再写账退出码 3，任何 `--force` 越不过 | `03:15` 补「退出码 3；`--force`、`--as-gyb --force` 都越不过，唯一出路是重新加载角色」 | `05:112` 退出码 3 那行加「含被销号会话再写账」（交 rl-part-05）；`04` 第七节那句（交 rl-part-04）；`01` 第二节 `--force` 段补一句例外 |
| 15 | `fix_for` 栏删掉，修法命令不带 `--fix-for` | `03:43` 可选栏改成两个：`force_reason`、`via`；`03` 事项 5 那句「七样加两个可选」→「加两个：`force_reason`、`via`」 | `05:212` 删那句（交 rl-part-05）；`04`/`02`/`00`/`09`/`06`/`01`/`14` 里提 `fix_for` 的各处 grep 删；设计文档「账本」一节「七样加两个可选：`fix_for`、`force_reason`」改成「`force_reason`、`via`」；施工计划同 |
| 16 | analysis 快车道没有补单，只能 `--dropped`；要留走正常路重做 | `07` 第七节开头补一句「合回（`--merged`）只对 deploy 开；analysis 的快车道只有 `--dropped`」；`07` 没写清第 5 条销 | `05:87` `ql close` 谁能调（交 rl-part-05）；`13:105`、`13:140`（销）；`11:120` 不动；设计文档快车道一节、施工计划测试 16 回写 |
| 17 | `adopted` 两边都标 | `04` 字段表加 `adopted`（`start` 那一版可选布尔）；`04:61` 行「账行标 `adopted`」明确是这一版（交 rl-part-04）；`03` runs 加一版 `adopted`（记新 holder 的 `session_id`、`ts`），runs 状态取值加 `adopted` | `12` 认领一段、`21` runs 两版处；`05:73` `rl run` 子命令加 `adopt RUN_ID`（或 `handoff start` 顺带写，交 rl-part-05 定形状）；设计文档 run 一节回写 |
| 18 | sessions amend 版 status 与其他栏照抄最新版、只换 model；closed 也能 amend | `03:176` 补这一句 | `04` sessions 表（交 rl-part-04）；`05:65` 附近 `session amend`（交 rl-part-05） |
| 19 | reclaim `--apply` 把超阈值的 `rejected` 单推回 `todo` | `04` 第八节四类处置加第五类「被打回的单子：推回待干，`actor` gyb、`via=reclaim`」；`04:69` 行不动（交 rl-part-04） | `01` reclaim 段、`05:170` 附近 reclaim 行（交 rl-part-05）；设计文档、施工计划 reclaim 句回写 |
| 20 | withdraw 只在 `in_progress` 收回时通知 holder | `04:71` 「有 holder 时」改成「从 `in_progress` 收回时」（交 rl-part-04） | `04` 第九节 withdrawn 通知段；`01`、`20`/`21`/`22` 抄的行 |
| 21 | reissue 新单一律从 `todo` 起 | `04:73` 「到」栏改成「旧单 `withdrawn`，新单 `todo`（`supersedes` 指旧单）」（交 rl-part-04） | `02` reissue 处；`20`/`22` 抄的行；施工计划第四节表 |
| 22 | 快车道不进 runs | `03:104` 删「快车道用 `ql_tag`」 | `07:56`、`07:150`（销）、`23:24`（销）；施工计划 `:65` |
| 23 | `rl inbox` 只读不关；通知类 issue 做完了收件人自己 close；`doctor --ack/--unack` 是写命令、只有 gyb | `05:100` 查询七类里 inbox 加「只读」、doctor ack 挪出查询、进 gyb 的写命令；`05` inbox 一节删「读过即关」（交 rl-part-05）；`03:92` close 写权补「通知类 issue 的 assignee 也能关」 | 「读过即关」出现在 `04`/`09`/`10`/`11`/`12`/`13`/`14`/`20`/`21`/README 与两份源文档，逐处删；`01` 推送/通知段 |
| 24 | `.doctor-acks.jsonl` 普通文件，总规矩不管 | `05:212` 段补一句（交 rl-part-05） | `03` 总规矩不动；`08` 那条 |
| 25 | 退出码加 5「用法错」，内部错 1；非零退出标准错误第一行固定原因种类，`--json` 给 `error.kind` | `03` 退出码表（定义处）加两行 | `05:104` 表照抄一字不差（交 rl-part-05）；`12:158` 等写「0/2/3/4」的改「0/1/2/3/4/5」；施工计划退出码段 |
| 26 | feedback `applied_to` 至少一个即可 | `03:152` 补「至少写一个」 | `05:206` doctor 第 17 项改成「`applied_to` 为空」（交 rl-part-05）；`09` feedback 段 |
| 27 | (b)(c) 认；(d) 不认——grants 可在角色会话里 `--as-gyb --quote` 替 gyb 写 | `01:21` 授权段改成「授权只有 gyb 能写；裸终端直接写，角色会话里 `--as-gyb --quote` 替 gyb 写也收」 | `05:25`（「唯一一本只收裸终端的账」句删，交 rl-part-05）、`05:27` 销「还没逐条裁」；`00:21`、`00:72` (d) 行改；`02:133`、`08:173`、`09:124`、`10:151`；设计文档 `:17` 末句、`:37`；施工计划 `:21` 三条标日期、(d) 改。`decisions.gyb.jsonl` 只收裸终端那条不受影响 |
| 28 | 上位规矩改了：角色被拉起不自动查收件箱，先干拉它起来的那张单；`rl inbox` 谁需要谁敲，gyb 要看就说 | 定义处 `01`（gyb 的收件箱与角色总规矩）或 `00`：把「所有角色上线第一个动作是 `rl inbox`」改成「`rl inbox` 是查询，不是上线动作；被派单拉起的会话先干那张单」 | 「上线第一个动作」出现在 `00`/`02`/`04`/`05`/`06`/`10`/`11`/`12`/`13`/`14`/`20`/`21`/`22`/`24`/README 与两份源文档（原则 6 补的「上线第一动作」），逐处改。问题 28 原问 gyb 另裁：**run 不查 inbox**（run 只关注自己那张发射单，一般不会有没带单子的 run 会话，原话「run只需要关注自己的工单，一般不会空run，不需要查」）——rl-part-05 在 `c2f69c8` 删掉的「run 的 inbox 例外」改回并扩大成「run 不查 inbox」（交 rl-part-05）；`12:19` 那段同改 |
| 29 | `holder_alive`、`age_hours` 算法认 | 无改动，`05` 那两句去掉「待 gyb 过目」（交 rl-part-05） | — |

### 底座三份冻结（2026-08-17，gyb 定：「我想给345都定了，新的session不再管他们，要管之后得了」）

`03-ledgers.md` 冻结在 `884ac0b`、`04-handoffs-and-sessions.md` 冻结在 `9b78d7c`、`05-rl-cli.md` 冻结在 `77213e5`。从此：
- 新统筹**不改这三份**，也不再给 rl-part-03/04/05 派活；三个 part session 可以关。
- 三份是定义处，传播只从它们往外传：落地清单里凡是「引用处」的活照做，凡是标「交 rl-part-04/05」「03 定义处」的都已完成，别再动。
- 以后别的 part 定稿或 gyb 新裁决要动到这三份的（字段、状态、命令、退出码、转移表），不改它们：写进 sync-inbox 一段标「冻结后待议」，攒着问 gyb 要不要解冻；gyb 说解冻才动，动完标新的冻结 commit。
- 三份文末「要同步到别处的」里没标已同步的条目，是留给统筹往引用处传的清单，传完在那三份里标「已同步」——这一种改动（只加标注、不改正文）允许，commit 前缀 `research-loop sync:`。

### 新统筹起手要做的

1. `git status`、`git log --oneline -10`（HEAD `5401b7b` 之后应无 sync commit）；读 `sync-inbox.md` 第一段问题 6 和末段 7–29 的裁决原话。
2. ListAgents；rl-part-03/04/05 若还在，不用再联系（三份已冻结）；以后新开的 part session 起来会自己报到。
3. 按上表落地。04、05 已落完（表里「交 rl-part-04/05」的格子都算完成，只剩别处）；**03 也已落完 `81377ac`（2026-08-17，rl-part-03 会话干的，第 220 行那两处漏列和白名单措辞一并落了；问题 31 已写进 inbox 末段等 gyb）**，剩下的是统筹改 `01`/`02`/`06`–`14`/`20`–`25`/README，再两份源文档逐句回写并各加一行「2026-08-17 按 sync-inbox 问题 N 的裁决改了哪句」；HANDOFF 四点五节表按新字段（`ql_tag`、`adopted`、`actual_seconds` 在 04；退出码六个在 03）改；inbox 每条「待落地」改「已落地 <commit>」。commit 前缀 `research-loop sync:`。
4. 落地后做一轮小互查（问题 28 那条动了十几份，最容易漏），派 opus 审、统筹逐条核；发现新的两说立新问题攒着，按本节三条规矩问 gyb。

### 2026-08-17 晨 rl-hub-v2 对 04/05 落地的质量检查（三路 opus 审 + 统筹逐条核）结果

- 04 `a7d1ec9`、05 `1b37593` 十几条裁决内容全落、没落反、没发明；摩擦一节两份都一字未动。审出的补改：05 五处已改 `2c965d9`（grants 两处旧说法、runs 三版、退出码 5 行「同上」、reclaim 第五类——问题 19 是统筹打包时漏发给 05 的）；05 又三处小活已改 `2dcdd23`、04 八处已改 `cec2cc9`。到此 04、05 只剩问题 30 一处等裁。
- **03 落地清单漏了两处，落 03 时一并**：`03:128` grants 段「授权只有 gyb 在裸终端能写……角色会话里替 gyb 批授权不收」按问题 27 改成「角色会话里 `--as-gyb --quote` 替 gyb 写也收」；`03:92` 后半句「`rl inbox` 关读到的通知类 issue」按问题 23 删，只留「`rl handoff accept` 关 answered issue」。连带 `03:228`（可选栏与「退出码四个」）、`03:236`（doctor `fix_for`）跟着改。存疑一处：`03:9` 脏树白名单 `loop/*.jsonl` 字面盖住 `loop/.doctor-acks.jsonl`，而问题 24 说总规矩不管它——落 03 时写成「九本账的 jsonl」或明写 ack 文件另算，别让白名单顺带把它收进去。
- **新攒的、等 gyb 的**（问题 30 起，接着 inbox 编号）：30. sessions amend 版谁能写——05 定稿写「amend 是 gyb」，04 这次按统筹给 gyb 的选项 A 括号写成「gyb 裸终端或该角色的活会话」，两说；已裁 B（gyb 或该角色活会话都能，原话「B」），04 `9b78d7c`、05 `ddafd84` 已改；03:176 落地时同写。**04、05 这一轮到此全部齐了。**31. runs 的 `relink` 版（和新加的 `adopted` 版）`status` 填什么，03:13 说校验按 status 查而 03:100 只给了 launched/finished 两个值——空白，落 03 时问。措辞题两条不问 gyb、统筹落地时顺手：`04:9`「快车道补单是 deploy 开给 deploy」与 `04:19`/`04:49`「from_role 就是 owner，补单 owner 记 gyb」用同一个「开」字指两件事，改成「deploy 敲命令开、owner 记 gyb」；`04:5` 源栏「上线第一动作」加作废标注。
5. 空下来的：第五节还悬着的、`30` 未写。

### 2026-08-17 夜 rl-hub-v3 的落地结果

引用处传播已落 `70c766b`（一轮 workflow：五组 opus 按文件组施工、每组一路 opus 审、末尾一路漏网扫描；统筹逐条裁、亲自补漏，各份文末裁决记录里「rl-hub-v3 审后补」的行是补的）。上面表里问题 6–31 的「引用处与源文档」栏全部完成；inbox 每条已标「已落地」；三份冻结件「要同步到别处的」已标已同步。新攒的等 gyb：inbox 问题 32（宿主白名单 `loop/*.jsonl` 字面盖住 `.doctor-acks.jsonl`）、33（reviewer 派 sonnet subagent 算不算 `dispatches_to` 的派活）。没做的、留给下一轮：`30-build-steps-verify-tests.md` 与 `plans/2026-08-17-research-loop-usage-scenarios.md` 是上一会话写的、还没 commit（README 的六处已裁标注也在同一份未提交的 README 里）；05 定稿滞后的旧账里还有 `14`/`24`/`25`「`rl inbox` 四类」对 `05` 五项、`21` 抄的转移表缺 `done_pending_review` amend 行、`20` 缺补单新建行、`13` 接口把 `run list` 默认过滤指到 `23` 而定义处是 `03`，这几处不属问题 6–31，未动。

## 九、2026-08-17 夜的交接：底座三份冻结、问题 6–31 全部传完，接下来逐份定稿其余十八份

上一个统筹叫 `rl-hub-v3`，干的事记在第八节末尾「2026-08-17 夜 rl-hub-v3 的落地结果」。HEAD `ed292b0` 之后工作树干净。

### 现在的状态

- 冻结三份：`03` `884ac0b`、`04` `9b78d7c`、`05` `77213e5`。冻结规矩见第八节「底座三份冻结」——不改它们、不派活；新裁决要动到它们的写进 `sync-inbox.md` 标「冻结后待议」攒着问 gyb。
- 问题 6–31 的裁决已经传到全部引用处（`70c766b`）并记账（`8501c67`）；`30` 已补写、第三轮预演场景清单已写（`ed292b0`）。
- README 进度表：除三份冻结和 `30` 之外，`00`/`01`/`02`/`06`–`09`/`10`–`14`/`20`–`25` 十八份状态都是「未开」——它们收过传播（正文里已有引用处改动和文末「裁决记录」的传播行），但各自「源文档没写清的」还没逐条问过 gyb，这就是接下来的活。
- 等 gyb 裁的：`sync-inbox.md` 末段问题 32、33——2026-08-18 已裁（都选 a）并落地。之后新攒的见 inbox 末尾（问题 34 起）。
- 05 定稿滞后的旧账（不属问题 6–31，谁开到那份顺手收）：`14`/`24`/`25`「`rl inbox` 四类」对 `05` 五项；`21` 抄的转移表缺 `done_pending_review` amend 行、`20` 缺补单新建行；`13` 接口把 `run list` 默认过滤指到 `23` 而定义处是 `03`。

### 接下来怎么干

1. 逐份定稿的走法照第三点五节那段贴话，两处按现状改：收件人名字写当时的统筹名（不再是 `rl-hub`）；「先从『源文档没写清的』那一节开始」之前加一句「先读文末『裁决记录』，传播进来的行别再问一遍」。顺序照第三点五节的建议：`02`、`06`，再角色五份 `10` 到 `14`，再角色对六份 `20` 到 `25` 和 `07`、`08`、`09`，`00`、`01` 随时可过（`01` 是身份规矩定义处，早过早稳）。
2. 统筹的活不变（第四节）：收回报、按四点五节判定义处再传引用处、两处不一致攒着问、传完标已同步与已处理、commit 前缀 `research-loop sync:`。三条工作规矩（第八节开头）照做：问裁决只讲逻辑不提文件；一轮一个问完等答、答齐一起改；讨论期间不开 Monitor。
3. 传播的干法这一轮试过一次好用，可以照搬：把引用处按文件组切成互不相交的几份交 opus 各干各的（不 commit），每组接一路 opus 审（missed / wrong / invented / contradiction / record_missing），末尾一路全局 grep 漏网，统筹逐条裁、亲自补，再一笔 commit。施工者的规矩：只改名下文件、摩擦一节不动、「没写清」被答掉的条目按 HANDOFF 表说销就销否则加「已裁」标注、每份文末裁决记录一问题一行、拿不准的报 undecided 不猜。

## 十、2026-08-18 的交接：`00`/`02`/`06` 三份定稿并传完，新统筹从这一节接手其余十五份

上一个统筹叫 `rl-hub-v4`，2026-08-18 干的事：收 `rl-part-02`、`rl-part-00`、`rl-part-06` 三份定稿的回报并往引用处传（commit `7c78db9`、`c33c4c1`、`3ad3da9`），落 gyb 裁的问题 32、33（`b91a0c0`），把动到冻结三份的句子攒成问题 34、35。HEAD `3ad3da9` 之后工作树干净。以第九节为底，下面只写变了的。

### 现在的状态

- 已定稿并传完六份：`03` `884ac0b`、`04` `9b78d7c`、`05` `77213e5`（冻结，规矩见第八节）；`00` `6ea0edc`、`02` `7549704`、`06` `d430192`（追裁 `f820504`）。这三份不冻结，但定稿了；它们文末「要同步到别处的」每条都标了「已同步 2026-08-18 rl-hub-v4」。2026-08-18 又定稿并传完两份：`09` `aaca3c9`（rl-hub-v5 传；冻结三份的五处记问题 37；retire 理由落哪一栏立问题 36 等 gyb）；`08` `eb02403`（rl-hub-v5 传；`06` 六处、`00` 原则 2 都是 gyb 原话「让统筹给06 00也改了」授权统筹直接改的；冻结三份的三条记问题 39；纪律句改法与 agent 定义母版流程立问题 38 等 gyb）。
- 还没开十三份：`01`、`07`、`10` 到 `14`、`20` 到 `25`（`08`、`09` 2026-08-18 已定稿）。README 进度表是准的。这十五份都收过传播（正文里有引用处改动，文末裁决记录里有传播行），各自「源文档没写清的」还没逐条问过 gyb。
- 等 gyb 的：没有。问题 36（废除理由写进废除版正文 `text`，裁 c）、38-1（纪律句改成「钩子拦不到的写法一律不许往四个角色目录和 `loop/` 写」，裁 b）、38-2（角色 agent 定义改动走母版流程，裁 a）2026-08-18 一轮三问裁完并已落地（rl-hub-v5）。inbox 问题 34（`02` 定稿动到 `03`/`05` 的四句）、35（`06` 定稿与追裁动到 `03`/`04`/`05` 的五处）、37（`09` 定稿动到冻结三份的七处）、39（`08` 定稿动到冻结三份的三条）都标「等最后一期」——gyb 2026-08-18 原话「你先不要改，等最后一期改」：冻结三份动不动，攒到全部 part 定稿之后一起裁。以后再有 part 定稿动到冻结三份的，接着往问题 35 后面编号，一样标「等最后一期」，不单独问。
- part session：`rl-part-00`、`rl-part-02`、`rl-part-06` 2026-08-18 都还挂着（idle），三份已定稿，不用再联系；`06` 定稿之后 gyb 又追裁过一条（状态文件位置），所以定稿了的 part session 还可能再报一次，收到照传即可。

### 2026-08-18 gyb 定的新规矩（在第八节三条工作规矩之外，以后每一轮都照做）

1. 两份源文档从此不再回写（`00` 定稿裁，gyb 选 b）：拆分文档定稿为准，两份源文档开头已加「已被取代、只留作历史」一句。第四节第 2 条里「包括两份源文档」那半句已删。传播只在 part 之间走。
2. 一切默认英语，任何 part 里不写语言相关的约束（`00` 定稿裁，原话「所有的东西都默认用英语，插件本体里面用英语写，然后不要出现语言相关的约束，就默认只有英语就可以了，不需要强调任何语言」）。看到「中文底稿」「正式版英文」这类字样就去掉。已存记忆 `research-loop-skill-in-english`。
3. 问 gyb 裁决，每个选项写利弊、给推荐；利大无弊标「强烈推荐」（已存记忆 `questions-with-recommendation`）。选择题式的裁决改成一轮三个（gyb 2026-08-18 在 rl-part-09 说「一次性给我三个问题，每个问题的选项给我利弊和推荐选项，如果利很大弊很小要标注强烈推荐」；开放讨论仍一轮一个）。第八节另外两条（只讲逻辑不提文件、讨论期不开 Monitor）照旧。
4. `30` 的施工步 2（架构总说明 `ARCHITECTURE.md`）不做、编号保留；第三轮模拟先不跑，等施工完成后再说；总验收过法是 gyb 每看完一对产出说一句「过 / 不过」加原因、记进 `00` 裁决记录、五对都过才算过（都是 `00` 定稿裁，已落 `30`）。（a）到（i）九条全裁完，不写回退清单。

### 这一轮定下来、传播时要记得的定义处变化（四点五节表已改）

- `02`：决定行按编号前缀落文件、`actor` 另记；行上加 `op` 字段（add/update/confirm/retire/merge）；过版判定「比最新一个非 confirm 版小才算过时」；`--with-runs` 第一跳按 `decision_refs`；发射单从父单抄 `decision_refs`（「发射单不引决定」作废）。
- `06`：钩子按敲出的路径判、不追软链接，另有白名单（配置项 `hooks.path_allowlist` 在 `08` 阈值表，默认空）；idea 可写 `notes/`（`notes/` 是 gyb 和 idea 写）；`reads` 栏两种写法（账写账名、目录写相对仓库根路径，备注移表下）；`dispatches_to` 机器不查；角色 json 改动同母版流程；测试 13 三样都查；会话状态文件放 `loop/.sessions/<session_id>.json`、销号钩子删、doctor 扫陈旧；两句 SKILL.md 纪律（Bash 绕钩子不许；一会话一角色）。五份角色 part（`10` 到 `14`）的 json 副本已按 `06` 五张表重抄——它们开的时候 part session 别再改回去，改 json 栏要报统筹回 `06`。
- `09`：`common/` 五个文件，判断类检查问题清单定名 `REVIEW-CHECKLIST.md`；`SPEC-TEMPLATE.md` 五栏（角色设定、使用场景、可用工具、限制条件、输出样式）是每份角色 SKILL.md 的骨架、「可用工具」只指到角色 json；`rule-NN`/`principle-NN` 编号一经定下不变；`rl decision retire` 必带理由（谁废都要，理由记进那一版，落哪一栏等问题 36）；feedback `target` 只认路径或带前缀编号、`rejected` 终态再提开新条；run 的 `reads` 加 feedback；`rules_version` 整数从 1 起、`GLOBAL-RULES.md` 头部一行是唯一真源、accept 加一并写回；`rl grant revoke` 列 grantee 活会话让 gyb 挑、撤销后再读算越权 reviewer 按时间戳查。
- `08`：配置新加 `launcher.abort_cmd`（宿主中断销号，留空跳过）、`repo_run`（env/entry/notes 三栏自然语言）、`host_ledgers`（path/note/kind，`kind: runs` 给 doctor 对账；new1 五项）；九本账路径钉死 `loop/`、配置里没有；`run.py` 门禁白名单归施工步 7 交付、init 不碰宿主代码；init 播 analysis 三样、幂等、默认模板逐项问。待验证第 8 条实测：skill 头部钩子不管 subagent、插件 agent 定义钩子被忽略、插件级钩子文件对 subagent 生效带 `agent_type`——钩子一份插件级、脚本先 `agent_type` 再状态文件判角色、状态文件 `agent_type` 非空不写、`agents/` 层五份只塑形、派活用它们起 subagent、init 不播不动 settings、不建 `workflows/`（定义处 `06`）；Bash 进钩子匹配范围（原则 2 改字，定义处 `00`/`06`）。
- 问题 32：宿主白名单照旧 `loop/*.jsonl` 字面，ack 文件顺带不算脏；问题 33：reviewer 起 sonnet subagent 逐题查不算派活。

### 旧账（第九节列的，未动，谁开到那份顺手收）

`14`/`24`/`25`「`rl inbox` 四类」对 `05` 五项；`21` 抄的转移表缺 `done_pending_review` amend 行、`20` 缺补单新建行；`13` 接口把 `run list` 默认过滤指到 `23` 而定义处是 `03`。

### 接下来怎么干

1. 贴话照第九节第 1 条，收件人写新统筹的名字。建议顺序：`01`（身份规矩定义处，早过早稳）、`07`、`08`、`09`，再角色五份 `10` 到 `14`，再角色对六份 `20` 到 `25`。开之前先 ListAgents 看那份是不是已经有 session 在手上。
2. 统筹的活照第四节和第九节第 2、3 条；源文档不再回写；动到冻结三份的攒进问题 35 之后编号、标「等最后一期」。
3. 每传完一份：目标 part 的裁决记录一行、来源 part「要同步到别处的」标已同步、inbox 段标已处理、README 进度表改状态、commit 前缀 `research-loop sync:`。这一轮三份都是统筹自己改的（每份牵连五到十处，没派 subagent）；牵连多到一份十几处以上再考虑第九节第 3 条的 opus 分组施工加审的干法。

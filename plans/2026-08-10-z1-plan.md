# z1 批次计划 — 最小规模从零重建，供 §5 活跑冒烟用，测完删产物

2026-08-10。用户裁决：可以从零部署一个特别小规模的测试，测试后把产生的东西删掉。
本批次回答一个问题：**METHOD.md §5 的三条冒烟验收线（出手可指认 / R3 token 比对 /
空注入对照）在当前代码上能不能走通。** 不出准确率结论——探针是小样训的，数字没有参照价值。

## Phase 0 五变量

| 变量 | 取值 | 说明 |
|---|---|---|
| BATCH | `z1` | run_id 前缀 |
| ENV | `appworld` | 官方题单三堆现成 |
| MODELS | `gptoss` | 现役 setting（METHOD.md §1） |
| CELLS | `ctool cgen` | 只训活跑要的两格，mtool/mext 不训 |
| DATA_ROOT | `/net/tokyo100-10g/data/str01_01/y-guo/reproduce/new1/pipeline/data/aw_z1_v1/gptoss` | 直接落 NFS（大产物铁律；本批测完全删，不做 home 软链） |

## 规模（特别小）

- 采集：train 12 题 / dev 4 题 / test_normal 4 题，共 20 题，`--api harmony
  --start-date 2026-07-31`（=rebuild.COLLECT_DATE，与 /render 及 vLLM 钉日期同口径），
  temperature 0.0，seed 全链 20260729。
- 训练：ctool / cgen 直接全量档跑小数据（数据本身就是冒烟规模，不再套 --smoke 子集；
  ctool 先 `--align-only` 过 G13）。
- 评测：只跑 eval-tool-causal 出 REPLAY_REPORT（活跑只需要它的 temperature 和
  logits；chosen_theta 小样下大概率 null，无所谓——θ 现在必传手动）。
  eval-ccall 不跑（CALLGEN_REPORT 活跑用不到）。
- 活跑冒烟：test_normal 取 2 题，probe 臂（θ 手动取低值保证出手，值记录在案）+
  --no-probe 臂 + chat 基线，三条验收线照 §5。

## 与标准流水线的三处偏离（都因"冒烟批、测完即删"而起，记录在案）

1. 不走 gen-launch manifest（它的客户端不带条数上限），采集客户端直接
   `run.py collect-aw --n <N>` 进 tmux；vLLM 服务端照 gpu-run 正规 launch 登记。
2. 跳过 G14 四格 smoke 与 G8/G12 复现验收（G8/G12 前提是"新代码喂旧数据"，
   本批代码未动 annotate/eval，且旧数据已不存在无从对拍）。G13 对齐检查照跑。
3. 产物直接写 NFS 绝对路径，不建 home 软链——测完 `rm -rf` 三个目录即净。

## DATA.md §7 检查清单过账

1. 数据版本 aw_z1_v1，写进 config 与 run_id。 2. 先验基线：eval 报告自带，
本批不出准确率结论。 3. 档位编号：单模型单环境，不适用。 4. 耗时对比：不做，不适用。
5. 种子 20260729 全链默认。 6. 重建对比：首建，不适用。 7. 探针多配置分身：不适用。
8. WORKPLAN 判断变动：收官时核，动了就补 TIMELINE。

## 删除清单（收官时执行）

- `/net/.../reproduce/new1/envs/runs/z1_aw/`（原始轨迹）
- `/net/.../reproduce/new1/pipeline/data/aw_z1_v1/`（数据集）
- `/net/.../reproduce/new1/pipeline/runs/z1_gptoss_ctool` `z1_gptoss_cgen`（权重）
- 活跑输出目录（NFS，路径发射时定）
- 留下的：本计划、RESULTS.md 里的 run 记录、冒烟验收报告 md、config（几百字节，入库）

# 校准批量 v0（2026-07-29）

每格 5 任务（BFCL 21 条顺带），三指标：准确率 / 每步思考中位（字符）/ 每轨迹调用数。

| 格子 | acc | 思考中位/步 | 调用数 |
|---|---|---|---|
| TALES cookingworld × q3.5 | 0.81 | 331 | 8.8 |
| TALES cookingworld × q3.6 | 0.81 | 709 | 8.8 |
| AppWorld dev前5 × q3.5 | 0.20 | 303 | 12.2 |
| AppWorld dev前5 × q3.6 | 0.40 | 301 | 12.0 |
| BFCL multi_turn × q3.5 | 未评(需 bfcl evaluate) | 299 | 8.9 轮 |

## 判读（对照目标带:acc 0.6–0.9,思考 ≥150 token≈450字符）

- **TALES 双模型都在带内**(0.81)。q3.6 思考量是 q3.5 的两倍(709 vs 331 字符),
  同分不同思考——投机空间上 q3.6 更肥。难度旋钮(加地点/食材)留着往上够。
- **AppWorld 低于带**(0.20/0.40):difficulty-2 任务对 27B 偏难。下一批改抽
  difficulty-1 子集,或加步数;两代模型在此拉开 2 倍差距,区分度好。
- **各格思考中位普遍在 300 字符(~80 token)/步**,只有 TALES×q3.6 到 ~190 token。
  比目标带薄——印证"任务要往难里调"的预判;掺干扰项/多跳化是下一个旋钮。
- 轨迹形态健康:AppWorld 平均 12 个调用/轨迹,BFCL 8.9 轮/任务,思考-调用
  交替结构齐全,截断探针 v1 与 C2-1 放量可直接用这批管线。

## v2 补测（动词表补全 BBQ 后重跑 + gpt-oss 行,2026-07-29）

- TALES 单房间,可赢局(seeds 13/14/15):**三模型全部 3/3 通关** — 该配置已被打穿。
- seeds 11/12 是**环境死局**:单房间配置(numLocations=1)没有 BBQ,菜谱却要求
  grill → 不可赢,三模型一致顶在 50/57。教训:难度校准前先验任务可解性。
- AppWorld 三模型阶梯:q3.5 1/5 → q3.6 2/5 → **gpt-oss 3/5**;
  gpt-oss(effort=high)思考 1–6K 字符/步,是三模型里投机窗口最肥的。
- 多房间加难版(numLocations=5,带门):qwen3.6 单 seed 40 步得 40 分未通关 —
  真难而非死局(BBQ 可达已验证),已作为 full_v1 全量配置。

## 遗留

- BFCL 准确率需跑 `bfcl evaluate`(状态比对),下批补。
- gpt-oss-120b 行待其服务上线后补(任务 #6)。
- tau2 行需先打思考持久化补丁,暂不入表。

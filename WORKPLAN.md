# 工作计划 — 合体版论文(2026-07-27 定稿)

> 论文身份见记忆 [[tool-call-speculation-direction]] / [[repeat-task-benchmark-direction]]。
> 四贡献:C1 时机-收益测量 / C2 可学习投机器 / C3 联合学习曲线 benchmark / C4 三条曲线。
> 无 ddl 压力(用户 2026-07-27 撤销时间盒),按依赖排序不按日历。

## 主路径:C2 三段管线(全部新建,最先动工)

### C2-1 数据生成管线 ⚠️ 全项目唯一未验证环节
- 在任务流(ALFWorld fig1 设置 + HotpotQA + 合成 lookup)跑 agent 轨迹
- 每轨迹存:①思考期间选定层隐藏状态(参考 PPRAG 30–45% 深度起步)②t=0 query 表示 + 记忆检索特征 ③实际工具调用+参数 ④时间戳
- oracle 回放标签器:采样位置注入实测 benefit(t) → 时机头标签
- 模型:Qwen3.5-4B 主力(与 oracle/fig1 同款)

### C2-2 四头训练
- 头①是否调用 / 头②工具分类:逻辑回归基线(When2Tool 配方)→ 小 MLP
- 头③参数:最省版=记忆候选选择 + 上下文复制(PASTE"参数=子串"依据);EAGLE 式生成头留下一版
- 头④时机:回放标签回归;学的是 benefit(t)↓ × predictability(t)↑ 的乘积

### C2-2t 文本探针线(full_v1 数据,2026-07-29 定稿,导师统计意见已并入)
- **数据**:full_v1 8789 事件,每事件切出**全部句子边界前缀**(自然语言按句切、
  代码按整行切,永远是从头开始的前缀,句中不切)≈7–9 万样本;one-hot 标签
  (采集与部署都是贪心解码,多续写软标签退化,免做);训练损失按事件等权(1/m_i);
  **轨迹级**四路切分 train 70 / calA 10(拟温度)/ calB 10(回放扫阈值)/ test 10
  (冻结后只跑一次);全库固定种子;顺产路由统计表(每工具参数数/逐字命中率/长度)
- **训练**:**一环境一模型,共训三个**(用户 2026-07-29 拍板,不做跨环境合训):
  ModernBERT-base(8192 ctx,mean pooling,sdpa,不用 flash-attn,不用 NightOwl)
  + 各自环境的工具分类头;报 micro/macro/分工具,必须打赢频率先验基线;
  已知风险:TALES 仅 60 条轨迹,cal/test 各约 6 条,置信区间会宽,
  必要时交叉验证或补采(管线现成)
- **评测=整轨迹回放**,不筛样本:test 轨迹按序回放每个句边界,只认每事件**首次**
  过阈,报 coverage / 触发精度 / earliness / 错误投机率;stop-time 校准图
  (只在首次触发点验校准);置信区间按轨迹 bootstrap。
  自坑清单:同份数据既校准又选阈又报分✗ / 测试集不存全边界✗ /
  同事件多前缀当独立样本✗ / 扫阈值只报最优✗ / 只报精度不报 coverage✗
- **v2 部署形态:流式因果探针**——~0.5B 因果 LM + 分类头,KV cache 增量编码
  每 token 只付单 token 前向(BERT 每探必重编码整前缀,是频率成本瓶颈);
  可与 4B 草稿工位合并为门卫+草稿一体;训练数据与 v1 同一份,零改造

### C2-2t 第二轮迭代 backlog(已记录,以后再决定,v1 一概不背)
1. **correctness gate**(= 时机头的统计版,对应 C2-3 关键图的"时机头开火"一侧):
   单独训"这次预测最终对不对"判官,cross-fitting 防泄漏,输入带路径特征
   (置信度涨势/预测翻转/当前深度)
2. 困难样本挖掘:训练轨迹回放后加采高置信错误位、首次错触发位、近阈值位,
   与随机前缀混合防自证偏差
3. 效用加权阈值:按工具副作用分开火线(只读工具激进、写操作保守),
   阈值按期望效用选而非只按精度
4. 匹配式改造:题干附工具描述做"配/不配"判断,泛化到未见过的工具表
5. 参数头:形态由路由统计表裁决(span 抽取 / 拼名回大模型续写 / 4B 草稿)
6. 若改采样解码:回头补同前缀多续写软标签(贪心下退化,采样下才有信息)

### C2-2t 文本探针线(full_v1 数据,2026-07-29 起的当前主力)
- v1:ModernBERT-base(8192 ctx,原生双头,不用 NightOwl 对照)微调
  「思考前缀→工具分类」;full_v1 8789 事件 × 每事件 4 个随机截断 ≈ 3.5 万样本,
  按轨迹切分防泄漏,超长从左截保思考尾部;交付三张图:分环境 acc /
  深度-准确率曲线(=开火时机标定表)/ 置信度-覆盖率曲线(=θ 阈值表)
- 参数头形态由造数据时顺产的路由统计表(每工具参数数/逐字命中率/token 长)裁决:
  命中率高→span 抽取头;复杂参数→拼名回大模型续写参数(验证免费)vs 4B 草稿,按统计定
- **v2 部署形态:流式因果探针(有前途,已立项)**——把探针从 BERT 换成
  ~0.5B 因果 LM + 分类头,KV cache 增量编码,每新 token 只付单 token 前向
  (BERT 每探一次要重编码整个前缀,是探测频率的成本瓶颈);天生流式、
  随时可读"当前预测+置信度";与 4B 草稿工位同物种,可合并为
  门卫+草稿一体模型(过阈值瞬间直接生成完整 tool call);
  训练数据与 v1 完全同一份,零改造。v1 曲线先出、部署期再换装

### C2-3 闭环 + 关键图
- 调度器(时机头仲裁,t=0 与思考中两触发点)→ 授权措辞注入 → 事后验证(SPORK 逐字判据)
- **关键图:置信度开火(SPORK 式)vs 时机头开火——前者落死区,后者绕开**
- 端到端计时带 serving 侧对照(PASTE 教训)

## C1 收尾(小,单卡)
- HotpotQA 真实多跳注入:comparison(独立两跳)与 bridge(参数依赖)分开报告
- 采样方差检查(堵"贪心单采样"边界)
- (可选)非 Qwen 家族一个点

## C3 装配(与 C2 完全并行,六件互相独立)
1. L3/L4 任务对设计(L4=表面相似实则不同,OEP 2605.18930 依托)
2. 指标形式化:Speedup@k / cost-to-competence / 负迁移指数;防幸存者偏差聚合
3. 补基线:oracle replay 天花板、全历史上下文(EvoMemBench 警示它能打)
4. 接 Evo-Memory 被试 3–4 种(ExpRAG/ReMem/AWM/DynamicCheatsheet),后端指本地 vLLM,论文注明"采用其统一实现"
5. Qwen3-8B 全矩阵复跑(纯烧卡,舰队并行)
6. TraceLab 相似度分布分析(纯 CPU):相邻任务相似度 / 相似任务间隔 / 假相似对频率

## C4 合成(最后,依赖 C2-3 + C3)
- 全矩阵带新被试重跑
- 投机器作为被试上 L0/L1/L2 流:**主图=出手时刻逐集前移 + 联合速度-准确率曲线**
- 无关任务流上投机器的负迁移读数(接 C3 探针)

## 核查与写作(穿插)
- 隔离区余项:Gim 2024;插话触发点若写 → SIA 全文重读;PreAct-Bench 若引 → 先核摘要
- 写作从 C3 协议节起笔(不依赖未完成实验)
- 语气与写作规则:humanizer-zh + humanizer-gyb

## 风险声明(唯一一条)
C2 关键图若失败(时机头不比置信度强)→ C2 声称降级,C1/C3/C4 非投机器部分无损,论文仍成立,主图换 C4 第一二条曲线。

## 环境纪律
uv-only;GPU:主力 A6000 单卡级,矩阵上舰队;不碰付费 API,user-sim/评分一律本地模型。

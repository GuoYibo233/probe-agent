# 工作计划 — 合体版论文(2026-07-30 修订)

> 论文身份见记忆 [[tool-call-speculation-direction]] / [[repeat-task-benchmark-direction]]。
> 四贡献:C1 时机-收益测量 / C2 可学习投机器 / C3 联合学习曲线 benchmark / C4 三条曲线。
> 无 ddl 压力,按依赖排序不按日历。
> **执行细则(任务卡 T0-T16,各带目的/依赖/验收标准/波次)见 `plans/2026-07-30-task-plan.md`,
> 本文只描述方向与结构;两文冲突以任务卡为准。**

## 2026-08-01 记入:流水线代码实现状态与待写清单

> 当天讨论与决定原文见 `plans/2026-08-01-direction-notes.md`(意见一:评测协议改
> θ 扫描曲线主报,与下面"价值主张"一节的主图定义一致;意见二:q35 搁置与 GPU 排期)。
> 注意:再往下的"C2-2t 文本探针线(当前主力)"一节写于 07-30,早于 07-31 的主线换轨
> (TIMELINE 2026-07-31:探针主线定为因果头 ctool/cgen,ModernBERT 分类头退为对照,
> appworld 成绩一律在官方分区上报),读旧节以 TIMELINE 为准。

### 已实现(全部实战验证过)

| 段 | 代码 | 状态 |
|---|---|---|
| 采集 | `envs/collect/run_{appworld,tales,alfworld}.py` | 幂等可续采;appworld 官方分区采齐(315 实例×3 模型),alfworld c2 批次在采(q36+gptoss 各 474 题) |
| 标注造数据 | `pipeline/annotate/` | 过 G8 复现线(喂旧输入九字段逐条一致);alfworld 已接入 |
| 训练四格 | `pipeline/train/` 四脚本 | c1 十二格实战跑完;ctool 自带增量对齐检查(FAIL 即退出) |
| 评测 | `pipeline/eval/` 三脚本 | 过 G12 复现线;每句边界打分全落盘(`logits_test.pt`),换 θ 重算不碰 GPU;报数层还是风险档双点,未改曲线主报 |
| 注入回放 | `pipeline/inject/` plan/run/score 三段 | 首次全量在跑(aw_gptoss_r10,θ=0.925);miss_policy 的 skip/oracle 通 |
| 记账 | `ops/record.py` / `ops/gpu_jobs.py` | 可用;已知毛病:watch 对 shiga 误报 EXIT(ssh host key 没过)/finish 改不了 start 的机位/"发射前 commit"37/37 次未执行 |

### 待写代码(四块)

1. **θ 扫描驱动**:换 θ 现在要手动重跑注入 run 段,缺一个"给一列 θ 自动循环"的壳;
   每 θ 约 2.5h GPU(实测 0.19 req/s)。这是意见一(曲线主报)的执行前提。
2. **execute 档**:`replay_inject.py` 的 miss_policy=execute(环境重放到该步+真执行
   预测调用取返回)未实现,接口已留。整题活跑、量任务成绩、让曲线纵轴成立的唯一缺口。
3. **tau2 采集器**:`envs/collect/` 无 run_tau2.py;tau2-bench 仓库已部署,只跑过自带 smoke。
4. **bfcl 进新流水线**:切分决定+用 `pipeline/annotate/` 重标旧轨迹(595 条×3 模型在手),
   纯 CPU 零采集。

动态时机策略(θ 不定死、学出手时刻)零代码;唯一证据是 closed_loop v0
(40 道合成题,静态阈值自己走进死区)。

### 资源与排序(2026-08-01 用户定,原文见 direction-notes 意见二)

- q35 相关采集全部搁置(ALFWorld 补采与旧九格补齐都不做);新喂 GPU 先排 q36/gptoss 侧。
- 紧张资源只算 H100/H200(即 tokyo108 一台);tokyo105/106/107 的 48G 卡随便并行。

## 2026-08-01 排程:θ 扫描曲线主报(用户已批)

> 计划全文与三处拍板记录见 `plans/2026-08-01-direction-notes.md` 意见四。
> 已批三决定:θ 六点格 {0.50, 0.70, 0.80, 0.875, 0.925, 0.95};bfcl 旧轨迹重标
> 豁免于 q35 搁置(存货重标零采集);execute 口径只补 3 个代表点(低/中/推荐工作点)。

### 第一阶段:CPU 写码(四块并行,按对主图的关键程度排)

| # | 块 | 内容 | 备注 |
|---|---|---|---|
| 1a | θ 扫描驱动壳 | 给一列 θ 循环"重排 plan→注入 run→score";每 θ 独立 run_id 独立落盘,`record.py` 各记一笔 | 主路径最优先;plan 段不碰 GPU |
| 1b | execute 档 | 环境重放到触发步→真执行预测调用→注入真实返回(含真实报错) | 已实现;口径见下方更正 |
| 1c | tau2 采集器 | `envs/collect/run_tau2.py`,对齐现有采集器幂等续采接口 | 写完不放量,放量机位待用户统一处理 |
| 1d | bfcl 重标 | 定切分 + `pipeline/annotate/` 重标旧轨迹 595 条×3 模型 | 已批豁免于 q35 搁置 |

### 更正:execute 档给不出任务级成绩(2026-08-01,实现阶段查明)

本节上表原写 execute 档"整题跑完看成败",上方 07-31 那节也写它是"整题活跑、
量任务成绩、让曲线纵轴成立的唯一缺口"——**两处都不成立**。execute 档一个事件
只**续写一步**,不跑到底、不调 `world.evaluate()`。它真正做到的是:猜错时也照样
在环境里执行预测出的调用、把真实返回(包括真实报错)注进去,于是**猜错的代价
进了账**——这修的是 skip 口径偏乐观的问题,量到的仍是**单步调用一致率**。

所以曲线的正确率轴,skip 与 execute 两档都是调用一致率,差别只在猜错的代价算不算。
**真正的任务级那条轴要另建 in-loop rollout**:采集循环里挂探针、触发就注入、
走完整题再评测。那是一批新采集,不在现有四块之内,属于新增工作量。
报告里把 execute 档写成"任务级正确率"就是虚报。

→ 这条动了本文件与 07-31 节的判断,**TIMELINE 需补一条**(人写,留给用户)。

### 第二阶段:θ 扫描发射(GPU,等 aw_gptoss_r10 收尾后排队)

- 格点 6 个:0.925 已跑完(aw_gptoss_r10)直接进曲线;新跑 5 点 {0.50, 0.70, 0.80, 0.875, 0.95}。
  0.95 探高 θ 端塌陷(closed_loop v0:lead≤32 平均省 −6.4 token),0.50 探低 θ 端正确率下界。
- 成本:θ=0.925 实测 2.5h(0.17 req/s,1750 条);低 θ 出手更多,按 2.5–4h/点估,串行约 15h 服务时间。
- 机位:复用 tokyo108 现成 gptoss 服务,不新占卡;θ 点串行排队,与 ALFWorld 采集共存不抢并行。
- 口径分层:第一遍全 6 点 skip+oracle 双口径;execute 落地后挑 3 个代表点补诚实口径,
  偏差大再考虑全补。
- 铁律:每 θ 点走 gpu-run 全生命周期,发射前 commit,`record.py` 双登记,run_id 四处一致。

### 第三阶段:c2 训练波(48G 卡,与 θ 扫描完全并行)

- 8 格训练等 ALFWorld 采集(q36 8 分片 + gptoss 14 分片,各 474 题)收完 + 标注过双验收线后开。
- 机位放 tokyo105/106/107 的 48G 卡,能开几格开几格;与 θ 扫描零冲突(训练不碰 tokyo108)。

### 记账债

- [x] `2026-07-31-direction-notes.md` 移 `plans/archive/` + 更新索引(2026-08-01 已做)。
- [ ] θ 扫描结论出来后补一条 TIMELINE(评测主报从风险档双点换成曲线)——TIMELINE 人写,留给用户。

## 价值主张(2026-07-29 用户定调,全部评测与汇报的口径)

探针的全部卖点 = **省 token + 降延迟**;准确率只会持平或略降。不许只报预测准不准。
两种部署用法分开算账:预取(零准确率损失,省延迟不省 token,限只读调用)/
截断(省 token 来源,错误直接成任务错误,θ 是"省多少 vs 错多少"旋钮)。
主图 = 省 token 曲线 vs 准确率损失曲线。

## 主路径:C2-2t 文本探针线(当前主力)

### 现状(2026-07-30)
- v2fix(fp32+autocast 修复版)bfcl 已过线:θ=0.8 触发率 0.755/触发精度 0.944/提前量 0.664,
  碾过频率先验 0.038(run `20260729_2235`)。appworld/tales 评测与 v3 三训在飞。
- 参数头三档定型:无参/选择/自由,档位表 `envs/bert_data/v2/PARAM_TIERS.md`
  (合计 10.9%/75.9%/13.2%,自由档被代理指标高估,真现编尾巴约 5%)。

### 五个对照实验(全批,消融跑满三环境;任务卡 T5-T8)
1. **去思考**:切 [THINKING] 段,量"零思考时刻能预测多少"——给加速架构画分布,审稿必问。
2. **去历史**:切 [HISTORY] 段,量思考单独的信号;与 1 合成信号来源分解表。
3. **跨模型双向矩阵**:按 model 字段过滤,训练侧×考试侧{qwen,gpt-oss} 四格,
   冷迁移/只换校准两种都报;bfcl 缺 gpt-oss 轨迹,补采批次(T1)后经 v3.1 补齐。
4. **抽取头**:选择档参数产线,触发时刻评测(值未出现即失败),报参数全对率+完整调用正确率,
   对标 SPORK 参数起步 7.6%。
5. **因果探针**:双底座 Qwen3-0.6B-Base + LFM2.5-350M-Base(用户定案);
   开训前必过分块增量对齐检查(铁律);产出分类差距 vs 探测成本,裁决 v2 部署形态。

### C2-3 闭环(关键图;任务卡 T9/T10)
管线:拦 token 流→切句→逐句探针→过 θ 按口径动作。**五口径(用户 2026-07-30 定稿)**:
基线 / 截断(扫 θ) / 预取(只读,tales 不做) / 影子记录(只记不执行,全程开,零成本) /
fork 对照(每触发事件首次过 θ 处 fork 一次,**上限一次,分支内探针关闭,禁止嵌套**;
实测省 token,与离线估算对账)。环境顺序 bfcl→appworld→tales(已批)。
计时带 serving 侧对照(PASTE 教训)。管线代码立即动工,与实验并行(已批)。

### 第二轮迭代 backlog(以后再决定,现役一概不背)
correctness gate / 困难样本挖掘 / 效用加权阈值(已并入三档分 θ) / 匹配式改造 /
参数头生成式升级 / 采样解码软标签。v2 流式因果探针已升格为实验 5,不再是 backlog。

## C1 收尾(小,单卡;任务卡 T11)
- HotpotQA 真实多跳:comparison 与 bridge 分开报告;采样方差检查;(可选)非 Qwen 家族一个点。

## C3 装配(与 C2 完全并行,六件互相独立;任务卡 T12)
1. L3/L4 任务对设计(L4=表面相似实则不同)
2. 指标形式化:Speedup@k / cost-to-competence / 负迁移指数;聚合必须含失败任务
3. 补基线:完美复用天花板、全历史上下文(EvoMemBench 警示它能打)
4. 接被试 4-5 种(官方码:A-Mem/Mem0/AWM/ExpeL+自建 ExpRAG),后端本地 vLLM
5. Qwen3-8B 全矩阵复跑(纯烧卡,舰队并行,随时插空)
6. TraceLab 相似度分布分析(纯 CPU)

## C4 合成(最后,依赖 C2-3 + C3;任务卡 T14)
- 全矩阵带新被试重跑;探针系统作为被试上任务流;
  **主图 = 提交调用时刻逐任务前移 + 联合速度-准确率曲线**。

## 核查与写作(穿插;任务卡 T15)
- 写作从 C3 协议节起笔(不依赖未完成实验),走 paper-write 规矩(数字标 run_id,改完必编译)。
- 隔离区余项:Gim 2024;插话触发点若写 → SIA 全文重读;PreAct-Bench 若引 → 先核摘要。
- 语气与写作规则:humanizer-zh + humanizer-gyb(一物一名)。

## 风险声明(唯一一条)
C2 关键图若失败(学到的开火策略不比置信度强)→ C2 声称降级,C1/C3/C4 非投机器部分无损,
论文仍成立,主图换 C4 第一二条曲线。

## 环境纪律
uv-only;GPU 全生命周期走 gpu-run skill;主力 A6000 单卡级,矩阵上舰队;
不碰付费 API,user-sim/评分一律本地模型;权重下到 NFS 模型目录。

# 12 Second-round documentation write-back: the three skill reference docs, MAP.md, run.py notes, gates.md

Status: resolved
Blocked by: 07, 08, 09, 10, 11 (dispatch only after all of them are merged into main)
Spec: `.scratch/kvshare-train/spec.md` 16.7 (checklist), 16.10 (silent-failure points #28-#32). Changes only in `.claude/skills/probe-pipeline/references/stage-commands.md`, `invariants.md`, `extending.md`, `gates.md`, `MAP.md`, `pipeline/eval/ACCEPT_EVAL.md`, and `run.py` (only the `notes` strings of the seven entries `train-cgen / train-cparam / train-ctool / eval-tool-causal / eval-tool-mbert / eval-ccall / eval-cparam`, no other key touched). Do not change any `.py` logic, do not change tests.

## Background

The second round merged five code tickets: the three eval scripts' `--overlong` (07), the trainer's `--gen-eval / --gen-bs` (08), parameterized alignment thresholds plus `--align-rule / --align-rel-tol` (09), `--mem-probe-pick` and `mem_probe_summary` (10), the `sweep-lr` task (11). CLAUDE.md's rule is that an extension must be written back into the skill, or the next person is working off a stale map.

## What to do

First read `git log --oneline 7668166..HEAD` and all five tickets' Comments; take the merged code's `--help` output as ground truth, not the ticket text.

1. `stage-commands.md` section 3: add to the parameter table `--overlong` (all three eval scripts, one sentence per value on its criterion), `--gen-eval / --gen-bs`, `--align-rule / --align-rel-tol / --align-tok-tol / --align-bf16-mean-tol / --align-bf16-max-tol / --align-baseline-factor` (ctool only has the first two), `--mem-probe-pick` (one sentence per value); one command each for `sweep-lr plan / report`; section 3's output-file list gains `SWEEP_REPORT.json/.md`, the `mem_probe_summary` event, and the `overlong_mode` and count keys in eval reports; section 3.1 rule 3's placement rule changes to "`mem_probe_summary.worst_gb x 1.1`, then apply the fragmentation discount" (still written as two separate clauses).
2. `invariants.md`: cgen / cparam training defaults to generation-based evaluation over 200 rows (decision 28, `val_exact_*` logged only, not used to pick best); the probe defaults to `cost`; eval defaults to `--overlong left`; the alignment rule defaults to `abs`, with threshold defaults equal to round one's constants.
3. `extending.md` section 5: add #28 through #34, wording per spec 16.10 but with each item's trigger condition checked against the merged code; section 3's "new-implementation precedent" paragraph gains a sentence: all four of round two's switches are parameters whose defaults equal the recommended values, and none of them add a new cell; section 3.4 (near line 167, the run_id's three-part shape) gains a sentence: the learning-rate sweep's run_id is four parts, `ks828<tag>_gptoss_cgen_lr<lr>`, output under `pipeline/runs/sweep/`, not entering the matrix or `summarize_matrix`. Add the same sentence to `stage-commands.md` section 3.2.
4. `MAP.md`: add a new row for `sweep_lr.py`; the three eval scripts' rows gain `--overlong`; `train_causal_share.py`'s row gains `--gen-eval / --gen-eval-at / --align-rule / --mem-probe-pick`; `share_data.py`'s row gains `full_token_ids / n_full_tokens / event_full_texts / select_keys / epoch_minibatches`.
5. `run.py`: `train-cgen / train-cparam`'s `notes` gain one sentence each for `--gen-eval`, `--align-rule`, `--mem-probe-pick`; `train-ctool`'s `notes` gain `--align-rule`; the three eval tasks (`eval-tool-causal / eval-ccall / eval-cparam`)'s `notes` gain `--overlong`; `eval-tool-mbert`'s `notes` state "the mbert head only supports `--overlong left`." Change only the strings, not the structure; run `python3 run.py selfcheck` after.
6. `gates.md`: state that the smoke gate reads only `ALIGN_CHECK.json`'s `PASS`, and that `rule` being in the JSON doesn't affect the gate -- one sentence.
7. `extending.md` line 123's "new-implementation precedent" paragraph: change "the four eval scripts unchanged, not one character" to "scoring logic unchanged, not one character; input construction gained one switch, `--overlong`, in round two (behavior unchanged field-for-field when it's `left`, the default)"; `stage-commands.md` section 7, around line 517, change the placement note that mentions the old probe field (`longest_event`) to `mem_probe_summary.worst_gb` (and explain what `scope` means); in `pipeline/eval/ACCEPT_EVAL.md`, change criteria like "REPLAY_REPORT byte-for-byte unchanged" to "existing keys unchanged; round two added `overlong_mode`, `n_oow`, and three count keys."

## Acceptance

- `python3 run.py selfcheck` passes.
- `grep -n "overlong" .claude/skills/probe-pipeline/references/stage-commands.md MAP.md run.py` hits in all three; `grep -n "#34" .claude/skills/probe-pipeline/references/extending.md` hits; `grep -n "worst_gb\|runs/sweep" .claude/skills/probe-pipeline/references/stage-commands.md` both hit.
- Every parameter's documented default matches the corresponding script's `--help` (checked item by item in review).
- Do not change `.py` logic, do not change tests.

## Comments

- 2026-08-28 plan-8-28 closeout: wave6 implementation passed review with 0 fix rounds, branch `ticket/2026-08-28-wave6/T12` (base `ed88ad7`, head `4f10aec`), merged as `1998522` (no conflicts). Two review minors and two implementer concerns fixed directly by the main session after merging: dropped the `#` prefix on extending.md section 5's new rows to match the format of the preceding 27 rows; unified #30's phrasing on slot position to "the 6th slot, counting from 0"; folded spec 16.10's #35, #36, plus #37 (added after the ticket was dispatched, the grad-ckpt recompute kernel issue) into extending.md section 5. Cross-references in invariants / stage-commands pointing at "spec 16.10 #35/#36" were kept (the content lives in spec too). Also filled in two things settled only after the ticket was dispatched: stage-commands.md section 3's parameter table changed the description of `loop` to decision 32's three-group rule; section 3.1 gained readout rule 4 (a run with `--mem-probe` before 479aa4b has its first `step` event's peak reflecting the probe's number, decision 35). Two cannotVerify items (each switch's GPU runtime behavior, #28/#29/#34's trigger conditions checked only statically) are covered by round two's smoke testing and the `--overlong` end-to-end verification.

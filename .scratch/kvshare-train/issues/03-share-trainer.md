# 03 Trainer `pipeline/train/train_causal_share.py` and the registry

Status: resolved
Blocked by: 01
Spec: `.scratch/kvshare-train/spec.md` sections 4-9, section 12; the attention implementation follows `.scratch/kvshare-train/design-attention.md` (already landed); there is a CPU-verified reference implementation of the packed sequence and mask construction, `.scratch/kvshare-train/verify/packed_common.py` (read it only as a reference; the code must be rewritten against ticket 01's `share_data` interface, do not import that file directly)

## What to do

1. Create `pipeline/train/train_causal_share.py`. Data goes through ticket 01's `share_data`; the tokenizer and model go through the old script's `train_causal_callgen.build(dev, base, attn_impl=..., path=...)` (add two keyword arguments to `build()`, `attn_impl=None` and `path=None`, both `None` preserving the old behavior; this is the only change allowed to the old script, spec 3.4). `--base` accepts `qwen / qwen17 / qwen4` or a directory path (a path goes through `build(path=...)`, letting tests use a small model). The forward pass follows spec section 4 and the recommended form in `design-attention.md` (mask dtype, position_ids, kernel). Loss and updates follow section 5, evaluation and `best/` follow section 6, logging and heartbeat follow section 7 (the `step` event's `ips` is the cumulative row count for this epoch divided by elapsed seconds, `ips_win` is the windowed value, `rows` is the cumulative row count; `heartbeat.emit` at the same three points as the old script's lines 71, 481, 517, 554), the command line follows section 8's table, and the alignment check follows section 9. Wiring for `--lora` / `--grad-ckpt` / `--readonly-env` / `--force` / `READONLY.json` follows the old script.
2. `run.py`: change `CELLS["cgen"]` and `CELLS["cparam"]` to point at the new script with `["--mode", "cgen"]` / `["--mode", "cparam"]`; update `TASKS`'s `train-cgen` / `train-cparam` to match; add new `train-cgen-rows` / `train-cparam-rows` pointing at the old scripts (`stage="train", py="cprobe", gpu=True`, notes reading "row-by-row reference implementation, used only for alignment checks and comparison; its output does not enter the matrix, and its run_id must not use a live batch prefix").
   The command line follows the table in spec section 8, including `--log-every`, `--mem-probe`, `--max-events`, and how `--smoke` is taken (`--smoke` goes through `share_data.load_events(order="shortest")`; a bare `--max-events` goes through `order="random"`); `--mem-probe` follows spec section 10: first load the full train set (limit=0), use `share_data.worst_blocks` to take two blocks, each doing one forward-plus-backward pass, write two `mem_probe` events, zero the gradients, then subsample and start training. The `step` event's `train_s` pauses its clock during evaluation. `python3 run.py selfcheck` passes, `python3 -c "import ops.launch_probe"` passes, and `python3 run.py show train-cgen` prints a command containing `train_causal_share.py --mode cgen`.
3. `MAP.md` is untouched (the cgen, cparam rows and the `(reference)` row are written by ticket 04; give the wording in your report: "one event, one forward pass, sharing a prefix; upper limit 8192, overlong events dropped whole; 8 events per update; 1 epoch, evaluating val_ce every quarter epoch; `--tok-budget` controls memory").
4. The alignment check follows spec section 9's new approach: run it before `lora_util.wrap`, under `model.eval()`; write the original rows of the sampled events, sorted by `(event, sent_idx)`, into a temp jsonl, and feed the same file to both `share_data.load_events(limit=0)` and the old script's `CallDS` / `ParamDS(limit=0)`; pair them by position and assert row-by-row that `text` matches and drop counts match, then compare per-row ce; the reference path runs the old `inst_ce` in batches of 4 rows (it computes full-vocabulary logits over all positions, and a whole event in one batch would OOM), and the baseline is likewise defined on batches of 4 rows; under fp32, `set_float32_matmul_precision("highest")` and turn off TF32, with an fp32 mask; `ALIGN_CHECK.json`'s key uses the uppercase `PASS`.
   Restate the four implementation constraints from spec section 4: `sdpa_kernel([EFFICIENT_ATTENTION])` is only wrapped on cuda (on CPU it would raise "No viable backend"); the mask's dtype equals the query's dtype (bf16 under autocast, fp32 under the fp32 check; if ticket 01's `batch_mask` only emits one dtype, the caller does a single `.to(dtype)`); pad positions padded up to 16 still need position_ids; only pass loss positions through `lm_head` (in both training and evaluation), never computing full-position logits.
5. Tests `tests/test_share_trainer.py`, the three items (a)(b)(c) of spec 12. Use `transformers.Qwen3Config` with random initialization for the small model (full-parameter, no LoRA attached), with `vocab_size` taken from the real tokenizer's vocabulary size (skip if the tokenizer doesn't exist); `save_pretrained` into a `tempfile` directory, and save the real tokenizer into the same directory. (c) uses `main()` to run `--base <that directory> --smoke --max-events 6 --log-every 1 --device cpu --align-events 2`. The test's import of the training scripts must catch `SystemExit` as skip, per `tests/test_cparam_assembly.py` lines 21-29.

## Acceptance

- `python3 -m unittest tests.test_share_trainer tests.test_share_data tests.test_cparam_assembly tests.test_lora_merge` passes (also under cprobe-env).
- `grep -n "heartbeat.emit" pipeline/train/train_causal_share.py` hits three places (startup, every step, and the finish with `status="done"`).
- `grep -n "sdpa_kernel" pipeline/train/train_causal_share.py` hits: the training forward pass, the evaluation forward pass, and the alignment-check forward pass are all wrapped in `sdpa_kernel([SDPBackend.EFFICIENT_ATTENTION])` (this context has no effect on the CPU kernel, so it doesn't affect the CPU tests).
- `cprobe-env/bin/python pipeline/train/train_causal_share.py --mode cgen --data pipeline/data/nyapass_aw_v1/gptoss --out pipeline/runs/smoke/share_cpu_cgen_smoke --device cpu --smoke --max-events 6 --log-every 1 --align-events 2 --base qwen --force` runs to completion on CPU (the output directory is an existing smoke-test location under an NFS symlink; the fp32 `best/` for 0.6B, about 2.4 GB, must not be written to /tmp or home; 0.6B fp32 on CPU with 6 short events can take a few minutes; `--smoke` takes the first N events by ascending full-text token count, so all 6 events are the shortest ones), producing `ALIGN_CHECK.json` (`PASS` true, `max_abs_diff <= 2e-5`, `n_rows` equal to the number of rows surviving in the reference path among the sampled events), `train_log.jsonl` with all five event types, and `best/meta.json` (containing `trainer: "share"`, `call_sep`, `max_len`, `data`). Run cparam the same way, `--out pipeline/runs/smoke/share_cpu_cparam_smoke`.
- Zero changes to the old scripts other than `build()`'s two keyword arguments (`git diff --stat` shows only that one spot in `train_causal_callgen.py`; no change in `train_causal_param.py`).
- No changes to `EVAL_CELLS`, `summarize_matrix.py`, or the four eval scripts.
- The GPU alignment check and smoke testing are not part of this ticket: just write the prepared commands into the report.

## Comments

- 2026-08-28 plan-8-28 closeout: wave2 implementation passed review with 2 fix rounds, branch `ticket/2026-08-28-wave2/T03` (base `2216c44`, head `312038d`), merged into `12c4a2b` (`train_causal_share.py` 788 lines, `tests/test_share_trainer.py` 424 lines, `run.py` +57/-?, the old script `train_causal_callgen.py`'s `build()` got its two keyword arguments). Main-session re-check: `python3 run.py selfcheck` all 76 tasks in place; `python3 -c "import ops.launch_probe"` ok; `python3 run.py show train-cgen` / `train-cparam` print commands containing `train_causal_share.py --mode`; under cprobe-env, `test_share_trainer test_ctool_readpos test_share_data test_cparam_assembly test_lora_merge` Ran 58 tests OK; `grep -c heartbeat.emit` 3, `grep -c sdpa_kernel` 3. Leftover minors (verbatim): F2 `ALIGN_CHECK.json` writes an extra `bf16_warn` diagnostic key not in the spec list; N2 `TestRefForwardUsesInstCe` only tests the matching path, not the branch where the assertion fires. Implementer concerns (verbatim): the alignment-candidate sampler `_align_candidates` does not reuse `load_events` (to avoid per-row tokenizing the whole val set just to sample 6 candidates); the reference path's per-token CE is computed by hand in `_ref_forward` (`inst_ce` only returns the row mean), and a runtime assertion checks that the hand-computed row mean matches `inst_ce` (`REF_INST_CE_DRIFT_TOL`) -- handed to 8-28-assistant-2 to cross-check against design; `--mem-probe` and the cuda branch have not been run on GPU; `--mem-probe`'s full-set load does not apply the `--readonly-env` filter (ro=None); `done.wall_s` includes `--mem-probe` time; a temporary symlink under `.gitignore`'s `*-env/` rule did not match in the new worktree (the temp symlink was deleted, never entered a commit). Two cannotVerify items (cuda behavior, an actual selfcheck run) have already been run by the main session or are handed to GPU smoke testing.

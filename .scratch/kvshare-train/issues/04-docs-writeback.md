# 04 probe-pipeline skill write-back

Status: resolved
Blocked by: 02, 03 (and also waiting on the main session's GPU smoke-test ruling: the fixed values for `--max-len` and `--tok-budget` will be given in this ticket's Comments -- do not start without that Comment; the Comment is already at the end of this file)
Spec: `.scratch/kvshare-train/spec.md` sections 8, 13; the write-back rules are `.claude/skills/probe-pipeline/SKILL.md` Phase E (lines 231-261) and `references/extending.md` section 6

## What to do

Work through `extending.md` section 6's checklist item by item, touching only the six files below, in the style each file already uses:

1. `MAP.md`'s training table at line 88: change the program column for the cgen and cparam rows to `pipeline/train/train_causal_share.py`, and the key-settings column to the wording from ticket 03's report; fill the ctool row's key-settings column with the wording from ticket 02's report; add a new `(shared)` row for `pipeline/train/share_data.py` (wording from ticket 01's report); add a new `(reference)` row for the two old scripts, reading "row-by-row reference implementation, used only for alignment checks and comparison, its output does not enter the matrix."
2. `.claude/skills/probe-pipeline/references/stage-commands.md`: add `train_causal_share.py`'s real commands to section 3's training-command-and-parameter table (copy the CPU smoke command from ticket 03's report; the GPU command is generated by `python3 run.py show train-cgen`), and add `--mode / --tok-budget / --events-per-mb / --accum / --eval-per-epoch / --align-*` to the parameter table; the smoke-limit line at line 213 gains the new trainer's 40 training events / 16 eval events; section 3.4 or wherever the batch prefix lives gains `ks828` (the new convention's batch prefix, shaped like `ks828b06` / `ks828l17`); section 7's interface pitfalls gains three items: `--mode` is required, `train-cgen-rows` is a reference not a live task, and an event over `--tok-budget` becomes its own block. Change ctool's parameter table's default values (the fixed `--max-len`, `--accum 2`, `--align-tol` default 3e-4 -- change the line at line 210 that reads "default 1e-4; under long windows... the c1 batch uniformly uses 3e-4" to "default 3e-4 (as of 2026-08-28, long windows are now the norm above the 8192 limit; ks828b06 smoke hit maxdiff_hidden 1.03e-4, relative difference 1.46e-6, on an 8,167-token event, which the old 1e-4 blocked" -- and change MAP.md line 92's ctool row's "reference value `--align-tol 3e-4`" to "default 3e-4").
3. `references/invariants.md`: first `grep -n "4096\|accum 8\|32 events\|truncation\|epochs 3\|bs 4"` to find every line still stating the old convention (at least: the causal hyperparameter line's "bs 4 events / accum 8 / epochs 3," the cgen target-construction line's "max_length = 4096 minus ... left truncation," the limit line, and line 52's "smoke scale" -- the new trainer's `--smoke` takes 40 / 16 events by ascending full-text token count, not a random SEED sample), and change ctool's `--bs/--accum` per the fixed values in the Comments -- change them line by line to the new convention: the limit (the fixed value from the Comments), overlong events dropped whole, 8 events per update (cgen/cparam is 4 events per logical mini-batch times an accumulation of 2, ctool is bs 4 times accum 2), cgen/cparam 1 epoch evaluating every quarter, ctool 3 epochs, the fixed `--tok-budget` value. Keep one sentence in each place noting "the np821 batch used 4096 / 32 events / 3 epochs."
4. `references/extending.md`: add three rows to section 5's silent-failure table. #25: location `train_causal_tool.py` and `eval_tool.py`'s read-position rule; trigger condition is when the token before the cut point spans across the cut and the text after the cut is whitespace; symptom is that 6.3% of cut points read the hidden state of the word before the sentence-final punctuation, and the training and offline-eval numbers are internally consistent so nothing errors out; note that the rule was changed on 2026-08-28 to align the read position with the token boundaries from tokenizing the full text once, and **do not write this as "the live-run misalignment is fixed"** (the token boundaries the model generates during a live run may differ from those produced by tokenizing the offline full text once; that's a separate pitfall, spec 11.3's closing paragraph). #26: `train-cgen-rows` / `train-cparam-rows` go through the old scripts (4096, left truncation, 3 epochs); their `meta.json` output has no `trainer` field, and their run_id shape matches the live tasks', so feeding them into eval would silently mix them into the matrix with no way to tell them apart. #27: `share_data.py`'s top-level import of the old training scripts causes `eval_tool` / `eval_mbert_call` under mbert-env to raise `SystemExit` during import (the version gate lives at the old scripts' module level), symptom being that `eval-tool-mbert` dies the instant it runs and prints "wrong interpreter?" Add a sentence after the "new-cell / new-training-method axis" criterion at the start of section 3 (lines 113-121): swapping in a new implementation (cell name, data, and eval unchanged, only the training script replaced) goes down the "new training method" path, precedent being `train_causal_share.py` on 2026-08-28; add a sentence to section 3.4 noting that the new convention's batch prefix is `ks828`.
5. `.claude/skills/probe-pipeline/SKILL.md`: wherever Phase 0 or the corresponding section names the cgen/cparam training script, change it to the new script's name (`grep -n callgen` first to find them).
6. `references/gates.md`: change only the explanatory text that states the old convention's numbers (4096 / 32 events / truncation); do not add a gate number, do not change any gate's semantics.

## Acceptance

- `grep -n '^#' .claude/skills/probe-pipeline/references/*.md`: every section number and gate number cited resolves correctly (the self-check at SKILL.md lines 259-260).
- No new gate number is added (there is no new gate this round).
- Do not touch `TIMELINE.md` (the main session writes it).
- The report lists every change by file and line number.

## Comments

- 2026-08-28 plan-8-28's smoke-test ruling (this Comment is the ticket's start gate). Fixed values: all three cells keep `--max-len` default 8192, no retreat; the new trainer's `--tok-budget` default is 16384 (`--eval-tok-budget` default is 2x, i.e. 32768); `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` is not adopted; ctool defaults to `--bs 2 --accum 4` (still 8 events per update), `--align-tol` default 3e-4. Basis (cite the numbers and their source, `pipeline/runs/smoke/ks828b06_*` and `ops/runs.jsonl`, when writing them into invariants / stage-commands): the speed tier `ks828b06_gptoss_cgen_speed_b16k` (H100, 450 events, 20,641 rows, 57 updates) has cumulative ips at 1,600 / 9,600 / 19,200 rows of 185.6 / 189.5 / 184.1 against the old trainer's 2.76 / 3.26 / 3.97, windowed values 185.6 / 203.8 / 145.3 against 2.77 / 3.94 / 6.24, a training step peak of 60.59 GB (56.4 GiB, 39% headroom against 93.10 GiB), and a longest-event probe of 31.4 GB; `b24k` is 15% slower with a peak of 80.91 GB; `b16k_es` is 3% slower with the same peak. The alignment check (6 events, 154 rows, 2,877 tokens) has max_abs_diff 5.48e-6, max_tok_diff 4.29e-5, bf16 mean 8.43e-3. ctool smoke: on H200, alignment maxdiff_hidden 1.03e-4 PASSes under 3e-4, `dropped_events` 1/3 matching plan section 4's row for 8192; on H100, `--bs 4` OOMs on the first training batch (process at 92.94 GiB); `--bs 2 --accum 4` runs to completion, with an nvidia-smi max sample of 56,859 MiB. Add a row to stage-commands.md section 3.1's measured-peak table: "ctool 0.6B full-parameter, 8192 x bs 2: 56,859 MiB (H100, nvidia-smi sampling)" and "cgen new trainer, 16384 budget: 60.59 GB allocated (H100)."
- 2026-08-28 plan-8-28 closeout: wave4 implementation passed review with 1 fix round, branch `ticket/2026-08-28-wave4/T04` (base `d6aa99d`, head `39da84e`), merged as `9fc83b7` (five files, +57/-14: extending.md's section 3 new-implementation precedent and section 5's #25-#27, gates.md's old numbers, invariants.md lines 45/47/48/53, stage-commands.md's section 3 command table / section 3.1 peak table / line 213 smoke limit / section 7, MAP.md's cgen/cparam/ctool rows and the (shared)(reference) rows). Implementer's call (main session agreed on re-check): leftover `--bs 4 --accum 2` in the body of items 2 and 3 was unified to `--bs 2 --accum 4` per this Comment and per code (decision 15, `8fa95dc`); `grep callgen` in SKILL.md hit zero, unchanged. Leftover minor N1: the sha `9601eb0` cited in report section 3 does not exist; the actual tip is `39da84e`. Concerns (verbatim): stage-commands.md section 3.1's peak table crams two single-cell datapoints into a table shaped for the three-cell same-batch case (using -- as a placeholder); #25's trigger condition was not verified against the old code line by line (taken as written in spec 11.3 and the ticket text).

# Contract errata

Decisions the per-folder build plans had to settle where
`notes/plans/2026-09-17-contracts.md` contradicts itself or leaves a needed
detail open. The contracts document is never edited; this file records what the
build does instead. Format:
`- <contracts section>: <what it says> -> <what the build does> (planner: <folder>)`

- 4.1 `open_env`: says it "returns an instance" without naming the class -> the build instantiates the module's one strict subclass of `Environment` and refuses, naming the module, on zero or more than one (planner: environments)
- 4.1 `open_env`: does not say it reads `constants/path_datasets.yaml` although the 0.2 annotation does -> `open_env` reads it first and refuses an environment with no block, naming the blocks the file has, before importing anything; each environment file reads its own block itself (planner: environments)
- 4.2 against 1.1 (`task_text`): 1.1 has `agent/loop.py` capture the task text after `Environment.open` while stating that the nine methods do not hand it out, leaving no path to it -> `open` sets the instance attribute `task_text` (AppWorld's `world.task.instruction`), which `agent/loop.py` reads and `close` clears (planner: environments)
- 2.3 `requested_pairs`: does not state the order of the cross with seeds -> the build crosses task-major, every seed of one task adjacent, as `legacy/envs/collect/run_appworld.py:176` does (planner: environments)
- 4.2 `build_call`: says a value holding a top-level `,`, `=`, quote or bracket is quoted with `repr()` -> the build wraps it in a bare `'` (or `"` when the value holds a `'`, `ValueError` when it holds both) and also quotes an empty value, because `repr()` escapes newlines and backslashes that `split_args` never unescapes: `repr()` fails 2.5's round-trip gate on 4 of the first 476 real actions, bare quoting passes 4,040 of 4,040 over all 315 p1 trajectories (planner: environments)
- 4.2 `complete_call`: "finish a call the probe generated but cut off" and "None when it cannot be balanced" cannot both hold -> the build extracts the first `apis.<app>.<api>(...)` whose parentheses balance (`legacy/pipeline/inject/parse_call.py:27-69`) and returns None when none does, appending nothing (planner: environments)
- 4.2 `step`: does not say what `observation` holds when `action` is None -> the build writes `NO_CODE_BLOCK` with `error_kind` None and `completed` False, today's record mark (`legacy/pipeline/inject/rebuild.py:78`) (planner: environments)
- 4.2 `open`: "named so two seeds never share the benchmark's own output directory" names no shape -> AppWorld's `experiment_name` is `<task_id>__s<seed>` (1.1's `record_id`) and `close` deletes `<home>/experiments/outputs/<experiment_name>` (planner: environments)
- 4.3 `speculate`: gives the clock assertion no baseline source, and legacy skipped the guard when its first read failed -> `open` reads and stores the frozen clock and `speculate` raises when there is none, so the guard is never silently off (planner: environments)
- 4.2 `judge`: promises a dict with a boolean `success` while AppWorld's `evaluate()` can raise -> on any exception or non-conforming shape the build returns `{"success": false, "eval_error": "<message>"}` (planner: environments)
- 4.4 and 6.3: say the benchmark package is imported inside five methods and give the environment a `data` column AppWorld cannot be pointed at -> the `from appworld import AppWorld` statement sits in `open` alone (the other four use the stored handle), `APPWORLD_ROOT` is set to the `home` column instead of a process `chdir` (`appworld/common/path_store.py:14`), and construction refuses when `data` is not `<home>/data` (planner: environments)
- 6.1: gpt_oss_120b's result block writes `dtype: bfloat16` -> models/table.yaml writes `dtype: auto`, the value legacy/serve_preset.py:41-48 effectively used (it passes no --dtype), since an explicit bfloat16 over the mxfp4 checkpoint is a different server (planner: models)
- 6.1: qwen3_0pt6b's result block writes `dtype: bfloat16` -> models/table.yaml writes `dtype: float32`, because legacy/pipeline/train/train_causal_tool.py:196 loads the backbone fp32 and autocasts bf16; the bf16 cast stays on the restore path as qwen.DTYPE (planner: models)
- 6.2: `load(row, cfg, *, probe_kind, n_labels, labels, ckpt_dir)` carries no device while 7.2's serve takes --device -> `load(..., device="cpu")`, and a restore with no row takes its dtype from the backbone module's DTYPE on cuda and float32 on cpu (planner: models)
- 6.2: Outputs says logits and hidden are "both aligned to input_ids" and also that a classifier's logits carry one row per event -> a classifier's logits are [n_events, n_labels] with hidden [B, T, dim]; a generator's logits are [B, T, vocab] with hidden None (planner: models)
- 6.2: Batch["event_end"] is "the position of the last non-pad token of each event", which cannot name the sequence an event sits in -> event_end is a LongTensor of shape [n_events, 2] holding (row, col) index pairs into input_ids, the two lists legacy/pipeline/train/train_causal_tool.py:170-186 carried (planner: models)
- 6.2: the probe object has no name for moving to a card, for the optimizer's parameter list or for gradient checkpointing -> Probe subclasses torch.nn.Module with backbone, head, tokenizer, max_len, labels, probe_kind, lora and offers enable_grad_checkpointing() (legacy/pipeline/train/lora_util.py:122-138); forward moves every Batch tensor to the probe's device (planner: models)
- 6.2: end_of_turn runs under `venv: any`, where openai_harmony is absent (facts table) -> models/agent_models/gptoss.py carries END_IDS = (200002, 200012) as a module literal, measured from openai_harmony 0.0.8's stop_tokens_for_assistant_actions() on 2026-09-17, with an acceptance command comparing the two (planner: models)
- 7.3/6.2: a p2 entry's system_text is "applied by the family's wrap_prefetch" with no place named -> wrap_prefetch puts system_text + "\n\n" in front of the body inside the prefetch message, and is unchanged on None (planner: models)
- 6.2: render_ids types effort and date as `str | None` -> gptoss.render_ids refuses None for either, naming the field, since 5.2 resolves both before the freeze and legacy/pipeline/inject/harmony_render.py:86 refuses an unpinned date (planner: models)
- 7.1: the agent service's serve command line has no replica while 7.4 names the file service_agent_<replica>.json -> serve takes `--replica <i>` (default 0) and jobs/launch.py passes it (planner: models)
- 7.1 / 1.5: `--attach-only` writes `attached_to: <the owning run's run_id>` into the endpoint file while the pinned command line gives the attaching piece no channel to that run_id (`GET /v1/models` exposes only the served model name and `max_model_len`, 7.4's precheck errata) -> `serve --attach-only` also takes `--attached-to <run_id>`, required alongside `--attach-only` and refused without it, on the same command line as `--replica`; `jobs/launch.py` is the side that already knows the answer, from the live registry row it searched to find the attachable server (7.4), and passes it (planner: models)
- 7.1: `stream(prompt_ids, generation, seed)` gives `generation` no shape and the per-request max_tokens (max_step_tokens minus the ids already generated, legacy/pipeline/inject/live_appworld.py:449) no source -> generation is a dict with exactly max_tokens, temperature, top_p, stop, built by agent/generate.py; top_p, stop and seed are left out of the body when None; a 400 re-raises as urllib.error.HTTPError and everything else retries three times (planner: models)
- 7.3: writes `probe_client.render(...) -> prefix_ids` while 7.2's route returns a body -> every probe client method returns the route's decoded JSON dict, render included, and a caller takes ["prefix_ids"] (planner: models)
- 1.5: service_<kind>_<replica>.json's fields are named in prose only -> the file holds kind, replica, base_url, host, port, pid, started_at, flags, claims, attached_to; claims is the frozen result block plus role and family for the agent service and {family, weights, score_train_key, gen_train_key, max_len, temperature} for the probe service; the agent base_url ends in /v1, the probe's does not (planner: models)
- 7.2: the probe server's concurrency is open while one service answers sample.pieces loop processes at once (legacy served one driver, probe_server.py:277) -> ThreadingHTTPServer with one lock around every model call (planner: models)
- 6.2: the two exceptions to "every keyed column is read from the frozen setting" leave out the agent service, which 0.2 gives the models/__init__.py import -> models/agent_models/service.py resolves family and weights live through models.agent(alias) and compares both against cfg.models.agent_row before its first use, refusing on a difference (planner: models)
- Part 0.2 (`eval/utils/probe_eval.py` reads) / 2.5: the generator-eval build-key gate is stated over "its own and the referenced eval run's train `meta.json`", with no path from this run to the referenced eval's train key -> the build reads the referenced eval run's own `meta.json` `upstream["train"]` first, then that train run's `meta.json` `upstream["build"]`, both located with `run_dir_of(..., debug=False)` (planner: eval)
- Part 1.3: the prediction row carries no cut index while the first-crossing rule needs an order within an event -> cuts are ordered by `depth` ascending, tie-broken by `example_id` ascending (planner: eval)
- Part 1.4: "the three exact-match tiers are today's", and today's `params_all_ok` is true for every event whose ground truth has no arguments whatever was generated (`eval_causal_call.py:401`, `eval_causal_param.py:284`) -> the build drops that short-circuit and scores the union count (`n == 0 or ok == n`), so a spurious argument on a no-argument call is wrong (planner: eval)
- Part 1.4 (`temperature`): the fit method is unstated and today's is torch LBFGS, which `venv: any` forbids -> `probe_eval.fit_temperature` minimises the mean NLL over `log T` with NumPy, a grid of 81 points over `[-4, 4]` then a golden-section refinement inside the winning cell to a tolerance of `1e-6` (planner: eval)
- Part 1.4 (`ci`): the interval's shape is unstated -> `ci` is a dict from statistic name to a `[lo, hi]` pair, and `bootstrap_ci(df, group, stat, *, n, seed)` resamples the distinct `task_id` values with replacement `n` times using `numpy.random.default_rng(seed)` and takes today's sorted-index bounds `v[int(n*0.025)]` and `v[int(n*0.975)]`, rounded to 4 (planner: eval)
- Part 1.4 (`n_events`): "events per split" names no frame -> distinct `event_id` per `split` of the prediction frame the method was handed (planner: eval)
- Part 2.6 (`match`): the return shape is `bool | dict[str, bool]` and cparam's is unstated -> cparam's `match` returns the same three-key dict cgen's does, and `report` uses only its `params_all_ok` (planner: eval)
- Part 0.2 (`eval/method_table.py` reads `jobs/runs.jsonl`) / 8.0: the registry offers no raw-row reader -> `table` calls `registry.ls(workflow)` and the folded row must carry the start row's `stage`, `key`, `dir`, `diff`, `parent` and `swept` (planner: eval)
- Part 8.6 (`run.py table`): the risk tier is unstated and today's is a fixed 0.05 -> one table row per `(backbone, method, risk target)`, every risk in the report, and the cell is `mean ± sample standard deviation` over the group's runs, the bare value for a group of one (planner: eval)
- Part 0.1 (`venv: any`) and the venvs precondition: NumPy is absent from `external/appworld/venv` -> the `any` import test for `eval/` runs over the three interpreters of `constants/path_datasets.yaml`'s `venvs:` map after NumPy is installed there (a main-session prerequisite), and the system `python3` (3.10, no NumPy) is not in that map and not part of the check (planner: eval)
- Part 1.5 (`done.json.metrics`): the key names are unstated -> `eval` writes `<stat>@<risk>` (`coverage@0.05`, `trig_acc@0.05`, `earliness@0.05`, `wrong_spec@0.05` for a classifier; `tool_ok@<risk>`, `params_all_ok@<risk>`, `full_call_ok@<risk>` for a generator) and `score` writes `success`, `base_success`, `delta_success`, `tokens_out`, `spec_exec_ok`, `spec_tool_agree`, `spec_call_agree`, `n_records`, with null-valued entries omitted (planner: eval)
- Part 0.2 (`eval/utils/probe_eval.py` imports): the annotation line does not carry `data/__init__.py`, so `write_frame` is unreachable -> `probe_eval` writes `probe_report.json` and `fires.parquet` itself with `json` and `polars.write_parquet`, each through a temporary name in the same directory and a rename, mirroring `write_frame`'s rule (planner: eval)
- Part 1.4 / 2.6: `risk_targets` and `n_events` are fields of both report shapes but are not in the identity block `probe_eval.run` assembles -> the method's `report` hook returns them, and only the eight names of `IDENTITY_FIELDS` are refused (planner: eval)
- 7.3: a `FORMATS` entry has four fields and `render` takes no head, so legacy's head-seam rule has no home in the table -> `agent/inject.py` applies it, since it holds the head text (planner: agent)
- 7.3: `stream(clients, cfg, prefix_ids, seed)` cannot express the shrunken budget a resend after a fire needs (`live_appworld.py:446-450`: the kept ids count against the step budget, the discarded overflow does not) -> the exported function takes a keyword-only `budget=None` (planner: agent)
- 6.2: the family comparison ("both compare `module.NAME` against `cfg.models.agent_row["family"]` before their first use") has no stated frequency -> it is taken once per process, not once per step (planner: agent)
- 1.1 / 5.2: `gen.gen_ids` and `spec.overflow_ids` are "null unless `store_token_ids`" and the field is spelled twice (`sample.store_token_ids`, `inject.store_token_ids`) -> the producing step reads it off the one section the setting holds, through the `cfg.inject is None` test of 5.1 (planner: agent)
- 7.3: `StepResult.prefix_sha` has no stated formula -> legacy's, sha1 of the comma-joined decimal ids (`live_appworld.py:379-383`), as `ids_sha` in `agent/generate.py`, which `agent/inject.py` imports (planner: agent)
- 7.3 / 6.2: `parse` returns only the two grown channel texts while `find_head` needs the cut's offset inside the raw generated text -> the build derives `ts = len(raw) - len(thinking_so_far)` while `raw.endswith(thinking_so_far)` holds and stops scoring for the rest of the step when it stops holding (the streaming form of `live_appworld.py:151-162,462-467`) (planner: agent)
- 6.2: `end_of_turn(ids)` gets no caller in `agent/` -> a step ends when its stream ends (`live_appworld.py:539-554`) (planner: agent)
- 7.2: the injector's `/health` refusal (`decode`, `encode_special`, the two train keys) is scoped by arm in legacy (`live_appworld.py:702-704`) -> it is taken under all three arms, because an inject run's probe piece always loads both checkpoints (2.3) (planner: agent)
- 5.2: `inject.fire_nth_cut > 0` leaves the score call unstated -> no `/score` call is made at all and the fire is the `fire_nth_cut`-th counted cut, with `conf` and `pred_label` null (`live_appworld.py:477-480`) (planner: agent)
- 5.2: `inject.chunk_tokens` and `inject.tail_tokens` are keyed fields with no reader, since legacy's v4 rewrite to one stream per step left them unused (`live_appworld.py:95-108,442-455`) -> the port follows v4 and carries them in the setting and in `meta.inject` only; worth the owner's eye, since either they get a reader or they leave the key (planner: agent)
- 5.2: `split`, `seeds`, `tasks`, `n_tasks`, `max_steps`, `store_token_ids`, `pieces` and `replicas` are spelled once under `sample` and once under `inject` -> `agent/loop.py` binds the one section the setting holds and reads all eight off it (planner: agent)
- 1.1 / 5.3: `meta.arm` has four values and the `inject.arm` axis three -> `agent/loop.py` writes `"sample"` when `cfg.inject is None` and `cfg.inject.arm` otherwise, and `ARMS` holds only the three axis values (planner: agent)
- 1.1: "canonical JSON text" is unspelled for `meta.generation`, `meta.inject` and `final.judge` while a string equality compares two of them -> the build writes `json.dumps(obj, sort_keys=True, ensure_ascii=False, separators=(",", ":"))` (planner: agent)
- 1.1 / 3.4: nothing says where the loop gets `meta.owner_session`, and a loop piece reads no `meta.json` (7.4) -> it computes 3.4's session name, `f"{cfg._stage}-{cfg._key}-{piece}"`, and `jobs/launch.py` must spell it the same (planner: agent)
- 1.1: `meta.version` and every row's `ts` have no stated stamper -> `data/trajectory_record.py`'s writer stamps them, not `agent/loop.py`, the way `data/probe_output.py`'s writer stamps `version` (2.6) (planner: agent)
- 1.1: `to_messages`'s `upto_step` has no stated inclusivity -> it is exclusive, so the messages for step N carry the rows of steps 0..N-1 (planner: agent)
- 1.7 / 7.3: 7.3 says the loop appends `(observation.action, observation.observation)` "after each `Environment.step`" while both legacy sides append only for a step that produced an action (`live_appworld.py:816`, `legacy/pipeline/annotate/build.py:81-82,117`) and the offline and live histories must be identical (1.7) -> the loop appends only when `observation.action` is not None, and `data/build_training_dataset.py` does the same (planner: agent)
- 1.1 / 7.1: `final.abort` is defined only for the 400 case, so a task that raises anything else would leave a record with no `final` row -> the loop keeps legacy's guard (`live_appworld.py:739-756`) and writes a `final` row with `abort=f"task_error:{type(e).__name__}"`, `success=false` and a `judge` of `{"success": false, "task_error": "..."}`, and a failing `judge()` writes `{"success": false, "eval_error": "..."}` the same way (`run_appworld.py:229-235`) (planner: agent)
- 8.5: four `DEFAULTS` entries are listed while the verdict rules name five more numbers inline -> `DEFAULTS` also carries `stall_mult: 5.0`, `typical_beats: 20`, `min_intervals: 3`, `recent_beats: 10`, `slow_ratio: 0.5` under their legacy names (`legacy/ops/verdicts.py:16-27`), and `port_fail_rounds` and `sample_interval_s` are dropped with the sampler (planner: jobs)
- 8.5: `judge(piece)` and `judge_service(piece)` take the type `dict` with no listed keys -> the piece dict is `{kind, alive, status, done, total, has_beat, beat_ts, beat_age_s, since_launch_s, port_ok}`, assembled by `registry.ls` (planner: jobs)
- 8.4: one line format is shown for two sinks ("into `heartbeat/<piece>-<launch>.jsonl` ... as well as to stdout") -> the file holds the bare JSON object per line and stdout keeps legacy's `@hb ` prefix (`legacy/ops/heartbeat.py:15,32`) (planner: jobs)
- 2.5 / 8.0 / 8.6: the card-busy test is put on `jobs/launch.py` while `registry.free()` reports free cards under that same test -> the test lives in `jobs/registry.py` (`cards_busy`/`free`, which already probes `nvidia-smi` per its 0.2 reads line) and `jobs/launch.py` calls `registry.free()` inside its lock hold (planner: jobs)
- 8.1: the start row is appended inside the lock while its `cpu` piece entry carries a `pid` that exists only after the process starts -> `run.py` spawns the CPU process inside the hold, records its pid in the start row, releases the lock, then waits (planner: jobs)
- 3.4 / 1.5: the piece command carries no redirection while `log/<piece>.txt` is required -> the tmux inner command ends `2>&1 | tee -a <run_dir>/log/<piece>.txt` (`legacy/ops/launch_cmd.py:196-202`), appending because the file name carries no launch index (planner: jobs)
- 8.6: running anywhere but `login_host` is refused with no comparison rule, while this machine answers `shiga` to `hostname` and the `hosts:` list calls it `tokyo105` -> both sides are normalized through the `hosts:` entries' `alias` column (`legacy/ops/launch_common.py:41`) (planner: jobs)
- 5.1: none of the loader's named functions hands `run.py` the resolved upstream map before `freeze`, which 2.5's two inject gates and 5.4's temperature read need -> `experimental_settings/schema.py` also offers `upstream(stage, setting) -> dict[str, str]` (the map `freeze` writes into `_upstream`, which `freeze` itself calls) and `module_version(repo_relative_path) -> int` (3.3's text reader), and `run.py` calls both (planner: jobs)
- 8.2 / 8.0: `run.py` alone writes finish rows while `sync()` returns "the finish rows it wrote" -> `registry.sync()` appends them itself through `append_finish`, and it counts as one of run.py's five places because `run.py` is its only caller (planner: jobs)
- 8.2: `RESULTS.md` is named as rendered from `runs.jsonl` with no fixed shape -> one markdown table, newest run first, one row per `run_id` folded from its newest start and newest finish row, columns `run_id | started | stage | workflow/setting | commit | status | numbers | report` (`legacy/ops/record.py:143-172`), without that file's per-run detail blocks (planner: jobs)
- 0.2: `run.py`'s subcommands are listed with no fixed argv shape for the walk -> `run.py <workflow> <setting> [<setting> ...] [--debug] [--allow-dirty] [section.field=value ...]`, with the ten subcommand names reserved and `selfcheck` refusing a workflow file whose stem is one of them (planner: jobs)
- 8.1: the git fields are sourced from `legacy/ops/record.py:58-83` while `legacy/ops/runmeta.py:46` writes the full sha -> `commit` is the short sha (`git rev-parse --short HEAD`, `record.py:75`) (planner: jobs)
- 8.1: an alive check is named and never defined -> a `loop`, `train` or `cpu` piece passes when its log file grows inside a 30 s window polled every 5 s and its session exists with no `Traceback` in the last 4 KB (`legacy/ops/launch_cmd.py:220-242`); a `service` piece passes when its `service_<kind>_<replica>.json` appears and its port answers, bounded by `DEFAULTS["launch_timeout_s"]` (planner: jobs)
- 1.1: `done_pairs(dir, pairs)`, `read_dir(dir, pairs)` and `release(dir, live_sessions, unowned_age_s)` take a `dir` that is never said to be the run directory or its `records/` subdirectory -> `jobs/launch.py` and `run.py` pass `<run_dir>/records` (planner: jobs)
- 3.4 / 9(c)#8: a resolved reference is always keyed without the debug overlay, so a `--debug` run points at real upstream directories -> at the end of construction no real run exists, so the acceptance `--debug` walk of `inject.yaml` pins `inject.probe_score`, `inject.probe_gen` and `score.baseline` with the `key:` form of 5.4 to the debug keys G1-G3 produced, and accepts the `pinned` flag with the inheritance check, the shared-build-key gate of 2.5 and 5.7's baseline split/seed superset refusal skipped (planner: wrapup)
- 5.7: the command-line override is written `section.field=value` with no rule for the right-hand side -> the build parses the right-hand side with `yaml.safe_load`, so a reference can be pinned from the command line (`inject.probe_score={key: {train: <hex>, eval: <hex>}}`) and the acceptance walk needs no edit to gyb's YAML files (planner: wrapup)
- 0.1: `venv: any` is defined over every interpreter of the `venvs:` map of `constants/path_datasets.yaml` -> the acceptance runs the import test over exactly those three interpreters and not over the system `python3` (3.10, no NumPy), under which every `eval/` file legitimately fails to import because 0.2 gives them NumPy (planner: wrapup)
- 9(a)#13: the obsolete `notes/CONTEXT.md` entries "must be rewritten in the same commit" -> `notes/` is gyb's and an agent appends only a `TIMELINE.md` entry, so the migration ticket lists the obsolete entries (Sampler, Window, Escalation line, Incident agent, Autopsy, Incident record, Sampling history, the sampler's half of Verdict, Ledger, Refire's quota sentence, shardable, Monitoring parameters, chat baseline) inside the TIMELINE entry and the CONTEXT.md rewrite goes to gyb (planner: wrapup)
- 9(b)#16: rendering `RESULTS.md` into `notes/` is not applied, so "the four-ledger rule in CLAUDE.md stays literally true" fails -> the rewritten `CLAUDE.md` names five record layers with `jobs/runs.jsonl` and `jobs/RESULTS.md` beside the registry and drops the claim that the ledgers live in one place (planner: wrapup)
- 2.3 / 8.6: no subcommand offers a dry run and every walk launches -> the CPU acceptance of a workflow file's `--debug` load calls `schema.load(..., debug=True)`, `schema.key` and `schema.run_dir` directly and uses `run.py where`, and the walk itself is a GPU acceptance (planner: wrapup)
- 6.3: the `hosts:` example lists 105:4, 106:8, 107:8, 108:8 cards -> `constants/path_outputs.yaml` is filled from the measured table in `.claude/skills/gpu-run/references/gpu_state.md` (tokyo105/shiga 8x A6000, tokyo106 10x A6000, tokyo107 4x RTX 6000 Ada, tokyo108/saitama 3x H100 NVL + 3x H200 NVL), since 6.3's block is an illustration and the card counts differ (planner: wrapup)
- Part 1 (`write_frame`): `read_frame` picks jsonl or parquet by the suffix and `write_frame`'s suffix handling is unstated -> `write_frame` accepts `.parquet` only and raises on any other suffix, since a task record's jsonl is written row by row by `Writer.row` under Part 1's flush rule and no jsonl bulk writer has a caller (planner: data)
- Part 1 (`read_frame`'s version source): "a recorded version above `version`" without naming where the file records it -> the file's recorded version is the maximum non-null value of the declared `version` column, and 0 when that column is absent (planner: data)
- Part 1 (`schema`'s value type): `schema: dict` with no value type -> `dict[str, polars.DataType]`, in the order the format declares its columns, and `read_frame` returns the columns in that order (planner: data)
- Part 1 (`read_frame`'s type check): "a column present but of the wrong type still fails loudly" reads as a dtype comparison, which a jsonl file always fails (JSON integers infer as `Int64` against a declared `Int32`, measured 2026-09-17) -> the check is the read itself: `pl.read_ndjson(path, schema=<declared dtypes of the present columns>)` and `pl.read_parquet(...).cast(<same>, strict=True)` both raise on a value the declared dtype cannot hold, while widening a JSON integer, list or partial struct to the declared dtype is silent and intended (planner: data)
- Part 1 (`read_frame` and a null column): a jsonl column every line writes as JSON `null` infers as Polars `Null` and would fail any type check -> such a column counts as absent and is filled from `DEFAULTS`, and `REQUIRED` therefore names no column a valid file may leave null throughout (`action` and `abort` on a task record, the per-`PROBE_KIND` columns on a prediction) (planner: data)
- Part 1.1 (`dir`): `open_record`, `done_pairs`, `read_dir` and `release` take a `dir` whose meaning is unstated -> it is the run directory, and `data/trajectory_record.py` appends `records/` itself (planner: data)
- Part 1.1 (offered names): the list gives no way for a caller to name the record files it read, which 2.5 requires for `build`'s `consumed.json` -> `record_path(dir, task_id, seed) -> Path` is added to `data/trajectory_record.py`'s offered names, and `done_pairs`, `read_dir` and `release` build their paths through it (planner: data)
- Part 1.1 (who stamps a row): unstated -> `Writer.row(kind, **fields)` stamps `type` and `ts` on every row and, on a `meta` row, `version` and `record_id`; every other column is the caller's, and an undeclared field name raises (planner: data)
- Part 1.1 (`record_id`): listed as a `meta` column while `read_dir` concatenates many files, leaving non-meta rows unattributable -> `read` and `read_dir` fill `record_id` on every row from that file's `meta` row, while `Writer.row` still stamps it only on the `meta` row (planner: data)
- Part 1.1 (`to_messages`'s shape): the six parameters are pinned and the message list is not -> `developer` (`instructions`, plus `"\n\n" + extra_developer` when it is not None), `user` (`task_text`), then per step `s < upto_step` an `assistant` message carrying that step's `gen.content` and a `user` message carrying `env.result`, or `no_code` when `env.action` is null; an earlier step's `reasoning` is never resent (`legacy/envs/collect/run_appworld.py:191-215`), and per-turn framing text belongs to the environment's `INSTRUCTIONS` and to `step`'s observation (planner: data)
- Parts 1.2 and 1.3 (the writer's signature): "write, read" with no signature -> `write(path: Path, df: DataFrame) -> None`, which stamps `version`, selects the declared columns in order and calls `write_frame`, and `read(path: Path) -> DataFrame` (planner: data)
- Part 1.7 (the two guards): `cuts` divides by `max_cuts - 1` and `assemble`'s clip slices `cap - 60` and `-40`, with no stated floor -> `cuts` raises `ValueError` naming `max_cuts` below 2 (today's hard stop, `legacy/pipeline/annotate/build.py:353-355`) and `assemble` raises `ValueError` naming `probe_result_cap` below 100 (planner: data)
- Part 0.2 (`data/build_training_dataset.py`'s annotation lines): its `imports:` names no third-party and its `reads:` no `constants/` file, while 2.5 requires `consumed.json` to carry each split file's path and sha1, which only `constants/path_datasets.yaml` holds -> `imports: ...; [polars, PyYAML]` and `reads: the sample run's task records, constants/path_datasets.yaml (the splits block of cfg.data.env), the environment's split task-id files` (planner: data)
- Part 0.1 (`venv: any`): the dispatch brief reads it as the three venvs plus system `python3`, which has no Polars (3.10.12, measured 2026-09-17) -> the build keeps 0.1's definition, the interpreters of the `venvs:` map; only `data/probe_input.py`, whose import list is `re`, is additionally checked under system `python3` (planner: data)
- Part 8.4 (`build`'s heartbeat unit): `unit` for `build` is `row`, while the countable unit before the example frame exists is the record file -> `build` emits one beat per record read, with `done` and `total` counted in records, and keeps 8.4's word `row`, because the verdict functions read the word and only diff `ts` and `done` (planner: data)
- Part 2.5 (the thinking-prefix gate): "a `text` whose thinking part is not a prefix of the record's thinking" has no executable spelling -> `row.text.endswith(thinking[:cut])`, since `assemble` puts the thinking prefix last; splitting `text` on the `[THINKING]` marker would mis-fire on an observation that contains that marker (planner: data)
- Part 1.5 (`build`'s `done.json`): `counts` and `metrics` are stated for `sample` and `inject` only -> `build` writes `counts = {records, events, events_skipped_no_action, events_skipped_short_think, examples, examples_train, examples_val, examples_test, tools, tools_unseen_in_train}`, `metrics = {}` (1.5 names train, eval and score as the metric owners) and `report = "report.md"` (planner: data)
- 2.6 (`loss`, `reference_loss`): the alignment gate sums `loss` over batches and compares "both normalised per example row", without saying what one call returns -> both hooks return the unnormalised weighted sum over their own rows and `trainer.run` divides, so the sum over batches is additive (planner: train)
- 6.2 (`Batch["event_end"]`): one position per event -> an int64 `[n_positions, 2]` tensor of `(sequence index, token position)` pairs naming every position the head is applied at, one per example row for a classifier and one per target token for a generator; `Outputs.logits` has one row per entry in that order (planner: train)
- 6.2 (`Outputs.logits`): "the LM head's" for a generator -> the LM head applied at `event_end` only; full-position logits are never computed (`legacy/pipeline/train/train_causal_share.py:140-175`) (planner: train)
- 6.2 (`Batch`): only four keys are read outside the method -> two more, `mb` (int) and `mb_weight` (float), are required and read by `train/utils/trainer.py` (never by `base.py`), so the trainer reproduces legacy's per-logical-minibatch normalisation without reading a method-specific key set (planner: train)
- 6.2 (the probe object): `base.load` reads no `cfg.train.grad_ckpt` and no name gives the trainer a handle on the module -> three names are added to `Probe`: `trainable_parameters()`, `set_training(flag)`, `grad_checkpointing(enabled)`, keeping every `probe.tuning` branch inside `base.py` (planner: train)
- 1.6 (`last/`): the optimizer and scheduler state have no home -> `trainer.run` writes `<run_dir>/last/optimizer.pt` itself beside `Probe.save`'s output; `rng_state` in `last/meta.json` is `{"torch": hex, "cuda": hex|null}` and the epoch order is recomputed from `random.Random(cfg.train.seed + epoch)` (planner: train)
- 2.6 (`validate`): nothing says how often validation runs and 5.2 has no field -> validation runs at every epoch end, plus once at the end of training when `train.max_steps` cut the last epoch short; `best/` is chosen over those points (planner: train)
- 2.6 (a generator's `validate`): the generation budget and row count have no source -> `max_new` is `cfg.train.predict.max_new` and the rows are a deterministic subsample of size `GEN_N = 200`, a module-level constant of each generator method file covered by that file's VERSION (planner: train)
- 1.3 (`gen_tokens`): `Probe.generate` returns strings only, so the column has no producer -> the method computes `len(tok(text_pred, add_special_tokens=False)["input_ids"])` (planner: train)
- 2.6/1.6 (cparam's separator): `Probe.generate(texts, max_new, call_sep)` appends one separator to every text while cparam's tail is per row (`call_sep + tool + "("`) -> `train/methods/cparam.py` builds the whole prompt per row and passes `call_sep=""`, keeping `CHECKPOINT_META["call_sep"] = "\n[CALL] "` and `param_only: true` (planner: train)
- 0.2 (`train/utils/trainer.py` third-party imports): `[torch, peft]` -> `[torch]`; LoRA lives in `models/probe_models/base.py` and the LR schedule is a `torch.optim.lr_scheduler.LambdaLR` (planner: train)
- 5.2 (the physical block budget): no field gives one -> each method file uses `2 * cfg.train.max_len` for training and twice that for validation, matching legacy's 16384/32768 at max_len 8192; the split changes no number (planner: train)
- 6.2 (`Probe.score` and `predict`): `predict` writes what `Probe.score` returns -> `train/methods/ctool.py`'s `predict` computes the class logits through its own packed batches and `Probe.forward`, the path the alignment gate covers, because `score` is one sequence per text and a prediction pass costs `build.max_cuts` times the packed pass; `Probe.score` stays the served form (planner: train)
- 5.2 (`train.max_len`): "tokens per event; a longer event is dropped whole" -> applied in each method's `batches` over the longest text of the event; those rows get no prediction row and the count is `done.json`'s `counts.dropped_overlong` (planner: train)
- 1.2/1.3 (the format files' reader and writer): neither section names the functions -> `training_data.read(path)`, `training_data.write(path, df)`, `probe_output.read`, `probe_output.write`, plus the module-level `SCHEMA` dict each format file already passes to `read_frame` (agrees with the data planner's line above) (planner: train)
- 4.3 `speculate`'s `spec_s`: the section lists the field without saying how the clock is read, and AppWorld freezes the driver process's clock while a world is open -> the build reads `time.clock_gettime(time.CLOCK_MONOTONIC)` at both ends of the three-step; measured 2026-09-17 with a world open, `time.time()`, `time.monotonic()`, `time.perf_counter()` and `datetime.now()` all return a 0.0 delta across a 0.25 s sleep while `clock_gettime(MONOTONIC|REALTIME)` and `os.times().elapsed` return 0.25, which is why every `gen.wall_s` in the legacy live run `pipeline/inject/runs/fmt_smoke/p2_e1/live_50e1ac9_1.jsonl` is 0.0 (planner: environments; the same applies to every wall-clock field a loop piece writes — `ts`, `gen.wall_s`, `final.wall_s`, `final.finished_at`, the heartbeat — which belongs to the record, agent and registry planners)
- 1.1 (the `dir` parameter): the data plan makes `dir` the run directory and has `data/trajectory_record.py` append `records/`, while the jobs and eval plans pass `<run_dir>/records` -> `dir` is the run directory everywhere; `open_record`, `record_path`, `done_pairs`, `read_dir` and `release` are all called with the run directory and only `data/trajectory_record.py` spells `records/` (planner: integrator)
- 2.5 (a non-null `env.action` that `split_args` returns None for): stated as a hard stop, which stops every build over real data (measured over the 315 p1 trajectories on 2026-09-17: 34 of 4,074 non-null actions, 0.83%, in 22 trajectories — 24 code blocks that call no api at all, in 13 trajectories, plus 10 whose api call never closes its parentheses for the paren walk `split_args` uses, in 9 trajectories) -> the event is skipped and counted as `counts.events_skipped_no_call` and listed in `report.md`, exactly as a null action is; the same reasoning 2.5 uses to demote the unseen-tool gate to a report line (planner: integrator)
- 6.2 (gradient checkpointing and the trainer's handles): the models plan names `enable_grad_checkpointing()` and the train plan `grad_checkpointing(enabled)` -> `Probe` offers `trainable_parameters()`, `set_training(flag)` and `grad_checkpointing(enabled)`, the three names `train/utils/trainer.py` calls (planner: integrator)
- 6.3 (the hosts inventory): the settings plan writes 4/8/8/8 cards -> `constants/path_outputs.yaml` carries the measured counts of `.claude/skills/gpu-run/references/gpu_state.md` — tokyo105 (alias shiga) 8, tokyo106 10, tokyo107 4, tokyo108 (alias saitama) 6, 28 in total (planner: integrator)
- 5.2 (`train_probe.yaml`'s named settings): the settings plan ships `ctool_qwen3_0pt6b` and `cgen_qwen3_0pt6b` only, while the loader's `param_only` refusal and the cparam --debug walk both need a cparam setting -> the file also ships `cparam_qwen3_0pt6b`, whose `eval.theta_from` is `train_probe/ctool_qwen3_0pt6b` (planner: integrator)
- 0.1 (the interpreter in an acceptance command): `external/` is git-ignored and is not materialised in a worktree, so a relative `external/probe-env/bin/python` does not resolve where an implementer works -> every acceptance command names the interpreter by its absolute path in the main tree, which is what `constants/path_datasets.yaml`'s `venvs:` map holds (planner: integrator)

Added by the round-1 ticket review, 2026-09-17:

- 8.5 (the piece dict): the pinned key set `{kind, alive, status, done, total, has_beat, beat_ts, beat_age_s, since_launch_s, port_ok}` carries no rate, while `judge`'s `slowed` rule reads `recent_rate` and `avg_rate` (`legacy/ops/verdicts.py:110-113`), so `slowed` could never be returned and one of 8.5's six verdicts would never print -> the piece dict also carries `avg_rate: float | None` and `recent_rate: float | None`, computed by `registry.ls` from the piece's beat files through `rates(first_beat, recent_beats)`; `slow_ratio` stays in `DEFAULTS` because a live rule now reads it (planner: reviewer round 1)
- 2.5 / 8.5 (the launch gate's third clause): 2.5 says the clause holds "only while no piece of that row has been observed dead ... so the timeout covers a piece that has not appeared yet and not one that has already gone", while 8.5 makes a piece `dead` on `alive is False` alone — which would drop the clause for every freshly launched run whose tmux sessions have not started yet, the exact race the disjunction exists to prevent (9(c)#9, `M-J8`) -> for the launch gate a piece counts as **observed dead** only when it is not alive **and** has emitted at least one heartbeat beat (a `cpu` piece: has a recorded pid); a piece with no beat at all inside `launch_timeout_s` has not appeared yet and the third clause still holds. `jobs/launch.gate_open_row` therefore takes a fifth argument, `beats`, mapping `run_id -> {piece index: [beat ts, ...]}` (a missing entry means that piece has emitted nothing), which also supplies the gate's second clause; `jobs/launch.launch` builds it by reading `<row["dir"]>/heartbeat/<piece>-*.jsonl` for each open row (planner: reviewer round 1)
- 2.6 / 1.3 (`train/methods/ctool.py`'s `Batch["target"]`): 2.6 pins `batches(df, tok, cfg)` while the target must be "class indices in `labels` order", and neither the split frame nor `cfg` carries that order — recomputing it over a split gives a different order from `head_labels` over the whole frame, which is the silent mislabelling 1.3 exists to prevent -> `Batch["target"]` carries the class **name** per decision row, and `loss(probe, batch)` and `reference_loss(probe, df)` map it to an index through `probe.labels`, which `trainer.run` passed into `base.load`; `validate` and `predict` read the class order off `probe.labels` for the same reason (planner: reviewer round 1)
- 5.2 / 3.3 (where `generation.{stop, effort, date}` are resolved): 5.2 says the loader "resolves null against the family module ... and writes the resolved value into `settings.yaml`", while 3.3 lists that resolution among the four that "happen before the diff is taken" inside `key` — and `probe.lora_targets`, the fourth of those four, is explicitly left null on the dataclass -> `load` resolves `generation.stop`, `generation.effort` and `generation.date` onto the `Setting` from `module_literal('models/agent_models/<family>.py', ...)` right after the model-row expansion, so `freeze` writes real values; `probe.lora_targets` is resolved inside `key` only. The asymmetry is deliberate and has a reason: `freeze` projects the `generation` section and `models/agent_models/service.py`, `agent/generate.py` and `agent/loop.py` read `cfg.generation.{date, effort, stop}` off the frozen file, while no stage program ever reads `probe.lora_targets` off it — `models/probe_models/base.py` takes the backbone's `LORA_TARGETS` itself (planner: reviewer round 1)
- 1.1 (`Writer.row` and the stamped columns): "It stamps `type=kind` and `ts` on every row and, on a `meta` row, `version` and `record_id`; every other column is the caller's, and an undeclared field name raises" leaves it open what a caller passing `type`, `ts`, `version` or `record_id` gets -> passing a stamped column raises, naming the column and the row kind; the stamp is the one writer of those four (planner: reviewer round 1)
- 8.6 (`ls` and the host probe): `ls` computes the piece verdicts through `live_sessions()`, one `ssh` per host, which an implementer is forbidden to run -> `ls` calls `live_sessions()` only when the folded row set is non-empty, so `run.py ls` and `eval/method_table.table()` against an empty ledger issue no ssh at all; `cards_busy` / `free` stay the only other host probers (planner: reviewer round 1)

Added by the round-2 ticket review, 2026-09-17:

- Part 1 / 1.1 (`data/trajectory_record.REQUIRED`): the round-1 rule says `REQUIRED` names no column a valid file may leave null throughout, but the ticket's set still named `step`, `reasoning`, `content`, `result` and the whole `final` block -> `REQUIRED` is the twelve columns every record file carries, `{type, ts, version, record_id, stage, env, task_id, seed, split, task_text, agent_model, owner_session}`; the `gen`/`env` columns go because a task that aborts before its first `gen` row leaves a two-row `meta`+`final` file that `is_done` calls done and `read_dir` therefore reads, and the `final` columns go because `read` must still return a killed writer's file to its last flushed row (ticket 02's `D2`) (planner: reviewer round 2)
- 1.1 / 7.1 (the per-task guard's `final` row): the guard's row is specified by three fields only and may fire before the `meta` row is written -> the guard writes every column of 1.1's `final` block whatever the abort (`completed=False`, `steps`, the token and wall accumulators) and writes the `meta` row first when it fires before the normal one did, so every record file is `meta`-first and the `REQUIRED` set above holds (planner: reviewer round 2)
- 7.3 (`agent/inject.py`'s `bounds`): the chunk-boundary list is described as "the pair at the end of each chunk" with no initial value, while `token_boundary` returns `None` when no boundary is at or before `pos` and step 6 subscripts its result -> `bounds` starts as `[(0, 0)]`, verbatim from `live_appworld.py:433`, so a fire inside the first chunk has a starting point (planner: reviewer round 2)
- 2.1 / 7.3 (`agent/loop.main`'s second parameter): the program column spells `main(run_dir, piece)` while the prose rotates with `triples[piece:]` and beats with `registry.beat(run_dir, piece)`, both of which need an integer -> `piece` is the `(i, n)` pair parsed from `--piece i/n`, and the rotation, the heartbeat and `meta.owner_session` all name `i` (planner: reviewer round 2)
- 8.0 / 3.4 (`registry.session_alive` and `live_sessions`): specified as one `ssh -o BatchMode=yes <host> "tmux ls ..."` per host with no local case -> a host that normalises to `login_host` through the `hosts:` `alias` column is probed with a local `bash -c "tmux ls -F '#S'"` and no `ssh`, the short-circuit `legacy/ops/launch_common.py:41-67` already has for both the session test and the tmux launch; without it every probe on the machine `run.py` and the loop pieces run on pays an `ssh` to itself and reports **alive** fail-closed when `ssh` to self is unconfigured, which makes `jobs/launch.refire`'s liveness refusal unfalsifiable (planner: reviewer round 2)
- 2.5 (the ledger exemption): stated as behaviour of `git_state` with no addressable name, while proving it otherwise means dirtying the real `jobs/RESULTS.md`, which the spec's section 9 forbids -> `jobs/launch.py` offers `is_ledger_path(path) -> bool` as a pure test (true for `jobs/runs.jsonl`, `jobs/RESULTS.md` and any `*.lock`), and ticket 12's `B3` exercises the exemption over a fabricated path list (planner: reviewer round 2)
- 8.6 (`run.py --help`): the subcommand list is fixed and its layout is not, while ticket 16's and ticket 17's `C8` both recover the existing set with `^\s{2,}([a-z][a-z0-9_-]*)` over the rendered help -> `run.py --help` prints one subcommand per line, indented by exactly two spaces, the name first (planner: reviewer round 2)
- 0.2 (the tree listing) vs the fixed tree's `notes/` line: 0.2's tree lists no root-level `docs/` while the tree's `notes/` line says notes holds "plans/ and docs/ (moved whole)", and no ticket performed the move -> ticket 18 runs `git mv docs notes/docs`, and ticket 16's repo-review skill and ticket 17's `CLAUDE.md` both cite `notes/docs/agents/issue-tracker.md` and `notes/docs/agents/triage-labels.md` (planner: reviewer round 2)
- 5.4 / 2.1 (`{probe_score_method}` under a `key:` or `dir:` reference): the inject key folds the `VERSION` of the referenced setting's `eval/methods/<m>.py`, and 5.7 checks that `inject.probe_gen` names a whole-call generator, while a `key:` or `dir:` reference carries only stage entries, an inject setting may state no `probe` section, and `key` never touches the output tree (3.2 rule 2), so the method has no source and `schema.key("inject", ...)` raised on the pinned form section 4 of the construction plan uses -> gyb's ruling, 2026-09-17: a `key:` or `dir:` reference in `inject.probe_score` and in `inject.probe_gen` carries a sibling entry `method: <a value of the probe.method axis>`, written `{key: {train: <hex>, eval: <hex>}, method: ctool}`; it is required on those two fields in the `key:` and `dir:` forms, refused on `eval.theta_from` and `score.baseline` and on every name-form reference (the loader reads the method off the named setting), it fills `{probe_score_method}` and runs `inject.probe_gen`'s whole-call-generator check, and a name-form and a `key:`-form reference to the same runs key identically. `schema.py` cannot verify the stated method; `run.py` (ticket 14) compares it with `probe.method` in the referenced train run's frozen `settings.yaml` before it starts the stage, next to 5.4's `done.json` check. The acceptance walk of the construction plan's section 4 therefore writes `method:` inside both pinned probe references (planner: owner ruling, wave 2 reconciliation)
- 5.2 vs 2.1 / 2.2 / 3.3 (`train.checkpoint_hours`): 5.2's `train` table marks `checkpoint_hours` `key = no`, while 2.1's train row reads the whole `train` section, 2.2's "minus" column for train is empty and 2.2 names 2.1's column as the authoritative one, so the field enters the train key -> the build follows 2.1/2.2/3.3 and ticket 04's `STAGES` literal: a non-default `train.checkpoint_hours` moves the train key and every key below it. Open for the owner: whether the field leaves the key (planner: post-merge review wave 2, finding SCHEMA-1, refuted as a code defect)
- 2.5 / integrator entry 2 (the `split_args` counts over the p1 corpus): measured as 4,074 non-null actions, 4,040 parsed, 34 returning None (24 code blocks that call no api, 10 "whose api call never closes its parentheses") -> re-measured on 2026-09-17 after the call finder of `data/environments/appworld.py` became quote-aware (post-merge finding ENV-1, fix confirmed by gyb): 4,074 non-null actions, **4,050 parsed, 4,050 round-tripped, 24 returning None**, all 24 code blocks that call no api, in 13 of the 315 trajectories (0.59%). The 10 former "unclosed" actions were well-formed calls with a `(` inside a string value and now parse; 13 further actions that the old walk cut inside a password string holding `)` now parse to the full call. `counts.events_skipped_no_call` (ticket 09) therefore has one kind of input over p1 (planner: wave 2 reconciliation)

Added by the wave-3 precheck, 2026-09-17:

- 7.1 (the agent service's health row) / 8.4 / 8.5: ticket 07 words the row as "`/health` answers within `registry.DEFAULTS["launch_timeout_s"]`", while 8.4 forbids either service importing `jobs/registry.py`, 8.5's list of the five rules that read the constant does not hold the agent service, and 7.1 words the same row as "answers before `jobs/launch.py`'s alive check gives up on the piece" -> the service's check row is "`/health` answers", asked with a request timeout of the service's own; the deadline over the whole start is `jobs/launch.py`'s alive check (the 8.1 entry above), and neither service file imports `jobs/registry.py` in any spelling (planner: wave 3 precheck)
- 7.1 (`--attach-only`) / 7.4: "compares it and every keyed column against `cfg.models.agent_row`" cannot be executed over `GET /v1/models`, whose model card carries the served model name (`id`), `max_model_len` and `root` and no value for `role`, `family`, `dtype`, `quantization`, `env_result` or `extra_flags`, so a literal comparison refuses every attach -> the service compares the served model name and `max_model_len` and refuses on a difference; the comparison of the whole `result:` block stays where 7.4 already puts it, on `jobs/launch.py`, through the `claims` of the serving run's `service_agent_<replica>.json`. An assumption of the wave-3 precheck, open for the owner (planner: wave 3 precheck)
- 8.4 (eval's heartbeat `total`): ticket 08's `run` emits `hb.emit(0, total, "item")` at step 2, two steps before the prediction frame is read, and defines `total` nowhere -> `registry.beat(run_dir, 0)` stays at step 2 and the first `emit` follows the read of the prediction frame, with `total` the frame's distinct `event_id` count, the number `done.json`'s `counts["events"]` records; the same shape tickets 09, 10, 11 and 13 spell out (planner: wave 3 precheck)
- 7.3 / 1.7 (what the agent loop appends to the probe's history): 7.3 appends "after each `Environment.step`" and ticket 11 appended `(obs.action, obs.observation)` whenever `obs.action` is not None, while the merged `data/build_training_dataset.py` appends `(action.strip(), result)` and leaves a step whose stripped action is empty out of its history; `AppWorld.step` returns the code block unstripped (`'print(1+1)\n'`, and `''` for an empty block), so the live probe text differed from the training text on nearly every step, which 1.7 forbids -> gyb's ruling, 2026-09-18: the live side moves. `agent/loop.py` appends `(obs.action.strip(), obs.observation)` to `history` only when `obs.action` is a string whose `strip()` is non-empty; the record's `env` row keeps `obs.action` as the environment returned it; the builder is unchanged. Ticket 11's step 3 carries the clause (planner: owner ruling, after the wave 3 post-merge review findings BUILDER-1 and BUILDER-2)

# Contract errata

Decisions the per-folder build plans had to settle where
`notes/plans/2026-09-17-contracts.md` contradicts itself or leaves a needed
detail open. The contracts document is never edited; this file records what the
build does instead. Format:
`- <contracts section>: <what it says> -> <what the build does> (planner: <folder>)`

- 4.1 `open_env`: says it "returns an instance" without naming the class -> the build instantiates the module's one strict subclass of `Environment` and refuses, naming the module, on zero or more than one (planner: environments)
- 4.1 `open_env`: does not say it reads `constants/path_datasets.yaml` although the 0.2 annotation does -> `open_env` reads it first and refuses an environment with no block, naming the blocks the file has, before importing anything; each environment file reads its own block itself (planner: environments)
- 4.2 against 1.1 (`task_text`): 1.1 has `agent/loop.py` capture the task text after `Environment.open` while stating that the nine methods do not hand it out, leaving no path to it -> `open` sets the instance attribute `task_text` (AppWorld's `world.task.instruction`), which `agent/loop.py` reads and `close` clears (planner: environments)
- 2.3 `requested_pairs`: does not state the order of the cross with seeds -> the build crosses task-major, every seed of one task adjacent, as `legacy/envs/collect/run_appworld.py:176` does (planner: environments)
- 4.2 `build_call`: says a value holding a top-level `,`, `=`, quote or bracket is quoted with `repr()` -> the build wraps it in a bare `'` (or `"` when the value holds a `'`, `ValueError` when it holds both) and also quotes an empty value, because `repr()` escapes newlines and backslashes that `split_args` never unescapes: `repr()` fails 2.5's round-trip gate on 4 of the first 476 real actions, bare quoting passes 4,040 of 4,040 over all 315 p1 trajectories (planner: environments)
- 4.2 `complete_call`: "finish a call the probe generated but cut off" and "None when it cannot be balanced" cannot both hold -> the build extracts the first `apis.<app>.<api>(...)` whose parentheses balance (`legacy/pipeline/inject/parse_call.py:27-69`) and returns None when none does, appending nothing (planner: environments)
- 4.2 `step`: does not say what `observation` holds when `action` is None -> the build writes `NO_CODE_BLOCK` with `error_kind` None and `completed` False, today's record mark (`legacy/pipeline/inject/rebuild.py:78`) (planner: environments)
- 4.2 `open`: "named so two seeds never share the benchmark's own output directory" names no shape -> AppWorld's `experiment_name` is `<task_id>__s<seed>` (1.1's `record_id`) and `close` deletes `<home>/experiments/outputs/<experiment_name>` (planner: environments)
- 4.3 `speculate`: gives the clock assertion no baseline source, and legacy skipped the guard when its first read failed -> `open` reads and stores the frozen clock and `speculate` raises when there is none, so the guard is never silently off (planner: environments)
- 4.2 `judge`: promises a dict with a boolean `success` while AppWorld's `evaluate()` can raise -> on any exception or non-conforming shape the build returns `{"success": false, "eval_error": "<message>"}` (planner: environments)
- 4.4 and 6.3: say the benchmark package is imported inside five methods and give the environment a `data` column AppWorld cannot be pointed at -> the `from appworld import AppWorld` statement sits in `open` alone (the other four use the stored handle), `APPWORLD_ROOT` is set to the `home` column instead of a process `chdir` (`appworld/common/path_store.py:14`), and construction refuses when `data` is not `<home>/data` (planner: environments)
- 6.1: gpt_oss_120b's result block writes `dtype: bfloat16` -> models/table.yaml writes `dtype: auto`, the value legacy/serve_preset.py:41-48 effectively used (it passes no --dtype), since an explicit bfloat16 over the mxfp4 checkpoint is a different server (planner: models)
- 6.1: qwen3_0pt6b's result block writes `dtype: bfloat16` -> models/table.yaml writes `dtype: float32`, because legacy/pipeline/train/train_causal_tool.py:196 loads the backbone fp32 and autocasts bf16; the bf16 cast stays on the restore path as qwen.DTYPE (planner: models)
- 6.2: `load(row, cfg, *, probe_kind, n_labels, labels, ckpt_dir)` carries no device while 7.2's serve takes --device -> `load(..., device="cpu")`, and a restore with no row takes its dtype from the backbone module's DTYPE on cuda and float32 on cpu (planner: models)
- 6.2: Outputs says logits and hidden are "both aligned to input_ids" and also that a classifier's logits carry one row per event -> a classifier's logits are [n_events, n_labels] with hidden [B, T, dim]; a generator's logits are [B, T, vocab] with hidden None (planner: models)
- 6.2: Batch["event_end"] is "the position of the last non-pad token of each event", which cannot name the sequence an event sits in -> event_end is a LongTensor of shape [n_events, 2] holding (row, col) index pairs into input_ids, the two lists legacy/pipeline/train/train_causal_tool.py:170-186 carried (planner: models)
- 6.2: the probe object has no name for moving to a card, for the optimizer's parameter list or for gradient checkpointing -> Probe subclasses torch.nn.Module with backbone, head, tokenizer, max_len, labels, probe_kind, lora and offers enable_grad_checkpointing() (legacy/pipeline/train/lora_util.py:122-138); forward moves every Batch tensor to the probe's device (planner: models)
- 6.2: end_of_turn runs under `venv: any`, where openai_harmony is absent (facts table) -> models/agent_models/gptoss.py carries END_IDS = (200002, 200012) as a module literal, measured from openai_harmony 0.0.8's stop_tokens_for_assistant_actions() on 2026-09-17, with an acceptance command comparing the two (planner: models)
- 7.3/6.2: a p2 entry's system_text is "applied by the family's wrap_prefetch" with no place named -> wrap_prefetch puts system_text + "\n\n" in front of the body inside the prefetch message, and is unchanged on None (planner: models)
- 6.2: render_ids types effort and date as `str | None` -> gptoss.render_ids refuses None for either, naming the field, since 5.2 resolves both before the freeze and legacy/pipeline/inject/harmony_render.py:86 refuses an unpinned date (planner: models)
- 7.1: the agent service's serve command line has no replica while 7.4 names the file service_agent_<replica>.json -> serve takes `--replica <i>` (default 0) and jobs/launch.py passes it (planner: models)
- 7.1 / 1.5: `--attach-only` writes `attached_to: <the owning run's run_id>` into the endpoint file while the pinned command line gives the attaching piece no channel to that run_id (`GET /v1/models` exposes only the served model name and `max_model_len`, 7.4's precheck errata) -> `serve --attach-only` also takes `--attached-to <run_id>`, required alongside `--attach-only` and refused without it, on the same command line as `--replica`; `jobs/launch.py` is the side that already knows the answer, from the live registry row it searched to find the attachable server (7.4), and passes it (planner: models)
- 7.1: `stream(prompt_ids, generation, seed)` gives `generation` no shape and the per-request max_tokens (max_step_tokens minus the ids already generated, legacy/pipeline/inject/live_appworld.py:449) no source -> generation is a dict with exactly max_tokens, temperature, top_p, stop, built by agent/generate.py; top_p, stop and seed are left out of the body when None; a 400 re-raises as urllib.error.HTTPError and everything else retries three times (planner: models)
- 7.3: writes `probe_client.render(...) -> prefix_ids` while 7.2's route returns a body -> every probe client method returns the route's decoded JSON dict, render included, and a caller takes ["prefix_ids"] (planner: models)
- 1.5: service_<kind>_<replica>.json's fields are named in prose only -> the file holds kind, replica, base_url, host, port, pid, started_at, flags, claims, attached_to; claims is the frozen result block plus role and family for the agent service and {family, weights, score_train_key, gen_train_key, max_len, temperature} for the probe service; the agent base_url ends in /v1, the probe's does not (planner: models)
- 7.2: the probe server's concurrency is open while one service answers sample.pieces loop processes at once (legacy served one driver, probe_server.py:277) -> ThreadingHTTPServer with one lock around every model call (planner: models)
- 6.2: the two exceptions to "every keyed column is read from the frozen setting" leave out the agent service, which 0.2 gives the models/__init__.py import -> models/agent_models/service.py resolves family and weights live through models.agent(alias) and compares both against cfg.models.agent_row before its first use, refusing on a difference (planner: models)
- Part 0.2 (`eval/utils/probe_eval.py` reads) / 2.5: the generator-eval build-key gate is stated over "its own and the referenced eval run's train `meta.json`", with no path from this run to the referenced eval's train key -> the build reads the referenced eval run's own `meta.json` `upstream["train"]` first, then that train run's `meta.json` `upstream["build"]`, both located with `run_dir_of(..., debug=False)` (planner: eval)
- Part 1.3: the prediction row carries no cut index while the first-crossing rule needs an order within an event -> cuts are ordered by `depth` ascending, tie-broken by `example_id` ascending (planner: eval)
- Part 1.4: "the three exact-match tiers are today's", and today's `params_all_ok` is true for every event whose ground truth has no arguments whatever was generated (`eval_causal_call.py:401`, `eval_causal_param.py:284`) -> the build drops that short-circuit and scores the union count (`n == 0 or ok == n`), so a spurious argument on a no-argument call is wrong (planner: eval)
- Part 1.4 (`temperature`): the fit method is unstated and today's is torch LBFGS, which `venv: any` forbids -> `probe_eval.fit_temperature` minimises the mean NLL over `log T` with NumPy, a grid of 81 points over `[-4, 4]` then a golden-section refinement inside the winning cell to a tolerance of `1e-6` (planner: eval)
- Part 1.4 (`ci`): the interval's shape is unstated -> `ci` is a dict from statistic name to a `[lo, hi]` pair, and `bootstrap_ci(df, group, stat, *, n, seed)` resamples the distinct `task_id` values with replacement `n` times using `numpy.random.default_rng(seed)` and takes today's sorted-index bounds `v[int(n*0.025)]` and `v[int(n*0.975)]`, rounded to 4 (planner: eval)
- Part 1.4 (`n_events`): "events per split" names no frame -> distinct `event_id` per `split` of the prediction frame the method was handed (planner: eval)
- Part 2.6 (`match`): the return shape is `bool | dict[str, bool]` and cparam's is unstated -> cparam's `match` returns the same three-key dict cgen's does, and `report` uses only its `params_all_ok` (planner: eval)
- Part 0.2 (`eval/method_table.py` reads `jobs/runs.jsonl`) / 8.0: the registry offers no raw-row reader -> `table` calls `registry.ls(workflow)` and the folded row must carry the start row's `stage`, `key`, `dir`, `diff`, `parent` and `swept` (planner: eval)
- Part 8.6 (`run.py table`): the risk tier is unstated and today's is a fixed 0.05 -> one table row per `(backbone, method, risk target)`, every risk in the report, and the cell is `mean ± sample standard deviation` over the group's runs, the bare value for a group of one (planner: eval)
- Part 0.1 (`venv: any`) and the venvs precondition: NumPy is absent from `external/appworld/venv` -> the `any` import test for `eval/` runs over the three interpreters of `constants/path_datasets.yaml`'s `venvs:` map after NumPy is installed there (a main-session prerequisite), and the system `python3` (3.10, no NumPy) is not in that map and not part of the check (planner: eval)
- Part 1.5 (`done.json.metrics`): the key names are unstated -> `eval` writes `<stat>@<risk>` (`coverage@0.05`, `trig_acc@0.05`, `earliness@0.05`, `wrong_spec@0.05` for a classifier; `tool_ok@<risk>`, `params_all_ok@<risk>`, `full_call_ok@<risk>` for a generator) and `score` writes `success`, `base_success`, `delta_success`, `tokens_out`, `spec_exec_ok`, `spec_tool_agree`, `spec_call_agree`, `n_records`, with null-valued entries omitted (planner: eval)
- Part 0.2 (`eval/utils/probe_eval.py` imports): the annotation line does not carry `data/__init__.py`, so `write_frame` is unreachable -> `probe_eval` writes `probe_report.json` and `fires.parquet` itself with `json` and `polars.write_parquet`, each through a temporary name in the same directory and a rename, mirroring `write_frame`'s rule (planner: eval)
- Part 1.4 / 2.6: `risk_targets` and `n_events` are fields of both report shapes but are not in the identity block `probe_eval.run` assembles -> the method's `report` hook returns them, and only the eight names of `IDENTITY_FIELDS` are refused (planner: eval)
- 7.3: a `FORMATS` entry has four fields and `render` takes no head, so legacy's head-seam rule has no home in the table -> `agent/inject.py` applies it, since it holds the head text (planner: agent)
- 7.3: `stream(clients, cfg, prefix_ids, seed)` cannot express the shrunken budget a resend after a fire needs (`live_appworld.py:446-450`: the kept ids count against the step budget, the discarded overflow does not) -> the exported function takes a keyword-only `budget=None` (planner: agent)
- 6.2: the family comparison ("both compare `module.NAME` against `cfg.models.agent_row["family"]` before their first use") has no stated frequency -> it is taken once per process, not once per step (planner: agent)
- 1.1 / 5.2: `gen.gen_ids` and `spec.overflow_ids` are "null unless `store_token_ids`" and the field is spelled twice (`sample.store_token_ids`, `inject.store_token_ids`) -> the producing step reads it off the one section the setting holds, through the `cfg.inject is None` test of 5.1 (planner: agent)
- 7.3: `StepResult.prefix_sha` has no stated formula -> legacy's, sha1 of the comma-joined decimal ids (`live_appworld.py:379-383`), as `ids_sha` in `agent/generate.py`, which `agent/inject.py` imports (planner: agent)
- 7.3 / 6.2: `parse` returns only the two grown channel texts while `find_head` needs the cut's offset inside the raw generated text -> the build derives `ts = len(raw) - len(thinking_so_far)` while `raw.endswith(thinking_so_far)` holds and stops scoring for the rest of the step when it stops holding (the streaming form of `live_appworld.py:151-162,462-467`) (planner: agent)
- 6.2: `end_of_turn(ids)` gets no caller in `agent/` -> a step ends when its stream ends (`live_appworld.py:539-554`) (planner: agent)
- 7.2: the injector's `/health` refusal (`decode`, `encode_special`, the two train keys) is scoped by arm in legacy (`live_appworld.py:702-704`) -> it is taken under all three arms, because an inject run's probe piece always loads both checkpoints (2.3) (planner: agent)
- 5.2: `inject.fire_nth_cut > 0` leaves the score call unstated -> no `/score` call is made at all and the fire is the `fire_nth_cut`-th counted cut, with `conf` and `pred_label` null (`live_appworld.py:477-480`) (planner: agent)
- 5.2: `inject.chunk_tokens` and `inject.tail_tokens` are keyed fields with no reader, since legacy's v4 rewrite to one stream per step left them unused (`live_appworld.py:95-108,442-455`) -> the port follows v4 and carries them in the setting and in `meta.inject` only; worth the owner's eye, since either they get a reader or they leave the key (planner: agent)
- 5.2: `split`, `seeds`, `tasks`, `n_tasks`, `max_steps`, `store_token_ids`, `pieces` and `replicas` are spelled once under `sample` and once under `inject` -> `agent/loop.py` binds the one section the setting holds and reads all eight off it (planner: agent)
- 1.1 / 5.3: `meta.arm` has four values and the `inject.arm` axis three -> `agent/loop.py` writes `"sample"` when `cfg.inject is None` and `cfg.inject.arm` otherwise, and `ARMS` holds only the three axis values (planner: agent)
- 1.1: "canonical JSON text" is unspelled for `meta.generation`, `meta.inject` and `final.judge` while a string equality compares two of them -> the build writes `json.dumps(obj, sort_keys=True, ensure_ascii=False, separators=(",", ":"))` (planner: agent)
- 1.1 / 3.4: nothing says where the loop gets `meta.owner_session`, and a loop piece reads no `meta.json` (7.4) -> it computes 3.4's session name, `f"{cfg._stage}-{cfg._key}-{piece}"`, and `jobs/launch.py` must spell it the same (planner: agent)
- 1.1: `meta.version` and every row's `ts` have no stated stamper -> `data/trajectory_record.py`'s writer stamps them, not `agent/loop.py`, the way `data/probe_output.py`'s writer stamps `version` (2.6) (planner: agent)
- 1.1: `to_messages`'s `upto_step` has no stated inclusivity -> it is exclusive, so the messages for step N carry the rows of steps 0..N-1 (planner: agent)
- 1.7 / 7.3: 7.3 says the loop appends `(observation.action, observation.observation)` "after each `Environment.step`" while both legacy sides append only for a step that produced an action (`live_appworld.py:816`, `legacy/pipeline/annotate/build.py:81-82,117`) and the offline and live histories must be identical (1.7) -> the loop appends only when `observation.action` is not None, and `data/build_training_dataset.py` does the same (planner: agent)
- 1.1 / 7.1: `final.abort` is defined only for the 400 case, so a task that raises anything else would leave a record with no `final` row -> the loop keeps legacy's guard (`live_appworld.py:739-756`) and writes a `final` row with `abort=f"task_error:{type(e).__name__}"`, `success=false` and a `judge` of `{"success": false, "task_error": "..."}`, and a failing `judge()` writes `{"success": false, "eval_error": "..."}` the same way (`run_appworld.py:229-235`) (planner: agent)
- 8.5: four `DEFAULTS` entries are listed while the verdict rules name five more numbers inline -> `DEFAULTS` also carries `stall_mult: 5.0`, `typical_beats: 20`, `min_intervals: 3`, `recent_beats: 10`, `slow_ratio: 0.5` under their legacy names (`legacy/ops/verdicts.py:16-27`), and `port_fail_rounds` and `sample_interval_s` are dropped with the sampler (planner: jobs)
- 8.5: `judge(piece)` and `judge_service(piece)` take the type `dict` with no listed keys -> the piece dict is `{kind, alive, status, done, total, has_beat, beat_ts, beat_age_s, since_launch_s, port_ok}`, assembled by `registry.ls` (planner: jobs)
- 8.4: one line format is shown for two sinks ("into `heartbeat/<piece>-<launch>.jsonl` ... as well as to stdout") -> the file holds the bare JSON object per line and stdout keeps legacy's `@hb ` prefix (`legacy/ops/heartbeat.py:15,32`) (planner: jobs)
- 2.5 / 8.0 / 8.6: the card-busy test is put on `jobs/launch.py` while `registry.free()` reports free cards under that same test -> the test lives in `jobs/registry.py` (`cards_busy`/`free`, which already probes `nvidia-smi` per its 0.2 reads line) and `jobs/launch.py` calls `registry.free()` inside its lock hold (planner: jobs)
- 8.1: the start row is appended inside the lock while its `cpu` piece entry carries a `pid` that exists only after the process starts -> `run.py` spawns the CPU process inside the hold, records its pid in the start row, releases the lock, then waits (planner: jobs)
- 3.4 / 1.5: the piece command carries no redirection while `log/<piece>.txt` is required -> the tmux inner command ends `2>&1 | tee -a <run_dir>/log/<piece>.txt` (`legacy/ops/launch_cmd.py:196-202`), appending because the file name carries no launch index (planner: jobs)
- 8.6: running anywhere but `login_host` is refused with no comparison rule, while this machine answers `shiga` to `hostname` and the `hosts:` list calls it `tokyo105` -> both sides are normalized through the `hosts:` entries' `alias` column (`legacy/ops/launch_common.py:41`) (planner: jobs)
- 5.1: none of the loader's named functions hands `run.py` the resolved upstream map before `freeze`, which 2.5's two inject gates and 5.4's temperature read need -> `experimental_settings/schema.py` also offers `upstream(stage, setting) -> dict[str, str]` (the map `freeze` writes into `_upstream`, which `freeze` itself calls) and `module_version(repo_relative_path) -> int` (3.3's text reader), and `run.py` calls both (planner: jobs)
- 8.2 / 8.0: `run.py` alone writes finish rows while `sync()` returns "the finish rows it wrote" -> `registry.sync()` appends them itself through `append_finish`, and it counts as one of run.py's five places because `run.py` is its only caller (planner: jobs)
- 8.2: `RESULTS.md` is named as rendered from `runs.jsonl` with no fixed shape -> one markdown table, newest run first, one row per `run_id` folded from its newest start and newest finish row, columns `run_id | started | stage | workflow/setting | commit | status | numbers | report` (`legacy/ops/record.py:143-172`), without that file's per-run detail blocks (planner: jobs)
- 0.2: `run.py`'s subcommands are listed with no fixed argv shape for the walk -> `run.py <workflow> <setting> [<setting> ...] [--debug] [--allow-dirty] [section.field=value ...]`, with the ten subcommand names reserved and `selfcheck` refusing a workflow file whose stem is one of them (planner: jobs)
- 8.1: the git fields are sourced from `legacy/ops/record.py:58-83` while `legacy/ops/runmeta.py:46` writes the full sha -> `commit` is the short sha (`git rev-parse --short HEAD`, `record.py:75`) (planner: jobs)
- 8.1: an alive check is named and never defined -> a `loop`, `train` or `cpu` piece passes when its log file grows inside a 30 s window polled every 5 s and its session exists with no `Traceback` in the last 4 KB (`legacy/ops/launch_cmd.py:220-242`); a `service` piece passes when its `service_<kind>_<replica>.json` appears and its port answers, bounded by `DEFAULTS["launch_timeout_s"]` (planner: jobs)
- 1.1: `done_pairs(dir, pairs)`, `read_dir(dir, pairs)` and `release(dir, live_sessions, unowned_age_s)` take a `dir` that is never said to be the run directory or its `records/` subdirectory -> `jobs/launch.py` and `run.py` pass `<run_dir>/records` (planner: jobs)
- 3.4 / 9(c)#8: a resolved reference is always keyed without the debug overlay, so a `--debug` run points at real upstream directories -> at the end of construction no real run exists, so the acceptance `--debug` walk of `inject.yaml` pins `inject.probe_score`, `inject.probe_gen` and `score.baseline` with the `key:` form of 5.4 to the debug keys G1-G3 produced, and accepts the `pinned` flag with the inheritance check, the shared-build-key gate of 2.5 and 5.7's baseline split/seed superset refusal skipped (planner: wrapup)
- 5.7: the command-line override is written `section.field=value` with no rule for the right-hand side -> the build parses the right-hand side with `yaml.safe_load`, so a reference can be pinned from the command line (`inject.probe_score={key: {train: <hex>, eval: <hex>}}`) and the acceptance walk needs no edit to gyb's YAML files (planner: wrapup)
- 0.1: `venv: any` is defined over every interpreter of the `venvs:` map of `constants/path_datasets.yaml` -> the acceptance runs the import test over exactly those three interpreters and not over the system `python3` (3.10, no NumPy), under which every `eval/` file legitimately fails to import because 0.2 gives them NumPy (planner: wrapup)
- 9(a)#13: the obsolete `notes/CONTEXT.md` entries "must be rewritten in the same commit" -> `notes/` is gyb's and an agent appends only a `TIMELINE.md` entry, so the migration ticket lists the obsolete entries (Sampler, Window, Escalation line, Incident agent, Autopsy, Incident record, Sampling history, the sampler's half of Verdict, Ledger, Refire's quota sentence, shardable, Monitoring parameters, chat baseline) inside the TIMELINE entry and the CONTEXT.md rewrite goes to gyb (planner: wrapup)
- 9(b)#16: rendering `RESULTS.md` into `notes/` is not applied, so "the four-ledger rule in CLAUDE.md stays literally true" fails -> the rewritten `CLAUDE.md` names five record layers with `jobs/runs.jsonl` and `jobs/RESULTS.md` beside the registry and drops the claim that the ledgers live in one place (planner: wrapup)
- 2.3 / 8.6: no subcommand offers a dry run and every walk launches -> the CPU acceptance of a workflow file's `--debug` load calls `schema.load(..., debug=True)`, `schema.key` and `schema.run_dir` directly and uses `run.py where`, and the walk itself is a GPU acceptance (planner: wrapup)
- 6.3: the `hosts:` example lists 105:4, 106:8, 107:8, 108:8 cards -> `constants/path_outputs.yaml` is filled from the measured table in `.claude/skills/gpu-run/references/gpu_state.md` (tokyo105/shiga 8x A6000, tokyo106 10x A6000, tokyo107 4x RTX 6000 Ada, tokyo108/saitama 3x H100 NVL + 3x H200 NVL), since 6.3's block is an illustration and the card counts differ (planner: wrapup)
- Part 1 (`write_frame`): `read_frame` picks jsonl or parquet by the suffix and `write_frame`'s suffix handling is unstated -> `write_frame` accepts `.parquet` only and raises on any other suffix, since a task record's jsonl is written row by row by `Writer.row` under Part 1's flush rule and no jsonl bulk writer has a caller (planner: data)
- Part 1 (`read_frame`'s version source): "a recorded version above `version`" without naming where the file records it -> the file's recorded version is the maximum non-null value of the declared `version` column, and 0 when that column is absent (planner: data)
- Part 1 (`schema`'s value type): `schema: dict` with no value type -> `dict[str, polars.DataType]`, in the order the format declares its columns, and `read_frame` returns the columns in that order (planner: data)
- Part 1 (`read_frame`'s type check): "a column present but of the wrong type still fails loudly" reads as a dtype comparison, which a jsonl file always fails (JSON integers infer as `Int64` against a declared `Int32`, measured 2026-09-17) -> the check is the read itself: `pl.read_ndjson(path, schema=<declared dtypes of the present columns>)` and `pl.read_parquet(...).cast(<same>, strict=True)` both raise on a value the declared dtype cannot hold, while widening a JSON integer, list or partial struct to the declared dtype is silent and intended (planner: data)
- Part 1 (`read_frame` and a null column): a jsonl column every line writes as JSON `null` infers as Polars `Null` and would fail any type check -> such a column counts as absent and is filled from `DEFAULTS`, and `REQUIRED` therefore names no column a valid file may leave null throughout (`action` and `abort` on a task record, the per-`PROBE_KIND` columns on a prediction) (planner: data)
- Part 1.1 (`dir`): `open_record`, `done_pairs`, `read_dir` and `release` take a `dir` whose meaning is unstated -> it is the run directory, and `data/trajectory_record.py` appends `records/` itself (planner: data)
- Part 1.1 (offered names): the list gives no way for a caller to name the record files it read, which 2.5 requires for `build`'s `consumed.json` -> `record_path(dir, task_id, seed) -> Path` is added to `data/trajectory_record.py`'s offered names, and `done_pairs`, `read_dir` and `release` build their paths through it (planner: data)
- Part 1.1 (who stamps a row): unstated -> `Writer.row(kind, **fields)` stamps `type` and `ts` on every row and, on a `meta` row, `version` and `record_id`; every other column is the caller's, and an undeclared field name raises (planner: data)
- Part 1.1 (`record_id`): listed as a `meta` column while `read_dir` concatenates many files, leaving non-meta rows unattributable -> `read` and `read_dir` fill `record_id` on every row from that file's `meta` row, while `Writer.row` still stamps it only on the `meta` row (planner: data)
- Part 1.1 (`to_messages`'s shape): the six parameters are pinned and the message list is not -> `developer` (`instructions`, plus `"\n\n" + extra_developer` when it is not None), `user` (`task_text`), then per step `s < upto_step` an `assistant` message carrying that step's `gen.content` and a `user` message carrying `env.result`, or `no_code` when `env.action` is null; an earlier step's `reasoning` is never resent (`legacy/envs/collect/run_appworld.py:191-215`), and per-turn framing text belongs to the environment's `INSTRUCTIONS` and to `step`'s observation (planner: data)
- Parts 1.2 and 1.3 (the writer's signature): "write, read" with no signature -> `write(path: Path, df: DataFrame) -> None`, which stamps `version`, selects the declared columns in order and calls `write_frame`, and `read(path: Path) -> DataFrame` (planner: data)
- Part 1.7 (the two guards): `cuts` divides by `max_cuts - 1` and `assemble`'s clip slices `cap - 60` and `-40`, with no stated floor -> `cuts` raises `ValueError` naming `max_cuts` below 2 (today's hard stop, `legacy/pipeline/annotate/build.py:353-355`) and `assemble` raises `ValueError` naming `probe_result_cap` below 100 (planner: data)
- Part 0.2 (`data/build_training_dataset.py`'s annotation lines): its `imports:` names no third-party and its `reads:` no `constants/` file, while 2.5 requires `consumed.json` to carry each split file's path and sha1, which only `constants/path_datasets.yaml` holds -> `imports: ...; [polars, PyYAML]` and `reads: the sample run's task records, constants/path_datasets.yaml (the splits block of cfg.data.env), the environment's split task-id files` (planner: data)
- Part 0.1 (`venv: any`): the dispatch brief reads it as the three venvs plus system `python3`, which has no Polars (3.10.12, measured 2026-09-17) -> the build keeps 0.1's definition, the interpreters of the `venvs:` map; only `data/probe_input.py`, whose import list is `re`, is additionally checked under system `python3` (planner: data)
- Part 8.4 (`build`'s heartbeat unit): `unit` for `build` is `row`, while the countable unit before the example frame exists is the record file -> `build` emits one beat per record read, with `done` and `total` counted in records, and keeps 8.4's word `row`, because the verdict functions read the word and only diff `ts` and `done` (planner: data)
- Part 2.5 (the thinking-prefix gate): "a `text` whose thinking part is not a prefix of the record's thinking" has no executable spelling -> `row.text.endswith(thinking[:cut])`, since `assemble` puts the thinking prefix last; splitting `text` on the `[THINKING]` marker would mis-fire on an observation that contains that marker (planner: data)
- Part 1.5 (`build`'s `done.json`): `counts` and `metrics` are stated for `sample` and `inject` only -> `build` writes `counts = {records, events, events_skipped_no_action, events_skipped_short_think, examples, examples_train, examples_val, examples_test, tools, tools_unseen_in_train}`, `metrics = {}` (1.5 names train, eval and score as the metric owners) and `report = "report.md"` (planner: data)
- 2.6 (`loss`, `reference_loss`): the alignment gate sums `loss` over batches and compares "both normalised per example row", without saying what one call returns -> both hooks return the unnormalised weighted sum over their own rows and `trainer.run` divides, so the sum over batches is additive (planner: train)
- 6.2 (`Batch["event_end"]`): one position per event -> an int64 `[n_positions, 2]` tensor of `(sequence index, token position)` pairs naming every position the head is applied at, one per example row for a classifier and one per target token for a generator; `Outputs.logits` has one row per entry in that order (planner: train)
- 6.2 (`Outputs.logits`): "the LM head's" for a generator -> the LM head applied at `event_end` only; full-position logits are never computed (`legacy/pipeline/train/train_causal_share.py:140-175`) (planner: train)
- 6.2 (`Batch`): only four keys are read outside the method -> two more, `mb` (int) and `mb_weight` (float), are required and read by `train/utils/trainer.py` (never by `base.py`), so the trainer reproduces legacy's per-logical-minibatch normalisation without reading a method-specific key set (planner: train)
- 6.2 (the probe object): `base.load` reads no `cfg.train.grad_ckpt` and no name gives the trainer a handle on the module -> three names are added to `Probe`: `trainable_parameters()`, `set_training(flag)`, `grad_checkpointing(enabled)`, keeping every `probe.tuning` branch inside `base.py` (planner: train)
- 1.6 (`last/`): the optimizer and scheduler state have no home -> `trainer.run` writes `<run_dir>/last/optimizer.pt` itself beside `Probe.save`'s output; `rng_state` in `last/meta.json` is `{"torch": hex, "cuda": hex|null}` and the epoch order is recomputed from `random.Random(cfg.train.seed + epoch)` (planner: train)
- 2.6 (`validate`): nothing says how often validation runs and 5.2 has no field -> validation runs at every epoch end, plus once at the end of training when `train.max_steps` cut the last epoch short; `best/` is chosen over those points (planner: train)
- 2.6 (a generator's `validate`): the generation budget and row count have no source -> `max_new` is `cfg.train.predict.max_new` and the rows are a deterministic subsample of size `GEN_N = 200`, a module-level constant of each generator method file covered by that file's VERSION (planner: train)
- 1.3 (`gen_tokens`): `Probe.generate` returns strings only, so the column has no producer -> the method computes `len(tok(text_pred, add_special_tokens=False)["input_ids"])` (planner: train)
- 2.6/1.6 (cparam's separator): `Probe.generate(texts, max_new, call_sep)` appends one separator to every text while cparam's tail is per row (`call_sep + tool + "("`) -> `train/methods/cparam.py` builds the whole prompt per row and passes `call_sep=""`, keeping `CHECKPOINT_META["call_sep"] = "\n[CALL] "` and `param_only: true` (planner: train)
- 0.2 (`train/utils/trainer.py` third-party imports): `[torch, peft]` -> `[torch]`; LoRA lives in `models/probe_models/base.py` and the LR schedule is a `torch.optim.lr_scheduler.LambdaLR` (planner: train)
- 5.2 (the physical block budget): no field gives one -> each method file uses `2 * cfg.train.max_len` for training and twice that for validation, matching legacy's 16384/32768 at max_len 8192; the split changes no number (planner: train)
- 6.2 (`Probe.score` and `predict`): `predict` writes what `Probe.score` returns -> `train/methods/ctool.py`'s `predict` computes the class logits through its own packed batches and `Probe.forward`, the path the alignment gate covers, because `score` is one sequence per text and a prediction pass costs `build.max_cuts` times the packed pass; `Probe.score` stays the served form (planner: train)
- 5.2 (`train.max_len`): "tokens per event; a longer event is dropped whole" -> applied in each method's `batches` over the longest text of the event; those rows get no prediction row and the count is `done.json`'s `counts.dropped_overlong` (planner: train)
- 1.2/1.3 (the format files' reader and writer): neither section names the functions -> `training_data.read(path)`, `training_data.write(path, df)`, `probe_output.read`, `probe_output.write`, plus the module-level `SCHEMA` dict each format file already passes to `read_frame` (agrees with the data planner's line above) (planner: train)
- 4.3 `speculate`'s `spec_s`: the section lists the field without saying how the clock is read, and AppWorld freezes the driver process's clock while a world is open -> the build reads `time.clock_gettime(time.CLOCK_MONOTONIC)` at both ends of the three-step; measured 2026-09-17 with a world open, `time.time()`, `time.monotonic()`, `time.perf_counter()` and `datetime.now()` all return a 0.0 delta across a 0.25 s sleep while `clock_gettime(MONOTONIC|REALTIME)` and `os.times().elapsed` return 0.25, which is why every `gen.wall_s` in the legacy live run `pipeline/inject/runs/fmt_smoke/p2_e1/live_50e1ac9_1.jsonl` is 0.0 (planner: environments; the same applies to every wall-clock field a loop piece writes — `ts`, `gen.wall_s`, `final.wall_s`, `final.finished_at`, the heartbeat — which belongs to the record, agent and registry planners)
- 1.1 (the `dir` parameter): the data plan makes `dir` the run directory and has `data/trajectory_record.py` append `records/`, while the jobs and eval plans pass `<run_dir>/records` -> `dir` is the run directory everywhere; `open_record`, `record_path`, `done_pairs`, `read_dir` and `release` are all called with the run directory and only `data/trajectory_record.py` spells `records/` (planner: integrator)
- 2.5 (a non-null `env.action` that `split_args` returns None for): stated as a hard stop, which stops every build over real data (measured over the 315 p1 trajectories on 2026-09-17: 34 of 4,074 non-null actions, 0.83%, in 22 trajectories — 24 code blocks that call no api at all, in 13 trajectories, plus 10 whose api call never closes its parentheses for the paren walk `split_args` uses, in 9 trajectories) -> the event is skipped and counted as `counts.events_skipped_no_call` and listed in `report.md`, exactly as a null action is; the same reasoning 2.5 uses to demote the unseen-tool gate to a report line (planner: integrator)
- 6.2 (gradient checkpointing and the trainer's handles): the models plan names `enable_grad_checkpointing()` and the train plan `grad_checkpointing(enabled)` -> `Probe` offers `trainable_parameters()`, `set_training(flag)` and `grad_checkpointing(enabled)`, the three names `train/utils/trainer.py` calls (planner: integrator)
- 6.3 (the hosts inventory): the settings plan writes 4/8/8/8 cards -> `constants/path_outputs.yaml` carries the measured counts of `.claude/skills/gpu-run/references/gpu_state.md` — tokyo105 (alias shiga) 8, tokyo106 10, tokyo107 4, tokyo108 (alias saitama) 6, 28 in total (planner: integrator)
- 5.2 (`train_probe.yaml`'s named settings): the settings plan ships `ctool_qwen3_0pt6b` and `cgen_qwen3_0pt6b` only, while the loader's `param_only` refusal and the cparam --debug walk both need a cparam setting -> the file also ships `cparam_qwen3_0pt6b`, whose `eval.theta_from` is `train_probe/ctool_qwen3_0pt6b` (planner: integrator)
- 0.1 (the interpreter in an acceptance command): `external/` is git-ignored and is not materialised in a worktree, so a relative `external/probe-env/bin/python` does not resolve where an implementer works -> every acceptance command names the interpreter by its absolute path in the main tree, which is what `constants/path_datasets.yaml`'s `venvs:` map holds (planner: integrator)

Added by the round-1 ticket review, 2026-09-17:

- 8.5 (the piece dict): the pinned key set `{kind, alive, status, done, total, has_beat, beat_ts, beat_age_s, since_launch_s, port_ok}` carries no rate, while `judge`'s `slowed` rule reads `recent_rate` and `avg_rate` (`legacy/ops/verdicts.py:110-113`), so `slowed` could never be returned and one of 8.5's six verdicts would never print -> the piece dict also carries `avg_rate: float | None` and `recent_rate: float | None`, computed by `registry.ls` from the piece's beat files through `rates(first_beat, recent_beats)`; `slow_ratio` stays in `DEFAULTS` because a live rule now reads it (planner: reviewer round 1)
- 2.5 / 8.5 (the launch gate's third clause): 2.5 says the clause holds "only while no piece of that row has been observed dead ... so the timeout covers a piece that has not appeared yet and not one that has already gone", while 8.5 makes a piece `dead` on `alive is False` alone — which would drop the clause for every freshly launched run whose tmux sessions have not started yet, the exact race the disjunction exists to prevent (9(c)#9, `M-J8`) -> for the launch gate a piece counts as **observed dead** only when it is not alive **and** has emitted at least one heartbeat beat (a `cpu` piece: has a recorded pid); a piece with no beat at all inside `launch_timeout_s` has not appeared yet and the third clause still holds. `jobs/launch.gate_open_row` therefore takes a fifth argument, `beats`, mapping `run_id -> {piece index: [beat ts, ...]}` (a missing entry means that piece has emitted nothing), which also supplies the gate's second clause; `jobs/launch.launch` builds it by reading `<row["dir"]>/heartbeat/<piece>-*.jsonl` for each open row (planner: reviewer round 1)
- 2.6 / 1.3 (`train/methods/ctool.py`'s `Batch["target"]`): 2.6 pins `batches(df, tok, cfg)` while the target must be "class indices in `labels` order", and neither the split frame nor `cfg` carries that order — recomputing it over a split gives a different order from `head_labels` over the whole frame, which is the silent mislabelling 1.3 exists to prevent -> `Batch["target"]` carries the class **name** per decision row, and `loss(probe, batch)` and `reference_loss(probe, df)` map it to an index through `probe.labels`, which `trainer.run` passed into `base.load`; `validate` and `predict` read the class order off `probe.labels` for the same reason (planner: reviewer round 1)
- 5.2 / 3.3 (where `generation.{stop, effort, date}` are resolved): 5.2 says the loader "resolves null against the family module ... and writes the resolved value into `settings.yaml`", while 3.3 lists that resolution among the four that "happen before the diff is taken" inside `key` — and `probe.lora_targets`, the fourth of those four, is explicitly left null on the dataclass -> `load` resolves `generation.stop`, `generation.effort` and `generation.date` onto the `Setting` from `module_literal('models/agent_models/<family>.py', ...)` right after the model-row expansion, so `freeze` writes real values; `probe.lora_targets` is resolved inside `key` only. The asymmetry is deliberate and has a reason: `freeze` projects the `generation` section and `models/agent_models/service.py`, `agent/generate.py` and `agent/loop.py` read `cfg.generation.{date, effort, stop}` off the frozen file, while no stage program ever reads `probe.lora_targets` off it — `models/probe_models/base.py` takes the backbone's `LORA_TARGETS` itself (planner: reviewer round 1)
- 1.1 (`Writer.row` and the stamped columns): "It stamps `type=kind` and `ts` on every row and, on a `meta` row, `version` and `record_id`; every other column is the caller's, and an undeclared field name raises" leaves it open what a caller passing `type`, `ts`, `version` or `record_id` gets -> passing a stamped column raises, naming the column and the row kind; the stamp is the one writer of those four (planner: reviewer round 1)
- 8.6 (`ls` and the host probe): `ls` computes the piece verdicts through `live_sessions()`, one `ssh` per host, which an implementer is forbidden to run -> `ls` calls `live_sessions()` only when the folded row set is non-empty, so `run.py ls` and `eval/method_table.table()` against an empty ledger issue no ssh at all; `cards_busy` / `free` stay the only other host probers (planner: reviewer round 1)

Added by the round-2 ticket review, 2026-09-17:

- Part 1 / 1.1 (`data/trajectory_record.REQUIRED`): the round-1 rule says `REQUIRED` names no column a valid file may leave null throughout, but the ticket's set still named `step`, `reasoning`, `content`, `result` and the whole `final` block -> `REQUIRED` is the twelve columns every record file carries, `{type, ts, version, record_id, stage, env, task_id, seed, split, task_text, agent_model, owner_session}`; the `gen`/`env` columns go because a task that aborts before its first `gen` row leaves a two-row `meta`+`final` file that `is_done` calls done and `read_dir` therefore reads, and the `final` columns go because `read` must still return a killed writer's file to its last flushed row (ticket 02's `D2`) (planner: reviewer round 2)
- 1.1 / 7.1 (the per-task guard's `final` row): the guard's row is specified by three fields only and may fire before the `meta` row is written -> the guard writes every column of 1.1's `final` block whatever the abort (`completed=False`, `steps`, the token and wall accumulators) and writes the `meta` row first when it fires before the normal one did, so every record file is `meta`-first and the `REQUIRED` set above holds (planner: reviewer round 2)
- 7.3 (`agent/inject.py`'s `bounds`): the chunk-boundary list is described as "the pair at the end of each chunk" with no initial value, while `token_boundary` returns `None` when no boundary is at or before `pos` and step 6 subscripts its result -> `bounds` starts as `[(0, 0)]`, verbatim from `live_appworld.py:433`, so a fire inside the first chunk has a starting point (planner: reviewer round 2)
- 2.1 / 7.3 (`agent/loop.main`'s second parameter): the program column spells `main(run_dir, piece)` while the prose rotates with `triples[piece:]` and beats with `registry.beat(run_dir, piece)`, both of which need an integer -> `piece` is the `(i, n)` pair parsed from `--piece i/n`, and the rotation, the heartbeat and `meta.owner_session` all name `i` (planner: reviewer round 2)
- 8.0 / 3.4 (`registry.session_alive` and `live_sessions`): specified as one `ssh -o BatchMode=yes <host> "tmux ls ..."` per host with no local case -> a host that normalises to `login_host` through the `hosts:` `alias` column is probed with a local `bash -c "tmux ls -F '#S'"` and no `ssh`, the short-circuit `legacy/ops/launch_common.py:41-67` already has for both the session test and the tmux launch; without it every probe on the machine `run.py` and the loop pieces run on pays an `ssh` to itself and reports **alive** fail-closed when `ssh` to self is unconfigured, which makes `jobs/launch.refire`'s liveness refusal unfalsifiable (planner: reviewer round 2)
- 2.5 (the ledger exemption): stated as behaviour of `git_state` with no addressable name, while proving it otherwise means dirtying the real `jobs/RESULTS.md`, which the spec's section 9 forbids -> `jobs/launch.py` offers `is_ledger_path(path) -> bool` as a pure test (true for `jobs/runs.jsonl`, `jobs/RESULTS.md` and any `*.lock`), and ticket 12's `B3` exercises the exemption over a fabricated path list (planner: reviewer round 2)
- 8.6 (`run.py --help`): the subcommand list is fixed and its layout is not, while ticket 16's and ticket 17's `C8` both recover the existing set with `^\s{2,}([a-z][a-z0-9_-]*)` over the rendered help -> `run.py --help` prints one subcommand per line, indented by exactly two spaces, the name first (planner: reviewer round 2)
- 0.2 (the tree listing) vs the fixed tree's `notes/` line: 0.2's tree lists no root-level `docs/` while the tree's `notes/` line says notes holds "plans/ and docs/ (moved whole)", and no ticket performed the move -> ticket 18 runs `git mv docs notes/docs`, and ticket 16's repo-review skill and ticket 17's `CLAUDE.md` both cite `notes/docs/agents/issue-tracker.md` and `notes/docs/agents/triage-labels.md` (planner: reviewer round 2)
- 5.4 / 2.1 (`{probe_score_method}` under a `key:` or `dir:` reference): the inject key folds the `VERSION` of the referenced setting's `eval/methods/<m>.py`, and 5.7 checks that `inject.probe_gen` names a whole-call generator, while a `key:` or `dir:` reference carries only stage entries, an inject setting may state no `probe` section, and `key` never touches the output tree (3.2 rule 2), so the method has no source and `schema.key("inject", ...)` raised on the pinned form section 4 of the construction plan uses -> gyb's ruling, 2026-09-17: a `key:` or `dir:` reference in `inject.probe_score` and in `inject.probe_gen` carries a sibling entry `method: <a value of the probe.method axis>`, written `{key: {train: <hex>, eval: <hex>}, method: ctool}`; it is required on those two fields in the `key:` and `dir:` forms, refused on `eval.theta_from` and `score.baseline` and on every name-form reference (the loader reads the method off the named setting), it fills `{probe_score_method}` and runs `inject.probe_gen`'s whole-call-generator check, and a name-form and a `key:`-form reference to the same runs key identically. `schema.py` cannot verify the stated method; `run.py` (ticket 14) compares it with `probe.method` in the referenced train run's frozen `settings.yaml` before it starts the stage, next to 5.4's `done.json` check. The acceptance walk of the construction plan's section 4 therefore writes `method:` inside both pinned probe references (planner: owner ruling, wave 2 reconciliation)
- 5.2 vs 2.1 / 2.2 / 3.3 (`train.checkpoint_hours`): 5.2's `train` table marks `checkpoint_hours` `key = no`, while 2.1's train row reads the whole `train` section, 2.2's "minus" column for train is empty and 2.2 names 2.1's column as the authoritative one, so the field enters the train key -> the build follows 2.1/2.2/3.3 and ticket 04's `STAGES` literal: a non-default `train.checkpoint_hours` moves the train key and every key below it. Open for the owner: whether the field leaves the key (planner: post-merge review wave 2, finding SCHEMA-1, refuted as a code defect)
- 2.5 / integrator entry 2 (the `split_args` counts over the p1 corpus): measured as 4,074 non-null actions, 4,040 parsed, 34 returning None (24 code blocks that call no api, 10 "whose api call never closes its parentheses") -> re-measured on 2026-09-17 after the call finder of `data/environments/appworld.py` became quote-aware (post-merge finding ENV-1, fix confirmed by gyb): 4,074 non-null actions, **4,050 parsed, 4,050 round-tripped, 24 returning None**, all 24 code blocks that call no api, in 13 of the 315 trajectories (0.59%). The 10 former "unclosed" actions were well-formed calls with a `(` inside a string value and now parse; 13 further actions that the old walk cut inside a password string holding `)` now parse to the full call. `counts.events_skipped_no_call` (ticket 09) therefore has one kind of input over p1 (planner: wave 2 reconciliation)

Added by the wave-3 precheck, 2026-09-17:

- 7.1 (the agent service's health row) / 8.4 / 8.5: ticket 07 words the row as "`/health` answers within `registry.DEFAULTS["launch_timeout_s"]`", while 8.4 forbids either service importing `jobs/registry.py`, 8.5's list of the five rules that read the constant does not hold the agent service, and 7.1 words the same row as "answers before `jobs/launch.py`'s alive check gives up on the piece" -> the service's check row is "`/health` answers", asked with a request timeout of the service's own; the deadline over the whole start is `jobs/launch.py`'s alive check (the 8.1 entry above), and neither service file imports `jobs/registry.py` in any spelling (planner: wave 3 precheck)
- 7.1 (`--attach-only`) / 7.4: "compares it and every keyed column against `cfg.models.agent_row`" cannot be executed over `GET /v1/models`, whose model card carries the served model name (`id`), `max_model_len` and `root` and no value for `role`, `family`, `dtype`, `quantization`, `env_result` or `extra_flags`, so a literal comparison refuses every attach -> the service compares the served model name and `max_model_len` and refuses on a difference; the comparison of the whole `result:` block stays where 7.4 already puts it, on `jobs/launch.py`, through the `claims` of the serving run's `service_agent_<replica>.json`. An assumption of the wave-3 precheck, open for the owner (planner: wave 3 precheck)
- 8.4 (eval's heartbeat `total`): ticket 08's `run` emits `hb.emit(0, total, "item")` at step 2, two steps before the prediction frame is read, and defines `total` nowhere -> `registry.beat(run_dir, 0)` stays at step 2 and the first `emit` follows the read of the prediction frame, with `total` the frame's distinct `event_id` count, the number `done.json`'s `counts["events"]` records; the same shape tickets 09, 10, 11 and 13 spell out (planner: wave 3 precheck)
- 7.3 / 1.7 (what the agent loop appends to the probe's history): 7.3 appends "after each `Environment.step`" and ticket 11 appended `(obs.action, obs.observation)` whenever `obs.action` is not None, while the merged `data/build_training_dataset.py` appends `(action.strip(), result)` and leaves a step whose stripped action is empty out of its history; `AppWorld.step` returns the code block unstripped (`'print(1+1)\n'`, and `''` for an empty block), so the live probe text differed from the training text on nearly every step, which 1.7 forbids -> gyb's ruling, 2026-09-18: the live side moves. `agent/loop.py` appends `(obs.action.strip(), obs.observation)` to `history` only when `obs.action` is a string whose `strip()` is non-empty; the record's `env` row keeps `obs.action` as the environment returned it; the builder is unchanged. Ticket 11's step 3 carries the clause (planner: owner ruling, after the wave 3 post-merge review findings BUILDER-1 and BUILDER-2)

Added by the wave-4 precheck, 2026-09-18:

- 8.6 / 8.1 / 3.3 (`eval/method_table.py`'s cell): ticket 10 read the registry row's `diff` as a nested dict (`diff["models"]["probe"]`, `diff["probe"]["method"]`), while 8.1 makes `diff` "`settings_diff.yaml` as JSON" and the merged `schema.fields_of` writes that file as a flat map of dotted field name to value; and an eval row's `diff` never holds `models.probe` under any spelling, because `STAGES["eval"]`'s projection is `probe.method` plus the `eval` section, so every table row would print the schema default backbone -> `method` is `diff.get("probe.method")` with the schema default when absent; `backbone` is read off the **train** row: `upstream["train"]` from the eval run's `meta.json` (the file errata entry "Part 0.2 / 2.5" already has `probe_eval` read), then the row of the same `registry.ls(workflow, debug=False)` result whose `stage` is `train` and whose `key` equals it, then that row's `diff.get("models.probe")` with the schema default when absent; an eval row with no `meta.json` or no matching train row prints `?`. No `settings.yaml` is opened. The source of `backbone` is an assumption of the wave-4 precheck, open for the owner (planner: wave 4 precheck)
- 8.6 (`run.py table`) against ticket 10's `M-E2`: `M-E2` runs `run.py train_probe cgen_qwen3_0pt6b --debug` and then expects `run.py table train_probe` to show the `exact` numbers, while a `--debug` walk's start row carries `debug: true` (3.4) and 8.6 pins `table(workflow, out)` over `registry.ls(workflow, debug=False)`, which hides such rows -> the build keeps 8.6 and the errata entry on `registry.ls` as they are, and `M-E2` cannot show an eval row as written. Open for the owner: `M-E2` over a non-debug run, or a way for `table` to show debug rows (planner: wave 4 precheck)
- 6.2 / 7.3 (`parse(text_delta, state)` inside the two steps): ticket 11 spelled the call `state = mod.parse(delta, state)`, while 6.2 keeps the family's bookkeeping "in `state` and never in the returned dict" and the merged `models/agent_models/gptoss.parse` stores its accumulator in the dict it is handed and returns a new two-key dict, so the rebinding parsed every chunk after the first alone; and 7.3 does not say what the accumulator becomes after a splice rewrites `raw` -> one `state` dict per step, created as `{}`, handed to every call and never rebound, with the invariant `state["raw"] == raw`; after a fire's reset (`raw = head_txt + note`) `agent/inject.py` rebuilds it with `state = {}` and one `mod.parse(raw, state)` call (planner: wave 4 precheck)
- 1.1 (`spec.n_checked`): the table says "how many cuts were scored before this one", while `legacy/pipeline/inject/live_appworld.py:476,515-516` increments before the score call and writes that value, 5.2's `fire_nth_cut` fires when the count equals it (`:477-480`), and ticket 11's `C4` asserts 1 for a step whose only cut fires -> `n_checked` counts the cuts of the step up to and including this one, a counted cut under `fire_nth_cut > 0` being one that would have been scored (planner: wave 4 precheck)
- 2.3 (`teardown_services`): written as `ssh <host> tmux kill-session -t <session>` for every service piece, while 3.4 places a `--render-only` probe service on `login_host` and the entry on `registry.session_alive` above records that an `ssh` to this machine is not to be relied on -> a host that normalises to `login_host` through the `hosts:` `alias` column is reached with a local `tmux kill-session` and no `ssh`, the branch `jobs/launch.py` already takes to start a piece; a session counts as ended only when the command reached its host (planner: wave 4 precheck)
- 0.2 / 2.1 / 2.6 (`eval/methods/`): the tree gives eval one file per probe method (`eval/methods/ctool.py`, `cgen.py`, `cparam.py`) and the stage table runs `eval.methods.{method}` -> gyb's ruling, 2026-09-18, after reading the built files: the folder goes away and the three files fold into `eval/utils/probe_eval.py`, because nothing in `ctool.py` but `match` is specific to ctool (its report holds for any classifier probe) and `cgen.py` and `cparam.py` differ in 29 diff lines on ticket 10's branch. `probe_eval.py` then holds one classifier report, one generator report, the three `match` functions, a column-zero table method -> `PROBE_KIND`, and a column-zero table of per-method match versions (`MATCH_VERSION`), so that `train` and `inject` fold only their own method's match version and an eval-side `VERSION` bump re-keys no train run; the eval program is `eval.utils.probe_eval` for every method, the method being `probe.method` of the frozen setting. The tree has 31 Python files after it. When: after wave 4 has merged and before wave 5 (ticket 13) is dispatched, as one change of its own that also rewrites `schema.py`'s stage table and `_method_kind`, the tree document, the contracts, `README.md` and tickets 13, 14, 15 and 18. `train/methods/` stays one file per method. Not done yet; the marker is the `TODO(gyb, 2026-09-18)` at the top of `eval/methods/ctool.py` (planner: owner ruling, review of waves 2 and 3)
- 3.3 / 8.6 (what a `VERSION` bump invalidates): 3.3 folds every listed module's `VERSION` into the key, so one bump re-keys every stage that lists the file and nothing can say "this bump leaves the sample outputs usable"; 8.6's `behind` flag says a run's recorded `VERSION` is lower than the current one and cannot say whether the run is still usable -> gyb's ruling, 2026-09-18 (the outputs that matter are the ones a large model produced: `sample`, `train`, `inject`; a bump must never silently cost them): an agent may bump `VERSION`, and **every bump carries one entry in a version table in the same file**. Each file that carries `VERSION` also carries, at column zero, exactly once, a plain literal `VERSION_HISTORY = {<new version>: {"why": "<one sentence>", "stale": (<stage names>)}, ...}` with one entry for every version from 2 to `VERSION`; `stale` names the stages whose existing outputs can no longer be used after that bump, and **a missing `stale` means every stage** (the safe default: an unstated bump invalidates). `schema.py` folds into a stage's key not the file's `VERSION` but its **effective version for that stage**: the highest version whose entry makes that stage stale, or 1 when none does; with every table empty the effective version is 1 everywhere and every key is what it was. `_versions` in `settings.yaml` and `meta.json` keeps recording the file's real `VERSION` (with `_commit`, the record of which program made the output), so flipping a table entry from stale to usable brings the old directory back under its old key with no rerun. A version from 2 to `VERSION` with no entry counts as stale for every stage, and a file with no `VERSION_HISTORY` literal has an empty table (so a fixture stub holding only `VERSION = <n>` still keys, every stage stale from 2 on); `schema.py` refuses, naming the file, an entry that is not a mapping, an entry key outside 2..`VERSION`, and a `stale` value that is not a stage name. The strict shape (one entry with a non-empty `why` for every version from 2 to `VERSION`) is `run.py selfcheck`'s to enforce, not the key's. Every such file carries, directly above `VERSION`, the comment block that states this rule, so an agent editing the file reads it there; `.scratch/from-zero/spec.md` section 5 states it for implementers. `run.py ls` (ticket 14) and `run.py selfcheck` (ticket 15) read the same tables: `ls` tells a run that is behind and usable from one that is stale, `selfcheck` checks the table's shape in every versioned file (planner: owner ruling, review of waves 2 and 3)

Added by the owner's rulings at the wave-4 closeout, 2026-09-18 (given in session fork3's conversation; record `.scratch/from-zero/sdd/2026-09-18-wave4/owner-rulings.json`):

- 5.2 vs 2.1 / 2.2 / 3.3 (`train.checkpoint_hours`, the entry above marked open): ruled — the field stays in the train key, as the code already had it (planner: owner ruling)
- 2.6 (`validate`'s cadence, construction plan section 6 item 3): ruled — validation runs once at every epoch end (plus once at the end when `train.max_steps` cut the last epoch short), as the train plan's entry above already has it; no `evals_per_epoch` field is added (planner: owner ruling)
- 5.2 (`train.warmup_ratio`, construction plan section 6 item 4): ruled — the default is `0.05`, the share the old trainer hard-coded; a setting that writes `warmup_ratio: 0.05` is now at the default and the field is absent from its diff (merged as 2169c4e) (planner: owner ruling)
- 5.2 (`inject.chunk_tokens`, `inject.tail_tokens`; supersedes the entry above that carried them "in the setting and in `meta.inject` only"): ruled — the two fields leave the schema and the key, since nothing reads them; the loader refuses a YAML that still writes one, naming the field; 5.2's table and the inject row of the keyed-field table still list them and are not edited (merged as 2169c4e) (planner: owner ruling)
- 2.5 (the builder's round-trip gate, and `build_call` refusing a value that holds both quote kinds or ends in an odd number of backslashes): 2.5 makes a call that does not round-trip a hard stop -> ruled, as a general principle for rare data problems: the problem is reported in the log and the run continues wherever it can. `data/build_training_dataset.py` treats an event whose call `build_call` refuses or that fails the round-trip gate exactly like one whose action `split_args` returns None: skipped, counted under `counts.events_skipped_no_call`, listed in `report.md` with the record, the step and the reason; `build_call` itself still refuses and names the value (merged as 5a0ad25). Consequence recorded by the re-reviewer: a systemically broken `build_call` now degrades a build instead of stopping it, and only `report.md` and the examples-to-events ratio show it (planner: owner ruling)
- 1.5 / 2.5 (`eval/score_run.py`'s `run_report.json`, `report.md` and `done.json` metrics): the pinned field list is the planner's, and the owner asked for none of it beyond accuracy -> ruled: the report keeps the identity fields, `run = {n_records, n_abort, success, success_no_abort}`, `baseline` (the same block or null), `paired = {n, success, base_success, delta_success}` and `spec = {n, tool_agree, call_agree, recalled}`; everything else (step, token, discard and wall statistics, `spec.exec_ok`, `conf_mean`, `discarded_chars`, `error_kinds`, the `resume` block, the `by_seed` block) is removed with a `TODO(gyb, 2026-09-18)` in the module docstring listing the removed names; `done.json`'s metrics are `success`, `base_success`, `delta_success`, `spec_tool_agree`, `spec_call_agree`, `n_records`; `score.by_seed` is now a keyed setting nothing reads. A spec row whose `gen_call` does not parse, or whose step's `env.action` is null, counts as not agreeing. What a score report holds is the owner's to decide later; ticket 10's A8 assertions on the removed fields are retired (merged as 014f57f) (planner: owner ruling)
- 1.4 / 2.6 (the generator accuracy's matching rule: tool name equal, argument lists compared name by name in the union-by-key walk, all equal): ruled open — the rule stays as built and carries a `TODO(gyb, 2026-09-18)`; the owner decides the matching method later (planner: owner ruling)
- 8.6 / 8.1 (`eval/method_table.py`'s backbone; supersedes the wave-4 precheck entry above): ruled — an eval run's train reference may be a registry row or a directory the owner pointed at directly, so `backbone` is read from the train run's own directory: the eval run's `meta.json` `upstream["train"]` (always a key, a `dir:` reference being recorded by its directory's tail name), `schema.run_dir_of("train", key, debug=<the eval row's debug flag>)`, that directory's `meta.json` `diff.get("models.probe")`, the schema default when absent, `?` when either `meta.json` is missing; no registry lookup and no `settings.yaml` opened (merged as b72fe5b) (planner: owner ruling)
- 8.6 (`run.py table` and debug rows; closes the wave-4 precheck entry above): ruled — debug runs must be visible: `table(workflow=None, out=None, *, debug=False)` passes `debug` to `registry.ls`, the switch `run.py ls` has; `run.py table [workflow] --debug` (ticket 14) exposes it, and 8.6's pinned two-parameter signature is superseded (merged as b72fe5b) (planner: owner ruling)
- 5.5 / 8.6 (grouping a sweep's children under their parent): ruled — the settings a sweep expands to are parallel; `method_table` groups by the setting name alone and reads neither `parent` nor `swept`; the two start-row fields stay in 8.1's table and `jobs/launch.py` writes them null (merged as b72fe5b) (planner: owner ruling)
- 8.0 / 3.4 (`registry._alive_on` and the failed-host set): `failed_hosts` holds canonical names while a piece may be recorded under its alias, so a piece on an unreachable aliased host was reported dead against the fail-closed rule -> `_alive_on` canonicalises the piece's host through the `hosts:` alias column before the test, the same comparison `jobs/launch._piece_alive` makes (merged as 772ecb6). Open, recorded by the re-reviewer: `cards_busy` still keys busy cards by the raw host string, which is latent today because no GPU piece is recorded under an alias (planner: owner ruling)
- 0.2 / 2.1 / 2.6 (`eval/methods/`, the ruling entry above marked "Not done yet"): done on 2026-09-18 by session fork3, merged as f13f0ad (branch `ticket/2026-09-18-wave4/eval-methods-fold`, opus implementer, two re-review rounds; record `.scratch/from-zero/sdd/2026-09-18-wave4/eval-methods-fold.json`). `eval/utils/probe_eval.py` now carries `PROBE_KIND = {"ctool": "classifier", "cgen": "generator", "cparam": "generator"}` and `MATCH_VERSION = {"ctool": 1, "cgen": 1, "cparam": 1}` at column zero, `match_ctool`, `match_cgen`, `match_cparam` (the generator comparison body written once), one classifier report and one generator report behind `report(method, ...)`, and `run(run_dir)` reading `cfg.probe.method`; the eval program is `eval.utils.probe_eval` for every method. `schema.py`: `STAGES["eval"]["program"]` is that module, `_method_kind` reads the `PROBE_KIND` table, and a versions entry spelled `<path>#<TABLE>.<method>` takes one method's entry out of a column-zero table — `train` folds `eval/utils/probe_eval.py#MATCH_VERSION.{method}`, `inject` folds the same for `{probe_score_method}`, `eval` folds the file's `VERSION`. Its `VERSION` stays 1 (no eval run exists). The tree has 31 Python files. Tickets 13, 14, 15, 16 and 18 and `README.md` were rewritten to the new home; tickets 04 (its fixture still stubs `eval/methods/<m>.py`, and its `B4` axis check is guarded on that directory and now skips silently) and 08 (its selfcheck note that `probe_eval.py` imports no `data/environments/__init__.py` is now false, the generator report needs `open_env`) carry Comments instead. **Not rewritten, by the repo rule that an agent never edits `notes/`:** the tree document `notes/plans/2026-09-14-structure-from-zero.md` (still 34 files with `eval/methods/`) and the contracts (0.2's tree, 2.1's `eval.methods.{method}` program cell, 2.6's per-method file hook, 3.3's versions lists); this entry is the record until the owner rewrites them (planner: owner ruling, applied by fork3)
- 0.2 (the read-only hook, "left unarmed"): armed on 2026-09-18 on the owner's order, in `.claude/settings.json` beside the existing `worktree` entry; confirmed through the harness (an `Edit` on a setting file is refused with the hook's message). The hook's Bash test is a substring match over the whole command line: a command that mentions a protected path together with `cp `, `tee`, `>` or another listed word is refused whatever it does, so a fixture script that copies the settings directory with `cp -a`, or a heredoc whose text mentions a setting file, is refused from now on; such text goes through the Write tool and such a fixture is built from `git show` (planner: applied by fork3)
- 0.2 (the four `agent/` file names): gyb's ruling, 2026-09-18, after reading the built tree: the names `loop.py`, `generate.py`, `inject.py` and `inject_format.py` do not say what the files do -> renamed in place, no other change: `agent/loop.py` -> `agent/run_tasks.py` (the task walk), `agent/generate.py` -> `agent/step_without_probe.py` (one step, no probe), `agent/inject.py` -> `agent/step_with_probe.py` (one step with the probe attached), `agent/inject_format.py` -> `agent/injected_text_formats.py` (the five formats of the text written into the thinking). The stage table's program cell is `agent.run_tasks` and its versions lists name the new paths; `README.md`, the two service files' comments, ticket 15 and the spec follow; each renamed file carries a `TODO(gyb, 2026-09-18)` naming its old name, to be deleted once every program of the tree is written. Ticket 11, the wave-4 records and the errata entries above keep the old names as history. The tree document and the contracts in `notes/` still carry the old names (agents never edit `notes/`). No run exists, so no key is invalidated (planner: owner ruling, applied by fork3)
- 3.3 / 8.6 (the ruling entry above, "what a `VERSION` bump invalidates"): done on 2026-09-18, merged as 39b3d60 (branch `owner/2026-09-18-version-history`; fork1's opus implementer wrote it before the `eval/methods/` fold and its session ended before the commit; session new1-97 committed and rebased it, then an opus finisher and an opus adversarial reviewer ran four rounds; record `.scratch/from-zero/sdd/2026-09-18-wave5/version-history-report.md`). `schema.py` offers `version_history(rel_path)` and `effective_version(rel_path, stage)`; `key()` folds the effective version, `versions_of` / `freeze` / `load_frozen` keep the real `VERSION`; all 18 versioned files carry the rule block and `VERSION_HISTORY = {}`; spec section 5 states the rule. Three decisions the ruling did not make, taken in the code and open to the owner's reversal: (a) a stage-table entry spelled `<path>#<TABLE>.<method>` (the fold's per-method match version) folds the table value itself and has no `VERSION_HISTORY` axis; (b) the inject row's stand-in for the probe_score eval key is declared in the stage table as `eval/utils/probe_eval.py@eval`, and such an entry folds the highest version that made either the stage being keyed or the stood-for stage stale, so a `stale: ("eval",)` bump on that file moves the inject key; the `@<stage>` part never reaches a payload, so every key equals what it was before the change; (c) the eval row's other file, `data/probe_output.py`, has no stand-in on the inject row, so an eval-only bump of it moves the referenced eval key and leaves the inject key (a five-line comment under the `@eval` entry says so; adding `data/probe_output.py@eval` there moves every inject key once and is the owner's call). Pre-existing, reported and unchanged: `agent/injected_text_formats.py`'s `FORMATS` is a dict of `Format(...)` calls that `ast.literal_eval` refuses, while spec section 5 lists it among the loader's literals (planner: applied by new1-97)
- Added by the wave-5 post-merge review (2026-09-18/19, session new1-97; records `.scratch/from-zero/sdd/2026-09-18-wave5/post-merge-review.json` and `post-merge-fix*.md`, fixes merged as three branches ending at 660030e). (a) 2.5 / ticket 13 sections 3-4 (`reference_loss`'s "left-truncated prompt" for cgen and cparam, ported from the old collate): dropped. `reference_loss` scores exactly the rows `batches` keeps and shows them the same tokens: the same whole-event overlong drop (errata 5.2), the same `MAX_TGT_TOK` drop, no prompt truncation; the packed side cannot truncate (it is built on the shared token prefix) and the gate's slice cannot know which rows a method drops (2.6). The drop rules are written a second time inside each `reference_loss` on purpose (2.6). Consequence, open: a gate slice whose every event is over `train.max_len` compares zero rows and passes with `packed = plain = 0`; `align_check.json`'s `n_rows` is the slice height. (b) 2.3 / 2.4 / ticket 14 step 2 ("every other stage skips on the presence of `done.json`"): the contracts win, `eval` and `score` always recompute inside their key and get a new finish row when `done.json`'s `finished_at` is later than the newest one (8.2); `run.py` does this now. (c) 8.3: `run.py` folds a finished stage's `done.json` `stage_extra` into `meta.json` on its walk, under the lock, which is how the class order reaches `eval/utils/probe_eval.py`. (d) 3.4 / 9(c)#8, the `--debug` walk of the construction plan's section 4: `eval.theta_from` on its lines 3 and 4 (`cgen_qwen3_0pt6b`, `cparam_qwen3_0pt6b`) is the name form, which is keyed without the overlay (3.1, 3.4) and so points at the real ctool eval directory that the debug walk never made; the two lines take the `key:` form, `"eval.theta_from={key: {eval: <KE>}}"` with `<KE>` from `run.py where train_probe ctool_qwen3_0pt6b eval --debug`, the same device this errata already chose for `inject.yaml`'s references; a pinned `key:`/`dir:` reference is located under the root its own key names (`run.py` reads the debug flag off the key payload). (e) 8.4 / ticket 13: the trainer beats during the prediction step with unit `step` and `total = len(predict.splits) + 1`, so a piece never reads done before `predictions.parquet` exists; the stall line over a long prediction pass and the progress display jumping from `N/steps` to `0/3` are open for the owner (a beat per prediction chunk, or a distinct unit, would close them). (f) 6.2 / ticket 06 `base.load`: a restore handed the model row and the frozen setting keeps the row's dtype (never the serving `DTYPE`) and rebuilds the adapter the checkpoint's `tuning` names, so a resumed LoRA run's optimizer state loads and a reloaded `best/` keeps the training dtype; the service's load (no row, no cfg) is unchanged; `models/probe_models/base.py` is at `VERSION = 2` with `VERSION_HISTORY = {2: {..., "stale": ("train",)}}`, the first real entry of the mechanism, so every train key moved once (no train run exists). Open, minor: a resumed LoRA run loads the pre-crash Adam moments onto a freshly initialised adapter. (g) 8.6: the `ls` line now carries the eight flags including `behind` (recorded `_versions` below the current `VERSION` with the key unchanged = behind and usable; key moved = stale, naming the file and quoting the entry's `why`), the beat age, sessions, cards, `done/total unit` and rate; `registry.ls` gained the parameters `run.py` passes. Not done: the second re-review of the walk branch's round-2 commit (`ls` reads a setting once per stage, not once per ledger row) was lost to the session limit; that commit is merged on the fixer's own measurement and falls to the final review (planner: applied by new1-97)
- wave-6 precheck, ticket 15: Contracts 0.1 and spec section 3 name only two non-`ast` spellings of an annotation entry, `used by: <file> (by name)` and `used by: none (program)`; contracts 0.2 writes a third on the two empty package markers, `used by: <files> (as their package)` (contracts lines 332 and 360, README.md:169 and :190), and nothing imports either package, so ticket 15's selfcheck checks such a line against the package directory's `.py` siblings instead of against the import graph. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15: Contracts 0.1's five-line annotation format defines neither the brace alternation nor the `;`-separated reader's note, and contracts 0.2 uses both (braces at contracts 272, 366, 468, 512; prose clauses reproduced at README.md:176, :183, :211, :241, :295); ticket 15 now defines for selfcheck that a brace alternation expands to one entry per alternative before the split, and that a fragment counts as an entry only when the whole trimmed fragment is a repo file path. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15: Contracts 8.6's check 5 calls `DEFAULTS` and `REQUIRED` literals and contracts Part 1 reads `SCHEMA` the same way; in the tree all nine bindings in the three `data/` format files are `ast.AnnAssign` whose values `ast.literal_eval` refuses (a `polars` dtype dict, a dict comprehension over `SCHEMA`, a `frozenset({...})` call), and `data/trajectory_record.py:95` adds `DEFAULTS["n_inject"] = 0`, so selfcheck reads all three off the `ast` nodes and matches a column-zero binding on the target's `id`. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15: Spec section 5 lists `FORMATS` among the literals `ast.literal_eval` succeeds on, which the errata's last entry already records as false; the resolution taken in ticket 15 is that `literal_of` stays strict (check 4's whole content is that a value parses as a literal) and check 3 alone reads `FORMATS` through a keys-only reader over its `ast.Dict`, so `agent/injected_text_formats.py` is not changed. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15: Contracts 8.6's check 4, "one integer `VERSION` per module the stage table names", is not executable as written: six `versions` entries are templates (`{env}`, `{method}`, `{probe_score_method}`, `{family}`, `{backbone}`) and two carry an `@<stage>` or `#<TABLE>.<name>` tail, `schema._substitute` needs a loaded `Setting` that selfcheck does not have, and `{family}` and `{backbone}` must be expanded over `models/table.yaml` rows split by `role` or the check names two files that do not exist; resolved that way the six stage rows name exactly the 22 files that carry a column-zero `VERSION`. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15: Contracts 8.6's check 1, "every entry names a file that exists", is false of the README's `tests/` entry (README.md:322), which is a directory; the README carries 43 entries of which 31 are `.py`, and selfcheck's count of 31 covers the `.py` entries while the existence test covers all 43 and accepts a directory. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15: Contracts 8.6's check 10, "each `any` file imported under every interpreter of the `venvs:` map", is ambiguous against contracts 0.1's compound spelling: four files write `venv: any at import, <venv> to run` (`data/environments/appworld.py`, `models/agent_models/gptoss.py`, `models/agent_models/service.py`, `models/probe_models/service.py`), and a `venv == "any"` test skips them silently; the reading is that the value begins with `any`, which covers 20 of the 31 files today. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15: Contracts 8.6's check 7 says "every family's `DEFAULT_EFFORT` a member of its own `EFFORTS`, or both empty", and contracts 6.2 types the empty case as `EFFORTS = ()` with `DEFAULT_EFFORT = None`; ticket 15's check 7 had dropped the alternative and now carries it with the `None` spelled out. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15: Six annotation lines of contracts 0.2 disagree with the tree's `ast` import graph and are corrected in README.md by ticket 15 while contracts stay as they are (agents never edit `notes/`): contracts 272 and 366 name `train/methods/{ctool,cgen,cparam}.py` as users of `data/training_data.py` and `models/probe_models/base.py`, and contracts 480, 487 and 495 name `models/probe_models/base.py` and `data/training_data.py` among the three method files' imports, but the three method files import only `train/utils/trainer.py`, `eval/utils/probe_eval.py` and (cgen, cparam) `data/environments/__init__.py` and reach the other two through the trainer; contracts 364's companion line for `models/probe_models/service.py` writes its qualifier bare, `models/probe_models/base.py inside serve()`, which no strip rule removes although the edge is real. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 16: Contracts 8.1 and 8.6 say `runs.jsonl.lock` is released after the start row is appended and before any tmux session is started; on disk `run.py` (walk, run.py:1037-1054) calls `jobs/launch.launch` inside `with registry.lock():`, and `registry.lock()` is the re-entrant depth-counted manager (jobs/registry.py:124-156), so `jobs/launch.py`'s own release comment at line 870 only drops a nested depth and the flock is in fact held through both tmux waves, the alive checks and the probe service's `check` client. The shape comes from merged ticket 14 step 7, so the code matches its ticket and the contract sentence is the one that is false. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 16: Contracts 2.5 names two `run.py`-held `inject` gates; since the `5.4 / 2.1` ruling there are three, the third being `run.py`'s comparison of a `key:`/`dir:` reference's stated `method:` against the referenced train run's frozen `probe.method` (run.py:870-886, called at run.py:1031). (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 16: Contracts 0.3 and 0.4 are stale in the same way 0.2 / 2.1 / 2.6 / 3.3 already are: 0.3 still counts 34 Python files and names the pair `train/methods/<m>.py` + `eval/methods/<m>.py`, and 0.4's table still lists `eval/methods/<m>.py` as a new file and `agent/inject_format.py` under its old name. The tree has 31 Python files and `README.md` section 3 is the current statement of what each extension touches. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15, open for the owner: Six annotation lines in contracts 0.2 (lines 272, 364, 366, 480, 487, 495) are wrong about the tree, and the ticket corrects only their README copies because agents never edit `notes/`. The ticket now carries the assumption that README.md is the authority selfcheck checks and that contracts 0.2 stays as written until you rewrite it; if you would rather the contracts be the authority, the three method files and `models/probe_models/service.py` would have to import what the contracts claim, which is a code change no ticket in this wave makes. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15, open for the owner: `agent/injected_text_formats.py`'s `FORMATS` is a dict of `Format(...)` calls, which spec section 5 says should be a plain literal. The ticket now assumes the file is left alone and check 3 reads its keys through a second, keys-only parser; the alternative is to split the table into a literal key list plus a separate call table, which changes a file this ticket may not touch and moves nothing else. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15, open for the owner: `SCHEMA`, `DEFAULTS` and `REQUIRED` in the three `data/` format files are annotated assignments whose values are a polars dtype dict, a comprehension and a `frozenset(...)` call, while contracts 8.6 calls them literals. The ticket now assumes selfcheck reads them structurally off the `ast` nodes and the three files are unchanged; making them literal instead would mean rewriting the three files' column declarations, which no ticket in this wave covers and which would move no key. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 15, open for the owner: For the two empty package markers the ticket now requires the `used by: … (as their package)` line to **equal** the set of `.py` siblings in that directory, so a marker that stopped naming a newly added sibling fails. A subset test would let a new file be added without touching the marker's line; equality is the stricter reading and is the one written in, on the ground that the README is the tree's index. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 16, open for the owner: `run.py` holds `runs.jsonl.lock` across `jobs/launch.launch`, so the flock is held through both tmux waves, the alive checks (up to `launch_timeout_s`) and the probe service's `check` client, against contracts 8.1 and 8.6, which require the release before any tmux session starts. Merged ticket 14 step 7 prescribed this shape, so the fix is either a `run.py` change or an errata entry blessing it — not something wave 6 can settle. Assumption the ticket now carries: the Phase 4 row no longer says the lock is released, and no longer says it is held either, so the skill stays true whichever way you rule. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 16, open for the owner: `run.py` prints the monitoring line as `monitor with \`run.py ls {cfg._file}\`` (run.py:1063) even on a `--debug` walk, where `ls` without `--debug` shows nothing. That is merged ticket 14 code, outside ticket 16's file list. Assumption the ticket now carries: the gpu-run skill's Phase 5 tells the operator to add `--debug` when monitoring the Phase 3 smoke, so the skill is correct while the printed line stays incomplete. (planner: fork5, wave-6 precheck)
- wave-6 precheck, ticket 16 against ticket 17's `C15`: ticket 17 scans every `*.md` under `.claude/` for bare ledger names with no exempt section, and ticket 16's phase table wrote `runs.jsonl.lock` bare -> ticket 16 spells every ledger and notes file with its directory (`jobs/runs.jsonl.lock`) and gains acceptance `W5`, ticket 17's pattern list over the three skill directories; found by session wave7's precheck (planner: fork5, wave-6 precheck)
- wave-6 close, open for the owner: run.py holds jobs/runs.jsonl.lock across jobs/launch.launch (both tmux waves, the alive checks, the inject check client), against contracts 8.1 / 8.6 which release it before any tmux session starts; merged ticket 14 code. The gpu-run skill states what the code does. (planner: fork5, wave-6 post-merge review)
- wave-6 close, open for the owner: run.py retry clears the continue markers first and unconditionally (done.json, consumed.json; for train also last/, train_log.jsonl, train_done.json, align_check.json) before the dirty-tree gate, the launch gate and any liveness test, so retry against a live train run deletes its checkpoint directory and log and then launches nothing; merged ticket 14 code. The gpu-run skill now tells the operator to kill first. (planner: fork5, wave-6 post-merge review)
- wave-6 close, open for the owner: kill / refire / retry take no --debug and cannot address a --debug run; contracts 8.6 pins their argv. (planner: fork5, wave-6 post-merge review)
- wave-6 close, open for the owner: run.py prints the monitoring line without --debug on a --debug walk; README.md line 19 spells the launch command with python3, which cannot import run.py's dependencies (both wave 5's lines). (planner: fork5, wave-6 post-merge review)
- wave-6 close, open for the owner: jobs/launch.py's endpoint-timeout exit (the file exists, the port answers, no base_url) is the one failure path that leaves the service pieces up and an open launching row instead of calling teardown_services. (planner: fork5, wave-6 post-merge review)
- wave-6 close, open for the owner: tests/test_registry_concurrent_append.py is isolated only when it runs in its own process: run together with tests/test_packed_loss.py in one process (unittest discover) it appends its 160 fixture rows to the real jobs/runs.jsonl, re-renders jobs/RESULTS.md, took 4856 s and failed once here (2026-09-20, main session; the two files were restored to HEAD, which held 0 rows). Two review findings that reported this were refuted by their verifiers as outside wave 6's diff; the behaviour is real. README names only the first test's run command. (planner: fork5, wave-6 post-merge review)
- Added by the section-4 `--debug` walks (2026-09-20, session new1-08; six defects found by the walks, each fixed at its cause before the next launch). (a) 7.1: `models/agent_models/service.py`'s `_request` decoded every body as JSON, and vLLM's `/health` answers 200 with an empty body, so `_wait_healthy` never returned and the launch waited out `launch_timeout_s` -> an empty body returns `None`. (b) 6.1 / ticket 13: errata 6.1 loads the probe float32 because the old trainer autocasts bfloat16, and the new trainer had no autocast, so cgen and cparam ran out of memory on a 48 GB card -> the training, validation and prediction forwards run under bfloat16 autocast on a card, the alignment gate stays float32; `train/utils/trainer.py` VERSION 2, stale train. (c) 8.1 (`alive_check`, "never defined"): a fixed 30-second window closed a train run `launch_failed` whose first log line came after 42 s on tokyo108, while the run went on to `done.json` -> each piece is up, down (session ended, or a `Traceback`) or waiting; the check returns on all up, on the first down, or at `launch_timeout_s`. (d) 6.2: `Probe.generate` sent the whole validation frame through one `backbone.generate` call -> `GENERATE_BATCH = 8` prompts per call, the old trainer's `--gen-bs`; no VERSION bump, because no generator train run had ever finished. (e) 3.4 / 5.4: `run.py` located a pinned reference under the root its own key names, while `eval/utils/probe_eval.py`, `eval/score_run.py` and `jobs/launch.py` located the same run with `debug=False` -> one function, `schema.referenced_run_dir(stage, key)`, used by all four. (f) 8.2 ("every reader takes the newest start row and the newest finish row"): a relaunch after a `failed` or `launch_failed` finish appends its start row below that finish row, so the relaunch read as finished at five places in `jobs/registry.py`: `open_runs` left it out, the walk never backfilled its `ok` row, and `ls` showed the stale status with `orphan` -> `fold` clears the finish row at a start row, so the finish is the newest one that follows the newest start. `eval` and `score` append start then finish on every recomputation and read the same as before. (planner: main session, wave 7)
- section-4 walks, open for the owner: the trainer's step beats reach `done == total` before the last validation and the prediction step, and 8.5 judges a piece `done` on `done >= total`, so a train piece that dies in its last validation reads `done` in `ls` and `run.py sync` never closes it (seen on train-b45633250e8b and train-5b398c217e13 before fix (d)). `retry` cannot address a `--debug` run (wave-6 item), so the markers of those two debug directories were cleared by hand. A relaunch into the same run directory appends to the same `log/<piece>.txt`, so an old `Traceback` stays in the file; `alive_check` reads only the last 4 KB. tokyo108 cards 0 and 1 are still held by the tmux sessions `new1_fmt_smoke_srv_t108g0` and `new1_fmt_smoke_probe_t108g1` of 2026-09-12 (the old tree's probe server and a vLLM engine, 7 days up). (planner: main session, wave 7)
- 1.5 / 8.2 (`done.json` and the finish row a reader owes it): 1.5's file table (:1404) gives `done.json` the fields `{stage, key, commit, finished_at, counts, versions, metrics, report}`, and 8.2 (:4493-4497) appends a finish row "whenever `done.json`'s `finished_at` is later than the newest finish row for that `run_id`" -> `registry.write_done` also stamps `launch`, the length of `meta.json`'s append-only `launches` list at the moment it writes (8.3), and one rule closes a run for every reader: the file belongs to the launch that is open when its `launch` is that run directory's current ordinal. The `finished_at` comparison is retired as that rule: both stamps it compares come from `_now()` at minute resolution and a stage that always recomputes (2.4) finishes inside one minute, so a `done.json` of the previous computation that shares a minute with the open start row passed the test and closed the launch in flight with that earlier computation's counts, metrics and report. A `done.json` written before this field existed carries no `launch` and belongs to no open launch, which leaves the launch to write its own row. Of the two closers, `jobs/registry.sync` (`jobs/registry.py:1089`) tests the ordinal today; the walk's `run.py._backfill_finish_row` (`run.py:1744-1769`) still holds the retired comparison, and that function is the code this ruling changes — until it is changed the two closers disagree, and the walk can close a live launch with an earlier computation's `done.json` (planner: main session, wave 7 final review; commit 977210d)
- 1.6 / 2.4 (the resume checkpoint's names and what `retry` clears): 1.6's layout (:1468-1475) names `best/` and `last/`, and 2.4 (:2061) says `run.py retry` "deletes `last/`, `train_log.jsonl` and the markers and then launches normally" -> `train/utils/trainer.py` writes the resume checkpoint whole into `last.tmp/` and renames it into place, holding the checkpoint it replaces as `last.prev/` for the instant between the two renames, so `last/` is always a complete checkpoint; `_settle_last` finishes a swap a kill interrupted at the next start, where a `last.prev/` with no `last/` beside it takes the name back and a leftover `last.tmp/`, or a `last.prev/` a complete `last/` has replaced, is removed. A kill lands inside that window, so "start fresh" owes the removal of every name the checkpoint carries, and `run.py._clear_continue_markers`'s clear list (`run.py:725`) is the code these two directories change: it removes `last/` alone today, so a `retry` after a kill inside a swap leaves a `last.prev/` that `_settle_last` renames back to `last/` and the run resumes instead of starting fresh. `README.md`'s `train/utils/trainer.py` entry names all four directories the trainer writes, the gpu-run skill's two `retry` lists name what the clear list removes today, and the skill's Phase 6b bullet tells the operator to remove a leftover `last.tmp/` or `last.prev/` by hand before typing `retry` (planner: main session, wave 7 final review; commit 2a9e2a1)
- 8.5 (the `escalated` flag and the verdict `ls` prints): 8.5 (:4689) says "`escalated` survives as a flag `ls` prints, not as something that starts a process" -> `jobs/registry.py` computes it in `judge` and `judge_service` (`jobs/registry.py:879`) and puts it on every piece row (`:900`), while `run.py._format_piece` (`run.py:568-579`) prints `<index>:<verdict>@<host>:<session> cards=<gpus>` and no mark, so a reader of `ls` never sees it and `.claude/agents/job-monitor.md`'s autopsy branch for an escalating `suspected stall` tests a value its only source of truth never shows. The printer is the code this ruling changes: `_format_piece` appends `(escalated)` to the verdict when the piece row's `escalated` is true, and the two documents keep 8.5's wording — the final review's D6 verdict rejected the opposite path, which rewrites the reader to match a printer missing a field. That printer change is owed and unmade: `run.py` is outside the file set of the documents branch `fix/2026-09-20-final-documents`, which restored `.claude/agents/job-monitor.md`'s two escalation conditions and the gpu-run skill's "What is gone" sentence to the contract's wording and changed nothing in `ls`. Until `_format_piece` carries the mark, an escalating stall reads in `ls` as a bare `suspected stall` and job-monitor reports it without a log tail. When the mark lands, a dead piece prints `0:dead(escalated)`, because `judge` and `judge_service` both return `("dead", True)`, and the gpu-run skill's Phase 5 list of what the `ls` line carries (`.claude/skills/gpu-run/SKILL.md:138-146`) names the mark in the same commit as the printer (planner: main session, wave 7 final review)
- 8.4 (eval's heartbeat `total`; supersedes the wave-3 precheck entry above that made it the frame's distinct `event_id` count): an event count cannot advance, because the report has no per-event loop — it walks the whole frame once per theta and once or more per risk target — so a total in events left `done` at 0 for the whole run and `run.py ls` read `progress=0/16 item` for a finished eval (post-merge finding EVAL-BEAT-1) -> `total` is the report's pass count, `eval/utils/probe_eval.report_passes(method, cfg)`: for a classifier one pass per theta on the grid plus three per risk target, for a generator two per risk target. The unit stays `item`, `registry.beat(run_dir, 0)` stays at step 2, the first `emit` still follows the read of the prediction frame, one beat closes each pass, and the last beat reads `done == total`. `done.json`'s `counts["events"]` still records the frame's distinct `event_id` count. Ticket 08's step-2 sentence falls with the same ruling (planner: post-merge review wave 7, finding EVAL-BEAT-1)
- 8.5 (the `escalated` mark, the entry above marked "owed and unmade"): done on 2026-09-21 by the main session. `run.py._format_piece` appends `(escalated)` to the verdict when the piece row's `escalated` is true, so a dead piece prints `0:dead(escalated)` and a stall past three times its line prints `suspected stall(escalated)`; the gpu-run skill's Phase 5 list names the mark in the same commit, and `.claude/agents/job-monitor.md`'s two `escalated=true` conditions now test a value `ls` shows (planner: main session, after wave 7)
- 2.6 (the eval hook table's `report` row): the table gives `report(pred_df, cfg, ref, labels)` -> the built hook is `eval/utils/probe_eval.report(method, pred_df, cfg, ref, labels, hb)`: `method` picks the classifier or generator shape by `PROBE_KIND`, and `hb` is the heartbeat the report beats once per pass, the 8.4 entry above; the table's row is not edited (planner: main session, wave 7 final review `stale_sentences`, recorded after wave 7)
- 1.5 / 8.2 (`done.json`'s `launch` stamp, the entry above that says the walk's `run.py._backfill_finish_row` "still holds the retired comparison"): done on 2026-09-20 by commit 8ab6ff5, read against the code on 2026-09-23. `_backfill_finish_row` (`run.py:1815-1838`) appends the `ok` finish row only when `done.get("launch") == registry.launch_ordinal(run_dir)` (`run.py:1833`), the same test `jobs/registry.sync` makes (`jobs/registry.py:1149`), and no `finished_at` comparison is left in `run.py`; the two closers agree, and a `done.json` of an earlier launch leaves the launch in flight open (planner: implementer, 2026-09-23 inventory close-out)
- 1.6 / 2.4 (the resume checkpoint's three names and what `retry` clears, the entry above that says `run.py._clear_continue_markers` "removes `last/` alone today"): done on 2026-09-20 by commit 8ab6ff5, read against the code on 2026-09-23. `_clear_continue_markers` (`run.py:749-767`) removes, for `train`, `train_log.jsonl`, `train_done.json` and `align_check.json` and then every directory among `("last", "last.tmp", "last.prev")` (`run.py:764`), so a `retry` after a kill inside the swap leaves no `last.prev/` for `_settle_last` to rename back, and the run starts fresh. Open: the gpu-run skill's "Start fresh" bullet (`.claude/skills/gpu-run/SKILL.md:253-264`) still says `retry` "deletes `last/` alone" and tells the operator to remove `last.tmp/` and `last.prev/` by hand before `retry`, and its later list (`:276-278`) names `last/` alone; both sentences are now false about the code and were outside the file set of the change that wrote this entry (planner: implementer, 2026-09-23 inventory close-out)
- 6.2 / ticket 06 (entry (f) of the wave-5 post-merge review above, "Open, minor: a resumed LoRA run loads the pre-crash Adam moments onto a freshly initialised adapter"): superseded. Commit 5bb8271 (2026-09-22) recorded the defect and its fix as an owner TODO, and commit 24a9f19 (2026-09-22) implemented it: `train/utils/trainer.py._save_last` writes the resume checkpoint with `probe.save(..., merged=False)`, so under LoRA `last/` holds the adapter's own A and B (peft's adapter save) and `meta.json` carries `adapter_only: true` (`models/probe_models/base.py:81-115`); `base.load` on such a checkpoint takes the backbone from the alias the checkpoint names, rebuilds the adapter from `cfg.probe`, and loads the saved A and B into it, refusing an adapter whose tensors the rebuilt one lacks (`models/probe_models/base.py:235-244`, `:299-315`); the trainer then loads `optimizer.pt` onto those same tensors. `best/` stays merged, the form the probe service loads. Not yet shown by a run: `base.py:79-80` says the adapter-only `last/` was exercised on the cpu with a small random-weight model only, and no LoRA setting exists, so no card run has resumed from one (planner: implementer, 2026-09-23 inventory close-out)
- 2.5 / ticket 13 (the alignment gate's stop conditions; closes the consequence that entry (a) of the wave-5 post-merge review above records as open, "a gate slice whose every event is over `train.max_len` compares zero rows and passes with `packed = plain = 0`"): 2.5 (:2145-2146) says `train` "stops on a mismatch above `1e-4`", one stop condition -> the gate has two, done on 2026-09-23 by the change that adds this entry. `train/utils/trainer.py._run_alignment_gate` (`train/utils/trainer.py:526-580`) counts `n_compared`, the rows it compared, as the summed length of the `example_id` list of every batch `method.batches` yields for the gate slice (`:546-548`); those are the rows `reference_loss` scores, by the same drop rules (entry (a)). `PASS` is `n_compared >= 1 and diff <= tol` (`:558-560`), and `align_check.json` carries `n_compared` beside `n_rows`, the slice height. A slice whose every event the method drops compares no row: the gate writes `align_check.json` with `PASS: false` and stops with a message that gives the slice's event count and how many of those events tokenize past `train.max_len` (`:567-576`); a mismatch above `tol` stops as before (`:577-580`). Both stop messages name the method by its file, `train/methods/<probe.method>.py`, read off the frozen setting (`:566`), because a method file runs as a program and its `__name__` is `__main__`. Both losses are still divided by `n_rows`, the slice height, and not by `n_compared` (the owner's decision). No `VERSION` bump: the change stops only a gate that compared zero rows, and every one of the 15 `align_check.json` files under the outputs root's `debug/train/` has nonzero `packed` and `plain` (read on 2026-09-23). The wave-6 close entry above that reports `tests/test_registry_concurrent_append.py` appending its fixture rows to the real `jobs/runs.jsonl` is closed in two parts. The leak itself was closed on 2026-09-20 by commit 74cfc76: the forked child loads the temporary tree's `jobs/registry.py` by file path under a name of its own (`tests/test_registry_concurrent_append.py:48-56`; `.scratch/from-zero/sdd/2026-09-20-wave7/wave-result.md` item 9). The entry's last sentence, "README names only the first test's run command", is closed by the change that adds this entry: `README.md`'s `tests/` line gives both run commands, each in its own process, and `tests/test_packed_loss.py` puts the repo root on `sys.path` itself, so the command `CLAUDE.md` spells imports `data` (planner: implementer, 2026-09-23 inventory close-out, from its review)
- 6.2 (`Batch`; extends the entry above that names the trainer's two extra required keys, `mb` and `mb_weight`): the alignment gate reads a third -> every method's `batches` yields `example_id` (`list[str]`, one entry per row the batch scores, which are the rows `reference_loss` scores), and `train/utils/trainer.py` requires it and reads it only in `_run_alignment_gate` (`train/utils/trainer.py:548`), to count the rows the gate compared (the 2.5 entry above). `base.py` never reads it. `ctool`, `cgen` and `cparam` carry it today (`train/methods/ctool.py:183`, `train/methods/cgen.py:193`, `train/methods/cparam.py:220`); a new method whose batches carry no `example_id` stops at the gate with `KeyError: 'example_id'` on its first fresh launch with `train.align_check` true (planner: implementer, 2026-09-23 inventory close-out, from its review)
- 1.6 / 2.4 (closes the "Open:" clause of the 2026-09-23 close-out entry above that says the gpu-run skill's "Start fresh" bullet "still says `retry` 'deletes `last/` alone'"): done on 2026-09-23 by commit 87453a0, the same day's merge that rewrote the skill's Phase 6b. The "Start fresh" bullet (`.claude/skills/gpu-run/SKILL.md:265-286`) now says that once every piece is dead `retry` deletes, for `train`, `train_log.jsonl`, `train_done.json`, `align_check.json` and the resume checkpoint under each of its three names, `last/`, `last.tmp/` and `last.prev/`, which is `run.py._clear_continue_markers`'s list; the instruction to remove `last.tmp/` and `last.prev/` by hand is gone, and the skill names no `retry` list that holds `last/` alone (planner: implementer, 2026-09-23 follow-up)
- 8.1 / 8.3 / 8.4 / 8.5 (which heartbeat file a verdict reads): 8.4 names the file `heartbeat/<piece>-<launch>.jsonl` with `<launch>` = 1 + the largest existing, and the readers took the newest file on disk as the piece's -> a relaunch, `refire` or `retry` starts a piece in a directory that still holds its earlier incarnations' files, and until the new process opens its own the newest file is the earlier one's, which can end with the `status: "done"` row: `ls` printed `done` for a piece that had not started, a piece that died before its first beat never read `dead` (so `launch_failed` never selected its run), and the launch gate's third test read a not-yet-started loop piece as observed dead from its old beats. Every `loop`, `train` and `cpu` piece entry now carries `beat_launch`, the `<launch>` its incarnation opens (`registry.next_beat_launch`, the number `registry.beat` computes), written before the session or process starts by `jobs/launch.launch`, `jobs/launch.refire` and `run.py._start_cpu_stage`; `registry.current_beats` reads the newest file at or above it, and `ls`, `sync`, `cards_busy`, `alive_check` and the gate's beats (`launch.read_beats_for_run`, which now takes the pieces) all read through it. An entry written before the field existed reads from `<launch>` 0, the old rule, so the runs launched before 2026-09-23 keep their verdicts (planner: implementer, 2026-09-23 follow-up)
- 8.5 (`judge_service` on an attached agent service): the row calls a service `dead` whenever its session is gone -> an `--attach-only` piece's own session writes `service_agent_<replica>.json` with `attached_to` and ends (7.1, 7.4), so the row read the whole attached run's service as `dead(escalated)`. The piece is judged by what serves its run, the owning server's port it records: `done` once its run's work is done, `healthy` while the port answers, `dead` when the port stops answering while work is owed. `attached` is read from the piece's endpoint file; its own session still decides the `orphan` flag, which it raises only when its host's `tmux ls` probe does not answer (fail-closed, every session there counts as alive). An entry written before `beat_launch` existed reads its newest heartbeat file, the old rule, so for those entries an incarnation that died before its first beat still reads the earlier incarnation's rows. Recorded beside the two earlier 8.5 divergences in the `TODO(gyb, 2026-09-23)` above `registry.judge` (planner: implementer, 2026-09-23 follow-up)
- 2.5 / 8.6 (`cards_busy`, and the second consequence the deleted owner TODO above `judge_service` recorded): a piece of an open run reserved its cards while its session was live or its start row was younger than `launch_timeout_s`, so the service pieces of a finished `sample` or `inject` run, which the last loop piece ends before the wrap-up walk writes the finish row, kept their cards reserved up to `launch_timeout_s` after the start row -> `cards_busy` reserves a card while its piece's session is live, or while the start row is young and `_judge_pieces` (the verdict `ls` prints) does not call the piece `done`; this relies on the `beat_launch` entry above, since an earlier incarnation's finish row would otherwise release a card of a launch that is still starting (planner: implementer, 2026-09-23 follow-up)
- 8.6 (`run.py kill` on a `cpu` piece): the row says kill ends a `cpu` piece by its `pid` -> `registry.kill` signals a `cpu` piece only while its run is open (no finish row after the newest start row, the `open_runs()` scope `run.py._live_pieces` judges a pid in), because a CPU stage's walk closes the run once the process exits and the recorded pid may since name another process; `registry.end_pid` counts a pid this user may not signal (`PermissionError`) as no process of this run, as the remote `kill -TERM` branch already did (planner: implementer, 2026-09-23 follow-up)
- 6.3 / 3.4 / 8.0 / 8.5 / 8.6 (where the cluster inventory lives; owner's order of 2026-09-23, "the card information is written in one file under constants/"): 6.3 puts `hosts:` (name, alias, card count) in `constants/path_outputs.yaml`, and 3.4 ("**Hosts.**" and the third placement bullet), 8.0's `live_sessions` row, 8.5's `tmux ls` sentence and 8.6's `run.py free` row read it there -> the inventory is `constants/cards.yaml`: one entry per host with `name`, `alias` where one exists, and a `cards:` list with one entry per card index carrying `model` and `memory_gib` (the MiB total divided by 1024 and rounded down: 47 on tokyo105/106/107, 93 on tokyo108 cards 0-2, 140 on cards 3-5; the MiB figures, driver and the 2026-09-17 measurement date are comments per host). `constants/path_outputs.yaml` keeps `root`, `debug_subdir` and `login_host` only. The one loader is `jobs/registry.hosts()`, which returns each host with `cards` as the length of its per-card list and `memory_gib` per card index; `registry.card_memory_gib()` and `registry.canonical_host()` serve `jobs/launch.py` and `run.py`, whose own host-list readers and alias normalisers are removed. Like the old block it is a location and a hardware fact and enters no key (planner: main session, 2026-09-23)
- 3.4 (first bullet) and 7.4 (first paragraph) (which cards an agent service may land on; owner's order of 2026-09-23): both say an agent-service piece is placed on its table row's `serving.host` "and nowhere else", while the build since 2026-09-22 lets a server that starts its own process fall back to any host with enough free cards, which would place the `gpt_oss_120b` server on a 47 GiB card (RTX A6000 or RTX 6000 Ada) when tokyo108 is full, as the TODO in `jobs/launch.place()` that this entry removes said; no run has started it on such a card -> `jobs/launch.place()` holds one rule: a piece lands only on free cards at least as large as the cards it was declared for, counted per host as the free cards whose `memory_gib` meets the requirement, with the candidate order unchanged (`prefer_host` first). An agent service's requirement is the smallest card of its table row's serving host (`jobs/launch._declared_card_gib`; the owner chose that host because its cards hold the model, so `models/table.yaml` gains no field); a train piece and a checkpoint-loading probe service name no host, so their requirement is 0 and the card count alone decides, as before. `_claim_cards` claims the first cards that meet the requirement, and `refire` reads the agent alias from the run's frozen `settings.yaml` to hold a refired agent service to the same requirement. Under `--cards` the requirement filters the pool's cards, and a pool that cannot hold a piece because named cards are too small refuses the launch naming each such card, its size and the requirement, as `restrict_to_pool` names a card that is not free (planner: main session, 2026-09-23)

Added by the owner's rulings of 2026-09-24 (record: .scratch/from-zero/sdd/2026-09-24-rulings/rulings.md):

- 8.1 / 8.6 (the lock across `jobs/launch.launch`; closes the three entries above that begin "wave-6 precheck, ticket 16: Contracts 8.1 and 8.6 say `runs.jsonl.lock` is released after the start row is appended", "wave-6 precheck, ticket 16, open for the owner: `run.py` holds `runs.jsonl.lock` across `jobs/launch.launch`" and "wave-6 close, open for the owner: run.py holds jobs/runs.jsonl.lock across jobs/launch.launch"): the owner's ruling 10 of 2026-09-24 is "The registry lock is released once the start row is written; the service start, the alive checks and the loop start run outside it; a failed start re-takes the lock to append the `launch_failed` row." The walk has done this since commit 7e347b2 (2026-09-20, "launcher: alive_check as one rule, teardown of every piece, lock released before tmux, refire's start row"). Line numbers are those of the tree after the change that adds this entry, read on 2026-09-24. `run.py._stage_step`'s hold (`run.py:2117-2137`) covers `launch.git_state`, `schema.freeze` and `registry.write_meta`, plus a CPU stage's process start, and ends before a card stage calls `launch.launch` at `run.py:2140` outside any hold; neither caller of `_stage_step` holds the lock (`cmd_walk` through `_walk_one`, `run.py:1762-1783`; `cmd_retry`, `run.py:778-810`). `jobs/launch.launch` takes its own hold (`jobs/launch.py:965-1126`) over the launch gate, the attach test, the card reservation, the port assignment, the start row with `status: "launching"` (`registry.append_start` at `:1115`, a depth increment of the re-entrant `registry._Lock`, `jobs/registry.py:163-191`, `:201-205`) and the `meta.json` rewrite (`:1122`). The `with` block ends at `:1126`, and `_start_wave`, `alive_check`, `_wait_for_endpoint`, the probe service's `check` client and the loop wave (`:1129-1172`) run with it released. None of them takes it again, and neither does `teardown_launch` or the `teardown_services` it calls (`:658-702`, which read `meta.json` and `registry.open_runs()` and end sessions): `append_start`, `append_finish` and `write_meta` are the only registry functions that take the lock (`jobs/registry.py:203`, `:210`, `:270`). Every outcome other than `up` returns to `run.py`, which re-takes the lock and appends the `launch_failed` finish row (the `outcome != "up"` branch, `run.py:2145-2152`, the append at `:2148-2151`). One difference from 8.6's wording remains and is recorded here without a ruling: 8.6 (:4729-4736) describes one span from `git_state` to the start row, with `jobs/launch.py`'s hold falling inside `run.py`'s as a counter increment, while the code holds two consecutive spans, `run.py`'s (git state, freeze, `meta.json`) and `launch.launch`'s (gate to start row); the gate, the reservation and the start row still share one hold, which is what 8.6's failure of two `run.py` calls both appending needs. `refire` holds the lock through its one session start, and the change that adds this entry puts that hold inside `launch.refire`. `run.py.cmd_refire`'s own hold covers `launch.git_state` and `schema.freeze` only (`run.py:730-735`), and `launch.refire` is called after it ends (`run.py:738`); before the change `cmd_refire` called `launch.refire` inside that hold. `launch.refire` takes one hold (`jobs/launch.py:1269-1346`) over the read of the piece's entry (`refire_target`), the liveness test, the claim release, the card re-probe and claim, the start row with `status: "launching"` (`:1320`), the `meta.json` rewrite (`:1339`) and `tmux new-session` (`_start_tmux`, `:1345`). The session start is inside the hold because the liveness test is refire's only guard against a second refire of the same piece, and the session that test looks for exists only once `tmux new-session` has returned: a hold that ended before it would let a second `run.py refire` pass the liveness test, append its own start row, rewrite `meta.json`, and either fail on the session name (same host) or start a second incarnation on another host writing into the same run directory. `launch()` has no such window because the launch gate refuses a key whose open start row is younger than `launch_timeout_s` while none of its pieces has been observed dead (`gate_open_row` clause (c), `jobs/launch.py:220`), and refire has no gate clause. The hold is one bounded session start: refire runs no alive check, and `_remote_run` gives up after 20 seconds (`jobs/launch.py:423-432`). A `tmux new-session` that fails exits inside the hold (`:1346`) and leaves the `launching` start row with no finish row; `run.py` appends no `launch_failed` row for a refire, and `run.py ls` (`run.py:608`) and `sync` (`jobs/registry.py:1297`) close the row with one once it is older than `launch_timeout_s` and every piece it names reads `dead` (`registry.launch_failed`, `jobs/registry.py:883`), so the run stays open while a sibling piece lives. A CPU-only check that replaced `registry.lock` with a depth recorder and every registry writer with a recorder saw, for a loop piece and a train piece through both `run.py.cmd_refire` and `launch.refire` and for a failing `tmux new-session`, `cmd_refire`'s hold end after `schema.freeze`, then the liveness test, the claim release, the card re-probe, the start row, `write_meta` and `_start_tmux` at depth 1, with the hold's exit right after `_start_tmux` (planner: owner ruling 10 of 2026-09-24, verified by implementer, 2026-09-24)

- 3.4 / 5.4 (a name-form reference inside a `--debug` walk; closes the entry above "3.4 / 9(c)#8", which pinned `inject.yaml`'s three references with the `key:` form to the debug keys, and entry (d) of the wave-5 post-merge review, which did the same for `eval.theta_from` of `cgen_qwen3_0pt6b` and `cparam_qwen3_0pt6b`): contracts 3.1 (:2425-2428, "a resolved reference ... passes `debug=False` ... so a debug run points at real upstream directories") and 3.4 (:2627-2632, "a resolved reference is always keyed without the debug overlay") -> owner ruling 9 of 2026-09-24: a `--debug` walk of a setting whose reference is in name form resolves the reference under the debug outputs root, to the referenced train run of the same debug walk. `schema._resolve_name_ref` (`experimental_settings/schema.py:781`) loads the named setting under the referring setting's debug flag; `_resolve_ref` (`:829`) takes that flag from its three callers, `_finalize` (`:1004`), `_substitute`'s `{probe_score_method}` branch (`:1242-1243`) and `upstream()` (`:1353`), so a `--debug` walk's name-form reference keys to the referenced setting's own debug key, the run a `--debug` walk of that setting produces, and a walk without `--debug` keys to the real run as before. `run.py._upstream_dirs` (`run.py:1900-1921`) locates a `same`-source upstream and a name-form reference under the walk's own root through `schema.run_dir_of(..., debug=cfg._debug)` and a pinned `key:`/`dir:` reference through `schema.referenced_run_dir`, which tries both roots; the `TODO(gyb, 2026-09-22)` above it is gone. No key without `--debug` moved: `run.py where` over every setting and stage of the three workflow files, 18 lines, is identical before and after (recomputed by the finder against a `git archive` of the previous HEAD). Six debug keys moved, exactly the stages that hold a name-form reference or fold one of those stages' keys: `train_probe/cgen_qwen3_0pt6b` eval 4d8209bdcf35 -> 64464e0a216c, `train_probe/cparam_qwen3_0pt6b` eval a09bded5fb96 -> f3f1719bd3a3, `inject/probe_p1_e1_theta_0pt80` inject 2466c5b5ffa1 -> 583c0e97913c and score 50993dbe639f -> 4586e0cdeba8, `inject/no_probe_p1_e1_theta_0pt80` inject 9b981540467c -> 210ff860f998 and score c160d9bc669c -> ecd526d476c3; the two new eval keys are the directories the earlier `key:`-pinned debug walks already made, since a pinned reference to the same keys and the name form key identically. The debug walks of `cgen_qwen3_0pt6b`/`cparam_qwen3_0pt6b` eval and of `inject.yaml` no longer pin their references by hand; the gpu-run skill's Phase 3 says a smoke of a referring setting needs the debug walks of the settings it names first. The debug overlay sets `seeds: [42]` on both `sample` and `inject` and never `split`, so 5.7's `score.baseline` superset check compares the same splits as before and equal seed sets; open, recorded by the finder: an inject setting that states `inject.tasks`, or a baseline that states `sample.tasks`, passes that check and can fail at the debug score stage's per-pair test (`eval/score_run.py:195-199`), because the debug baseline holds the first `n_tasks` tasks of each split only; no current setting states `tasks` (planner: owner ruling 9 of 2026-09-24, applied by implementer, 2026-09-24)

- 7.1 / 7.4 (`--attach-only`, closes the entry above beginning "7.1 (`--attach-only`) / 7.4"): owner ruling 11 of 2026-09-24 accepts the attach-only comparison as that entry describes it: the service compares the served model name and `max_model_len` and refuses on a difference, and the comparison of the whole `result:` block (with `role` and `family`) stays on `jobs/launch.py`, through the `claims` of the serving run's `service_agent_<replica>.json`; the entry's "open for the owner" is closed. Line numbers are those of the tree at commit 76cab53 (2026-09-24). The code: vLLM 0.26.0's model card (`ModelCard`, `external/vllm-env/.../vllm/entrypoints/openai/engine/protocol.py:89-97`, filled at `.../openai/models/serving.py:64-76`) carries `id`, `object`, `created`, `owned_by`, `root` (the weights path the server was started from, `build_command`'s positional argument at `models/agent_models/service.py:54`, not the table's `weights` alias), `parent`, `max_model_len` and `permission`, and no value for any other keyed column. `serve()` refuses `--attach-only` without `--attached-to` (`models/agent_models/service.py:197-201`) and calls `_attach` (`:202`), which reads `GET <base_url>/models` (`:122`), refuses when it lists no model (`:124-125`), compares the first model card's `id` with the frozen row's `served_model_name` (`:128-129`) and its `max_model_len` with the row's `max_model_len` (`:130-131`), and exits naming each difference (`:132-134`); on a match `serve` writes `service_agent_<replica>.json` (`:203-206`) with `pid: null`, `claims` equal to its own frozen `models.agent_row` (`claims = dict(row)`, `:194`) and `attached_to` the `--attached-to` run_id, and returns without starting vLLM. The `family`/`weights` test at `:184-189` runs on both paths and compares the live table row with the frozen row, not with the server. A server this piece starts itself writes the same `claims` with `attached_to: null` (`:218-221`). `models.agent_row` is `{role, family, **result}` (`experimental_settings/schema.py:743-745`). `jobs/launch.launch` runs the attach search for `sample` and `inject` only (`jobs/launch.py:987-990`), under the launch hold, with this run's `setting.models.agent_row` (`:957`); `_find_attach_target` (`:798-838`) walks `registry.open_runs()`, reads each `service_agent_*.json`, and keeps a file only when every key of this run's `agent_row` equals the file's `claims` value for value (`:829-830`), its `host` is in the named pool when one is given (`:832-833`), the run's `meta.json` (or start row) lists a `service` piece with that endpoint file and port (`:834-835`), and the port answers (`:836`); the match returns that run's `run_id`, host and port, and replica 0 alone becomes the attached piece (`:1045`), launched with `--attach-only --attached-to <that run_id>` on the matched port with no card (`:1054-1055`, `:1061-1064`, `_agent_service_cmd` `:396-397`). One difference from the entry's wording remains and is recorded here without a ruling: the search does not require the matched file's `attached_to` to be null, and an attaching run's own piece is a `service` piece whose endpoint file carries the owning server's port (the piece record at `:1065-1068`; the file is written from `--port` at `models/agent_models/service.py:203-206`, set at `:1054-1055`), so an open attaching run's `service_agent_0.json` passes every test and `attached_to` then names that attaching run rather than the run whose session holds the vLLM process. This happens when such a file is reached before the owning run's, which the order of `registry.open_runs()` (first appearance of each `run_id` in `jobs/runs.jsonl`, `registry.fold`, `jobs/registry.py:439-457`) allows, or when the owning run is closed while its server stays up because `teardown_services` skipped it (`jobs/launch.py:666-668`); the compared values are the same either way, since that file's `claims` matched the owner's when it attached; but the teardown skip of 2.3 (`jobs/launch.py:667`, through `_attached_elsewhere`, `:642`) and `run.py kill`'s refusal (`jobs/registry.py:1228-1232`) then look for the attaching run's `run_id`, so once that run closes, the owning run's teardown (`agent/run_tasks.py:253`, `run.py:1884`) ends the server while the later run still uses it; when the owner is already closed only the naming differs (planner: owner ruling 11 of 2026-09-24, verified by implementer, 2026-09-24)

- 3.3 / 8.6 (the three version-history decisions (a), (b), (c) of the entry above "done on 2026-09-18, merged as 39b3d60", which that entry took "in the code and open to the owner's reversal"): owner ruling 13 of 2026-09-24 accepts all three as coded, and the "open to the owner's reversal" wording is closed. Line numbers are those of the tree at commit 76cab53 (2026-09-24). The code today: (a) `_entry_parts` (`experimental_settings/schema.py:1252-1269`) splits a `<path>#<TABLE>.<method>` entry into the module path and the table entry, and `_key_versions` (`:1309-1330`) folds `_table_entry_version` (`:1298-1306`, the value of `<TABLE>[<method>]` in that module) for it and reads no `VERSION_HISTORY` for it (`:1325-1327`), so the train row's `eval/utils/probe_eval.py#MATCH_VERSION.{method}` (`:250`) and the inject row's `eval/utils/probe_eval.py#MATCH_VERSION.{probe_score_method}` (`:305`) fold `MATCH_VERSION[<method>]` (the table at `eval/utils/probe_eval.py:40`) with no history axis; (b) the inject row lists `eval/utils/probe_eval.py@eval` (`:304`), `_entry_parts` takes the part before `@` as the entry's name (`:1267`), and `_key_versions` folds `_effective_version_over(path, (stage, stands_for))` (`:1328-1329`), the highest version from 2 to `VERSION` whose entry made either stage stale, else 1 (`:474-482`), so a bump of that file stating `stale: ("eval",)` or `stale: ("inject",)` moves the inject key; `versions_of` (`:1272-1295`, the `_versions` record and the registry row's `versions`, `jobs/launch.py:1107`) and `key`'s payload (`:1374-1376`) carry the name without `@eval`, and `versions_of` and `_key_versions` of `inject/probe_p1_e1_theta_0pt80`, computed on 2026-09-24, hold no name with `@`; (c) the inject row's `versions` tuple (`:291-305`) holds no `data/probe_output.py` entry, which the eval row lists (`:267`) and the train row lists (`:251`), and the five-line comment `:299-303` says so; that comment sits directly above the `@eval` entry, not under it as the entry above words it. Found while verifying and recorded here without a ruling: `run.py._stale_sentence` (`run.py:375-400`) splits each recorded name on `@` (`:389`) and widens to the stood-for stage when one is present (`:392-393`), but the names it reads are a registry row's `versions` (`run.py:464`), written by `schema.versions_of`, which never carry `@`, so that branch never runs: when a `stale: ("eval",)` bump of `eval/utils/probe_eval.py` moves an inject key, `run.py ls` marks the old inject run as edited, but its stale sentence leaves that file out, and with no other bump `ls` prints no `stale=` part (`run.py:578-579`); `_behind_versions`'s docstring (`run.py:367-370`) likewise says a stand-in is compared under its `<path>@<stage>` spelling, while the comparison uses the bare path both sides record. The entry's last sentence, `agent/injected_text_formats.py`'s `FORMATS` refused by `ast.literal_eval`, is not closed here: the two wave-6 precheck entries for ticket 15 above ("Spec section 5 lists `FORMATS` among the literals ..." and "open for the owner: `agent/injected_text_formats.py`'s `FORMATS` is a dict of `Format(...)` calls ...") carry it forward, and owner ruling 14 of 2026-09-24 closes the second of them separately; the first records a resolution and carries no open wording (planner: owner ruling 13 of 2026-09-24, verified by implementer, 2026-09-24)

- 0.2 / 8.1 / 8.6 / Part 1 (the four selfcheck assumptions of the wave-6 precheck, the entries above beginning "wave-6 precheck, ticket 15, open for the owner", and the two beginning "wave-6 precheck, ticket 16, open for the owner"): owner ruling 14 of 2026-09-24 accepts all four: `README.md`, not contracts 0.2, is the authority selfcheck checks; `FORMATS` is read through a keys-only parser; `SCHEMA` / `DEFAULTS` / `REQUIRED` are read structurally; a package marker's `used by: ... (as their package)` line equals its directory's `.py` siblings. Line numbers are those of the tree at commit 76cab53 (2026-09-24). The code today: `cmd_selfcheck` parses `README.md` alone (`run.py:1667`, through `readme_entries`, `run.py:168-182`), check 2 compares each file's `imports:` and `used by:` against the real import graph (`run.py:1123-1174`, the comparison at `:1164-1168`), and no line of `run.py` reads `notes/` or the contracts file; `FORMATS` (`agent/injected_text_formats.py:59`, a plain `ast.Assign` of an `ast.Dict`) is read by `literal_keys_of` (`run.py:297-319`), which requires one column-zero `ast.Dict` display and `ast.literal_eval`s its keys one at a time, and check 3 compares those keys with `schema.AXES['inject.format']` (`run.py:1184-1192`), while `literal_of` (`run.py:276-294`), never called on it, would refuse the `Format(...)` values; check 5 (`run.py:1424-1481`) matches each name's one column-zero `ast.Assign` or `ast.AnnAssign` by the target's bare id (`_column_zero_ann_or_assign`, `run.py:1427-1440`), so `DEFAULTS["n_inject"] = 0` (`data/trajectory_record.py:95`) is not a second binding, requires `SCHEMA` to be an `ast.Dict` with literal keys (`:1459-1467`) and `REQUIRED` to be a `frozenset(...)` call with a literal argument (`:1468-1477`), and fails a `REQUIRED` name that `SCHEMA` does not declare (`:1478-1480`); `DEFAULTS` is checked only for exactly one binding (`:1450-1456`) and its value, a dict comprehension over `SCHEMA`, is not read; `_check_package_marker` (`run.py:1109-1120`, called at `:1161-1163`) requires the named set to equal the directory's `.py` files with `__init__.py` excluded, and the two markers' lines (`README.md:191`, `:212`) name `models/agent_models/{service,gptoss}.py` and `models/probe_models/{base,service,qwen}.py`, the two directories' siblings. `external/probe-env/bin/python run.py selfcheck` ends `selfcheck: 31 python files, 0 problems` and exits 0. Of the two "ticket 16, open for the owner" entries beside them, the lock entry is closed by ruling 10 (the ruling-10 entry above) and the monitoring-line entry by commit 87453a0 (2026-09-23), which made `run.py:2156-2157` print `--debug` exactly when the walk ran with it (`debug_flag = " --debug" if cfg._debug else ""`, then `monitor with \`run.py ls {cfg._file}{debug_flag}\``); the entry's `run.py:1063` is the line's old position; the monitoring-line half of the wave-6 close entry above ("run.py prints the monitoring line without --debug on a --debug walk; README.md line 19 ...") is fixed by the same commit and `README.md:19` now spells the command with `external/probe-env/bin/python`, and that entry is outside this ruling (planner: owner ruling 14 of 2026-09-24, verified by implementer, 2026-09-24)

- 2.3 / 2.4 / 8.6 / 9(c)#1 (what a walk prints and its exit code), open for the owner: the contracts name only the monitoring line a card launch prints (9(c)#1) and 8.6's print table covers the subcommands, not the walk, so a reused stage and a CPU stage finished in place printed nothing but the CPU program's `@hb ` heartbeat lines, and a walk or a `retry` whose CPU stage exited non-zero appended its `failed` finish row and exited 0 (repo test 2026-09-25, the 01:41-01:43 walks: an eval `ValueError` traceback, then rc 0) -> `run.py._stage_step` prints one line per outcome in the form of the launch branch: `run.py: reused <run_id> (<run_dir>)` on a skip, `run.py: <run_id> ok; report <run_dir>/<done.json's report, else done.json>` after a CPU stage's `ok` finish row and after a pair stage's wider request is completed, and `run.py: <run_id> failed (exit <rc>); its output is above; run directory <run_dir>` after a CPU stage's `failed` finish row; it returns `failed` for that last case, `_walk_one` hands the outcome back, and `cmd_walk` and `cmd_retry` exit 1 when a CPU stage failed. The failed line names no log file because a CPU stage's process writes to the typing terminal and to no `log/` file (its piece entry's `log` is None). A card launch that returned a non-`up` outcome still exits 0 after its `launch returned` line, as gpu-run Phase 4 describes; the owner decides whether that case exits 1 as well (planner: repo test 2026-09-25, implementer group runpy)

- 2.1 / 2.2 / 5.2 / 8.6 (selfcheck and the stage table's field lists), open for the owner: 2.1's sections column for `sample` and `inject` is an explicit field list (`experimental_settings/schema.py` `STAGES`), so a new `Sample` or `Inject` field added the way `README.md` section 3 recipe 3 describes enters no key and reaches no stage, and nothing in the eleven selfcheck checks compared that list with the dataclass fields (repo test 2026-09-25: a new `inject` field stated as 5 kept inject key 34276f9e71fb, while a new `train` field moved the train key, because `train` is keyed as whole sections) -> `run.py selfcheck` gains check 12: every dataclass field of every `schema.SECTION_CLASSES` section (a nested dataclass field such as `train.predict` expanded one level, `MODELS_READONLY` left out) must be named, itself or through a dotted prefix, by some `STAGES[*]` `sections`, `projection` or `projection_generator` entry or by `REF_FIELDS`, and a field none of them names is one problem line; the `meta` section is left out because 5.2 marks both its fields `key: no` and no stage reads them. Check 12 reports no problem on today's schema. `README.md` section 3 recipe 3 now says a new `sample` or `inject` field also goes into that stage's `sections` (keyed) or `projection` (not keyed) tuple (planner: repo test 2026-09-25, implementer group runpy)

{
 "workflow": "wave4-precheck",
 "run_id": "wf_c88843cf-eca",
 "base_head_at_precheck": "d277e70",
 "agents": 33,
 "counts": {
  "findings": 29,
  "confirmed": 16,
  "refuted": 13,
  "unverified": 0,
  "distinct_confirmed": 13
 },
 "rulings": {
  "T10-1": "ticket 10 body corrected (A6a assertion is one filter expression)",
  "T10-2": "ticket 10 body corrected (A8 baseline fixture in the non-debug tree)",
  "T10-3": "ticket 10 body corrected (A8 restores temperature before the second refusal)",
  "T10-4": "ticket 10 body corrected (split_args 3-tuple unpacked)",
  "T10-5": "ticket 10 body corrected, errata entry (flat dotted diff); same as X-1",
  "T10-6": "ticket 10 body corrected, errata entry (backbone from the train row); assumption, open for the owner; same as X-2",
  "T10-7": "no build change; errata entry and ticket 10 Comments; open for the owner",
  "T11-1": "ticket 11 body corrected, errata entry (parse accumulator never rebound, rebuilt after a splice)",
  "T11-2": "ticket 11 body corrected (p2_e2 template uses {result})",
  "T11-3": "ticket 11 body corrected, errata entry (n_checked includes this cut)",
  "T11-4": "ticket 11 body corrected (spec and resume rows carry step); same as X-4",
  "T12-1": "ticket 12 body corrected (B3 and B7 create their own temporary directory)",
  "T12-5": "ticket 12 body corrected, errata entry (teardown_services local branch)",
  "X-1": "see T10-5",
  "X-2": "see T10-6",
  "X-4": "see T11-4"
 },
 "result": {
  "lost": [],
  "confirmed": [
   {
    "id": "T10-1",
    "ticket": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md",
    "severity": "script-defect",
    "title": "A6a's last assertion raises ShapeError: a 40-row mask applied to a 20-row frame",
    "ticketQuote": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:372: `assert f.filter(f[\"split\"] == \"test\").filter(f[\"risk\"] == 0.05).height == 10`",
    "conflictSource": "Measured with the merged code, polars 1.44.2 (/home/y-guo/reproduce/new1/external/probe-env/bin/python): `f` has 40 rows, `f.filter(f[\"split\"]==\"test\")` returns 20, and applying the 40-element boolean series `f[\"risk\"] == 0.05` to it raises `ShapeError: filter's length: 40 differs from that of the series: 20`.",
    "detail": "The fixture itself is sound: I reproduced A6a's rows through data/probe_output.py and eval/methods/ctool.py:74 `report` in a temp directory and got `temperature 0.0202`, `chosen {'0.1': 0.5, '0.05': 0.5}`, 40 fires rows over ['test','val'] and exactly 10 rows with split==test and risk==0.05 — so `reference fires: 40 ['test', 'val']` is right and the Float32-vs-0.05 comparison works. Only the chained `.filter()` expression is wrong; it must be one expression over `f` (e.g. `f.filter((pl.col(\"split\")==\"test\") & (pl.col(\"risk\")==0.05))`). As written A6a can never print `A6a ok`, and A6, which depends on A6a's directories, never runs.",
    "needsOwner": false,
    "verdict": {
     "parameter": "\n",
     "refuted": false,
     "evidence": "Ticket line confirmed verbatim: /home/y-guo/reproduce/new1/.scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:372 `assert f.filter(f[\"split\"] == \"test\").filter(f[\"risk\"] == 0.05).height == 10`, with :370 `f = pl.read_parquet(edir / \"fires.parquet\")` and :376 `Expected: \\`reference fires: 40 ['test', 'val']\\`, then \\`A6a ok\\`, exit 0.`\n\nMeasured with /home/y-guo/reproduce/new1/external/probe-env/bin/python (polars 1.44.2) on a 40-row frame shaped like A6a's fires.parquet (20 val + 20 test, two risk values): `f.filter(f[\"split\"]==\"test\")` -> 20 rows; then `.filter(f[\"risk\"]==0.05)` -> `ShapeError: filter's length: 40 differs from that of the series: 20`. The single-expression form `f.filter((pl.col(\"split\")==\"test\") & (pl.col(\"risk\")==0.05))` -> 10. The 40-element mask is built from the original `f`, so the error occurs regardless of the risk column's dtype.\n\nNot settled elsewhere: the ticket's Comments section is the file's last line (`## Comments` at :615 of 615 lines, no body). /home/y-guo/reproduce/new1/.scratch/from-zero/contract-errata.md has no entry on A6a or on this filter; its only fires.parquet line is :47 (probe_eval writing the file itself). It is also not among the known facts given to the prechecker; known fact (f) concerns probe_eval.py's generator gates, not this assertion.\n\nConsequence: A6a exits non-zero and never prints `A6a ok`, and A6 (:378-:390) reuses A6a's EK0=`5555eeee5555` / BK0=`cccccccccccc` directories, so it cannot run either.",
     "correctedStatement": "A6a's last assertion at ticket line 372 chains two `.filter()` calls while building both boolean masks from the original 40-row `f`; the second mask (length 40) is applied to the 20-row result of the first filter, which polars 1.44.2 rejects with `ShapeError: filter's length: 40 differs from that of the series: 20`. The fixture data itself is correct (40 fires rows over ['test','val'], 10 with split==test and risk==0.05); only the assertion expression is wrong, and it must be a single expression over `f`, e.g. `f.filter((pl.col(\"split\") == \"test\") & (pl.col(\"risk\") == 0.05)).height == 10`."
    }
   },
   {
    "id": "T10-2",
    "ticket": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md",
    "severity": "script-defect",
    "title": "A8 builds the baseline run under debug/ while step 3 locates it without the debug overlay",
    "ticketQuote": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:164: \"`base_dir = schema.run_dir_of(\"sample\", cfg._upstream[\"baseline.sample\"], debug=False)` when `cfg.score.baseline` is set (a reference is located **without** the debug overlay)\" versus :488: `bdir = schema.run_dir_of(\"sample\", BK, debug=True)` and :567-568: \"delete the three fixture directories and name the keys (`1111aaaa1111`, `2222bbbb2222`, `3333cccc3333`, all under `debug/`)\"",
    "conflictSource": "experimental_settings/schema.py:1162-1168 `run_dir_of` puts a `debug=True` key under `<root>/debug/<stage>/<key>` and a `debug=False` key under `<root>/<stage>/<key>`; notes/plans/2026-09-17-contracts.md:2604+ (3.4) \"`--debug` applies only to the setting named on the command line and to the ... a debug run points at real upstream directories\", and .scratch/from-zero/contract-errata.md:81 \"a resolved reference is always keyed without the debug overlay, so a `--debug` run points at real upstream directories\".",
    "detail": "A8 writes the baseline's records and settings.yaml into `<root>/debug/sample/2222bbbb2222`, but score_run as the ticket specifies it looks for them at `<root>/sample/2222bbbb2222`. The same-setup gate (step 4) then fails on a missing settings.yaml rather than passing, so A8 cannot reach `A8 ok`, and neither can either refusal command (both re-run the same score run directory). The errata settles which side is right: the reference is non-debug, so the fixture's `bdir` must be created with `debug=False` and the cleanup note must stop calling `2222bbbb2222` a `debug/` key. I verified the rest of A8's mechanics against the merged code in a temp tree: `open_record`/`row`/`close` accept every field A8 passes (no stamped column, no undeclared name), `done_pairs(sdir, pairs)` returns both pairs, `read_dir` returns 8 rows with `success` [True, True], `load_frozen` parses all three fixture settings.yaml files, and `requested_pairs(env, [\"train\"], None, 2, [42])` returns [('train','82e2fac_1',42), ('train','82e2fac_2',42)].",
    "needsOwner": false,
    "verdict": {
     "refuted": false,
     "evidence": "Ticket step 3 (.scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:163-165): \"`scored_dir = schema.run_dir_of(scored, cfg._upstream[scored], debug=cfg._debug)`; `base_dir = schema.run_dir_of(\\\"sample\\\", cfg._upstream[\\\"baseline.sample\\\"], debug=False)` when `cfg.score.baseline` is set (a reference is located **without** the debug overlay).\"\n\nA8 fixture (:487-489): `sdir = schema.run_dir_of(\"sample\", SK, debug=True)` / `bdir = schema.run_dir_of(\"sample\", BK, debug=True)` / `rdir = schema.run_dir_of(\"score\", RK, debug=True)`, with `SK, BK, RK = \"1111aaaa1111\", \"2222bbbb2222\", \"3333cccc3333\"` (:486) and the score settings `_upstream: {{sample: {SK}, baseline.sample: {BK}}}` (:531), `score: {{baseline: 'baseline/gpt_oss_120b_appworld', by_seed: true}}` (:528), `_debug: true` (:533). The first refusal command re-points at the same baseline with `debug=True` (:558) and the cleanup note calls all three keys \"all under `debug/`\" (:567).\n\nexperimental_settings/schema.py:1162-1168:\n```\ndef run_dir_of(stage: str, key: str, *, debug: bool) -> Path:\n    cfg = _outputs_config()\n    root = Path(cfg[\"root\"])\n    if debug:\n        return root / cfg[\"debug_subdir\"] / stage / key\n    return root / stage / key\n```\nSo step 3 resolves the baseline to `<root>/sample/2222bbbb2222` while A8 writes its records and settings.yaml to `<root>/debug/sample/2222bbbb2222`.\n\nnotes/plans/2026-09-17-contracts.md:2425-2427: \"A **resolved reference** is a different case and passes `debug=False`: a reference is always keyed and located without the overlay (3.4), so a debug run points at real upstream directories.\" Same rule at :2627-2629.\n\n.scratch/from-zero/contract-errata.md:81: \"3.4 / 9(c)#8: a resolved reference is always keyed without the debug overlay, so a `--debug` run points at real upstream directories -> ... `score.baseline` ...\", which names `score.baseline` as one of the references.\n\nThe same ticket already applies this rule in the other fixture: :322-323 \"step 6 of `probe_eval.run` locates a resolved reference with `run_dir_of(..., debug=False)`, so **a referenced eval run and the train run it names are non-debug directories**\", and :334-335 builds those with `debug=False`. So A8 is inconsistent with the ticket's own step 3, with the contracts and with its own A6a precedent.\n\nTicket 10 has no Comments content (\":615 `## Comments`\" is the file's last line, wc -l = 615), so nothing later settles it; the errata's only entry on references (line 81) rules the other way. This is not one of the listed known facts.",
     "correctedStatement": "Confirmed as reported. A8 (ticket 10, lines 487-489) creates the baseline run directory with `debug=True`, but step 3 (:164) specifies `base_dir = schema.run_dir_of(\"sample\", cfg._upstream[\"baseline.sample\"], debug=False)`, matching contracts :2425-2427 and errata :81. With `run_dir_of` as merged (schema.py:1162-1168) the two paths differ (`<root>/debug/sample/2222bbbb2222` written vs `<root>/sample/2222bbbb2222` read), so the step-4 same-setup gate fails on a missing settings.yaml and A8 cannot reach `A8 ok`; both refusal commands, which re-run the same score directory, fail for that same reason instead of the intended one. The fixture's `bdir` must be created with `debug=False` (as A6a's referenced directories already are, :334-335) and the cleanup note (:567) must stop listing `2222bbbb2222` under `debug/`."
    }
   },
   {
    "id": "T10-3",
    "ticket": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md",
    "severity": "script-defect",
    "title": "A8's second refusal cannot print the missing-pair message: the same-setup gate still fires first",
    "ticketQuote": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:563-564: \"`exit=1`. Then delete one baseline record file and rerun the same command: / expected a message naming the missing `(task_id, seed)` pair, `exit=1`.\"",
    "conflictSource": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:170-173: step 4 is the \"**Same-setup gate** (2.5)\" and step 5 the \"**Baseline completeness gate** (2.5)\", in that order; the command at :555-559 has already rewritten the baseline's settings.yaml to `temperature: 0.7` and nothing changes it back.",
    "detail": "The first refusal command mutates the baseline's frozen generation section in place. The second refusal command is \"the same command\" over the same directories, so score_run refuses at step 4 on `generation.temperature` again and never reaches step 5. The observed output would be the step-4 message, not \"the missing `(task_id, seed)` pair\", and the implementer is forbidden by spec section 7 to edit the acceptance command. The fix is to restore `temperature: 1.0` before deleting the baseline record file.",
    "needsOwner": false,
    "verdict": {
     "refuted": false,
     "evidence": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:167-171 fixes the order inside \"What `main` does, in order\": \"4. **Same-setup gate** (2.5): `schema.load_frozen` on both upstream run directories and compare their `data`, `models.agent` and `generation` sections field by field; refuse, naming both directories and the first field that differs.\" / \"5. **Baseline completeness gate** (2.5): `done_pairs(base_dir, pairs)` must cover every pair\". The first refusal command at :555-559 mutates the baseline run's own settings.yaml in place: \"p = schema.run_dir_of('sample', '2222bbbb2222', debug=True) / 'settings.yaml'\" / \"p.write_text(p.read_text().replace('temperature: 1.0', 'temperature: 0.7'))\", and 2222bbbb2222 is BK, the score run's `_upstream[\"baseline.sample\"]` (:486, :488, :531). The second refusal at :563-564 reads \"Then delete one baseline record file and rerun the same command: expected a message naming the missing `(task_id, seed)` pair, `exit=1`.\" Grepping the whole ticket for \"temperature\" returns only :515 (the fixture writing `generation: {temperature: 1.0, ...}`), :559 (the mutation) and :562 (the expected step-4 message) — nothing restores 1.0, and rerunning :555-559 is a no-op because `temperature: 1.0` is no longer present. The baseline therefore still differs on `generation.temperature`, score_run refuses at step 4 again and step 5 is never reached. Nothing settles it: the ticket's \"## Comments\" heading at :615 is the file's last line (wc -l = 615, section empty); .scratch/from-zero/contract-errata.md has no entry on the score gates' order (its `score` lines are 46, 57, 81, 147); notes/plans/2026-09-17-contracts.md:2199-2226 states both gates without ordering them. .scratch/from-zero/spec.md:162 \"Never edit an acceptance command to make it pass.\" forbids the implementer from inserting the restore step.",
     "correctedStatement": "A8's second refusal cannot produce its expected output: the first refusal command (ticket :555-559) leaves the baseline's `generation.temperature` at 0.7 and nothing resets it, so the same-setup gate (step 4, :167-170) refuses again and the baseline completeness gate (step 5, :171-173) is never reached; the command prints the step-4 message naming `generation.temperature` rather than the missing `(task_id, seed)` pair, and spec.md:162 forbids the implementer from editing the command to restore `temperature: 1.0`."
    }
   },
   {
    "id": "T10-4",
    "ticket": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md",
    "severity": "implementer-will-trip",
    "title": "`match` is written with attribute access on `split_args`'s result, which is a plain tuple",
    "ticketQuote": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:49-56: \"1. `t = env.split_args(target)` ... 2. `p = env.split_args(pred)` ... 3. `tool_ok = p.tool == t.tool`.\"",
    "conflictSource": "data/environments/appworld.py:295-300 `split_args` returns `f\"apis.{app}.{api}\", args, (start, end)` — a bare 3-tuple; data/environments/__init__.py:54 declares the return type `tuple[str, list[tuple[str, str]], tuple[int, int]] | None`; notes/plans/2026-09-17-contracts.md:2870+ (4.2) gives the same tuple return. Measured: `env.split_args('apis.a.x(k=1)')` returns `('apis.a.x', [('k', '1')], (0, 13))`.",
    "detail": "Copying `p.tool` / `t.tool` into eval/methods/cgen.py raises `AttributeError: 'tuple' object has no attribute 'tool'` the first time A7 runs. The correct spelling is `tool, args, _ = env.split_args(...)` (or index 0 and 1). The same shorthand is implied for \"the two argument lists\" in step 4.",
    "needsOwner": false,
    "verdict": {
     "evidence": "Ticket, step 3 of `match` (the line is 56, not 49-56; steps 1-2 are lines 50 and 53):\n- .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:50 \"1. `t = env.split_args(target)`; **raise**, naming the target, when it is None\"\n- .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:53 \"2. `p = env.split_args(pred)`; when None, return all three False\"\n- .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:56 \"3. `tool_ok = p.tool == t.tool`.\"\n\nMerged code returns a bare 3-tuple, no attribute:\n- /home/y-guo/reproduce/new1/data/environments/appworld.py:295 `def split_args(self, text: str) -> tuple[str, list[tuple[str, str]], tuple[int, int]] | None:` … :300 `return f\"apis.{app}.{api}\", args, (start, end)`\n- /home/y-guo/reproduce/new1/data/environments/__init__.py:54 declares the same tuple return type\n- notes/plans/2026-09-17-contracts.md:2880 \"| `split_args` | `split_args(text: str) -> tuple[str, list[tuple[str, str]], tuple[int, int]] \\| None` | tool name, named arguments in call order, and the character span; or None |\"\n- No `Call` class or namedtuple exists: `grep -rn \"namedtuple\\|class Call\" data/environments/` returns nothing (contracts-synthesis-notes.md:161 records that draft A/C's `Call`/`parse_call` shape was \"Not applied\"; the rename is only open item 9(b)#14, contracts.md:5154).\n- The one merged caller spells it as a tuple: data/build_training_dataset.py:152 `parsed = env.split_args(action)` … :163 `tool, call_args, _span = parsed`.\n\nMeasured with /home/y-guo/reproduce/new1/external/appworld/venv/bin/python (nothing written): `env.split_args('apis.a.x(k=1)')` -> `('apis.a.x', [('k', '1')], (0, 13))`; `t.tool` -> `AttributeError: 'tuple' object has no attribute 'tool'`.\n\nNot settled elsewhere: ticket 10's `## Comments` heading is the last line of the file (line 615 of 615, empty section); the errata (.scratch/from-zero/contract-errata.md) has no entry on `split_args`'s return shape (its three `split_args` entries, lines 13, 122, 149, cover `build_call` quoting and the skip-on-None counts); no later line of ticket 10 respells step 3. It is not one of the listed known facts. The acceptance command A7 (ticket lines 456-476) calls `cgen.match(good, good, env)` directly, so a literal copy of line 56 fails there immediately.",
     "refuted": false,
     "correctedStatement": "The quote is at line 56 (not 49-56). The rest of the finding holds: ticket 10 line 56 writes `tool_ok = p.tool == t.tool` on values that `env.split_args` returns as a plain 3-tuple `(tool, args, (start, end))`, so the correct spelling is `tool, args, _ = env.split_args(...)` (or indices 0 and 1). The same tuple applies to step 4's \"two argument lists\" (index 1 of each result)."
    }
   },
   {
    "id": "T10-5",
    "ticket": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md",
    "severity": "implementer-will-trip",
    "title": "`method_table` reads the registry row's `diff` as a nested dict; it is flat with dotted keys",
    "ticketQuote": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:249-252: \"`backbone` is `diff[\"models\"][\"probe\"]` when present and the schema default for `models.probe` otherwise; `method` is `diff[\"probe\"][\"method\"]` when present and the schema default otherwise (`diff` is `settings_diff.yaml` as JSON, 8.1 ...)\"",
    "conflictSource": "experimental_settings/schema.py:1058-1073 `fields_of` — docstring: \"The 'fields' block of 3.3, = settings_diff.yaml: dotted field -> value, only fields differing from default\", and `out[dotted] = value` where `dotted` comes from `_stage_dotted_fields` (schema.py:1002-1019), e.g. `\"probe.method\"`; schema.py:1264,1277 `freeze` writes exactly that dict to settings_diff.yaml. Measured: `schema._stage_dotted_fields('eval')` == `['probe.method', 'eval.risk', 'eval.theta_grid', 'eval.bootstrap', 'eval.bootstrap_seed', 'eval.theta_from']`.",
    "detail": "`diff` is `{\"probe.method\": \"cgen\", ...}`, so `diff[\"probe\"]` is a KeyError and `diff.get(\"models\")` is None (a `None[\"probe\"]` TypeError). The correct lookups are `diff[\"models.probe\"]` and `diff[\"probe.method\"]`. With the \"when present\" guard written literally the code will not crash but will silently take the schema default for both columns on every row.",
    "needsOwner": false,
    "verdict": {
     "refuted": false,
     "evidence": "Ticket .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:248-252: \"2. Per row, resolve the cell **without opening any `settings.yaml`**: `backbone` / is `diff[\\\"models\\\"][\\\"probe\\\"]` when present and the schema default for / `models.probe` otherwise; `method` is `diff[\\\"probe\\\"][\\\"method\\\"]` when present / and the schema default otherwise (`diff` is `settings_diff.yaml` as JSON, 8.1, / so a default-valued field is absent from it).\"\n\nnotes/plans/2026-09-17-contracts.md:4449: \"| `diff` | dict | `settings_diff.yaml` as JSON: what the key was computed over, and what `run.py find` searches |\"\n\nexperimental_settings/schema.py:1058 (fields_of docstring): \"The 'fields' block of 3.3, = settings_diff.yaml: dotted field -> value, only fields differing from default.\"; schema.py:1072: `out[dotted] = value`, with `dotted` from `_stage_dotted_fields(stage)` (schema.py:1002-1019).\n\nexperimental_settings/schema.py:1265: `diff = fields_of(stage, setting)`; schema.py:1277: `_atomic_write_yaml(diff_path, diff)` where diff_path = run_dir/\"settings_diff.yaml\".\n\njobs/registry.py:877: `\"diff\": start.get(\"diff\"),` — the folded row carries the start row's diff unchanged.\n\nexperimental_settings/schema.py:248-250: `\"eval\": { \"sections\": (\"probe.method\", \"eval\"), \"models\": (),`.\n\nMeasured with /home/y-guo/reproduce/new1/external/appworld/venv/bin/python (read-only, wrote nothing): `schema._stage_dotted_fields('eval')` == `['probe.method', 'eval.risk', 'eval.theta_grid', 'eval.bootstrap', 'eval.bootstrap_seed', 'eval.theta_from']`.\n\nNot settled elsewhere: ticket 10's `## Comments` (line 615) is empty; the only diff-related errata entry (.scratch/from-zero/contract-errata.md:43) fixes only that `table` calls `registry.ls(...)` and which start-row fields the folded row carries; acceptance A9 (ticket lines 570-586) runs `method_table.table()` against an empty registry, so the lookup is never executed.",
     "correctedStatement": "The ticket tells the implementer to read the registry row's `diff` as a nested dict (`diff[\"models\"][\"probe\"]`, `diff[\"probe\"][\"method\"]`), but `diff` is `settings_diff.yaml` as JSON, which `schema.fields_of` writes as a flat map of dotted field -> value (schema.py:1058,1072; contracts:4449). `method` must be read as `diff[\"probe.method\"]`. `backbone` cannot be read from `diff` at all under any spelling: `STAGES[\"eval\"]` has `sections: (\"probe.method\", \"eval\")` and `models: ()` (schema.py:248-250), and `fields_of` iterates only `_stage_dotted_fields('eval')` == ['probe.method', 'eval.risk', 'eval.theta_grid', 'eval.bootstrap', 'eval.bootstrap_seed', 'eval.theta_from'], so no `models.*` key ever lands in an eval-stage diff — the finding's suggested `diff[\"models.probe\"]` is also wrong. Written literally with the \"when present\" guard the code does not crash but prints the schema default in the `backbone` and `method` columns on every row; written without the guard, `diff[\"probe\"]` raises KeyError. A9 catches neither, because it runs against an empty registry."
    }
   },
   {
    "id": "T10-6",
    "ticket": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md",
    "severity": "blocks-launch",
    "title": "An eval run's `diff` never carries `models.probe`, so the table's backbone column is always the schema default",
    "ticketQuote": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:249-251: \"Per row, resolve the cell **without opening any `settings.yaml`**: `backbone` is `diff[\"models\"][\"probe\"]` when present and the schema default for `models.probe` otherwise\"",
    "conflictSource": "experimental_settings/schema.py:248-249 `STAGES[\"eval\"][\"sections\"] = (\"probe.method\", \"eval\")` — the eval stage's diff is computed over those fields only; measured `schema._stage_dotted_fields('eval')` == `['probe.method', 'eval.risk', 'eval.theta_grid', 'eval.bootstrap', 'eval.bootstrap_seed', 'eval.theta_from']`, with no `models.probe` in it; `schema.SECTION_CLASSES['models']().probe` == `'qwen3_0pt6b'`.",
    "detail": "`settings_diff.yaml` of an **eval** run is stage-scoped, so `models.probe` is absent from it whatever backbone was trained — a `ctool_qwen3_1pt7b` eval row and a `ctool_qwen3_0pt6b` eval row carry identical diffs apart from nothing. Under the ticket's rule every row therefore reports `backbone = qwen3_0pt6b`, and contracts 8.6 (notes/plans/2026-09-17-contracts.md:4694, \"`run.py table [workflow]` | the backbone x method table\") and the ticket's own step 5 \"One table row per `(backbone, method, risk target)`\" collapse to one backbone. The backbone has to come from somewhere else (the train run named by the eval row's `upstream[\"train\"]`, or that run's own diff, or the eval run's settings.yaml which the ticket forbids opening) and the ticket names no such source; .scratch/from-zero/contract-errata.md:43 settles only that `table` goes through `registry.ls`, not where the backbone comes from.",
    "needsOwner": true,
    "verdict": {
     "refuted": false,
     "evidence": "Ticket line, as quoted: .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:248-251 — \"2. Per row, resolve the cell **without opening any `settings.yaml`**: `backbone` is `diff[\\\"models\\\"][\\\"probe\\\"]` when present and the schema default for `models.probe` otherwise; `method` is `diff[\\\"probe\\\"][\\\"method\\\"]` when present and the schema default otherwise (`diff` is `settings_diff.yaml` as JSON, 8.1, so a default-valued field is absent from it).\"\n\nThe diff is stage-scoped, measured on the merged tree with /home/y-guo/reproduce/new1/external/probe-env/bin/python:\n  schema.STAGES['eval']['sections'] == ('probe.method', 'eval')\n  schema.STAGES['eval']['models'] == ()\n  schema._stage_dotted_fields('eval') == ['probe.method', 'eval.risk', 'eval.theta_grid', 'eval.bootstrap', 'eval.bootstrap_seed', 'eval.theta_from']\n  schema.SECTION_CLASSES['models']().probe == 'qwen3_0pt6b'\n\nexperimental_settings/schema.py:1057-1059 — \"def fields_of(stage, setting) -> dict: \\\"\\\"\\\"The 'fields' block of 3.3, = settings_diff.yaml: dotted field -> value, only fields differing from default.\\\"\\\"\\\"\" and :1063 \"for dotted in _stage_dotted_fields(stage):\" — the loop body is the only writer of `out`, so no key outside that six-name list can ever appear in an eval run's settings_diff.yaml.\nexperimental_settings/schema.py:1267-1277 — `diff_path = run_dir / \"settings_diff.yaml\"` is written from `diff = fields_of(stage, setting)` (:1264).\n\nThe registry row carries exactly that file: notes/plans/2026-09-17-contracts.md:4451 — \"| `diff` | dict | `settings_diff.yaml` as JSON: what the key was computed over, and what `run.py find` searches |\"; jobs/registry.py:877 — `\"diff\": start.get(\"diff\"),` (the folded row passes the start row's diff through unchanged).\n\nNothing settles it: .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:615 \"## Comments\" is the last line of the file (the section is empty), and the only two eval-table errata entries, .scratch/from-zero/contract-errata.md:43 (\"the registry offers no raw-row reader -> `table` calls `registry.ls(workflow)` and the folded row must carry the start row's `stage`, `key`, `dir`, `diff`, `parent` and `swept`\") and :44 (the risk tier), name no source for the backbone.\n\nThe ticket's own A9 (lines 573-582) cannot catch this — it asserts only the header line and permits an empty body (\"its body may be empty while the registry holds no eval row\"); the row-level check is M-E2 at :607-610, which is marked \"GPU / main session — not yours\".",
     "correctedStatement": "Confirmed, with one addition the original finding understates. `settings_diff.yaml` holds FLAT dotted keys (`schema.fields_of` writes `out[dotted] = value`, experimental_settings/schema.py:1063-1071), so both subscript chains in ticket line 249-251 are wrong spellings of the merged format: `diff[\"probe\"][\"method\"]` raises KeyError where the real key is `diff[\"probe.method\"]`, and `diff[\"models\"][\"probe\"]` names a field that the eval stage's diff never contains at all, whatever the nesting — `models.probe` is not in `_stage_dotted_fields('eval')`, and `STAGES[\"eval\"][\"models\"]` is empty, so the eval key does not fold the probe row either. Consequence: every table row's `backbone` cell is the constant schema default `qwen3_0pt6b`, and the `(backbone, method, risk target)` grouping of step 5 (ticket line 258) collapses to one backbone. The ticket names no alternative source; the backbone is reachable only through the eval row's `upstream[\"train\"]` -> that train run's `meta.json` / `settings_diff.yaml` (the train stage does carry `models.probe`, schema.py:234-236 `STAGES[\"train\"][\"sections\"] = (\"models.probe\", \"probe\", \"train\")` and `\"models\": (\"probe\",)`), which the ticket does not mention."
    }
   },
   {
    "id": "T10-7",
    "ticket": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md",
    "severity": "script-defect",
    "title": "M-E2 evaluates a --debug run but `table` is pinned to `registry.ls(workflow, debug=False)`, which hides debug rows",
    "ticketQuote": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:608-610: \"`M-E2` the generator eval against a real classifier eval: `run.py train_probe cgen_qwen3_0pt6b --debug` then `run.py table train_probe` must show `exact` numbers with the same `n` as the classifier's `frozen` `n` at that risk, and a table row per `(backbone, method, risk)`.\"",
    "conflictSource": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:244: \"`rows = [r for r in registry.ls(workflow, debug=False) if r[\"stage\"] == \"eval\"]`\"; jobs/registry.py:920-921: `if not debug: entries = [e for e in entries if not e[\"start\"].get(\"debug\")]`; notes/plans/2026-09-17-contracts.md 3.4: \"Its registry rows carry `debug: true` and `ls` hides them\".",
    "detail": "A `--debug` train_probe walk appends a start row with `debug: true`. `registry.ls(workflow, debug=False)` drops it, so `table` sees no eval row and renders an empty body — the check as written can never show the `exact` numbers it demands. Either `table` takes a debug flag (and `run.py table` a `--debug` switch), or M-E2 must be run over a non-debug eval run, which at the end of construction means a real GPU run. This is a main-session check, not the implementer's, but it cannot pass as stated.",
    "needsOwner": true,
    "verdict": {
     "refuted": false,
     "evidence": "Ticket M-E2, /home/y-guo/reproduce/new1/.scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:607-610 (under the heading \"### GPU / main session — not yours\", :605): \"`M-E2` the generator eval against a real classifier eval: / `run.py train_probe cgen_qwen3_0pt6b --debug` then `run.py table train_probe` must show / `exact` numbers with the same `n` as the classifier's `frozen` `n` at that risk, / and a table row per `(backbone, method, risk)`.\"\n\nTicket step 1 of eval/method_table.py, same file :244: \"1. `rows = [r for r in registry.ls(workflow, debug=False) if r[\\\"stage\\\"] == \\\"eval\\\"]`.\" — immediately followed by \":245-247 **Decision already made (errata):** the registry offers no raw-row reader, so `table` calls `registry.ls(...)`\".\n\nMerged code, /home/y-guo/reproduce/new1/jobs/registry.py:891-892 `def ls(workflow: str | None = None, *, debug: bool = False, ...)` and :920-921: \"    if not debug: /        entries = [e for e in entries if not e[\\\"start\\\"].get(\\\"debug\\\")]\". The start row carries the flag: contracts Part 8.1 row \"| `debug` | bool | a debug run |\" (/home/y-guo/reproduce/new1/notes/plans/2026-09-17-contracts.md:4446), and jobs/registry.py:189 `\"debug\": False,` is the meta default.\n\nContracts confirm both halves of the conflict: :2621-2625 \"`--debug` is applied **before** keying and adds `debug: true` to the key ... Its registry rows carry `debug: true` and `ls` hides them / unless asked.\" and :5322-5324 \"The rows carry `debug: true` and `ls` hides / them unless asked.\" The \"unless asked\" hook does not exist for the table: the pinned signature has no debug parameter — :563 \"offers:  table(workflow: str | None = None, out: Path | None = None) -> str (8.6)\" and :4703 \"| `run.py table [workflow]` | ... the call that crosses the file boundary is pinned: `eval/method_table.table(workflow: str \\| None = None, out: Path \\| None = None) -> str` ...\". No flag is listed on `run.py table`.\n\nNothing settles it: .scratch/from-zero/contract-errata.md mentions method_table only at :43 (\"`table` calls `registry.ls(workflow)` and the folded row must carry the start row's `stage`, `key`, `dir`, `diff`, `parent` and `swept`\" — `registry.ls(workflow)` defaults to debug=False) and :44 (the risk-tier decision); neither touches debug. Ticket 10's Comments section is empty (file ends at :615 with \"## Comments\" and no body). The finding is not among the known facts (a)-(i).",
     "correctedStatement": "The conflict is real as stated, with two scope refinements. (1) The defect is confined to the M-E2 line, which the ticket itself files under \":605 ### GPU / main session — not yours\"; it does not reach the implementer's code or any of the implementer's acceptance commands A1-A9, and step 1 at :244 (`registry.ls(workflow, debug=False)`) is exactly what errata :43 and the contracts pin, so the implementer must write it that way. (2) The remedy the finding offers — \"either `table` takes a debug flag (and `run.py table` a `--debug` switch)\" — is not available to this ticket: contracts :563 and :4703 pin `table(workflow, out)` with no debug parameter, and the file tree and contracts are fixed, so only the owner can change it. What remains is: a `--debug` train_probe walk appends a start row with `debug: true` (contracts :2624), `registry.ls` drops such rows by default (jobs/registry.py:920-921), so `run.py table train_probe` after a `--debug` walk renders a body with no eval row and can never show the `exact` numbers M-E2 demands. As written, M-E2 can only pass over a non-debug eval run, which at the end of construction means a real GPU run. Owner's call."
    }
   },
   {
    "id": "T11-1",
    "ticket": ".scratch/from-zero/issues/11-agent-loop.md",
    "severity": "implementer-will-trip",
    "title": "`state = mod.parse(delta, state)` throws away the `raw` key that gptoss.parse accumulates into, so the channel split restarts at every chunk",
    "ticketQuote": ".scratch/from-zero/issues/11-agent-loop.md:129 — \"and call `state = mod.parse(delta, state)` per chunk, keeping the two keys `reasoning` and `content`.\"; the same call is repeated at .scratch/from-zero/issues/11-agent-loop.md:214 — \"2. Per chunk, `state = mod.parse(delta, state)`;\"",
    "conflictSource": "models/agent_models/gptoss.py:100 — \"state[\\\"raw\\\"] = state.get(\\\"raw\\\", \\\"\\\") + text_delta\" and models/agent_models/gptoss.py:119 — \"return {\\\"reasoning\\\": \\\"\\\\n\\\".join(reasoning), \\\"content\\\": \\\"\\\\n\\\".join(content)}\"",
    "detail": "The merged `parse` mutates the dict it is handed (it stores the accumulated `raw` there) and returns a NEW two-key dict that does not carry `raw`. Rebinding `state` to the return value therefore discards the accumulator, and the next chunk parses only its own delta. Measured with the merged file and B4's own two chunks under external/appworld/venv/bin/python: the literal ticket usage ends at {'reasoning': 'Done.', 'content': 'ok'}; keeping `state` as the accumulator and taking the result separately ends at {'reasoning': 'Hi there. Done.', 'content': 'ok'}. B4's assertion at .scratch/from-zero/issues/11-agent-loop.md:566 (`assert \"Hi there.\" in res.reasoning`) therefore fails on the literal spelling. In agent/inject.py the same spelling is NOT caught by any acceptance (C4 fires inside the first chunk of the first stream and never reads a second chunk's parse), but it corrupts `thinking_so_far`, hence `ts = len(raw) - len(thinking_so_far)` and hence `pos = ts + cut`, on every real multi-chunk step. The correct usage is one long-lived accumulator dict per stream, with the return value read for `reasoning`/`content`; the ticket also never says what that accumulator becomes after step 9's reset (`raw = head_txt + note`).",
    "needsOwner": false,
    "verdict": {
     "refuted": false,
     "evidence": "Ticket, literal spelling (both places):\n- .scratch/from-zero/issues/11-agent-loop.md:128-129 — \"`raw` (text) and `gen_ids`, and call `state = mod.parse(delta, state)` per / chunk, keeping the two keys `reasoning` and `content`.\"\n- .scratch/from-zero/issues/11-agent-loop.md:214-215 — \"2. Per chunk, `state = mod.parse(delta, state)`; / `thinking_so_far = state[\\\"reasoning\\\"]`.\"\n- .scratch/from-zero/issues/11-agent-loop.md:566 (acceptance B4) — \"assert \\\"Hi there.\\\" in res.reasoning and \\\"ok\\\" in res.content, res\"\n\nMerged code on disk at HEAD d277e70:\n- models/agent_models/gptoss.py:101 — \"state[\\\"raw\\\"] = state.get(\\\"raw\\\", \\\"\\\") + text_delta\" (the accumulator lives in the dict it is HANDED; the finding cited :100, the actual line is :101)\n- models/agent_models/gptoss.py:102 — \"full = state[\\\"raw\\\"].split(\\\"<|return|>\\\", 1)[0]\"\n- models/agent_models/gptoss.py:119 — \"return {\\\"reasoning\\\": \\\"\\\\n\\\".join(reasoning), \\\"content\\\": \\\"\\\\n\\\".join(content)}\" (the returned dict has no \"raw\")\n\nThe contract states the same rule explicitly, so this is not only a code mismatch:\n- notes/plans/2026-09-17-contracts.md:3712 — \"`parse(text_delta, state)` | `str, dict -> dict` | ... carrying `state` from chunk to chunk; returns the channels grown so far under **exactly two keys, `reasoning` and `content`** ... Per-family bookkeeping (which channel is open, a partial control token) lives in `state` and never in the returned dict.\" Rebinding `state` to the return value is therefore forbidden by the contract itself.\n\nMeasured with the merged file and B4's own two chunks, run outside the repo with /home/y-guo/reproduce/new1/external/appworld/venv/bin/python (nothing written into the repo or under the outputs root):\n  literal ticket spelling -> {'reasoning': 'Done.', 'content': 'ok'}\n  long-lived accumulator  -> {'reasoning': 'Hi there. Done.', 'content': 'ok'}\nSo B4's assertion at :566 fails on the literal spelling.\n\nNot settled elsewhere:\n- .scratch/from-zero/contract-errata.md:54 is the only parse entry for 7.3/6.2; it states that parse returns only the two grown channel texts and that the build derives `ts = len(raw) - len(thinking_so_far)`. It never corrects the `state =` rebinding.\n- Ticket 11's Comments section (:1090-1108) carries only the wave-3 history-rule ruling of 2026-09-18 (known fact d) and says nothing about parse state.\n- Every other \"state\" occurrence in the ticket (grep: 128, 214-215, 266, 649) repeats or depends on the same spelling; line 649's table entry `len(raw) - len(state[\"reasoning\"])` assumes the grown reasoning, which the literal spelling does not produce.\n- Ticket step 9 (:266-269) resets `raw`, `gen_ids`, `bounds`, the scored-cut set and `accepted` after a splice and never says what the parse accumulator becomes, so the finding's secondary point also holds.\n\nIt is none of the listed known facts (a)-(i).",
     "correctedStatement": "Ticket 11 spells the per-chunk parse call as `state = mod.parse(delta, state)` at :128-129 and :214, which contradicts both the merged models/agent_models/gptoss.py (the accumulator is stored into the dict passed in, :101, while the dict returned at :119 has only `reasoning` and `content`) and notes/plans/2026-09-17-contracts.md:3712 (\"Per-family bookkeeping ... lives in `state` and never in the returned dict\"). Written literally, the accumulator is discarded after the first chunk and every later chunk is parsed alone. The ticket's own acceptance B4 (:566) fails on it: measured, the literal spelling ends at {'reasoning': 'Done.', 'content': 'ok'} while one long-lived accumulator dict ends at {'reasoning': 'Hi there. Done.', 'content': 'ok'}. The correct usage is one accumulator dict per stream, passed to every `parse` call and never rebound, with the return value read for `reasoning`/`content`; in agent/inject.py the same wrong spelling would corrupt `thinking_so_far`, hence `ts = len(raw) - len(thinking_so_far)` and `pos = ts + cut`, on every multi-chunk step without any acceptance catching it. Step 9 (:266-269) additionally lists the post-splice reset of `raw`, `gen_ids`, `bounds`, the scored-cut set and `accepted` but never says what the parse accumulator becomes."
    }
   },
   {
    "id": "T11-2",
    "ticket": ".scratch/from-zero/issues/11-agent-loop.md",
    "severity": "implementer-will-trip",
    "title": "The `p2_e2` template is written with `{exec_out}` while every other entry and the stated `.format` call use `{result}`",
    "ticketQuote": ".scratch/from-zero/issues/11-agent-loop.md:60 — \"| `p2_e2` | `p2` | `True` | `SYSTEM_EXTRA` | `\\\"{call} = {exec_out}\\\"` |\"",
    "conflictSource": "legacy/pipeline/inject/inject_format.py:45 — \"\\\"p2_e2\\\": dict(place=\\\"after\\\", body=\\\"{call} = {result}\\\", system=SYSTEM_EXTRA),\"; and the ticket's own rule at .scratch/from-zero/issues/11-agent-loop.md:74-76 — \"The legacy templates spell the second placeholder `{result}` ... so the entries format with `.format(call=call, result=exec_out)`\"",
    "detail": "The ticket says the five bodies are ported \"byte for byte\" from legacy/pipeline/inject/inject_format.py:28-46 and that every entry is formatted with `.format(call=call, result=exec_out)`. Legacy's p2_e2 body is `\"{call} = {result}\"`. An implementer copying the table row literally and applying the stated `.format(call=..., result=...)` raises `KeyError: 'exec_out'` on that one entry, and A3 (.scratch/from-zero/issues/11-agent-loop.md:471, want `'p2_e2': 'apis.spotify.login(username=x) = OK'`) fails. The other four rows in the same table use `{result}`.",
    "needsOwner": false,
    "verdict": {
     "refuted": false,
     "evidence": ".scratch/from-zero/issues/11-agent-loop.md:51-52 — \"The five keys are the axis values of 5.3 and port / `legacy/pipeline/inject/inject_format.py:28-46` **byte for byte in the body / text**\".\n.scratch/from-zero/issues/11-agent-loop.md:56 — \"| `note` | `p1` | `False` | `None` | `\\\"[SYSTEM NOTE: prefetched {call} = {result}]\\\\n\\\"` |\" (sibling row uses `{result}`).\n.scratch/from-zero/issues/11-agent-loop.md:60 — \"| `p2_e2` | `p2` | `True` | `SYSTEM_EXTRA` | `\\\"{call} = {exec_out}\\\"` |\".\n.scratch/from-zero/issues/11-agent-loop.md:74-76 — \"The legacy templates spell the second placeholder `{result}` and `render`'s / parameter is `exec_out`, so the entries format with / `.format(call=call, result=exec_out)`; keeping the template strings unedited is / what makes the ported `note` format byte-identical to today's.\"\nlegacy/pipeline/inject/inject_format.py:45 — \"    \\\"p2_e2\\\": dict(place=\\\"after\\\", body=\\\"{call} = {result}\\\", system=SYSTEM_EXTRA),\". Lines 41-45 of that file show `{result}` in every body; `{exec_out}` appears nowhere in the legacy file.\n.scratch/from-zero/issues/11-agent-loop.md:471 (acceptance A3) — \" 'p2_e2': 'apis.spotify.login(username=x) = OK',\" with A3 calling `f.render(c, r, True, None)` where `r = 'OK'`; a body literally spelling `{exec_out}` formatted with `.format(call=call, result=exec_out)` raises `KeyError: 'exec_out'`, so A3 fails.\nNot settled elsewhere: `grep -n 'exec_out\\|{result}\\|p2_e2'` over the whole ticket returns no later line correcting the row; the Comments section (.scratch/from-zero/issues/11-agent-loop.md:1090-1112) covers only the step-3 history rule; contract-errata.md's only 7.3 entry (line 49) is about the head seam; notes/plans/2026-09-17-contracts.md never spells the body templates (grep for \"{call} = \" returns nothing there).",
     "correctedStatement": "Ticket 11's `FORMATS` table row for `p2_e2` (line 60) spells the body `\"{call} = {exec_out}\"`, while the legacy source the ticket says to port byte for byte spells it `\"{call} = {result}\"` (legacy/pipeline/inject/inject_format.py:45) and the ticket's own rule at lines 74-76 states the templates keep `{result}` and are formatted with `.format(call=call, result=exec_out)`. The other four rows use `{result}`. An implementer copying the row literally gets `KeyError: 'exec_out'` and acceptance A3 (line 471) fails."
    }
   },
   {
    "id": "T11-3",
    "ticket": ".scratch/from-zero/issues/11-agent-loop.md",
    "severity": "implementer-will-trip",
    "title": "`n_checked` is left to 1.1's definition (\"cuts scored BEFORE this one\") while C4 asserts the legacy meaning (this cut included)",
    "ticketQuote": ".scratch/from-zero/issues/11-agent-loop.md:259-260 — \"8. Write the `spec` row with all of 1.1's columns: `fire_index` (0-based within the step), `cut`, `n_checked`, ...\"; and .scratch/from-zero/issues/11-agent-loop.md:738 — \"assert spec[\\\"cut\\\"] == 18 and spec[\\\"n_checked\\\"] == 1, spec\"",
    "conflictSource": "notes/plans/2026-09-17-contracts.md:938 — \"| spec | `n_checked` | i32 | how many cuts were scored before this one |\"",
    "detail": "In C4 exactly one cut is enumerated and scored, and it is the one that fires. Under 1.1's wording (\"before this one\") the value is 0; the acceptance demands 1. Legacy is the source of the 1: legacy/pipeline/inject/live_appworld.py increments `n_checked += 1` before the score call and writes `n_checked=n_checked` into the spec row (the fire_nth_cut branch at :477-480 even compares `n_checked == a.fire_nth_cut`), so the first cut records 1. The ticket never states which meaning it wants, so an implementer who follows the cited 1.1 row fails C4 with a one-off value and no explanation.",
    "needsOwner": false,
    "verdict": {
     "refuted": false,
     "evidence": ".scratch/from-zero/issues/11-agent-loop.md:260 — \"8. Write the `spec` row with all of 1.1's columns: `fire_index` (0-based within the step), `cut`, `n_checked`, `conf`, ...\" (step 8 gives no definition of its own and defers to contracts 1.1).\nnotes/plans/2026-09-17-contracts.md:938 — \"| spec | `n_checked` | i32 | how many cuts were scored before this one |\".\n.scratch/from-zero/issues/11-agent-loop.md:738 — \"assert spec[\\\"cut\\\"] == 18 and spec[\\\"n_checked\\\"] == 1, spec\".\nC4 scores exactly one cut, the one that fires: running the merged code, data/probe_input.cuts_live(\"I will look it up. \", 4) returns [18] (single candidate), and the fixture's FakeProbe.score returns conf 0.99 against cfg.inject.theta=0.5 with fire_nth_cut=0, so the first scored cut fires. Contracts' \"before this one\" therefore yields 0, while the acceptance demands 1.\nLegacy carries the including-this-cut meaning: legacy/pipeline/inject/live_appworld.py:476 \"n_checked += 1\" (before the /score call), :480 \"fired=(n_checked == a.fire_nth_cut)\", :516 \"n_checked=n_checked, conf=s[\\\"conf\\\"],\".\nNot settled elsewhere: grep -n \"n_checked\" over .scratch/from-zero/issues/11-agent-loop.md returns only lines 260 and 738; .scratch/from-zero/contract-errata.md (whole file) has no n_checked entry (its cut-related entries are line 37 on prediction-row cut ordering and line 99 on the max_cuts floor); ticket 11's Comments (lines 1090-1111) cover only the 2026-09-18 step-3 history ruling.",
     "correctedStatement": "Ticket 11 never states the meaning of the `spec` row's `n_checked`: step 8 (line 260) points the implementer at contracts 1.1, whose row (notes/plans/2026-09-17-contracts.md:938) defines it as \"how many cuts were scored before this one\", while acceptance C4 (line 738) asserts `n_checked == 1` in a run where exactly one cut is enumerated and scored and that cut is the one that fires — the legacy meaning, this cut included (legacy/pipeline/inject/live_appworld.py:476,516). An implementer following the cited contracts row writes 0 and fails C4."
    }
   },
   {
    "id": "T11-4",
    "ticket": ".scratch/from-zero/issues/11-agent-loop.md",
    "severity": "blocks-launch",
    "title": "The spec and resume row column lists omit `step`, which 1.1 declares on every row and which ticket 10's scorer needs to reach \"the same step's env.action\"",
    "ticketQuote": ".scratch/from-zero/issues/11-agent-loop.md:259-265 (the spec row: `fire_index`, `cut`, `n_checked`, `conf`, `pred_label`, `gen_call`, `exec_code`, `arg_modes`, `exec_out`, `exec_ok`, `error_kind`, `note`, `format`, `head_tok`, `head_chars`, `note_tok`, `discarded_chars`, `overflow_ids`, `spec_s`) and .scratch/from-zero/issues/11-agent-loop.md:271-272 (the resume row: `fire_index`, `overflow_tok`, `new_tok`, `match_len`, `identical`, `stop_reason`) — neither list carries `step`",
    "conflictSource": "notes/plans/2026-09-17-contracts.md:906 — \"| all | `step` | i32 | step index; null on `meta` |\"; and .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:184-191 — \"per `spec` row: ... computed against the same step's `env.action` through `env.split_args` and `env.build_call`\"",
    "detail": "`data/trajectory_record.Writer.row` stamps only `type`, `ts` and (on meta) `version`/`record_id`; `step` is the caller's column (data/trajectory_record.py:121-138). The ticket's gen and env rows pass `step=step_index` (lines 372 and 375) but the spec and resume lists do not, and an implementer writing exactly the enumerated columns produces spec rows with a null `step`. Ticket 10, dispatched in the same wave, joins each spec row to the env row of the same step to compute `tool_agree`, `call_agree` and `recalled`; with `step` null on spec rows that join has no key. Legacy writes `step=step` on both rows (legacy/pipeline/inject/live_appworld.py:512 area and :396-408 `log_resume`). The defect is silent under C4 and C5, which never assert `step`, so it would only surface after a GPU inject run.",
    "needsOwner": false,
    "verdict": {
     "refuted": false,
     "evidence": ".scratch/from-zero/issues/11-agent-loop.md:258-265 — \"8. Write the `spec` row with all of 1.1's columns: `fire_index` (0-based within the step), `cut`, `n_checked`, `conf`, `pred_label`, `gen_call`, `exec_code`, `arg_modes`, `exec_out`, `exec_ok`, `error_kind`, `note`, `format` (`cfg.inject.format`), `head_tok` (`k`), `head_chars` (`len(head_txt) - ts`), `note_tok` (`len(note_ids)`), `discarded_chars` (`max(0, len(raw) - len(head_txt))`), `overflow_ids` (null unless `store_token_ids`), `spec_s`.\" — those 19 names are exactly contracts 1.1's `spec` block (notes/plans/2026-09-17-contracts.md:936-957, \"| spec | `fire_index` |\" through \"| spec | `spec_s` |\"), so the qualifier \"all of 1.1's columns\" covers the spec block and not the `all` row.\n.scratch/from-zero/issues/11-agent-loop.md:270-272 — \"10. The `resume` row is written when the next fire arrives or when the step ends with a fire outstanding (`:396-408`, `:527-528`, `:548-550`): `fire_index`, `overflow_tok`, `new_tok`, `match_len`, `identical`, `stop_reason`.\" — no `step`, and no \"all of 1.1's columns\" qualifier either.\nnotes/plans/2026-09-17-contracts.md:906 — \"| all | `step` | i32 | step index; null on `meta` |\".\ndata/trajectory_record.py:121-134 — `def row(self, kind: str, **fields: Any) -> None:` / `\"\"\"Write one JSON row, stamping type/ts (and, on a meta row, version/record_id).\"\"\"` / `row: dict[str, Any] = {\"type\": kind, \"ts\": time.clock_gettime(time.CLOCK_REALTIME)}` — only type/ts (plus meta's version/record_id) are stamped; `step` is a declared caller-supplied column (data/trajectory_record.py:18 `\"step\": pl.Int32,`).\n.scratch/from-zero/issues/11-agent-loop.md:372,375 — `writer.row(\"gen\", step=step_index, **asdict(res))` and `writer.row(\"env\", step=step_index, action=obs.action,` — the ticket passes `step` explicitly on the only two `row(...)` calls it spells out, and nowhere for spec/resume.\n.scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:184-191 — \"per `spec` row: `exec_ok`, `error_kind`, `conf`, `discarded_chars`, and the after-the-fact firing account — `tool_agree`, `call_agree`, `recalled` — computed against the same step's `env.action` through `env.split_args` and `env.build_call`\" — the join needs `spec.step`.\nNot settled elsewhere: .scratch/from-zero/contract-errata.md contains no entry about `step` on `spec`/`resume` rows (its only wave-4 ruling is entry 156, the history-strip rule), and ticket 11's Comments (.scratch/from-zero/issues/11-agent-loop.md:1090-1110) carry only that same history ruling. It is silent under the ticket's own acceptances: C4's assertions (.scratch/from-zero/issues/11-agent-loop.md:735-746) and C5 never read `spec[\"step\"]`, and `step` is outside the REQUIRED set (errata entry 139), so `read` does not reject a null.",
     "correctedStatement": "Ticket 11's `spec` row instruction (lines 258-265) and `resume` row instruction (lines 270-272) enumerate only contracts 1.1's `spec` and `resume` blocks and omit the `all`-kind column `step` (contracts:906). `Writer.row` stamps only `type`/`ts` (data/trajectory_record.py:121-134), so an implementer writing exactly the enumerated columns emits `spec` and `resume` rows with a null `step`, and ticket 10's per-spec-row `tool_agree`/`call_agree`/`recalled`, \"computed against the same step's `env.action`\" (10-eval-generators-scorer-and-table.md:184-191), loses its join key. The ticket does pass `step=step_index` on the gen and env rows (lines 372, 375), so the omission is specific to the two rows `agent/inject.py` writes."
    }
   },
   {
    "id": "T12-1",
    "ticket": ".scratch/from-zero/issues/12-launcher.md",
    "severity": "script-defect",
    "title": "B3 and B7 reuse shell variables set in B2 and B6; run as separate commands they write dirty.patch and meta.json into the repo root and still print the expected output",
    "ticketQuote": ".scratch/from-zero/issues/12-launcher.md:261 `g = launch.git_state(pathlib.Path('$T2'), True)` and :364 `rd = pathlib.Path('$T3')` — `T2` is created only in B2 (:238 `T2=$(mktemp -d)`) and `T3` only in B6 (:339 `T3=$(mktemp -d)`)",
    "conflictSource": ".scratch/from-zero/spec.md:199 \"Nothing large is written into the repo.\"; measured: under external/probe-env/bin/python, `pathlib.Path('')` is `PosixPath('.')`, so `Path('')/'dirty.patch'` resolves against the current directory",
    "detail": "B1-B8 are eight separate shell command blocks and the ticket says only \"Run from the repo root\"; a shell variable set in one block does not survive into the next (each Bash call starts a fresh shell). With `$T2` empty, B3's `git_state(Path(''), True)` writes `dirty.patch` into the repo root, and with `$T3` empty, B7's `(rd/'meta.json').write_text(...)` writes `meta.json` into the repo root and `refire(Path('.'), ...)` runs against it. Both blocks still print exactly the expected output (`refused: ...`, `refused: True`, `no ssh issued`), so the implementer gets no signal that two stray files now sit in the worktree that the workflow will commit or flag. Fix is to create the temp directory inside the same block that uses it.",
    "needsOwner": false,
    "verdict": {
     "correctedStatement": "Real script defect, with one correction to the cited rule. B2 sets `T2` and B6 sets `T3`, each inside its own fenced bash block; B3 (:261) and B7 (:364) are separate blocks that expand `$T2` / `$T3`. Nothing in the ticket tells the implementer to run the eight blocks in one shell — the acceptance preamble (:220) says only \"Run from the repo root and paste the real output\" — and each Bash tool call starts a fresh shell, so an unset variable expands to the empty string and `pathlib.Path('')` is `PosixPath('.')`. B3 then makes `git_state` write `dirty.patch` into the repo root (the ticket's own contract at :67-68: \"writes `<run_dir>/dirty.patch` (`git diff HEAD`) when the flag is given\"), and B7 writes `meta.json` into the repo root; both blocks still print exactly the expected output, so the implementer gets no signal. Neither name is in `.gitignore`. The rule actually broken is not spec.md:199 (\"Nothing large is written into the repo\" — these two files are small) but `.scratch/from-zero/prompts/implementer.md:55` (\"Only touch files within this ticket's scope\") together with the fixed-file-tree rule: two untracked files appear in the worktree that the implementer's \"commit by logical unit\" step (implementer.md:34) can sweep in. Fix as stated: create the temp directory inside the same block that uses it.",
     "evidence": ".scratch/from-zero/issues/12-launcher.md:220 \"Run from the repo root and paste the real output. `$PR` is …\" — no instruction to keep one shell across B1-B8.\n.scratch/from-zero/issues/12-launcher.md:238 `T2=$(mktemp -d)` (inside B2's fenced block) and :261 `g = launch.git_state(pathlib.Path('$T2'), True)` (inside B3's separate fenced block).\n.scratch/from-zero/issues/12-launcher.md:339 `T3=$(mktemp -d)` (B6) and :364 `rd = pathlib.Path('$T3')` (B7, which then does `(rd/'meta.json').write_text(json.dumps(...))`).\n.scratch/from-zero/issues/12-launcher.md:67-68 \"writes `<run_dir>/dirty.patch` (`git diff HEAD`) when the flag is given — which is why it takes the run directory\" (so B3's `allow_dirty=True` call does write a file).\nMeasured, writes nothing: `/home/y-guo/reproduce/new1/external/probe-env/bin/python -c \"import pathlib; p=pathlib.Path(''); print(repr(p), repr(p/'dirty.patch'), (p/'dirty.patch').resolve())\"` -> `PosixPath('.') PosixPath('dirty.patch') /home/y-guo/reproduce/new1/dirty.patch`; and `unset T2; echo \"[${T2}]\"` -> `[]`.\n/home/y-guo/reproduce/new1/.gitignore: no entry matching `dirty.patch` or `meta.json` (checked whole file).\n/home/y-guo/reproduce/new1/.scratch/from-zero/prompts/implementer.md:55 \"Only touch files within this ticket's scope, and only make changes inside your own worktree and branch\"; :34 \"Commit by logical unit\".\n.scratch/from-zero/issues/12-launcher.md:426-438 (Comments) covers only the `--attach-only` / `--attached-to` note; nothing there or in .scratch/from-zero/contract-errata.md settles the temp-directory scoping.",
     "refuted": false
    }
   },
   {
    "id": "T12-5",
    "ticket": ".scratch/from-zero/issues/12-launcher.md",
    "severity": "implementer-will-trip",
    "title": "`teardown_services` is specified as a bare `ssh <host> tmux kill-session` with no local short-circuit, while a `sample` run's render-only probe service sits on `login_host`",
    "ticketQuote": ".scratch/from-zero/issues/12-launcher.md:154-157 \"**`teardown_services(run_dir)` (2.3).** For each `meta.json` piece entry with `kind: service`, `ssh <host> tmux kill-session -t <session>`, **skipped for a piece whose owning `run_id` appears in the `attached_to` field of another live run's `service_<kind>_<replica>.json`**\"",
    "conflictSource": ".scratch/from-zero/contract-errata.md:143 \"a host that normalises to `login_host` through the `hosts:` `alias` column is probed with a local `bash -c \\\"tmux ls -F '#S'\\\"` and no `ssh` ... without it every probe on the machine `run.py` and the loop pieces run on pays an `ssh` to itself and reports **alive** fail-closed when `ssh` to self is unconfigured\"; jobs/registry.py:455-470 `_remote_shell` applies that short-circuit, and jobs/registry.py:979-982 `kill()` uses it",
    "detail": "The ticket carries the local short-circuit explicitly for `refire`'s session probe (:161-165) and for starting a piece (:128-129 \"A piece on another host is started as `ssh ...`\"), but states `teardown_services` as an unconditional `ssh`. By 3.4 (notes/plans/2026-09-17-contracts.md:2756-2758) a `--render-only` probe service piece is placed on `login_host`, so on every `sample` run the teardown's one local service session is the one it would try to reach over `ssh` to this machine. Under the same premise errata 143 records, that call fails and the session survives, which is exactly what M-J4 (\"both service tmux sessions gone\") and M-J5 (\"`teardown_services` has ended the service sessions\") assert. B6's fixture has no service piece, so no acceptance command covers it.",
    "needsOwner": false,
    "verdict": {
     "refuted": false,
     "evidence": "Ticket line, verbatim: .scratch/from-zero/issues/12-launcher.md:154-157 \"- **`teardown_services(run_dir)` (2.3).** For each `meta.json` piece entry with `kind: service`, `ssh <host> tmux kill-session -t <session>`, **skipped for a piece whose owning `run_id` appears in the `attached_to` field of another live run's `service_<kind>_<replica>.json`**. Returns the sessions it ended.\" — no local case, and no mention of host normalisation.\n\nThe same ticket carries the short-circuit twice for other operations, so nothing later in the ticket settles it for the teardown: :128-129 \"A piece on another host is started as `ssh -o BatchMode=yes <host> tmux new-session -d …`\"; :161-165 \"**A host that normalises to `login_host` is probed locally, with no `ssh`** (errata against 8.0/3.4; `legacy/ops/launch_common.py:41-67` has the same short-circuit for both the session test and the tmux launch)\". A ticket-wide grep for normalis/alias/local hits only :162, :180-181 (the host refusal), :200 (the legacy-source table row \"local-vs-ssh tmux launch and session test\") and the B7 text at :348-351 — never the teardown.\n\nPlacement premise confirmed: ticket :118-119 \"a `--render-only` probe piece takes no card and lands on `login_host`; loop pieces take no card and run on `login_host`\", and notes/plans/2026-09-17-contracts.md:2753-2760 \"a **probe-service** piece started with `--render-only` (the `sample` case, 2.3) **takes no card**, enters no card search and is placed on `login_host` beside the loop pieces.\" constants/path_outputs.yaml:12 \"login_host: shiga\".\n\nMerged code applies the short-circuit on every host-touching path: jobs/registry.py:455-470 `_remote_shell` (\"Run `script` on `host`: locally through `bash -c` when `host` normalises to this machine, over `ssh -o BatchMode=yes` otherwise (errata)\"), used at :496 and :509 (session probes), :560 (card probe) and inside `kill` (jobs/registry.py:948, call at :979-980 `ok, _out = _remote_shell(host, f\"tmux kill-session -t {shlex.quote(session)} 2>/dev/null; true\")`). `_remote_shell` is module-private and `registry.kill(run_id)` is a whole-run kill that raises on the `attached_to` test, so neither is offered to `jobs/launch.py` as a per-piece kill helper; an implementer following :154-157 literally writes a bare `ssh`.\n\nErrata rationale for the premise: .scratch/from-zero/contract-errata.md:143 \"without it every probe on the machine `run.py` and the loop pieces run on pays an `ssh` to itself and reports **alive** fail-closed when `ssh` to self is unconfigured\". That entry governs `registry.session_alive` / `live_sessions` only, so it does not settle the teardown.\n\nAcceptance coverage: B6 (.scratch/from-zero/issues/12-launcher.md:337-346) writes a `meta.json` whose only piece is `kind: loop`, so it returns `[]` without reaching any host; the only checks that would expose the local kill are M-J4 (:404-407 \"both service tmux sessions gone\") and M-J5 (:408-411 \"`teardown_services` has ended the service sessions\"), both in the \"GPU / main session — not yours\" block.\n\nNot one of the listed known facts (a)-(i).",
     "correctedStatement": "The ticket does not contradict the errata or the contracts here — notes/plans/2026-09-17-contracts.md:2026 words 2.3 the same unconditional way (\"For each piece entry with `kind: service`, `ssh <host> tmux kill-session -t <session>`\"). The accurate finding: ticket 12 spells the local-host short-circuit out for `refire`'s session probe (:161-165) and implies it for starting a piece (:128-129, \"A piece on another host is started as `ssh …`\"), but states `teardown_services` (:154-157) as a bare `ssh <host> tmux kill-session`, and offers the implementer no helper for it (`jobs/registry._remote_shell` at registry.py:455-470 is private; `registry.kill` is a whole-run kill). Since a `sample` run's `--render-only` probe service is placed on `login_host` (ticket :118-119, contracts:2755-2758), the one service session such a run tears down is the local one, which a literal `ssh` to this machine does not reach under the same \"ssh to self is unconfigured\" premise errata:143 records. No acceptance command in the ticket covers it (B6's fixture has no `service` piece); only the owner-side M-J4 and M-J5 assert the sessions are gone."
    }
   },
   {
    "id": "X-1",
    "ticket": "10",
    "severity": "blocks-launch",
    "title": "method_table subscripts settings_diff.yaml as a nested dict; the merged schema writes it flat with dotted keys",
    "ticketQuote": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:249-252 — \"2. Per row, resolve the cell **without opening any `settings.yaml`**: `backbone` is `diff[\"models\"][\"probe\"]` when present and the schema default for `models.probe` otherwise; `method` is `diff[\"probe\"][\"method\"]` when present and the schema default otherwise (`diff` is `settings_diff.yaml` as JSON, 8.1, so a default-valued field is absent from it).\"",
    "conflictSource": "experimental_settings/schema.py:1058-1073 — `def fields_of(stage, setting) -> dict:` / \"\"\"The 'fields' block of 3.3, = settings_diff.yaml: dotted field -> value, only fields differing from default.\"\"\" ... `out[dotted] = value` where `dotted` comes from `_stage_dotted_fields` (schema.py:1002-1019) and is a string such as `\"probe.method\"`; experimental_settings/schema.py:1264,1277 — `diff = fields_of(stage, setting)` ... `_atomic_write_yaml(diff_path, diff)`; jobs/registry.py:877 — `\"diff\": start.get(\"diff\")` (the row is passed through verbatim)",
    "detail": "settings_diff.yaml has no nesting: its keys are whole dotted field names. An implementer writing `diff[\"probe\"][\"method\"]` gets a KeyError on every row (the key present is `\"probe.method\"`), so `method_table.table()` raises as soon as the ledger holds one eval row — which is exactly the A9 case the ticket says must then print one line per (backbone, method, risk). The correct lookups are `diff.get(\"models.probe\")` and `diff.get(\"probe.method\")`.",
    "needsOwner": false,
    "verdict": {
     "refuted": false,
     "evidence": "TICKET (the instruction as written):\n.scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:248-252 —\n  248: \"2. Per row, resolve the cell **without opening any `settings.yaml`**: `backbone`\"\n  249: \"   is `diff[\\\"models\\\"][\\\"probe\\\"]` when present and the schema default for\"\n  250: \"   `models.probe` otherwise; `method` is `diff[\\\"probe\\\"][\\\"method\\\"]` when present\"\n  251: \"   and the schema default otherwise (`diff` is `settings_diff.yaml` as JSON, 8.1,\"\n  252: \"   so a default-valued field is absent from it).\"\n\nMERGED CODE (settings_diff.yaml is flat, keys are whole dotted names):\nexperimental_settings/schema.py:1058-1059 — `def fields_of(stage: str, setting: Setting) -> dict:` / `\"\"\"The 'fields' block of 3.3, = settings_diff.yaml: dotted field -> value, only fields differing from default.\"\"\"`\nexperimental_settings/schema.py:1064 — `for dotted in _stage_dotted_fields(stage):`\nexperimental_settings/schema.py:1072 — `out[dotted] = value`  (no nesting; the key is the string)\nexperimental_settings/schema.py:1002-1019 — `_stage_dotted_fields` appends the entry verbatim when it contains a dot (`names.append(entry)`), else `f\"{entry}.{f.name}\"` — so every key is a dotted string.\nexperimental_settings/schema.py:1264,1277 — `diff = fields_of(stage, setting)` ... `_atomic_write_yaml(diff_path, diff)` (diff_path = run_dir/\"settings_diff.yaml\", :1267).\njobs/registry.py:877 — `\"diff\": start.get(\"diff\"),` — the folded row carries the start row's diff verbatim; jobs/registry.py:419 confirms the flat shape in use: `if all((r.get(\"diff\") or {}).get(k) == v for k, v in fields.items())`.\nnotes/plans/2026-09-17-contracts.md:2476 — `\"fields\":   {dotted field name: value, for every field the stage reads`; :4449 — \"| `diff` | dict | `settings_diff.yaml` as JSON: what the key was computed over ... |\".\n\nNOT ALREADY SETTLED:\n.scratch/from-zero/contract-errata.md has no entry on the diff's shape (its two 8.6 entries, lines 43-44, cover only the ls-based reader and the one-row-per-(backbone, method, risk) rule; line 135 covers ssh).\n.scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:615 — \"## Comments\" is the last line of the file; the section is empty.\nThe ticket mentions `diff` nowhere else (grep -n 'diff' on the ticket returns only lines 120, 170, 247, 249, 250, 251; 120 and 170 are the word \"differs\").\n\nSECOND, INDEPENDENT DEFECT ON THE SAME LINE (backbone):\nexperimental_settings/schema.py:248-249 — `\"eval\": {` / `\"sections\": (\"probe.method\", \"eval\"),` — an eval-stage diff can only ever hold the keys `probe.method` and `eval.*`. `models.probe` is not among them under any spelling, so a lookup of the backbone in an eval row's diff never finds a value and every row would take the schema default.",
     "correctedStatement": "The nesting claim is confirmed: `settings_diff.yaml` (and therefore `row[\"diff\"]`) is a flat map whose keys are whole dotted field names, so `diff[\"probe\"][\"method\"]` raises KeyError on the first eval row and `method_table.table()` fails on exactly the case A9 exercises. The correct method lookup is `diff.get(\"probe.method\")`.\n\nThe finding's proposed backbone fix is incomplete. `diff.get(\"models.probe\")` is the right spelling but can never return a value: `STAGES[\"eval\"][\"sections\"]` is `(\"probe.method\", \"eval\")` (schema.py:248-249) and `fields_of` iterates only those (schema.py:1064), so an eval run's diff never carries `models.probe` and the backbone column would be the schema default on every row. The backbone of an eval row has to come from somewhere other than that row's own diff — the owner has to say where (the upstream train run's diff via `row[\"parent\"]`/upstream, or by opening a file, which line 248 forbids)."
    }
   },
   {
    "id": "X-2",
    "ticket": "10",
    "severity": "blocks-launch",
    "title": "an eval run's settings_diff.yaml never carries models.probe, so method_table's backbone column can only ever print the schema default",
    "ticketQuote": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:249-251 — \"Per row, resolve the cell **without opening any `settings.yaml`**: `backbone` is `diff[\"models\"][\"probe\"]` when present and the schema default for `models.probe` otherwise\"; :259-260 — \"**One table row per `(backbone, method, risk target)`**\"",
    "conflictSource": "experimental_settings/schema.py:248-249 — `\"eval\": {\"sections\": (\"probe.method\", \"eval\"),`; experimental_settings/schema.py:1002-1019 — `_stage_dotted_fields` expands only `STAGES[stage][\"sections\"]`; experimental_settings/schema.py:44 — `probe: str | None = \"qwen3_0pt6b\"`; jobs/registry.py:867-888 — `_ls_row` returns run_id, t, stage, key, dir, workflow, setting, parent, swept, diff, commit, status, pieces, progress, flags (no `upstream`)",
    "detail": "`fields_of(\"eval\", ...)` iterates the eval stage's own fields only — `probe.method` and the six `eval.*` fields — so `models.probe` is absent from every eval row's diff by construction, not because it held its default. With the rule as written the backbone column is the constant `qwen3_0pt6b` for every row, and the matrix's backbone x method axis collapses. The eval row also gives method_table no path to the train run that does carry `models.probe`: `registry.ls`'s folded row (jobs/registry.py:867-888) carries no `upstream` field, and the ticket forbids opening any `settings.yaml`. Whoever dispatches the ticket has to say where the backbone comes from (a new `upstream` field on the ls row, a lookup of the train row, or lifting the no-settings.yaml rule) before an implementer can write step 2.",
    "needsOwner": true,
    "verdict": {
     "refuted": false,
     "evidence": "Ticket rule: .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:248-251 \"Per row, resolve the cell **without opening any `settings.yaml`**: `backbone` is `diff[\\\"models\\\"][\\\"probe\\\"]` when present and the schema default for `models.probe` otherwise; `method` is `diff[\\\"probe\\\"][\\\"method\\\"]` when present and the schema default otherwise (`diff` is `settings_diff.yaml` as JSON, 8.1, so a default-valued field is absent from it)\"; :258-260 \"**One table row per `(backbone, method, risk target)`**\".\n\nMerged code:\n- experimental_settings/schema.py:248-251 — the eval stage entry: `\"eval\": {` / `\"sections\": (\"probe.method\", \"eval\"),` / `\"models\": (),` (train at :234-235 is `\"sections\": (\"models.probe\", \"probe\", \"train\")`).\n- experimental_settings/schema.py:1058-1072 — `def fields_of(stage, setting)`, docstring \"The 'fields' block of 3.3, = settings_diff.yaml\", loops `for dotted in _stage_dotted_fields(stage):`.\n- experimental_settings/schema.py:1002-1019 — `_stage_dotted_fields` iterates `for entry in STAGES[stage][\"sections\"]` only; `STAGES[stage][\"models\"]` never enters the diff.\n- experimental_settings/schema.py:1265-1277 — `freeze` writes `diff = fields_of(stage, setting)` into `settings_diff.yaml`. So an eval run's diff holds only `probe.method` and the `eval.*` fields; `models.probe` is absent by construction, not because it held its default.\n- experimental_settings/schema.py:44 — `probe: str | None = \"qwen3_0pt6b\"`, so the ticket's fallback makes the backbone column that one constant for every row, and contracts :561 calls the file \"the backbone x method table from the registry\".\n\nNo other path on the row:\n- jobs/registry.py:867-888 — `_ls_row` returns run_id, t, stage, key, dir, workflow, setting, parent, swept, diff, commit, status, pieces, progress, flags; no `upstream`. `ls` (jobs/registry.py:924) adds nothing to those rows. notes/plans/2026-09-17-contracts.md:4446 shows the start row does carry `upstream` (\"upstream or reference name -> key\"), but the folded row drops it, and .scratch/from-zero/contract-errata.md:43 fixes the folded row's fields as \"`stage`, `key`, `dir`, `diff`, `parent` and `swept`\". The ticket's own step 2 forbids opening `settings.yaml`.\n\nNot already settled and not a known fact: ticket 10's Comments section (:615) is empty; errata :43-44 cover the `registry.ls` call and the risk-tier/spread rule only, never where `backbone` comes from. Acceptance A9 (:578-584) runs against an empty ledger, so the rule is never exercised by the implementer's own check; the matrix check that would expose it, M-E2 (:606-610), is main-session work.",
     "correctedStatement": "Confirmed as stated, with one addition. Ticket 10 step 2 (:248-251) sources `backbone` from `diff[\"models\"][\"probe\"]`, which an eval run's `settings_diff.yaml` can never contain (schema.py:249 sections `(\"probe.method\", \"eval\")`; schema.py:1002-1019 and 1058-1072 expand only those sections; schema.py:1265-1277 writes that dict as the diff). Every table row then prints the schema default `qwen3_0pt6b` (schema.py:44) and the backbone axis collapses, while the folded ls row offers no `upstream` to reach the train run that does carry `models.probe` (jobs/registry.py:867-888; errata :43 fixes that field list). Addition: the same two ticket lines also use the wrong shape for `method` — `settings_diff.yaml` is a flat dotted-key map (`out[dotted] = value`, schema.py:1071; read flat at jobs/registry.py:419 `(r.get(\"diff\") or {}).get(k) == v`), so `diff[\"probe\"][\"method\"]` must be `diff[\"probe.method\"]`."
    }
   },
   {
    "id": "X-4",
    "ticket": "11",
    "severity": "implementer-will-trip",
    "title": "ticket 11's spec/resume column list omits `step`, which ticket 10's score_run needs to pair a spec row with its env row",
    "ticketQuote": ".scratch/from-zero/issues/11-agent-loop.md:259-265 — \"8. Write the `spec` row with all of 1.1's columns: `fire_index` (0-based within the step), `cut`, `n_checked`, `conf`, `pred_label`, `gen_call`, `exec_code`, `arg_modes`, `exec_out`, `exec_ok`, `error_kind`, `note`, `format` (`cfg.inject.format`), `head_tok` (`k`), `head_chars` ... `overflow_ids` ... `spec_s`.\"; :270-272 — the `resume` row list \"`fire_index`, `overflow_tok`, `new_tok`, `match_len`, `identical`, `stop_reason`\"",
    "conflictSource": ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:186-188 — \"the after-the-fact firing account — `tool_agree`, `call_agree`, `recalled` — computed against the same step's `env.action` through `env.split_args` and `env.build_call`\"; notes/plans/2026-09-17-contracts.md:906 — \"| all | `step` | i32 | step index; null on `meta` |\"",
    "detail": "`step` is an `all`-rows column of 1.1, so a `spec` row carries it, but ticket 11's enumeration is explicit and leaves it out, and neither C4 nor C5 asserts it (C4 checks fire_index, format, exec_ok, gen_call, cut, n_checked, note, head_tok, head_chars, note_tok, discarded_chars, overflow_ids). A spec row written without `step` gives `eval/score_run.py` no way to find \"the same step's `env.action`\", and score_run's `spec.tool_agree` / `call_agree` / `recalled` block — ticket 10 step 8 and the pinned `run_report.json` field list (ticket 10:204-205) — cannot be computed at all. `agent/inject.step` already receives `step_index`, so the fix is naming `step=step_index` in ticket 11's spec and resume column lists.",
    "needsOwner": false,
    "verdict": {
     "refuted": false,
     "evidence": ".scratch/from-zero/issues/11-agent-loop.md:259-265 — \"8. Write the `spec` row with all of 1.1's columns: `fire_index` (0-based within the step), `cut`, `n_checked`, `conf`, `pred_label`, `gen_call`, `exec_code`, `arg_modes`, `exec_out`, `exec_ok`, `error_kind`, `note`, `format` …, `overflow_ids` …, `spec_s`.\" (the enumeration is exactly 1.1's nineteen spec-kind columns, no `step`); :270-272 — the `resume` list \"`fire_index`, `overflow_tok`, `new_tok`, `match_len`, `identical`, `stop_reason`\", no `step`. | notes/plans/2026-09-17-contracts.md:906 — \"| all | `step` | i32 | step index; null on `meta` |\". | data/trajectory_record.py:121-137 — `def row(self, kind, **fields)` stamps only `type`/`ts` (plus `version`/`record_id` on `meta`) and copies `**fields` through; `step` is a declared SCHEMA column (:18) the caller must pass. Ticket 11's loop section passes it for the rows the loop owns: :372 `writer.row(\"gen\", step=step_index, **asdict(res))`, :375 `writer.row(\"env\", step=step_index, …)`. | legacy/pipeline/inject/live_appworld.py:515 `log.w(dict(type=\"spec\", step=step, cut=cut,` and :405 `log.w(dict(type=\"resume\", step=step, overflow_tok=len(ov),` — the very lines steps 8 and 10 port. | Consumer: .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:185-187 — \"the after-the-fact firing account — `tool_agree`, `call_agree`, `recalled` — computed against the same step's `env.action` through `env.split_args` and `env.build_call`\"; the pinned field list at :204-205 carries `spec: {n, exec_ok, tool_agree, call_agree, recalled, …}`. | Not settled elsewhere: .scratch/from-zero/contract-errata.md has no entry on the spec/resume `step` column (:139 only drops `step` from `REQUIRED`, so no validation catches the omission; :156 is the history ruling); ticket 11's Comments :1090-1113 cover only the history rule; no line of ticket 11 sections 3 or 4 names `step` for a `spec` or `resume` row. | Not caught by acceptance: ticket 11 C4 :729-746 asserts fire_index, format, exec_ok, gen_call, cut, n_checked, note, head_tok, head_chars, note_tok, discarded_chars, overflow_ids — never `step`; ticket 10's A8 fixture writes no spec rows at all (:539 `assert rep[\"spec\"][\"n\"] == 0`).",
     "correctedStatement": "Ticket 11's step-8 and step-10 enumerations (:259-265, :270-272) list only the kind-specific columns and omit the all-rows column `step` that contracts 1.1 (:906) puts on every row but `meta`. data/trajectory_record.py's Writer.row stamps only type/ts (and version/record_id on meta) and does not require a declared column, so an implementer copying the enumeration writes spec and resume rows with `step` null — contradicting 1.1 and leaving ticket 10's per-spec `tool_agree` / `call_agree` / `recalled` block (ticket 10:185-187) without the key it names for reaching \"the same step's `env.action`\". Neither the errata, ticket 11's Comments, nor C4/C5 supplies or checks it, and ticket 10's A8 fixture writes no spec rows, so nothing surfaces the gap. The fix is naming `step=step_index` in both lists, as legacy live_appworld.py:515 and :405 already do."
    }
   }
  ],
  "refuted": [
   {
    "id": "T10-8",
    "title": "A8 runs one seed while step 8 requires a sample standard deviation across seeds, with no rule for n = 1",
    "evidence": "Quotes are accurate but the conflict does not hold.\n\n1. The ticket pins the library for this file, and that library answers the n=1 case with null, not an error. notes/plans/2026-09-17-contracts.md:550 (the file card for `eval/score_run.py`): \"imports: experimental_settings/schema.py, data/trajectory_record.py, data/environments/__init__.py ... jobs/registry.py; [polars]\". Measured with the ticket's own interpreter (/home/y-guo/reproduce/new1/external/probe-env/bin/python, writes nothing): `pl.Series([1.0]).std()` -> `None`, and `pl.DataFrame({'a':[1.0]}).select(pl.col('a').std()).to_dicts()` -> `[{'a': None}]`. `None` serialises as JSON `null`, which is exactly what .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:208 already prescribes for a spread whose divisor (n-1) is 0: \"every rate rounded to 4, `null` when the denominator is 0.\" `statistics.stdev` is the only route that raises (confirmed: StatisticsError \"stdev requires at least two data points\"), and it is not the route the ticket's import list names.\n\n2. A8 never inspects `spread`, so nothing in the script depends on the choice. .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:544: `assert rep[\"spec\"][\"n\"] == 0 and rep[\"by_seed\"][\"42\"][\"n_records\"] == 2` — the only by_seed assertion, on the per-seed block, not on `mean` or `spread`. The surrounding asserts (:541-:547) touch `scored_stage`, `n_pairs`, `run`, `baseline`, `paired`, `done.json`, `report.md`, `consumed.json` and none of them read `spread`. The command therefore passes as written.\n\n3. The rule the finding says is missing is stated in the same ticket file, 250 lines above the acceptance block, for the sibling computation: :266-267 \"Each cell shows `mean ± spread` over the group's runs, spread being the sample standard deviation (ddof 1), and the **bare value when the group has one run**\", itself carried from .scratch/from-zero/contract-errata.md:44 (\"the cell is `mean ± sample standard deviation` over the group's runs, the bare value for a group of one\"). An implementer reading the whole ticket has both that precedent and :208's null rule.\n\nChecked and found nothing contrary: the ticket's Comments section (.scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:615) is empty; contract-errata.md's only stdev entry is :44; the contracts mention spread only at :562, :3214 (\"`by_seed` | bool | `true` | yes | report mean and spread across seeds\"), :3399, :4703 and :5283, none of which impose `statistics.stdev`; `eval/score_run.py` is not on disk yet (eval/ holds only methods/ctool.py and utils/probe_eval.py), so no merged code contradicts this either."
   },
   {
    "id": "T11-5",
    "title": "B2 ends with a bare `grep -c` expecting 0 — exactly the shape A5 wraps in `test` because it exits 1",
    "evidence": "B2's grep is at .scratch/from-zero/issues/11-agent-loop.md:515 (the finding cites 516) and its criterion at :517 reads exactly \"Expected: `clients ok`, then `0`.\" — output only. It does not claim exit 0, unlike the neighbouring blocks that do depend on the block's status: A5 at :496 \"Expected: `NO_CONTROL_TOKEN`, exit 0.\" and B3 at :523 \"Expected: `sig ok`, exit 0.\" The grep -c behaviour is not left for the implementer to discover — the same ticket states it twice, before and after B2: :496-498 \"Expected: `NO_CONTROL_TOKEN`, exit 0. The `test` wrapper is deliberate: `grep -c` **exits 1 when the count is zero**, so a bare `grep -c` expecting `0` makes a correct implementation look like a failed command.\" and D1 at :862-864 \"grep -n '^ *import models\\|...' agent/loop.py | grep -v ...\" / \"Expected: a version number, then **no output** from the grep (exit 1 from grep is the pass).\" So the ticket's own pass criterion for B2 is the two printed lines, and the reader has already been told that a zero count prints `0` and exits 1 on a correct implementation. Measured behaviour is not in dispute: `grep -c 'ZZZNOTHINGZZZ' /home/y-guo/reproduce/new1/CLAUDE.md` printed `0` with exit=1 under ugrep 7.8.4. The B2 block (:510-516) has no `set -e`, no `|| exit 1` and no other status-dependent wrapper, so nothing in the script itself turns the grep's exit 1 into a failed check on the ticket's stated terms. What remains is that B2 is worded less defensively than A5 — a consistency remark an implementer resolves correctly from :496-498, not something that makes them write wrong code, get stuck, or break a hard rule."
   },
   {
    "id": "T11-6",
    "title": "`env.complete_call(call)` is passed straight to `env.speculate` although the contract says it returns None when nothing balances",
    "evidence": "Ticket line, as quoted: .scratch/from-zero/issues/11-agent-loop.md:250-251 — \"`call = clients.probe.generate(..., cfg.inject.max_new)[\\\"call\\\"]`, `call = env.complete_call(call)`, / `spec = env.speculate(call)`\".\n\n1) The ticket copies the contracts, so it contradicts nothing. notes/plans/2026-09-17-contracts.md:4206 — \"the call through `Environment.complete_call`, `speculate`s it, builds the\" — the contracts' own step 4 prescribes exactly this sequence with no None branch, and the errata entry (.scratch/from-zero/contract-errata.md:14) only fixes what `complete_call` returns, not what the loop does with it. A ticket that mirrors the contracts is not a ticket/contract conflict.\n\n2) The claimed failure mechanism is false against the merged code. data/environments/appworld.py:219-223 — `def _requote(call: str, user_ns: dict) -> tuple[str, list[str]]:` / `found = _first_call_named(call or \"\")` / `if found is None:` / `return f\"print({call})\", [\"unparsable_raw\"]`. The `call or \"\"` guard means a None call never reaches a regex or a slice; `_requote(None, {})` returns `('print(None)', ['unparsable_raw'])` (run under /home/y-guo/reproduce/new1/external/appworld/venv/bin/python, nothing written). speculate (data/environments/appworld.py:339-346) raises only on a missing world or a missing frozen clock, then calls `_requote`, so no exception is raised, the per-task guard of step 8 is never reached, and no trajectory is aborted with `task_error:...`.\n\n3) Legacy behaved the same or worse: legacy/pipeline/inject/live_appworld.py:502 — `spec = speculate(world, g[\"call\"], t_frozen, dt_guard)` — passes the raw `/gen` output with no completion step at all, and legacy `requote` carries the same `text or \"\"` tolerance."
   },
   {
    "id": "T11-7",
    "title": "`meta.inject` on a sample run is left unspecified while 1.1 says the column is null there",
    "evidence": ".scratch/from-zero/issues/11-agent-loop.md:350 — \"- `writer.row(\\\"meta\\\", …)` with every column of 1.1's `meta` block:\" — the ticket binds the column list (and each column's stated value) to contracts 1.1, then at :357 only fixes the serialization spelling: \"`generation` and `inject` as **canonical JSON text** — `json.dumps(obj, sort_keys=True, ensure_ascii=False, separators=(\\\",\\\", \\\":\\\"))` (errata)\".\n\n.scratch/from-zero/contract-errata.md:61 — \"1.1: \\\"canonical JSON text\\\" is unspelled for `meta.generation`, `meta.inject` and `final.judge` while a string equality compares two of them -> the build writes `json.dumps(obj, sort_keys=True, ensure_ascii=False, separators=(\\\",\\\", \\\":\\\"))`\" — the errata entry the ticket cites decides only how an object is spelled, it does not touch the no-object case.\n\nnotes/plans/2026-09-17-contracts.md:921 — \"| meta | `inject` | str | the resolved `inject` section as canonical JSON text, or null on a sample run |\" — the very table the ticket sends the implementer to states the sample-run value, so the ticket and the contract do not disagree; the answer is in the cited source.\n\nConsequence check on the merged code: data/trajectory_record.py:33 types the column `\\\"inject\\\": pl.Utf8` and :86-89 REQUIRED = {\\\"type\\\", \\\"ts\\\", \\\"version\\\", \\\"record_id\\\", \\\"stage\\\", \\\"env\\\", \\\"task_id\\\", \\\"seed\\\", \\\"split\\\", \\\"task_text\\\", \\\"agent_model\\\", \\\"owner_session\\\"} — `inject` is nullable and not required, and `grep -rn \\\"meta.inject\\\\|\\\\\\\"inject\\\\\\\"\\\\]\\\" data/ eval/ jobs/ agent/` returns nothing, so no merged reader compares the column either way. Nothing in ticket 11's acceptance (D4, .scratch/from-zero/issues/11-agent-loop.md:1000-1013) turns on it."
   },
   {
    "id": "T11-8",
    "title": "D5 catches only `SystemExit` while step 4 states no exception type for the /health refusal",
    "evidence": "Ticket step 4 (.scratch/from-zero/issues/11-agent-loop.md:332-334) states the refusal's content without a type: \"4. The `/health` refusal (7.2): refuse unless the probe service reports `render == \\\"ids\\\"`, `family == cfg.models.agent_row[\\\"family\\\"]` and `weights == cfg.models.agent_row[\\\"weights\\\"]`, naming the field and both values.\" The type is pinned later in the same ticket, in that ticket's own acceptance check D5 (.scratch/from-zero/issues/11-agent-loop.md:1005 \"**D5 — the `/health` refusal, in its three forms.**\", :1016-1018 \"    try:\\n        loopfix.loop.main(rd, (0, 1))\\n        print(field, \\\"NO REFUSAL\\\")\\n    except SystemExit as e:\") with the required output at :1027-1030 (\"Expected: three lines `render refused: True True True` ... then `health ok`, exit 0\"). The implementer prompt makes that binding: .scratch/from-zero/prompts/implementer.md:15 \"Read the ticket, list out every one of its acceptance requirements.\" and :68 \"`DONE`: every requirement met, every acceptance command passing.\" — an implementer who raised ValueError would see D5 abort and would have to switch to SystemExit, so the ticket resolves it on its own. The quoted contracts passage is silent, not contradictory: notes/plans/2026-09-17-contracts.md:4093-4095 \"Before its first request, `agent/loop.py` refuses to run unless `/health` reports `render == \\\"ids\\\"`, **a `family` equal to `cfg.models.agent_row[\\\"family\\\"]` and a `weights` equal to `cfg.models.agent_row[\\\"weights\\\"]`**\"; `grep -n SystemExit notes/plans/2026-09-17-contracts.md .scratch/from-zero/contract-errata.md` returns nothing, so no source spells a different type. The merged tree's refusals use the same spelling D5 assumes (models/agent_models/service.py:187 \"raise SystemExit(\", models/probe_models/service.py:85 \"raise SystemExit(\")."
   },
   {
    "id": "T12-2",
    "title": "B5's free-card fixture keys a host by its alias `shiga`, which `registry.free()` never returns, and the expected values for the loop and probe pieces are not literals",
    "evidence": "1) The normalization rule the finding leans on is scoped to one comparison, not to `place`. `.scratch/from-zero/issues/12-launcher.md:179-183`: \"- **The host refusal (8.6).** `jobs/launch.py` refuses to run on any host but `login_host`, naming it. **Both sides are normalized through the `hosts:` entries' `alias` column**\". The errata entry it cites is scoped the same way, `.scratch/from-zero/contract-errata.md:73`: \"8.6: running anywhere but `login_host` is refused with no comparison rule ... -> both sides are normalized through the `hosts:` entries' `alias` column\". Neither the ticket's placement bullet (`12-launcher.md:111-120`) nor contracts 3.4 (`notes/plans/2026-09-17-contracts.md:2739-2766`) asks `place` to normalize anything; `place` is a pure function over the `free_by_host` dict it is handed, and B5's other lines require the same pass-through (`12-launcher.md:325` hands `serving_host='tokyo107'` and `:331` expects `tokyo107` back verbatim). A bare lookup is the reading the whole fixture pins, and it prints `shiga` on line 4, so B5 passes for the implementation the ticket describes.\n\n2) The production case the finding infers (an alias reaching `place` as `prefer_host`) does not arise: `prefer_host` is \"the host this run's agent service is on\" (`contracts:2755-2757`), the agent service is placed on its table row's `serving.host` (`contracts:2739-2740`), and the only such value on disk is a `hosts:` name, `models/table.yaml:18` \"    host: tokyo108\" — which is a key `registry.free()` returns (`jobs/registry.py:608-617`).\n\n3) B5 lines 1-2 are not ambiguous: `constants/path_outputs.yaml:12` \"login_host: shiga\" is the single configured value, and the same ticket's next fixture spells a loop piece's host that way, `.scratch/from-zero/issues/12-launcher.md:343`: \"{'pieces':[{'index':0,'kind':'loop','host':'shiga','session':'s-0'}]}\". The `_alive_on`/`failed_hosts` consequence is a property of already-merged `jobs/registry.py:515-520` under a piece host the contract itself fixes as `login_host` (`contracts:2765`), not a choice ticket 12 leaves to the wave-4 implementer."
   },
   {
    "id": "T12-3",
    "title": "The pinned piece command exports CUDA_VISIBLE_DEVICES on every piece, while 3.4 gives the two service pieces their cards as flags; `--device cuda:<id>` then names an invisible device",
    "evidence": "1. The ticket's template is the contracts' own template, verbatim — not a conflict with it. notes/plans/2026-09-17-contracts.md:2700 (section 3.4) \"cd <repo root> && CUDA_VISIBLE_DEVICES=<ids> <venv python> -m <module> \\ --run-dir <dir> --piece <i>/<n>\" is the same string as .scratch/from-zero/issues/12-launcher.md:122-124. The sentence at contracts:2715-2718 (\"Cards reach a loop or train piece as an environment variable, not a flag... Only the two services take cards as flags\") sits five lines below that template in the same section, so whatever ambiguity exists is the contracts' own, and the ticket reproduces 3.4 exactly.\n\n2. The load-bearing step of the failure scenario is false. The finding asserts \"per 7.2's command line, `--device cuda:3`\". 7.2's command line is contracts:3958 \"    [--device cuda:0] [--render-only]\" — the literal `cuda:0`, not `cuda:<id>`. The ticket binds the implementer to that literal at .scratch/from-zero/issues/12-launcher.md:138 \"**The two service command lines are 7.1's and 7.2's, verbatim.**\" So a probe service piece placed on card 3 is started with CUDA_VISIBLE_DEVICES=3 and `--device cuda:0`, and `cuda:0` is precisely the reserved card inside that process. models/probe_models/service.py:79,97-98 (`device = args.device`; `base.load(..., device=device)`) then loads on the reserved card. No raise.\n\n3. The agent service is unaffected for the same reason and one more: models/agent_models/service.py:64 `env[\"CUDA_VISIBLE_DEVICES\"] = str(gpus)` — the service sets the variable itself, from its own `--gpus` flag (declared at :223, passed at :197), for the vLLM subprocess. The value is read per process from the child's env, so an outer export of the same ids in the tmux command is redundant, not contradictory.\n\n4. Nothing else in the ticket contradicts this: piece_command's `gpus` parameter is exercised in B5 with an empty string for a loop piece (issues/12-launcher.md:328 `launch.piece_command('/p/py','agent.loop','/o/sample/k','0','6','', ...)`), matching contracts:2720 \"A loop piece gets no card of its own\". The errata contains no entry on CUDA_VISIBLE_DEVICES or on the service `--device` value (grep over .scratch/from-zero/contract-errata.md returns only line 22, which is about `base.load`'s default device, and it confirms the serve path takes `--device`)."
   },
   {
    "id": "T12-4",
    "title": "`schema.run_dir_of` takes a keyword-only `debug` with no default and the ticket does not say what to pass when resolving `--score-ckpt` and `--gen-ckpt`",
    "evidence": "Contracts 3.1 states the value outright, two lines past the sentence the finding treats as the only guidance:\n- notes/plans/2026-09-17-contracts.md:2419 \"`run_dir_of`'s caller passes the `_debug` flag of its own frozen `settings.yaml`.\"\n- notes/plans/2026-09-17-contracts.md:2425-2428 \"A **resolved reference** is a different case and passes `debug=False`: a reference is always keyed and located without the overlay (3.4), so a debug run points at real upstream directories.\"\n`inject.probe_score` / `inject.probe_gen` are resolved references (their _upstream entries are `probe_score.train` / `probe_gen.train`), so exactly one value applies.\n\nMerged code already shows both halves of the same rule, so the pattern is available to the implementer on disk:\n- eval/utils/probe_eval.py:213 `train_dir = schema.run_dir_of(\"train\", cfg._upstream[\"train\"], debug=cfg._debug)` (own workflow stage)\n- eval/utils/probe_eval.py:232 `ref_eval_dir = schema.run_dir_of(\"eval\", ref_key, debug=False)` and :244 `ref_train_dir = schema.run_dir_of(\"train\", ref_train_key, debug=False)` (resolved references)\n- data/build_training_dataset.py:33 own-stage form with `debug=cfg._debug`\n- .scratch/from-zero/contract-errata.md:36 \"both located with `run_dir_of(..., debug=False)`\"; sibling tickets spell it the same: .scratch/from-zero/issues/08-eval-library-and-classifier-metric.md:70,76 and .scratch/from-zero/issues/10-eval-generators-scorer-and-table.md:164.\n\nThe errata line the finding uses as the competing answer does not instruct jobs/launch.py at all and restates the same rule:\n- .scratch/from-zero/contract-errata.md:81 \"a resolved reference is always keyed without the debug overlay ... the acceptance `--debug` walk of `inject.yaml` pins ... with the `key:` form of 5.4 ... (planner: wrapup)\"\n- notes/plans/2026-09-17-construction-plan.md:587-612, section 4, is the owner-run GPU acceptance of the whole tree, not a wave-4 ticket acceptance; ticket 12 names `--score-ckpt` only once (.scratch/from-zero/issues/12-launcher.md:139) and carries no acceptance check that resolves it."
   },
   {
    "id": "T12-6",
    "title": "The `reads:` annotation omits the splits block of constants/path_datasets.yaml, which `meta.json.split_files`' path and sha1 need",
    "evidence": "1. The ticket line is a verbatim copy of the contracts' own 0.2 annotation, so it contradicts no source. .scratch/from-zero/issues/12-launcher.md:21 \"reads:   constants/path_datasets.yaml (the venv per environment and the venvs map),\" == notes/plans/2026-09-17-contracts.md:592 \"reads:   constants/path_datasets.yaml (the venv per environment and the venvs map),\".\n\n2. The errata entry the finding quotes is explicitly scoped to another file and another planner: .scratch/from-zero/contract-errata.md:100 \"Part 0.2 (`data/build_training_dataset.py`'s annotation lines): ... -> `imports: ...; [polars, PyYAML]` and `reads: the sample run's task records, constants/path_datasets.yaml (the splits block of cfg.data.env), the environment's split task-id files` (planner: data)\". It says nothing about jobs/launch.py, and its correction is already merged for the file it names: README.md:374 \"reads:   the sample run's task records, constants/path_datasets.yaml (the splits block of cfg.data.env,\". So ticket 12 contradicts no errata decision.\n\n3. The implementer is not left without a path source. `open_env` returns the instantiated environment (data/environments/__init__.py:63-80) and the merged AppWorld object holds the split-file paths as an attribute it read from the same file: data/environments/appworld.py:281 \"self.splits = block[\"splits\"]\", data/environments/appworld.py:292 \"text = Path(self.splits[split]).read_text()\". Whether the implementer takes the path from `env.splits[split]` or reads the yaml block directly in launch.py — a file it already reads for the venvs (contracts:592) — the resulting code is correct; nothing in ticket 12 forbids either route.\n\n4. No acceptance turns on the parenthetical. The only README check named is the import graph: .scratch/from-zero/issues/12-launcher.md:393 \"its `README.md` annotation line against the `ast`-parsed import graph; no `/home/` or `/net/` literal (B8)\". B8 (:381-387) greps only for absolute paths and VERSION.\n\n5. Ticket 12's instruction itself is complete enough to implement: :175-178 \"**`meta.json.split_files` (8.3).** Resolved and written **before the pieces start**, one entry per split file with its path, sha1 and resolved task-id list, through `env.tasks(split)`\", matching contracts:4524 \"for each split file read, its path, sha1 and the resolved task-id list. **Resolved and written by `jobs/launch.py` before the pieces start**\".\n\nThe residual point is a wording gap in a descriptive parenthetical that no check reads and that an implementer resolves on their own; it is not a contradiction with the contracts, the errata, the spec or the merged code."
   },
   {
    "id": "T12-7",
    "title": "The agent service piece's card count has no source in the ticket, while the probe service and the train piece are each given one",
    "evidence": "Ticket line, quoted correctly: .scratch/from-zero/issues/12-launcher.md:112-120 \"**Placement (3.4).** The agent service goes on its table row's `serving.host`; **an agent service the attach test matched takes no card, enters no card search and lands on that server's host**; a checkpoint-loading probe service (1 card) and a `train` piece (1 card) take the first host in `constants/path_outputs.yaml`'s `hosts:` list with enough free cards, preferring the host this run's agent service is on; a `--render-only` probe piece takes no card and lands on `login_host`; loop pieces take no card and run on `login_host`.\"\n\nNo conflict exists. (1) The ticket reproduces its cited source exactly: notes/plans/2026-09-17-contracts.md:2749-2766 gives the same four placement bullets — \"an **agent-service** piece is placed on its table row's `serving.host` and nowhere else (6.1)\"; \"an **agent-service** piece **the attach test of 7.4 matched to a live server** takes no card, enters no card search\"; \"a **probe-service** piece **that loads a checkpoint** (1 card, the `inject` case) and a **train** piece (1 card)\"; \"**loop** pieces take no card\". Contracts 3.4 itself names no card count for a non-attached agent service, so the ticket neither adds nor drops anything.\n\n(2) The number the finding says is uncited is named where the ticket sends the implementer. Ticket 12 line 23 lists \"reads: ... models/table.yaml (the serving block)\", and notes/plans/2026-09-17-contracts.md:3576 labels that column outright: \"| `gpu_memory_utilization`, `tensor_parallel_size` | serving | how much card it takes |\". models/agent_models/service.py:48 already spends it the same way (\"--tensor-parallel-size\", str(serving[\"tensor_parallel_size\"])).\n\n(3) Nothing can come out wrong today: models/table.yaml has exactly one `role: agent` row (models/table.yaml:6-21, gpt_oss_120b, `tensor_parallel_size: 1`); the three probe rows carry `serving: {}` (models/table.yaml:32, 38, 44). A literal 1 and `serving[\"tensor_parallel_size\"]` are the same value on every row on disk.\n\n(4) No acceptance command can fail on it. `place` takes the count as a parameter (.scratch/from-zero/issues/12-launcher.md:50 \"def place(kind, cards_needed, free_by_host, *, serving_host, prefer_host, attached)\"), and the ticket's own check hardcodes it for the only agent case it exercises: line 325 \"print(launch.place('service_agent', 1, free, serving_host='tokyo107', prefer_host=None, attached=True))\" — attached, i.e. the no-card branch. The non-attached agent-service count is never asserted.\n\n(5) .scratch/from-zero/contract-errata.md has no entry on card counts for the agent service (its only card entries are line 70, the busy test moving to jobs/registry.py, and lines 87/124, the hosts inventory counts), so nothing is being contradicted there either.\n\nThis is an implementer judgment call with one candidate value, labelled as such in the contracts section the ticket points at, and with identical behaviour whichever way it is read on today's table."
   },
   {
    "id": "T12-8",
    "title": "The merged agent service makes `--gpus` required even under `--attach-only`, while the ticket's attached branch takes no card",
    "evidence": "Ticket line settling it: .scratch/from-zero/issues/12-launcher.md:138 \"**The two service command lines are 7.1's and 7.2's, verbatim.**\" — the pinned line it points at is notes/plans/2026-09-17-contracts.md:3866-3867 \"python -m models.agent_models.service serve --run-dir <dir> --model <alias> \\ --port <n> --gpus <ids> [--attach-only]\", where `--gpus <ids>` is unbracketed and only `--attach-only` is bracketed, so the flag is on every agent-service command line including the attaching one. models/agent_models/service.py:223 `srv.add_argument(\"--gpus\", required=True)` is therefore satisfied by the ticket's own instruction; verified with a throwaway parser that `serve --gpus '' --attach-only` parses to {'gpus': '', 'attach_only': True} (run with /home/y-guo/reproduce/new1/external/appworld/venv/bin/python, nothing written), and models/agent_models/service.py:185-198 returns from the `--attach-only` branch before `build_command(row, m.serving, m.weights_path, args.port, args.gpus, ...)` at :199, so the value is never read on that path. The extra flag the attached branch actually needs is already named in the ticket's Comments at .scratch/from-zero/issues/12-launcher.md:430-431 (`--attached-to <run_id>`, errata .scratch/from-zero/contract-errata.md:30) — known fact (c). The finding's own detail concedes \"an implementer copying it verbatim passes something (an empty string works)\"; it rests on an implementer dropping a flag the ticket tells them to copy verbatim, which is not a contradiction between the ticket and the merged code."
   },
   {
    "id": "T12-9",
    "title": "B3's ledger assertion passes vacuously if `dirty_files` holds porcelain lines, the shape the legacy source the ticket cites produces",
    "evidence": "Ticket line, verified: .scratch/from-zero/issues/12-launcher.md:262 `print('no ledger file in dirty_files:', not any(launch.is_ledger_path(f) for f in g['dirty_files']))`, with the expected output at :270 `no ledger file in dirty_files: True`. Legacy source, verified: legacy/ops/record.py:72-78 `dirty = [l for l in g(\"status\", \"--porcelain\", raw=True).splitlines() if l.strip() and l[3:] not in LEDGER_PATHS]` ... `\"dirty_files\": dirty[:50]` — elements are porcelain lines. So the observation of fact is correct: under the legacy element shape the second assertion prints True without exercising anything.\n\nWhy this is not a finding under the stated bar:\n1. The acceptance command passes as written in both shapes — it prints the expected `True` either way — so it is not \"an acceptance command that cannot pass for a reason in the script itself\". The finding's own detail says \"The first assertion ... still holds either way\", and the finding's own severity is \"note\".\n2. The element shape is not left open in a way that can block or mislead an implementer: notes/plans/2026-09-17-contracts.md:4450 pins the start-row field as `| commit, branch, dirty, dirty_count, dirty_files | str/str/bool/int/list | the git state at launch, fail-closed ... from legacy/ops/record.py:58-83 |`, i.e. the same legacy function the ticket cites at .scratch/from-zero/issues/12-launcher.md:198 (`| dirty-tree probe, ledger exemption, fail-closed git | legacy/ops/record.py:51-82 |`). An implementer copying that source gets a defined shape.\n3. The exemption itself is unambiguously specified as a path test, so the implementer does not apply it to a porcelain line: .scratch/from-zero/issues/12-launcher.md:58-59 \"`is_ledger_path` is the ledger exemption of 2.5 as a pure test over one path string — true for `jobs/runs.jsonl`, `jobs/RESULTS.md` and anything ending `.lock`\"; the errata settles the same point at .scratch/from-zero/contract-errata.md:144 (\"`jobs/launch.py` offers `is_ledger_path(path) -> bool` as a pure test ... and ticket 12's `B3` exercises the exemption over a fabricated path list\"), which is exactly what line 260 does and what the errata asked B3 to prove. The legacy code strips the status code before the comparison (`l[3:] not in LEDGER_PATHS`, record.py:73-74), so a copier keeps the exemption correct.\n4. No merged code consumes the field: a grep of /home/y-guo/reproduce/new1/jobs and run.py for `dirty_files` returns nothing, so no shape disagreement with merged code exists.\n\nWhat remains is a remark that one acceptance assertion is weak (it does not pin the element shape), which an implementer resolves correctly on their own and which breaks no rule — a test-strength remark, not a contradiction with the contracts, the errata, the spec or another wave-4 ticket."
   },
   {
    "id": "X-3",
    "title": "ticket 12 never pins that a loop piece's piece index equals its --piece i, while ticket 11 spells owner_session and the heartbeat file from --piece i",
    "evidence": "The finding's failure mechanism does not exist. Ticket 11 does not derive a loop-local index: `.scratch/from-zero/issues/11-agent-loop.md:295` \"`piece` is the (i, n) PAIR parsed from --piece i/n\" and :298-299 \"`main`'s second parameter is the `(i, n)` tuple the `__main__` block parses out of `--piece i/n`\" — the number comes from the flag `jobs/launch.py` writes. Ticket 12 writes that flag and the log name from ONE argument: `.scratch/from-zero/issues/12-launcher.md:53` \"def piece_command(python, module, run_dir, piece, n, gpus, log)\"; :124 \"[--piece <i>/<n>] 2>&1 | tee -a <run_dir>/log/<piece>.txt\"; fixture :328 \"launch.piece_command('/p/py','agent.loop','/o/sample/k','0','6','', '/o/sample/k/log/0.txt')\" with expected :334 \"-m agent.loop --run-dir /o/sample/k --piece 0/6 2>&1 | tee -a /o/sample/k/log/0.txt\". So the loop's `i` IS the index launch assigned, and `owner_session` (11:360-361), `registry.beat(run_dir, i)` (11:341) -> `heartbeat/<i>-*.jsonl`, the gate's `heartbeat/<piece>-*.jsonl` read (12:96) and `registry._piece_verdict_dict`'s lookup by `piece[\"index\"]` (jobs/registry.py:693 `_beats_full(run_dir, piece_index)` with `prefix = f\"{piece_index}-\"`, :836) agree under any numbering — no divergence is possible. Loop-first numbering is additionally pinned by the finding's own quoted source, notes/plans/2026-09-17-contracts.md:4301-4304 \"a piece index is an index into the run's whole piece list, loop pieces included, so with `replicas: 1` and six loop pieces the service piece's index is not 0\" (false if services were numbered first), by ticket 12:107 \"Piece rule and venv come from `schema.STAGES`\" together with experimental_settings/schema.py:205-206 and 280-281 `\"pieces\": ((\"loop\", \"pieces\", None), (\"service_agent\", \"replicas\", None), (\"service_probe\", 1, ...))`, and by the fixture above using piece '0' of n '6' for a loop piece."
   }
  ],
  "unverified": [],
  "readLists": [
   {
    "key": "T10",
    "readList": [
     "/home/y-guo/reproduce/new1/.scratch/from-zero/issues/10-eval-generators-scorer-and-table.md (whole file, lines 1-616, Comments section included — it is empty)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/contract-errata.md (whole file, 157 lines)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/spec.md (whole file, sections 1-9)",
     "/home/y-guo/reproduce/new1/eval/utils/probe_eval.py (whole file)",
     "/home/y-guo/reproduce/new1/eval/methods/ctool.py (whole file)",
     "/home/y-guo/reproduce/new1/data/__init__.py (whole file)",
     "/home/y-guo/reproduce/new1/data/probe_output.py (whole file)",
     "/home/y-guo/reproduce/new1/data/trajectory_record.py (whole file)",
     "/home/y-guo/reproduce/new1/data/environments/__init__.py (whole file)",
     "/home/y-guo/reproduce/new1/data/environments/appworld.py (whole file)",
     "/home/y-guo/reproduce/new1/experimental_settings/schema.py (lines 25-364 dataclasses/AXES/STAGES/SECTION_CLASSES, 991-1140 fields_of/models_of/versions_of/upstream, 1145-1299 key/run_dir_of/freeze/load_frozen)",
     "/home/y-guo/reproduce/new1/jobs/registry.py (function index; lines 229-340 write_meta/write_done/Heartbeat/beat/_read_rows, 786-932 _orphan_session_row/_ls_row/ls)",
     "/home/y-guo/reproduce/new1/notes/plans/2026-09-17-contracts.md (1.4 lines 1245-1400; 2.6 lines 2294-2410; 4.2 lines 2870-2925; 8.1 lines 4388-4460; 8.4 lines 4554-4625; 8.6 lines 4694-4760; 0.2 eval block lines 498-580; 3.4 debug lines via grep)",
     "/home/y-guo/reproduce/new1/notes/plans/2026-09-17-construction-plan.md (section 1 lines 20-74; section 2.7 lines 392-430)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/prompts/implementer.md (GPU/ssh hard-rule lines, grep)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/issues/11-agent-loop.md (header, README line)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/issues/12-launcher.md (header)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/issues/08-eval-library-and-classifier-metric.md (README line)",
     "/home/y-guo/reproduce/new1/constants/path_datasets.yaml",
     "/home/y-guo/reproduce/new1/constants/path_outputs.yaml",
     "/home/y-guo/reproduce/new1/experimental_settings/train_probe.yaml",
     "/home/y-guo/reproduce/new1/experimental_settings/baseline.yaml",
     "/home/y-guo/reproduce/new1/README.md (eval/ block, grep)",
     "/home/y-guo/reproduce/new1/jobs/runs.jsonl (confirmed empty, 0 bytes)"
    ]
   },
   {
    "key": "T11",
    "readList": [
     ".scratch/from-zero/issues/11-agent-loop.md (whole file, 1112 lines, Comments included)",
     ".scratch/from-zero/contract-errata.md (whole file, 157 lines)",
     ".scratch/from-zero/spec.md (sections 1, 2, 3, 7, 8, 9)",
     ".scratch/from-zero/prompts/implementer.md (whole file)",
     ".scratch/from-zero/issues/10-eval-generators-scorer-and-table.md (lines 176-215, plus grep for spec/step)",
     ".scratch/from-zero/issues/12-launcher.md (grep for session name, --piece, agent.loop, attach)",
     "data/trajectory_record.py (whole file)",
     "data/probe_input.py (whole file)",
     "data/__init__.py (whole file)",
     "data/environments/__init__.py (whole file)",
     "data/environments/appworld.py (step, speculate, judge, close: lines 331-385)",
     "experimental_settings/schema.py (dataclasses and AXES lines 17-246, STAGES lines 246-330, _model_row line 616, _frozen_dict_to_dataclass/_refuse_unknown_frozen_fields lines 1183-1207, freeze/load_frozen lines 1245-1298)",
     "models/__init__.py (whole file)",
     "models/agent_models/gptoss.py (whole file)",
     "models/agent_models/service.py (whole file)",
     "models/probe_models/service.py (whole file)",
     "models/table.yaml (lines 1-45)",
     "jobs/registry.py (DEFAULTS grep, Heartbeat and beat: lines 255-320)",
     "README.md (head and grep for agent/)",
     "notes/plans/2026-09-17-contracts.md 0.2 tree, agent/ block (lines 398-453)",
     "notes/plans/2026-09-17-contracts.md 1.1 Task record (lines 803-982)",
     "notes/plans/2026-09-17-contracts.md 1.7 The probe's input (lines 1506-1600)",
     "notes/plans/2026-09-17-contracts.md 2.3 Pieces, claiming, done, continue (lines 1814-1900)",
     "notes/plans/2026-09-17-contracts.md 7.2 The probe service (lines 3950-4110)",
     "notes/plans/2026-09-17-contracts.md 7.3 The step interface (lines 4111-4262)",
     "notes/plans/2026-09-17-contracts.md 7.4 Ports and endpoint files (lines 4263-4322)",
     "notes/plans/2026-09-17-contracts.md 8.4 The heartbeat (lines 4554-4620)",
     "notes/plans/2026-09-17-construction-plan.md 2.5 agent (lines 305-352)",
     "legacy/pipeline/inject/inject_format.py (whole file)",
     "legacy/pipeline/inject/live_appworld.py (lines 151-190 think_span/sent_starts/sent_cuts, 320-410 token_boundary/find_head/ids_sha/log_resume, 411-570 gen_step)",
     "Executed read-only checks under external/appworld/venv/bin/python and external/probe-env/bin/python: gptoss.parse chunk accumulation, schema.load_frozen over a copy of the D-fixture settings.yaml in /tmp, models.agent('gpt_oss_120b') resolution, trajectory_record write/read round trips for the meta-only, meta+spec+resume and nofill-spec shapes, to_messages over Writer.frame(), and grep -c exit status"
    ]
   },
   {
    "key": "T12",
    "readList": [
     "/home/y-guo/reproduce/new1/.scratch/from-zero/issues/12-launcher.md (whole file, Comments included)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/contract-errata.md (whole file, 157 lines)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/spec.md (sections 5, 6, 7, 8, 9)",
     "/home/y-guo/reproduce/new1/notes/plans/2026-09-17-contracts.md sections 0.2 (jobs/launch.py and jobs/registry.py entries, lines 575-640), 2.1 (lines 1644-1689), 2.3 (lines 1930-2043), 2.5 (lines 2225-2293), 3.4 (lines 2604-2784), 7.1 (lines 3861-3949), 7.2 (lines 3950-4029), 7.4 (lines 4263-4322), 8.1 (lines 4388-4454), 8.3 (lines 4511-4553)",
     "/home/y-guo/reproduce/new1/jobs/registry.py (whole file: DEFAULTS, lock, open_runs, free/cards_busy, session_alive, live_sessions, _remote_shell, write_meta, stall_line_s, judge, ls, kill, sync)",
     "/home/y-guo/reproduce/new1/experimental_settings/schema.py (STAGES literal lines 197-326; upstream/key/run_dir_of/run_dir lines 1116-1195; name index)",
     "/home/y-guo/reproduce/new1/data/trajectory_record.py (lines 83-253: REQUIRED, record_path, Writer.row, is_done, owner, done_pairs, release, read, read_dir)",
     "/home/y-guo/reproduce/new1/data/environments/__init__.py (whole file: Environment, open_env, requested_pairs)",
     "/home/y-guo/reproduce/new1/data/environments/appworld.py (header lines 1-40 and tasks(), lines 289-310)",
     "/home/y-guo/reproduce/new1/models/agent_models/service.py (build_command lines 36-70, serve lines 164-213, main lines 216-232)",
     "/home/y-guo/reproduce/new1/models/probe_models/service.py (serve lines 60-108, main lines 257-274)",
     "/home/y-guo/reproduce/new1/constants/path_outputs.yaml (whole file)",
     "/home/y-guo/reproduce/new1/constants/path_datasets.yaml (whole file)",
     "/home/y-guo/reproduce/new1/models/table.yaml (whole file)",
     "/home/y-guo/reproduce/new1/experimental_settings/train_probe.yaml (whole file)",
     "/home/y-guo/reproduce/new1/legacy/ops/record.py (lines 45-90: LEDGER_PATHS and git_state)",
     "/home/y-guo/reproduce/new1/legacy/ops/launch_common.py (lines 38-70: ALIAS, local_host, has_session, tmux_launch)",
     "/home/y-guo/reproduce/new1/legacy/ops/launch_cmd.py (lines 193-245: build_inner, verify_alive; ALIVE_WINDOW_S/ALIVE_POLL_S at 56-57)",
     "/home/y-guo/reproduce/new1/README.md (jobs/launch.py mentions)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/issues/11-agent-loop.md (owner_session and endpoint-file lines only)"
    ]
   },
   {
    "key": "cross",
    "readList": [
     "/home/y-guo/reproduce/new1/.scratch/from-zero/issues/10-eval-generators-scorer-and-table.md (whole)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/issues/11-agent-loop.md (whole)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/issues/12-launcher.md (whole)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/contract-errata.md (whole, 157 lines)",
     "/home/y-guo/reproduce/new1/.scratch/from-zero/issues/*.md (Status and Blocked-by headers of all 18)",
     "/home/y-guo/reproduce/new1/notes/plans/2026-09-17-construction-plan.md sections 1 (lines 20-92) and 3 (the wave table, lines 543-602)",
     "/home/y-guo/reproduce/new1/notes/plans/2026-09-17-contracts.md 0.1 (88-118), 0.2 entries for agent/ (397-453), eval/ (503-569), jobs/launch.py and registry.py (571-621)",
     "/home/y-guo/reproduce/new1/notes/plans/2026-09-17-contracts.md 1.1 row table (905-960), 2.3 (1814-1933), 3.4 (2604-2784), 7.4 (4263-4322), 8.1-8.5 (4388-4627)",
     "/home/y-guo/reproduce/new1/jobs/registry.py (DEFAULTS, _runs_path, fold, render, open_runs, _folded_rows, find, where, _merge_pieces, write_meta, write_done, Heartbeat/beat, _ls_row, ls, and the function index)",
     "/home/y-guo/reproduce/new1/experimental_settings/schema.py (Models/Probe/Setting dataclasses, AXES, STAGES for sample/build/train/eval/inject/score, _stage_dotted_fields, fields_of, models_of, freeze, load_frozen)",
     "/home/y-guo/reproduce/new1/data/trajectory_record.py (SCHEMA, REQUIRED, _STAMPED, open_record, Writer.row/frame/close, to_messages, function index)",
     "/home/y-guo/reproduce/new1/data/environments/__init__.py (StepObservation)",
     "/home/y-guo/reproduce/new1/eval/utils/probe_eval.py (module head, IDENTITY_FIELDS, function index, run() lines 207-305)",
     "/home/y-guo/reproduce/new1/models/agent_models/service.py (_endpoint_path, _write_endpoint_file, _attach, serve's attach branch, Client signatures)",
     "/home/y-guo/reproduce/new1/models/probe_models/service.py (_endpoint_path, _write_endpoint_file, Client signatures)",
     "/home/y-guo/reproduce/new1/constants/path_outputs.yaml (whole)",
     "/home/y-guo/reproduce/new1/README.md (layout of the per-ticket annotation sections)",
     "/home/y-guo/reproduce/new1/jobs/runs.jsonl and jobs/RESULTS.md (both empty of runs)",
     "ran read-only: `grep -rn \"import torch|from torch|cuda|transformers\" eval/` (ticket 10's A2 — no match, exit 1 against the merged tree); `hostname` (shiga)"
    ]
   }
  ]
 }
}
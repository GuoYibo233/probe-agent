# research-loop 重设计：方向与决策档（2026-08-15）

这份文档记录起于 2026-08-15 的设计讨论（多轮续谈同档续记）的全部结论：哪些是你已经拍板的，哪些是我按你"先按自己理解干、记档待裁、不行返工"的指示暂定的。暂定条目逐条编号 P1 到 P9，你回复时点编号即可，被否决的条目我返工，不牵连其余。

讨论的起点是你对两轮验证结果的判断："我觉得这些不是简单迭代就能解决的，我们可能要稍微重新更改一下设计的方法。"经过逐条挖病根，这个判断成立的确切含义是：现有设计把正确性押在"很多个会话各自照散文规矩办事"上，而两轮验证的数字证明，押在机器上的部分（越权拒绝、格式校验、追溯链）两轮就收敛到零，押在会话自觉上的部分每轮都出一批新问题（第一轮确认 45 条、第二轮 48 条，不复发、全是新的）。病根不是补丁无效，是散文说明书的验收主张是开放的："零背景会话照着它能做对每件事"没有封闭清单，每轮验证照亮的是新走的路径，路径总数没人枚举过。

## 一、你已拍板的决策

1. **系统就是在设计一批 agent。** 你的原话："这个就相当于设计很多agent。他们要有角色设定，使用场景，可用的工具，限制的条件，以及输出的样式这些。我们要想办法给这些东西设计明白，并且还要设计他们之间交流的方法。"下文把"用这五栏规格定义的一类会话"统一叫**角色**，五栏分别是：角色设定、使用场景、可用工具、限制条件、输出样式。角色之间的交流方法单独设计。

2. **源为真，规格贴源解释源，必须带例子。** 你的原话："这个五栏规格应该和源放在一起，作为解释源的东西，并且要给出例子解释这些源。"真源是插件的代码和表；五栏规格不是第二个真源，是贴着源放的解释层。

3. **例子是活例子。** 规格里的每个例子都是可执行片段（一条命令加它应产出的账行、一个来件加它应触发的处理），机器定期在一次性沙盒里全部跑一遍，跑出来的结果和规格写的对不上就报错。例子同时兼任"规格没落后于源"的机器检查。你的原话："应该这里确实应该用a的。"

4. **使用场景以你指定为主，角色自判为辅。** 你的原话："首先最主要的是我指定的什么场景就是什么场景，我指定用什么agent的就用什么agent的。第2个是自己觉得该用的时候也可以使用。"

5. **角色一共五个。** 出主意的、部署代码的、跑实验的、统计结果的，加一个检查前四个的。你的原话："事实上我的实验也就这么几个，一个是项目idea，一个是部署代码，一个是运行，一个是统计结果。他们这些有各自的活干就行了。当然为了这些不出错，还需要一个额外的来检查这些的。"

6. **记录归跑实验的角色，统计角色只读。** 你的修正原话大意：统计角色是"给我统计实验结果、让我出实验报告的"，它"读取跑实验本身的人的记录，然后把它做成图表之类的"，还"统筹这些所有的记录"；记录本身由跑实验的角色落。这与现有设计"谁干活谁落账"的规矩一致。

7. **工作方式：先干、记档、待裁、可返工。** 你的原话："你先按照自己理解的干，然后把这些东西记到一个文档里面，等我能回复的时候再回复，如果不行的话就返工。"这条既是给我的指示，下面 P1 也把它定成角色遇到岔路时的缺省行为。

## 二、我的暂定决策（待你回复，逐条可否决）

**P1 中途岔路的缺省行为：按理解先干 + 记档待裁 + 可返工，两类硬例外。**
角色干活中途遇到规格没写的分岔，缺省不停工：按自己的理解选一条走，同时把"遇到什么岔、选了哪条、为什么"记成待裁条目挂给你，你异步裁决，裁错的返工。两类动作除外，必须硬停等你：一是**替你说话的写入**（记"用户已裁决""用户已授权"的账行），这正是第二轮验证里执行者伪造你裁决的那个口子，按理解干在这里等于开门造假；二是**不可逆或花大钱大卡的动作**（删数据、长时间占多卡的发射）。
理由：这就是你给我的工作方式推广到角色身上；比"一律硬停"省你的打断成本，比"事后检查兜底"少让错账污染下游。否决代价：改回硬停或改成枚举都只动规格文本，不动底座。

**P2 验收标准换掉"连续一轮零确认"。**
新标准分两段：交付段 = 机器检查全绿（现有三道门禁 + 全部活例子跑通）；长期段 = 装进真实工程用，以真实使用暴露的卡点为准。原标准对开放主张永远达不到，对封闭主张又太贵（一轮 15 个会话），废弃。
理由：活例子把"规格和源没脱节"变成每次提交都能跑的检查，验证轮的大部分职能被它吸收。否决代价：恢复验证轮即可，沙盒种子和脚本都还在。

**P3 大规模活会话验证降频，只在设计变更时做。**
两轮验证断过的地方都是它查出来的真设计洞，方法本身在干活，但一轮 15 个会话的成本只配得上大的设计变更（比如这次重构落地后验一次），不配日常迭代。日常的正确性由活例子和检查角色扛。

**P4 冒名写账收窄一个口。**
"记录你的裁决和授权"这一类写入口，机器上加一道只有你在场才过得去的关卡（具体形态实现时定：交互确认或一次性口令，选实现成本低的）。其余账不加关卡，检查角色的例行核对负责事后比对。全账本上锁被否决：把每层日常写账都拖上人工步骤，代价大于风险。

**P5 已收敛的底座保留，散文说明书退役。**
保留：账本通信（只增不改、出处链）、五张结构化表（写权限、账行格式、合法去向、路由、检查清单）、全部机器检查（体检、追溯链、格式校验、越权拒绝）、兜底发射器、319 项测试。这些是两轮验证实测收敛的资产。
退役：五份散文说明书和它们的参考文档，被五张"五栏规格 + 活例子"替代，贴源存放。
角色映射：想法层→出主意，部署层→部署代码，执行层→跑实验，监察面→检查；统计结果从三处（执行层记账、部署层批次报告、监察面核查）拆出成独立角色，只读账、出图表和实验报告、统筹记录秩序。统计与检查的边界：统计管"把记录整理成你能看的东西"，检查管"抓其余四个角色的错"，检查不出报告给你、只出问题清单。

**P6 交流方法保留现骨架，随角色数微调。**
卡点条流转（谁无权谁开条挂给有权的）、授权账、决定账原样保留；合法去向表按五角色重画（统计角色加进去）；路由表简化到五个角色加检查。

**P7 第二十号工单（约 30 处散文修补）搁置，第三轮验证取消。**
散文说明书退役后，给它打的 30 处补丁大部分白费，工单留档不执行；第三轮验证被 P2/P3 的新标准替代。（2026-08-15 注：随第五节生效即销案——T20 留档不执行、第三轮验证取消，已是定案。）

**P8 执行机制的自觉步骤挂机器核对。**
这次讨论之外顺手定的：工单收账这类流程里"靠会话自觉执行清单"的步骤（翻状态行、报告入库），做完必须跑一个机器核对，核对不过不算做完。这次收账漏翻八张工单状态行就是这么漏的。工具不重写，只加核对。

**P9 决定账的"分开"用字段不用分文件（2026-08-15 新增，待裁）。**
你裁定决定要按层分开（见第七节"决定账分域分层"条）。"分开"我落成同一本账加一个必填的域字段、外加按域分开的查询视图，不拆成五本账文件。理由：拆文件要把写入、校验、查询的机器全部乘五；字段加视图给到同样的分开效果，还保住现有查重和追溯链。否决代价：改拆文件只动账本层，行格式不变。

## 三、四类病根的诊断存档

1. **说明书面收敛慢**：验收主张开放（上文起点段已述）。治法 = 五栏规格封闭各栏 + 活例子钉住规格与源不脱节（决策 1/2/3）。
2. **冒名写账**：格式检查钉得住字段值，查不出写账的是谁；机器上所有会话同一个系统账号，文件权限分不开。治法 = P4 收窄"替你说话"的口 + P1 把这类写入定为硬停。
3. **接力走不通全程**：两轮断点（自决留痕无路、重试成功入账被查重拒）都是真设计洞，接力是照妖镜不是病灶；贵才是它的问题。治法 = 确定性部分交给活例子和整链机器测试（P2），活会话接力降频（P3）。
4. **执行机制不可靠**：调度卡死是派发规程漏禁一个会卡死的工具（已禁）；收账漏账是清单靠自觉（P8 治）。

## 四、词汇表（本文档内钉死，定稿后进正式词表）

（2026-08-15 注：本节是讨论早期的中文词，已被第七节"词汇表"条的定稿词单取代——正式名改用英文，多个词改了名。查最新口径以第七节为准，本节留档不删。）

- **角色**：用五栏规格定义的一类会话。五个：出主意、部署代码、跑实验、统计结果、检查。
- **五栏规格**：一个角色的定义文件，五栏 = 角色设定、使用场景、可用工具、限制条件、输出样式。
- **源**：插件的代码和表。真源；规格解释它，不复述它。
- **活例子**：规格里可机器执行并核对结果的示范片段。
- **岔路缺省**：P1 那条规则的简称——没写的分岔按理解先干、记档待裁、可返工，替用户说话与不可逆动作除外。
- **待裁条目**：角色按理解干了之后挂给你的那条记录：遇到什么岔、选了哪条、为什么。

## 五、P1 到 P8 的生效记录

2026-08-15 你的答复原话："还有什么问题吗？让我们继续啊。"P1 到 P8 就此记为生效。其后续谈对个别条目有修正：P6 里"路由表按五角色重画"被第七节的"菜单页"裁定覆盖；P2 的交付段补上"每个例子过你的手"（见第七节末条）。

## 六、设计过程重定（2026-08-15 续谈，本节取代旧的"下一步"计划）

你对过程的裁定原话："我觉得这个设计过程我们得想一下吧，不能像这样在很多迭代了，可能要重新定一下这个计划。"以及："我们应该先把一致性的东西定下来，然后再慢慢给它分化。而不应该是先给都设计出来，然后再给他们搞的一致。"

旧过程（大规格十六轮修订 → 二十张工单九波施工 → 两轮 27 个会话的验证）废弃。新过程两段：

**第一段：定共性，你把关。** 产出一份不长的共性定稿，装五样东西：① 角色规格模板；② 全角色通例；③ 交流协议的五角色版；④ 词汇表；⑤ 活例子的书写与运行约定。你逐条过目批准，它是五个角色共同的母版。五样清单你已点头（原话："我觉得首先觉得这5样都没什么问题"）。

**第二段：分化。** 母版批准后，五个角色逐个从它实例化：填五栏、写活例子、机器跑绿、例子送你逐个批，批完交付一个，再做下一个。这段你抽查即可，重关在第一段。

## 七、共性定稿五样的设计要点（已和你过完的部分）

**模板（第①样，已过）**：模板 = 五栏的填写说明 + 一张空表 + 一份填好的小样。五栏：角色设定（短散文，只有人能审）；使用场景（封闭列表，每行三件套——派发来源、做什么、产出什么，活例子块穿插其中；不设"其他情况"格，没列到的走岔路缺省）；权限边界（只引用分权矩阵里自己那一行）；限制条件（上半引用通例不复述，下半本角色特有禁令）；输出样式（全部指向既有格式定义，机器查引用存在）。给你审的底稿中文，进插件的正式版英文。

**场景进入靠显式指定，不猜话头（你的修正，已定）**：你的原话大意——角色"不需要根据我的话来自动判断"，大部分情况"由我亲自或者由上一个（角色）显式指定"。落法：三件套的"派发来源"只有两种写法——你点名，或上游角色的交接物点名（发射单点跑实验角色这样）。原设计的"一句话分流表"退役成给你看的菜单页，不再是机器判断依据。边界：场景之内的下一步（失败了先查分类表这类）照规格步骤走，不算猜。

**第三栏改权限边界（你的修正，已定）**：你的原话大意——这一栏"不是传统意义上的工具"，而是"idea 的人不能写代码，写代码的人不能运行程序，运行程序的人不能动 idea 和代码"。落法：共性定稿放一张**分权矩阵**，五角色乘五域（想法与原则、代码、运行、记录、报告），每格取"我管/可读/禁入"三值之一；每个域恰好一个"我管"，机器查缺重；每格标强制手段——账与报告的边界机器当场拦（现成），代码目录这类机器拦不住的标检查角色事后核对，不假装全能机拦。命令清单降级成"我管"域下的附注。现有机器强制的账本写权就是这张矩阵已经实现的那部分。

**通例（第②样，2026-08-15 已过一轮，九条候选已送你审）**：现有十条硬规矩逐条过筛。去向比原计划多一种，共四种——还成立的改写成角色语言、已被机器强制的只留引用、新设计下作废的删掉记因、不是管全部角色的下放到那个角色自己的规格。筛完的账：老 R1（原则条目怎么写）下放出主意角色规格；老 R10（说明书两级按需加载）随散文说明书退役作废记因；其余八条改写、新增一条，九条候选如下，每条都标了谁强制，没人强制的不收录——
① 岔路缺省（老 R5 重写：缺省干加记档待裁，替用户说话的写入与不可逆或大算力动作两类硬停。强制：assumption 格式机器查；替用户说话的写入 P4 机拦；不可逆动作自觉加 reviewer 角色事后核对）——**你已批**，并点明这条主要发生在部署代码角色（施工自由度规格写不满，两轮验证的分岔也全在这层），分化时部署代码角色的规格给它配最多活例子；
② 自决必留痕（留痕格式按下面"决定账分域分层"条。强制：机拦——必填字段与追溯链检查）——**你已批加修正**；
③ 机验人判（机械检查脚本跑，科学结论归用户。强制：报告结论词机拦；聊天场合自觉，reviewer 角色事后翻）——**你已批加修正**，修正见下面"反常结果先判 bug"条；
④ 数字只经脚本入账（禁止用眼睛读日志填数。强制：机拦——数字账仅两条脚本入口；reviewer 角色抽查复现命令）——**你已批加修正**，修正见下面"实验数据一个格式"条；
⑤ 证据体裁（数字配可复现的执行路径，机器已拦，只留引用。强制：证据检查器与机械校验器双重机拦）——**你已批加修正**，统计类证据的载体改 notebook，见下面"统计走 notebook"条；
⑥ 口径不发明（派生量只用批过的口径，细则归统计角色规格；notebook 里新开统计项目等于一次口径提案。强制：自觉打底，reviewer 角色对账上计算命令事后核对）——**你已批**；
⑦ 故障先自愈后升级（楼梯按五角色重画成跑实验→部署代码→出主意→用户；统计与检查不在楼梯上，卡住直接开条挂给出问题的角色。强制：错误分类脚本判；条目格式机拦；吞错靠 reviewer 角色事后核对）——**你已批加注**：分拣原则是病因落在哪个域就在哪个角色解决（你的原话："代码出bug了……或者是跑的实验中途出问题了……就都可以在代码层解决的，就在代码层解决。比如说这个代码在理论上就无法实现，在idea层，这些就要在idea层解决了"）——每层接到升级件先判病因是不是自己域里的，是就地修完为止，不是继续往上递；
⑧ 改规格走反馈账（角色不许自改通例与规格，用户批了才动母版。强制：机拦——审批行只有用户会话可写；git 历史留底）——**你已批**；
⑨ 越权即开条（新增：活超权限就开条挂给有权的角色，不许伸手。强制：账与报告的越权写机拦；代码目录靠 reviewer 角色事后核对）——**你已批**。
**九条全部过完（2026-08-15）**：①⑥⑧⑨原样批（①附部署层为主的注），②③④⑤批加修正（修正各见下方专条），⑦批加分拣原则注。通例定稿以这九条为准。

**决定账分域分层（你的裁定，2026-08-15）**：你的原话："我的每一个决定当然都是要有理由的……这个也像是就是像层层的，比如说我的某个代码决定，一定是因为我整个实验idea的决定。那么我希望这个代码决定和实验idea的决定，这两个要分开，然后这两个之间还是要可以互相引用的，就是做出来这种数据的结构。"落法：决定账每行新加一个必填的域字段，取值和分权矩阵的五个域同一套（想法与原则、代码、运行、记录、报告），同一本账按域分开看（"分开"落成字段还是拆文件见 P9）；再加一个"依据决定"引用字段，下层决定指回上层决定——运行决定指代码决定，代码决定指 idea 决定——顶端接现成的原则引用字段，追溯链检查扩过去查被指的决定存在。账本只增不改，所以存的引用只有新行指旧行一个方向；反向的"这条 idea 决定底下长了哪些代码决定"由查询命令现场汇出，不存进行里。这套结构对用户拍板和角色自决的行一视同仁。

**反常结果先判 bug 再汇报（你的裁定，2026-08-15，并进通例第③条）**：你的原话："如果结果非常反常，比如说0%或者是全对之类的，或者是实验本身跑的特别慢。这种就是有很大可能是bug的，这个你要判断一下，先判断是不是bug，如果是bug的话，那么就先尝试修复，如果不是的话，那么就原样汇报给我就行了。"落法：角色可下的判断加第四样——反常结果的 bug 排查。反常的触发两路：机器能标的先机器标（指标落在 0% 或全对这类极端值、实际耗时超出发射单预期耗时若干倍，倍数阈值分化阶段落表），机器标了必须排查；角色自己起疑也可以排查。排查针对产出数字的过程（代码、日志、数据管道），是工程判断不是科学结论，数字说明什么仍归你。三个出口（你的补充原话："判成bug的话，如果修不了，那么一个是说你可以向上面汇报，第2个是如果感觉就是修了之后，发现不是bug，那么也就回到那个判到不是bug的程度"）：判成 bug 且修得了——按第⑦条楼梯在权限内修，修好继续；判成 bug 但修不了——带证据向上汇报；修的途中发现其实不是 bug——退回不是 bug 的分支，数字原样汇报、不带解读。

**实验数据一个格式，统计只写一套脚本（你的裁定，2026-08-15，并进通例第④条）**：你的原话："统计的脚本记得要非常的统一。也就是说，整个是实验的，他们的这种产生的数据都应该是一个格式，不能一个实验室一个格式，然后这样的话统计也用一个格式去统计。"落法：所有实验的数字入账落同一个 schema，字段名与含义全工程一套；新实验接入时由它的指标命令把自己的输出折算成统一格式再入账，格式差异消化在入账口。统计角色的图表与报告脚本只吃统一格式，不为单个实验写专用统计脚本。强制手段：数字账 schema 校验机器现成，入不了格式的行当场拒；"新实验指标命令必须折算统一字段"写进跑实验角色的输出样式栏并配活例子。

**统计走 notebook（你的裁定，2026-08-15，并进通例第⑤条）**：你的原话："这个地方我觉得用notebook来实现吧。也就是说你把这个计数啊什么的时候，把这些东西都写在notebook里面，能让我自己去运行。就是专门做一个分析数据的文件夹这种感觉。里面有什么通用的这个分析数据的，比如说放数据的位置的，放什么库什么的。然后什么比如说统计所有平均值这些的……反正就是做一个比较方便的去统计数据的这么一个notebook吧。"落法：统计角色名下设一个数据分析文件夹，两层——公共件（数据位置配置：账本路径、NFS 产物根；公共函数库：读账、均值这类通用统计）+ 每个统计项目一个 notebook（读统一格式的账、算数、出图表，接第④条的"一个格式"）。统计报告里的数字来自 notebook 输出，用户核对就是自己打开 notebook 从头跑。强制手段：notebook 挂机器门禁定期重跑，跑不通或重跑输出与存档不一致即报警（与活例子同一套办法，例子检查程序管）。派生量口径照第⑥条仍要用户批。我先替你分的一处（可否决）：检查角色的核查类证据不进 notebook，仍用报告加粘贴命令——核查是一次性轻量检查，notebook 专给统计数据用。

**排查时唯一许用眼睛读的是抽查的实验原始输出（你的裁定，2026-08-15）**：你的原话："你唯一可以读的是抽查实验的结果，看一下我们的实验输出的具体的文字是什么样的，然后判断一下这个文字是怎么产生的，比如说文字如果全是乱码，那么可能就说明解码哪里有问题了，你是可以这么读的。"落法：角色可以抽样打开实验的原始输出文本，看文字本身长什么样，做产出过程的工程归因（乱码指向解码问题这类）。边界：这不破第④条——第④条禁的是用眼睛读出数字往账里填，抽查读的是文字形态不是入账数字；也不破科学结论归用户——判的是产出过程健不健康，不是结果好坏。

**交流协议五角色版（第③样，三件已过完）**：卡点、授权、决定机制原样保留。三件活：合法去向表按五角色重画；待裁条目不建新账本，是待决账的一种新条目类型，与既有"停工待裁"条目的唯一区别是不停工；四个旧英文角色名不动（少动源码枚举），统计角色新增一个英文名，候选给你挑。
**统计角色英文名已定（你选的，2026-08-15）**：**analysis**。（当时记的"现有枚举 idea/deploy/run/oversight 四个不动"其后被词表二次核查覆盖：oversight 改名 review，见词汇表条。）

**待裁条目落地（你已批，2026-08-15，"可以"）**：不建新账本，进现成卡点账当一种新条目类型；字段与停工待裁条几乎相同，多一个"已选哪条、干下去了"的字段，且不停工。用户答复三种：追认（记一笔完）、改选（返工）、升格（转正式决定进决定账）。塞老账本的理由：查询、格式校验、追溯链全现成白得；看未裁清单用现成查询命令加类型过滤。

**合法去向表五角色版（你已批，2026-08-15，"目前没问题"）**：交流分两套各走各的——**向下派活走交接物**：你或出主意角色把批过的工单交部署代码，部署代码把发射单（跑什么命令、预计耗时、指标命令）交跑实验，跑实验落统一格式的数字账（即给统计的交接物，不另发通知），统计把报告交你；这就是显式指定的第二种形式（上游交接物点名）。**向上反映问题走卡点条**：卡点条 = 公共账本里"这事我干不了或不归我干，请你处理"的一条记录，不一定是错误，三种用法（失败升级、请人拍板、越权请求）。去向表的边：故障楼梯边只有三条向上的（跑实验→部署代码→出主意→你，跳级机器拒）；越权开条的边按分权矩阵"我管"格现场定收件人，不逐条画死；待裁条目与替用户说话的硬停请示一律挂你；统计基本只有出边，进边只有"报告有问题挂给它"一种；检查的问题清单逐条开条挂给出错的角色，全量清单你用查询命令看，检查只接你点名的核查任务。开条收件人不在表里机器当场拒（拦截机制现成）。

**词汇表（第④样，2026-08-15 词单已过、英文名已定）**：每词四件——英文名（正式名）、中文解释名、一句话定义、一句"它不是什么"（两轮验证里好几条问题出在近义词边界上）。只收跨角色的词，角色私词留在各自规格里。你的裁定："都给我改成对应的英语单词吧，在agent里面的，比如scope input format这种常用的"——正式名一律用 agent 圈常用英文词，账本、代码、规格、聊天都用英文词，中文名降级成解释。首版词单经你二次核查后九处改名（2026-08-15，你逐个点名，codebase 与 verifier 两个由我挑、你批"可以"）。**定稿词单十七个**：role（角色；五个取值 idea/deploy/run/analysis/**reviewer**——检查角色由 oversight 改名，动源码枚举，测试与校验护着改；2026-08-15 终检查实 review 一词已被反馈账审批行占用，你裁定角色叫 reviewer，见第十节第 2 条）、spec（五栏规格）、codebase（源）、global rules（通例）、permission（分权表，原 permission matrix）、type of work（活的种类，原 domain，字段名落 work_type）、example（活例子，去掉 runnable）、verifier（例子验证程序，原 example runner）、issue（问题条，原 blocker，账本随之 blocked→issues）、assumption（待裁条目；先按假设干、留档待审）、handoff（交接物）、launch order（发射单；源码现名）、decision log（决定账）、grant（授权；源码现名）、feedback log（反馈账）、evaluation（口径，原 metric definition）、schema（统一格式，原 unified schema）。五栏栏名：persona（角色设定）、use cases（使用场景）、scope（权限边界）、constraints（限制条件）、output format（输出样式）。定义句与"它不是什么"句在共性定稿初稿里给用户逐条审。

**例子约定（第⑤样，已过）**：一个例子 = 前置沙盒状态 + 命令 + 期望结果，三件缺一不可。前置状态从封闭的沙盒底座清单里选，底座用现成沙盒脚本造，每个例子跑在干净副本里。比对按**结构化子集**：期望写"这些字段出现且等于这些值"，时间戳、绝对路径这类天然变动的字段列进豁免清单——不逐字节比，逐字节会逼人把例子写虚。例子挂进现有测试门禁，一个不过整体红。

**例子逐个过你的手（你的裁定，已定）**：你的原话："每个例子让我来单独把关吧，我多做一点，就是我来单独看一下这个例子行不行。"流程：机器先跑绿（跑不绿的不送你）→ 你单独看 → 点头的盖"已批"标记进规格 → 之后机器每次提交重跑全部已批例子，谁变假谁报警。分工：你判"是不是我要的行为"，机器判"还是不是真的"。此条补进 P2 交付段。

## 八、词汇修正（谈话中钉死）

- "契约"废，改叫**约定**；
- "跑器"废，改叫**例子检查程序**（扫描规格抽例子、沙盒真跑、比对、脱节报警的那个程序）——后随词表定英文名，正式名 **verifier**；
- 第⑤样全称"活例子的书写与运行约定"。

## 九、施工纪律与顺序（2026-08-15 续谈）

你在共性五样过完后追加的关口："我还要想一下施工的时候怎么样能不出现那种怎么都修改不过来的问题，这个可能是llm的局限性，但是我得想个顺序，能让你施工时候可靠。"

**实证底子**：本工程施工出过五类问题——实现者对规格空白自行理解、流程簿记漏做（八张工单状态行没翻、报告目录漏进 git）、编排事故（EnterWorktree 吊死六小时，两次）、修复循环空转（五轮打满，部分是评审的假 finding 烧轮次）、评审查不了的项积压人肉核对。现有门禁只查"仓库里的东西对不对"，流程侧没人管。

**四条施工纪律**（每条对着一类修不动的坑）：① 每步封闭验收——每张工单带一条命令，退出码 0 才算完，没有这条命令的工单不发；② 检查器先于被检查物——verifier 先造再写 spec；③ 一步一个动件——改名与改行为不同 commit，任何时刻仓库停在全绿态；④ 修三轮修不动就废掉重做，不叠第四个补丁——带着前三轮评审发现从头重写，比补丁链可靠。

**你的裁定（原话）**："我觉得不能打补丁啊 应该从施工顺序上动刀，先给共同的弄好，然后发展。"——施工检查机器不当补丁加，当共同底座的一部分排进第一段。

**三段施工顺序（你定的结构，2026-08-15 定稿，取代先前的两段版）**。你的结构原话："计划中首先先给共同的弄好，检查是否和意图一致，然后两两考虑角色之间的交流，给交流用的东西弄好，检查完整性一致性。接着根据定好的给每一个角色纯内部的东西弄好。"目的原话："这个的目的是，为了让设计出来的五个角色之间，交流一致，所以必须把硬的概念之类的首先规定好。"——一致靠先把硬概念规定死、后面全部引用，不靠事后对表。

第一段·共同：施工机器（工单验收命令字段、波脚本"验收红不算完+修三轮废掉重做"、收账核对必跑命令即 P8 落地、看门狗随发射自动起，改完拿一张微型工单全流程自证）、机械改名（oversight→reviewer、blocked→issues、决定账加 work_type 与依据字段；撞名已裁：角色叫 reviewer，反馈账的 review 审批行不动，见第十节第 2 条）、verifier、global rules 正文、词表、spec 模板。每步全绿一个 commit。收尾检查对的是**意图**：拿本档已批决策当核对清单逐条对，这一关用户把。
第二段·交流：permission 表、去向表、launch order 格式、issue 与 assumption 条目格式、决定账分域分层结构、数字账 schema——凡两个角色之间流动的都算。过法：五角色加用户两两过，每对要么列清流动什么（向下 handoff、向上 issue），要么明写"无直接边"（无边也是设计，写下来才查得了完整性）。两个检查：完整性=每对过到、每边有格式和 example；一致性=一条边两端同一——example 成对写，上游 example 的产出直接当下游 example 的输入，verifier 把两端接起来跑，接不上当场红。
第三段·角色内部：persona、内部 use cases、constraints 的本角色禁令、内部 example，逐个角色写→跑绿→用户逐例批→交付一个开下一个。内部的全部踩在前两段定死的格式上；要改前两段的东西必须停下走前两段的程序，不飞行中打补丁。

## 十、终检提出的七个问题（2026-08-15 你已逐条裁，裁决记在每条下）

设计与施工顺序都定了之后，我按"哪里还没人管"扫了一遍，扫出七个你还没裁过的问题。你的裁决：

1. **角色互相冒名没人管。** P4 只堵了"替你说话"这一个口；账行的 role 字段是会话自报的（所有会话共用一个系统账号，机器分不出谁是谁），deploy 自称 run 这类互相冒名现在没有任何检查。我的倾向是给 reviewer 角色加"笔迹核对"例行项。**你的裁决（原话）："我觉得这个可以相信agent足够可靠"**——不加检查，我的倾向作废。落法：这一项不进 reviewer 的例行清单，也不上机器关卡；账行的 role 字段仍照填，只是没人事后比对。
2. **review 撞名（已查实）。** 反馈账的审批行类型和 CLI 子命令已经叫 review（`schemas/feedback.review.schema.json`、`ledger.py feedback review`、`rows.json` 里 review 行类型），角色再叫 review 就一词两义。**你的裁决（原话）："叫reviewer"**——角色名定为 **reviewer**，反馈账那边一个字不动。词表里 role 的第五个取值随之从 review 改成 reviewer；施工第一段的机械改名由"oversight→review"改成"oversight→reviewer"，撞名隐患消失。
3. **存量账怎么迁。** **事实先行（2026-08-15 实测）：不存在要迁的账。** `find` 全仓扫过，`research-loop/` 插件目录下一个 `.jsonl` 都没有；仓库根的 `ops/runs.jsonl` 是 new1 工程自己的记账系统（`ops/record.py` 写的），不是插件的账本。插件只带 schema 与表，账本要等装进真实工程才落地，而它从没装进 new1。所以 blocked→issues 改名、decision log 加字段，改的都是格式定义与代码，没有存量行要迁，也不用背双格式兼容。**你的追问："那些还有啥能利用的吗"**——见第十一节资产账。
4. **新旧说明书并存窗口。** 第三段逐角色交付期间，旧的散文 SKILL.md 还在，会话进来读哪份没规定。**你的裁决："可以"**——按我的倾向办：交付一个角色，同一个 commit 里退役它对应的旧说明书，不留双真源窗口。
5. **example 里的贵动作。** **你的反问（原话）："example不是给这个role一个例子说他能干什么吗？不是真的会被运行的吧"**——这里我们的理解岔了，见第十二节，待你裁。
6. **五角色之外的活。** 文献调研、论文写作这类活不在任何角色的 use cases 里。**你的裁决（原话）："这是一个plugin啊，如果我不调用这个plugin那就是默认的东西啊"**——问题自解：插件没被调用的时候就是普通 Claude Code，不归这套账管，不需要在 spec 里写"系统外的活找你"这类兜底句，封闭列表也撑不开。落法：词表或模板里带一句边界说明即可（"本套规矩只在插件被调用时生效"），不设第六个角色，不设"其他"格。
7. **第一个真实试运行选谁。** **你的裁决："先不定"**——第三段交付完再挑。

## 十一、资产账：重设计之后现有的东西还有什么能用（2026-08-15 盘点）

（本节由一次全插件盘点填充：六个分片并行读代码、表、测试、技能文档，逐件判去向，判完由复核员对抗性核一遍。盘点结果回来后写在这里。）

## 十二、example 到底跑不跑（我们的理解岔了，待你裁）

你的反问原话："example不是给这个role一个例子说他能干什么吗？不是真的会被运行的吧"。

**岔在哪**：我一路按"真跑"设计，你这句话说的是"给人看的示范"。两种都合理，但选哪种决定了整套设计的一大块。

**真跑那条路（我此前的设计，你在讨论早期批过一次）**：2026-08-15 你批过决策 3"例子是活例子"，当时的记法是"机器定期在一次性沙盒里全部跑一遍，跑出来的结果和规格写的对不上就报错"，并批过例子约定（前置沙盒状态 + 命令 + 期望结果三件套、结构化子集比对、挂进测试门禁）。它扛的是一件具体的事：**spec 不会随着代码变化而悄悄变假**。散文说明书两轮验证收敛不了，病根就是没有任何机器手段判它还准不准；example 真跑是这套新设计里唯一钉住"spec 与 codebase 不脱节"的机器手段。代价：要造 verifier，发射类 example 不能真占 GPU（要干跑或桩发射器）。

**只给人看那条路（你这句话的字面意思）**：example 就是 spec 里的示范片段，写清这个角色能干什么、长什么样，人读了就懂，不进机器。代价：spec 与 codebase 脱节没有机器兜底，只能靠人和 reviewer 角色事后翻——这正好退回散文说明书那条收敛不了的老路。

**我的倾向**：保住真跑，但把"真跑"的范围缩到不贵的部分——账本类、格式类、命令类 example 全部真跑（这些在沙盒里几秒钟的事），发射类 example 干跑或用桩。这样既钉住不脱节，又不占算力。

裁法三选一：全真跑（原设计）、我的倾向（分级真跑）、只给人看（放弃机器兜底）。

## 十三、下一步（等你发话）

共性定稿五样全部过完（2026-08-15）：模板、例子约定、权限边界、通例九条（各带强制手段）、交流协议三件、词汇表词单与英文名，均经你逐条批。施工顺序三段定稿（第九节）。终检七问你已逐条裁（第十节）。**还悬着三条**：P9（决定账分开用字段还是拆文件）、reviewer 的核查证据不进 notebook（我替你分的）、第十二节 example 跑不跑。裁完动笔写共性定稿初稿（中文、带英文正式名），词的定义句、通例正文、各张表的成文都在初稿里给你逐条审。仓库现状：十九张工单完结、门禁全绿、工作树只有本文件改动。

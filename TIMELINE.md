# TIMELINE — 方向决策线

> **这个文件只增不改**，新的加在最上面。它不是计划书，是"什么时候因为什么改了主意"。
>
> 三个文件分工，别搞混：
> - `WORKPLAN.md` = 当前计划，会被覆盖重写 → 回答"现在要干什么"
> - `TIMELINE.md` = 决策历史，永不覆盖 → 回答"当初为什么这么定"
> - `RESULTS.md` = 实验数字总表（自动生成）→ 回答"数据长什么样"
>
> 什么时候该加一条：实验结论动了 WORKPLAN 里任何一条判断、读到的论文改变了取舍、
> 某个方向被证伪。纯粹的进度推进不用记——那是 `plans/` 里 worklog 的活。
>
> 每条至少写清：**决定了什么** / **被什么触发**（run_id 或论文）/ **作废了什么** /
> **对应 commit**。触发它的 run_id 要能在 `RESULTS.md` 里查到。

---

## 2026-08-02 — 活跑六臂收官：同框架内探针全面占优；effort 两条对照路线双双封死

- **决定**：任务级活跑（in-loop 注入，整题跑完看成败）的主结论按三条入账：
  ① **同一活跑框架内，探针不伤成绩反而抬分**——high 档 probe 11.9%(20/168) vs
  noprobe 7.1%(12/168)，token 省 7%（5.32M vs 5.72M），撞 64k 上下文墙少 12 题
  （27 vs 39，探针剪思考让历史滚得慢，附赠收益）；
  ② **“省 token 就拧小 effort”这条最显然的替代路线死了**——low 6.5%、med 7.1%，
  离 high 档 w0 的 28.6% 差 4 倍，med 比 low 多花 2.6 倍 token 几乎不涨分；
  ③ **探针跨 effort 档直接搬也死了**——low 档基本不触发（0.06 次/题，出手 10 次
  工具全错但无害），med 档频繁触发（0.72 次/题）却只对 29%，把成绩拖到 4.8%<7.1%：
  θ 与探针都必须按档标定。
- **红旗（绝对值口径未定，不许拿活跑数字对外比）**：活跑框架本身未对齐 w0——
  剔掉撞线题后同批 129 题上 noprobe 只有 9.3%，w0 有 31.8%。臂间对比内部有效，
  绝对成功率失真。归因候选：日期行（live 渲当天、采集钉 2026-07-31）、64-token
  分段边界的重分词漂移。下一步是修框架再谈绝对值，不是下结论。
- **触发**：run_id `20260802_0136_live_aw_gptoss` / `20260802_0240_live_aw_effort`
  / `20260802_0306_live_aw_probe_effort`（各臂 LIVE_REPORT 在
  `pipeline/inject/runs/live_aw_gptoss/<arm>/`）。
- **作废**：WORKPLAN 里“in-loop rollout 不做”的旧划界（用户 2026-08-02 点名推翻）；
  “effort 档位可作省 token 基线”的默认假设。
- **顺带**：静态分片被判死——最胖 10% 题占 19% 生成量零成功，一题堵死一条分片；
  live_appworld 已换动态领题（--pool，mkdir 原子票，commit 4c7588d），下轮生效。
  预算闸（60k/题零伤亡上限）经讨论暂不加。

## 2026-08-02 — 拼回八臂收官：转场+骨架(skel_switch)胜出，钉工具名反而拉高执行一致率

- **决定**：截断拼回的主形态定为 `skel_switch`（通道切换字节 + 围栏 + 骨架，
  骨架=探针预测工具名、不带尾左括号）——θ=0.925 下逐事件省 token 中位 **+239**
  （主对照=原轨迹，89% 事件为正）、工具名零改写、零"补完后又想"；思考段拼骨架的
  三种话术（bare/`Thus code:`/散文式）全部劣势（省 27–69、11–13% 被改写、
  接茬又想 235–730 字符），SYSTEM NOTE 注入两臂（+1/+181）也不如它。
  执行侧的意外收获：乙（骨架钉名）执行结果与原轨迹一致率 72.8%（猜对桶 80.3%），
  **反超**大模型自写整条的 switch_only（53.8%）——钉工具名把模型拉回原轨迹行为；
  switch_only 的低分多为"合理偏离"，即计划里预告的系统性低估，比一致率口径
  需带此脚注。甲（验名）接受率 66.8%；丙（逐 token 验收）接受长度中位 8/17、
  整条逐字同 12.8%，tokenizer-LCP 与 echo+logprobs 两路不一致率 12.6%
  （vLLM logprob 抖动的实测量级）。
- **触发**：run `20260801_2257_inject_aw_gptoss_splice`（八臂 7738 请求）
  + `20260801_2257_aw_gptoss_splice_th0925_plan`（plan 重跑，gen_call 与旧
  th0925 逐字复现 1061/1061，nofill 体检 +3≈0 放行）。
- **作废了什么**：思考段骨架话术三档不再作为拼回主形态排后续实验；
  "结果后停止标签开关"的独立消融并入 inject vs inject_stop 已出的对照
  （+1 vs +181，开关本身价值已量化）。挂起项里的候选C话术随裸骨架三档
  出数一并否决（三档全劣于转场，无再试理由）。
- **对应 commit**：`19ea26e`（八臂实现）+ `19e0308`（发射器）+ 本条所在 commit。
  甲丙对账细节在 run 目录 `ACCEPT_REPORT.json`，执行分桶数字在 RESULTS.md。

## 2026-07-31 — c1 十二格全收：因果头取代分类头当主线，appworld 官方分区成为标准场地

- **决定**：探针主线定为因果头（`ctool`/`cgen` 两格），ModernBERT 分类头
  （`mtool`/`mext`）退为对照，不再单独为它排新实验；appworld 的成绩一律在官方分区
  （`aw_official_v1`，train 90 / val 57 / test_normal 168 题）上报，
  旧的自切分数字不再作为主线引用。参数侧的主报数口径改用生成路线的严格判定。
- **触发**：新流水线 `pipeline/` 五段（collect / annotate / train / eval / inject）落成并双验收——
  annotate 段用新代码重跑 v3 当年输入，与旧数据九字段逐条比对**全零差异**
  （`pipeline/annotate/ACCEPT_V3DIFF.md`）；eval 段在 `--legacy-splits --cached-logits` 下
  重跑 v3 的 bfcl 分类头与因果头，`REPLAY_REPORT.json` 与旧产物**逐字节相同**
  （`pipeline/eval/ACCEPT_EVAL.md`）。据此采集 appworld 官方分区 594/594 全清
  （run `20260731_0607_w0_aw_official`，客户端墙钟 2h19m），
  再跑四格 × 三模型 12 个训练全部收官、11/12 格评测出数
  （`c1_q35_mext` 评测 N/A：上游 `c1_q35_mtool` 两档 θ 皆 null，没有触发点可评）。
  三条结论：
  ①**因果头全面胜出**——`c1_gptoss_ctool` 两档皆有解（风险 0.05 档 coverage 0.2961 /
  trig_acc 0.951，0.10 档 0.4963 / 0.9057），而同源 `c1_gptoss_mtool` 0.05 档无解、
  0.10 档未兑现；触发比例上 `c1_q35_ctool` 把 mtool 的"两档无解"变成双档有解，
  `c1_q36_ctool` 在 0.10 档 0.2911 是 mtool 0.1724 的 1.7 倍，
  `c1_gptoss_ctool` 0.4963 是 mtool 0.1239 的 4.0 倍。
  ②**难度迁移风险兑现了一例**——`c1_gptoss_mtool` 0.10 档在 val 上选中的 θ=0.975
  到 test 上 trig_acc 只有 0.8642，低于 0.90 契约线，说明 val 选点不保证 test 兑现。
  ③**宽松与严格判定的差距只出现在抽取路线**——三个 `cgen` 的
  `params_all_ok` 与 `params_all_ok_strict` 完全相等（0.9041 / 0.8582 / 0.8262，无归一化损耗），
  而 `c1_q36_mext` 是 0.9595 对 0.8874、`c1_gptoss_mext` 是 0.7887 对 0.4377，差距悬殊。
- **作废**：把 ModernBERT 分类头当主线报数的做法；appworld 上一代自切分的探针成绩
  作为主线引用（仍可作历史对照，但与官方分区的数不可直接比）；
  "宽松判定与严格判定的差距是各路线通病"这个默认假设（只在抽取路线成立）。
- **取舍**：两条工程决策记在案。其一，因果头训练前的对齐检查按 T8 先例把门槛放宽到
  `3e-4`——三个 `ctool` 的 hidden maxdiff 在 8.39e-5 ~ 1.68e-4、logits maxdiff 在
  1.69e-5 ~ 2.00e-5、相对差 2.3e-6 ~ 3.3e-6，判为数值噪声而非实现错误，全部 PASS 放行。
  其二，`c1_gptoss_cgen` 迁卡重发——首轮在 A6000 上 OOM，加 `--grad-ckpt` 后
  22.15s/step、ETA 26.7h，裁决改到 tokyo108 的 H200 上不带 grad-ckpt 重跑，
  实际墙钟 5h13m，残局留在 `pipeline/runs/_aborted_c1_gptoss_cgen_t106g3`。
- **commit**：本次记账提交（前一个 HEAD `9750953`）

## 2026-07-31 — 记录体系加第四层：数据设定归口 DATA.md

- **决定**：三层记录之外单开 `DATA.md` 回答"这批数据是怎么造出来的"，
  常驻、随数据版本更新。同时收口两处并行文档：全项目唯一词表定为
  `plans/PLAINWORDS.md`（任务卡 §0 的并行词表收编进去，§0 改成指针）；
  状态书同一时刻只留一份现役，旧的进 `plans/archive/`。
- **触发**：盘点记录现状发现三处冗余与漂移——两份状态书 63.3% 逐行重复；
  两份词表并存且任务卡 §0 的先验基线还写着 v2 那一代的数（0.672/0.226/0.038），
  v3 起已经变了；数据设定散在 `FULL_RUN_v1.md` / `RUNBOOK.md` / 四份
  `BUILD_REPORT.md` / `PARAM_LABEL_REPORT.md` / `GEN_REPORT.md` 至少五处，
  开新实验要拼五个文件才知道自己在用什么数据。
- **作废**：任务卡 §0 的并行词表；`plans/` 下多份状态书并存的做法；
  §0 里写死的那组先验基线数值（改查 `DATA.md` §4）。
- **取舍**：`DATA.md` 只写设定与口径不写结论，避免和 `RESULTS.md` 长成第二本账。
- **commit**：本次整理提交（前一个 HEAD `1993a98`）

## 2026-07-30 — 全历史基线：档位越远越不划算，选择性记忆的动机立住

- **决定**：C3 保留"把过去原样塞进提示"这条臂作为并列上界之一，
  并据此把"必须靠会筛选的机制"写进 C3 立论；不改主图设计。
- **触发**：run `20260730_fig1_fullhist_8b`——完全一样的题那一档做成轮数 31→47、
  总 token ×0.84（省在写出侧）；几乎一样的题 ×1.17；同类但换了东西的题 ×2.50。
  与 run `20260730_oracle_ceiling_8bfull` 的逐字回放上界（只在完全一样那一档非零，
  最多省 21.0%）构成上下界。
- **作废**：无（这条是补一条此前只有猜测的对照臂）。
- **commit**：`bac6964`（fullhist）/ `62a18c3`（oracle 上界）

## 2026-07-30 — 跨模型矩阵定部署结论：换 agent 模型时 bfcl 只需重做校准

- **决定**：探针的部署说明按环境分开写——bfcl 换 agent 模型只要重做校准就够，
  appworld 和 tales 必须重训。
- **触发**：run `20260730_1713_bert_t6_xqwen`——bfcl 主场 0.9748 / 冷迁移 0.897 /
  只换校准救回 0.971（触发比例减半）；appworld 只换校准救不满；tales 全弱。
- **作废**："探针跨 agent 模型一律要重训"这个默认假设（此前没有实测，按最坏情况写的）。
- **commit**：`5e87638`

## 2026-07-30 — bfcl 混训天花板：增益在覆盖不在精度

- **决定**：跨模型这条线的天花板行一律用混训模型报，且论文里要说清增益落在覆盖上；
  bfcl 的主线数据从 v3 换成 v3_1（两侧合流）。
- **触发**：run `20260730_1835_bert_t6_bfcl_mixed`——qwen 侧 0.9645/0.7478、
  gptoss 侧 0.9157/0.7615，对照纯 qwen 的 v3 天花板 0.509/0.349，
  精度没怎么动，触发比例大幅抬升。
- **作废**：用纯 qwen 的 v3 模型当 bfcl 天花板的做法。
- **commit**：`871f502`

## 2026-07-30 — 因果探针裁决 v2 部署形态：appworld 的门被打开

- **决定**：探针底座换成因果语言模型（Qwen3-0.6B-Base），ModernBERT 退为对照；
  窗口长度不再当作瓶颈，不再排"加长窗口"的实验。
- **触发**：run `20260730_1716_bert_t8_causal`——appworld 0.9621/0.3338，
  而 ModernBERT 读完整输入在同一环境同一档下找不到可用触发门槛；
  三环境触发比例全面 2.5–4.3 倍，探测成本降到 1/20–1/34；
  tales 的 8192 窗口对照只有噪声级收益。
- **作废**：上一条"投机门只在 bfcl 开"里 appworld 那一半（tales 那一半仍然成立，
  且 tales 在风险 0.1 档出现门槛迁移失守，是它特有的问题）；
  "窗口太短是 appworld 不达标的根因"这个假设。
- **commit**：`62a18c3`

## 2026-07-30 — v3 三环境终审：投机门只在 bfcl 开

- **决定**：ModernBERT 探针这条线上，appworld 与 tales 在 95% 精度标准下判不达标，
  不再靠加数据抢救；bfcl 是唯一达标环境。
- **触发**：run `20260730_0814_bert_replay_appworld_v3`（数据翻倍后精度仍钉在 0.933，
  风险 0.05 档无可行门槛，v2fix 那条 0.931 的悬案就此了结）与
  run `20260730_1645_bert_replay_tales_v3`（高门槛触发比例从 0 拉到 0.147，
  但精度天花板 0.84–0.85 离 95% 差 10pt，根因是开放动作空间）。
  bfcl 侧由 run `20260730_0204_bert_replay_bfclv3` 确认结论对重切分稳健（切分方差约 ±3pt）。
- **作废**："再翻一倍数据就能把 appworld/tales 拉过线"这个预期。
- **commit**：`9f91011`（appworld）/ `b55d520`（tales）/ `381b834`（bfcl 重切分）

## 2026-07-29 — v2fix 验证通过：bfcl 投机门能开了

- **决定**：fp32+autocast 修复版探针在 bfcl 上确立可行工作点，投机执行方向在
  bfcl 环境成立；继续按 WORKPLAN C2-2t 推进 appworld/tales 毕业后的同款评测。
- **触发**：run `20260729_2235_bert_replay_bfcl_v2fix`——θ=0.8（wrong_spec≤0.1 档）
  coverage 0.755 / trig_acc 0.944 / earliness 0.664；θ=0.925（≤0.05 档）
  coverage 0.620 / trig_acc 0.966。频率先验基线 0.038 被碾过；
  深度曲线 0.69→0.86 上行（废版全平 0.26，冻结症状消失）。
  训练侧 best_calA_weighted_acc 0.8057，为废版 0.3262 的 2.5 倍。
- **作废**：v2 负结果"bfcl 开不了投机门"的结论正式推翻
  （上一条里"待 v2fix 重测重判"的悬案了结）。
- **commit**：`3e36694`（发射时 HEAD，代码即 54a4a4b 修复版 + 8cce422 评测脚本）

## 2026-07-29 — v2 三训作废：bf16 硬训精度缺陷

- **决定**：C2-2t 探针训练一律用标准混合精度（fp32 权重 + autocast bf16）；
  `*_v2` 三个训练及其回放报告全部作废，不得引用。
- **触发**：bfcl 回放负结果（run `20260729_2106`，深度曲线平坦 0.26 /
  置信度趴地 / 无可行 θ）追因发现 lr 2e-5 的更新量低于 bf16 权重分辨率，
  模型近乎冻结；A/B smoke 修正版 acc 19 倍（0.156 vs 0.008）。
- **作废**：`envs/bert_runs/{tales,appworld,bfcl}_v2/` 全部产物；
  bfcl v2 回放的"开不了投机门"结论（待 v2fix 重测重判）。
- **commit**：`54a4a4b`（修复）/ `9c3852f`（作废的负结果记录）

## 2026-07-29 — 建立版本控制与三层记录体系

- **决定**：new1 纳入 git 管理；实验记录拆成三层——方向层 `TIMELINE.md`（人写）、
  数字层 `ops/runs.jsonl` → `RESULTS.md`（`ops/record.py` 写）、
  数据层留在 NFS 不进 git（靠 run_id 目录名 + 批次 report.md 追溯）。
  记录动作挂在 gpu-run skill 的 Phase 4 / Phase 6a 上，不靠人自觉。
- **触发**：盘点发现数据侧可追溯（每条轨迹带 meta 行、数据集有出厂报告并固定种子），
  但代码侧完全断链——目录不是 git 仓库，跑完就改脚本，旧结果对应的代码版本不可恢复。
  同时 `ops/jobs.json` 台账实际为空，真实批次一条都没登记过。
- **作废**：关键数字散落在各方向 `FINDINGS.md` / `ANALYSIS_*.md` 里、靠人翻的做法。
- **取舍**：用户 2026-07-29 拍板不加硬性拦截（不做"无 record 不许 finish"），
  漏记靠流程约束而非机制强制；方向层坚持人写，避免自动记录退化成流水账。
- **commit**：`47e74ee`（版本控制）+ 本次记录体系提交

# 第三档（思考中途注入）设计空间灵感记录

> 2026-07-25 与 Claude 讨论中产生。**全部是灵感/假设，零验证。** 引用前先做实验。
> 背景：KNOWLEDGE_MAP「工具调用投机 / 思考中途注入」章节；占位现状 = 椅子半占，
> 闭环（隐藏态触发→通用工具投机→中途回填→事后验证）仍空。

## 对付"模型迷惑"和"注错污染"的五层工事（从便宜到贵）

1. **措辞打预防针**（免费）：注入时明说身份——"〔系统预取的参考信息，可能无关，无关请忽略〕"。零成本，约束力弱。
2. **置信门槛 + 只注只读工具结果**（便宜）：不确定就不注；有副作用的工具永不投机。
3. **并行调用滑块**（中等，用户提出）：探针不确定参数时多路并发（只读工具便宜）。两种用法：
   - 激进：把最可能的一路抢先注入（省"想"+省"等"，但可能注错）；
   - 保守：全部攥着不注，等模型写出真实调用后比对参数、命中即秒回（= 先验证后交付，注错风险为零，但只省"等"）。
   - **滑块含义：几路并行 × 抢先/验证后，就是在"省得多"和"错得起"之间调档。**
4. **训练模型接受注入**（贵，用户提出）：SFT 教模型处理异步塞入的结果。
   - 先例：SIA 2605.13360 的 clock-based training + 合成数据 SFT（已核实，KNOWLEDGE_MAP A 级）——证明"训练适应异步注入"可行。
   - **往前一步的空点：训练它处理注错的结果**——造数据时故意混入错误注入，教模型"用之前先核对，对不上就无视"。"接受但不轻信"这个训练目标疑似无人做过（未系统核查），单独可成论文核心。
   - 代价：丢掉"agent 模型零改动"卖点，只适用开放权重模型。
5. **纠错注入**（事后补救）：注错且已被采用后补一条更正。能否把模型拉回来未知——Laban 2505.06120（"走错就迷路不回头"）在此直接相关，预期悲观，值得测。

## 结构性想法

- **双档产品结构**：免训练档（层1-3，处处能用含闭源 API，收益保守）+ 训练档（加层4，收益激进，限开放模型）。论文里两档都报，天然构成消融。
- **"递一件 vs 递一盘"**（用户的器械护士图像）：高置信注入单个结果 vs 低置信注入多个候选/仅提示。"递一盘"占上下文、分散注意力，有无用武之地是开放设计，无任何证据。
- **注入失败的软兜底**：模型无视注入照样发调用 → 结果当缓存秒回，退化为普通投机。最坏≈不注入，最好多偷一段思考时间。

## 触发点三:用户插话预取(2026-07-27,用户提出,次要方向,未核查)

场景:主循环正忙(长动作执行中),用户的新消息已完整到达但排队等待。用一个快速预测器立刻读新消息,预测它将引发的增量工具调用,趁主循环忙完前先执行。

- **关键性质:提前量窗口外生**——窗口=主循环剩余忙碌时间,系统自知,时机头在此触发点不需要学习;压力全在内容预测(新消息+当前轨迹+记忆,与 t=0 头同款特征)。
- 架构定位:同一投机器的第三触发点(t=0 任务开始 / 思考中 / 插话时),非新架构。
- 与 SIA 边界:SIA=当前任务输入未到齐时赌完整性;此处=新完整输入已到但无人处理。重叠于"用户与 agent 并行说话",机制不同。
- **占位核查 TODO**(声称前必做):重读 SIA 全文、核 Gim 2024(异步函数调用源头,仍在隔离区)、搜 agent 插话/interruption 处理文献。

## 判决性实验（先于一切设计）

**上帝视角注入（oracle injection）**：回放已有轨迹，在思考不同位置手动注入事后已知的正确结果，画"注入提前量 vs 思考缩短量 vs 准确率"曲线。单卡可跑，无需探针。
- 省不动 → 第三档整体死刑，以上全部作废；
- 省得动 → 曲线本身即论文 Fig 1，再回来按层做消融。
- 对应 KNOWLEDGE_MAP 开放问题 ⑥（另见 ⑦：PPRAG 隐藏态预测器搬到通用工具是否成立）。
